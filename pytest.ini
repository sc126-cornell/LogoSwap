[pytest]
# -p no:cacheprovider avoids a benign cache-write conflict when the repo lives in a
# Dropbox-synced folder (Dropbox transiently locks the .pytest_cache dir on Windows).
addopts = -p no:cacheprovider
testpaths = tests
