---
phase: 03-logo-placement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/config.py
  - app/services/logo.py
  - app/api/logos.py
  - app/main.py
  - web/js/api.js
  - web/js/logos.js
  - web/js/regions.js
  - web/index.html
  - web/styles/app.css
  - web/styles/tokens.css
  - logos/manifest.json
  - logos/placeholder.png
  - tests/test_logo.py
  - tests/conftest.py
autonomous: true
requirements:
  - LOGO-01
user_setup: []

must_haves:
  truths:
    - "使用者可在側欄縮圖網格中瀏覽固定商標庫並挑選一個 logo（D-05 側欄縮圖選擇器，LOGO-01）"
    - "GET /logos 回傳商標庫清單（id + 顯示名），不洩漏任何檔案系統路徑（LOGO-01）"
    - "商標庫為空或缺檔時 GET /logos 回 {logos: []}，選擇器顯示空狀態而非 500，流程退化為純移除（D-04 / A2）"
    - "未受信任的 logo_id（../ 或不存在）解析時回 404 logo_not_found，永不建構路徑、永不 500（T-03-01）"
    - "選定的 logo_id 為純前端狀態，更換選取時沿用 Phase 2 stale 機制使既有結果失效（D-05 / Pitfall 5）"
    - "商標庫資產為 PNG 去背（含 alpha）格式，manifest 可容納多個版本（水平/直式/深淺）；載入時以 Pillow 驗證為合法 PNG，不支援 SVG（D-03，LOGO-01）"
  artifacts:
    - path: "app/services/logo.py"
      provides: "fitz-free 商標庫服務：load+validate manifest、list_logos()、resolve(logo_id)->bytes（manifest 白名單）、LogoError"
      contains: "def list_logos"
    - path: "app/api/logos.py"
      provides: "GET /logos 清單端點 + GET /logos/{id}/image 縮圖位元組端點"
      contains: "/logos"
    - path: "web/js/logos.js"
      provides: "側欄縮圖網格、單選狀態、空/載入/失敗狀態、logo 變更觸發 stale"
      contains: "initLogos"
    - path: "logos/manifest.json"
      provides: "固定唯讀商標庫描述（至少一筆 placeholder）"
      contains: "id"
    - path: "tests/test_logo.py"
      provides: "LOGO-01 清單 + 路徑穿越拒絕測試（Wave 0 scaffold）"
      contains: "def test_list_logos"
  key_links:
    - from: "web/js/logos.js"
      to: "GET /logos"
      via: "api.listLogos()（唯一 server seam）"
      pattern: "api\\.listLogos"
    - from: "app/services/logo.py"
      to: "config.LOGOS_DIR / entry['file']"
      via: "manifest 字典白名單查找 + is_relative_to(LOGOS_DIR) 防護"
      pattern: "is_relative_to"
    - from: "app/main.py"
      to: "LogoError"
      via: "@app.exception_handler(LogoError) -> 結構化 4xx"
      pattern: "LogoError"
---

<objective>
建立「瀏覽並挑選我司商標」的垂直切片（LOGO-01）：固定唯讀商標庫（`logos/` + `manifest.json`）、讀取/驗證它的 fitz-free `logo.py` 服務（含 `logo_id`→bytes 的 manifest 白名單防護）、`GET /logos` 清單 + `GET /logos/{id}/image` 縮圖端點,以及側欄縮圖網格選擇器(沿用雙主題 token 與繁中文案)。完成後使用者可實際在 UI 看到商標庫、點選一個 logo,選取狀態以 accent ring 標示,選取會被記為前端 `selectedLogoId`,並在更換時觸發 Phase 2 的「結果失效」機制。

此切片本身即端到端可操作:有商標庫資產 → 後端列出 → 前端網格顯示 → 使用者點選。logo 的「實際置入 PDF」是 03-02 的切片(消費此處建立的 `logo.resolve` 與 `selectedLogoId`)。

Purpose: 交付 LOGO-01(固定商標庫瀏覽/挑選)的完整可操作切片,並建立 03-02 置入切片所需的 `logo.resolve(logo_id)->bytes` 契約與 `selectedLogoId` 前端狀態。
Output: `logo.py` 服務、`logos.py` 路由、`logos.js` 選擇器、`logos/` 資產(seed placeholder)、`config.LOGOS_DIR`、`main.py` 的 `LogoError` handler、`api.js` 兩個 seam helper、`index.html` 選擇器區段、`app.css` 網格樣式、`tests/test_logo.py`(Wave 0 scaffold)。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 3 source-of-truth artifacts (read before implementing)
@.planning/phases/03-logo-placement/03-CONTEXT.md
@.planning/phases/03-logo-placement/03-RESEARCH.md
@.planning/phases/03-logo-placement/03-PATTERNS.md
@.planning/phases/03-logo-placement/03-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — use directly, no exploration. -->

The path-traversal defense to MIRROR (app/storage.py:36-89). The logo_id analog is STRONGER:
logo_id is a DICT KEY into the parsed manifest, never a path segment. The manifest entry's
`file` field (an admin-controlled bare basename) is what joins to LOGOS_DIR.

  _SESSION_ID_RE = re.compile(r"^[A-Za-z0-9_-]{16,64}$")
  def validate_session_id(session_id): ...  # allowlist regex
  def subdir(kind, session_id):
      ...
      resolved = dest.resolve()
      if not resolved.is_relative_to(data_dir.resolve()):   # containment assert
          raise InvalidSessionId(...)

Typed-error shape to MIRROR (app/services/render.py — RenderError; app/services/redact.py:39-49):
  class RedactError(Exception):
      def __init__(self, code: str, message: str) -> None:
          super().__init__(message); self.code = code; self.message = message

Lazy config read for monkeypatchable tests (app/storage.py:68-70):
  def _data_dir() -> Path:
      return Path(config.DATA_DIR)

Graceful manifest read (mirror app/storage.py:156-170 read_session_meta):
  try: data = json.load(open(path, encoding="utf-8"))
  except (FileNotFoundError, ValueError, OSError): return None

main.py error-handler + status-table pattern to MIRROR (app/main.py:84-106):
  _PROCESS_STATUS = {"residual_content": 422, ...}
  @app.exception_handler(RedactError)
  async def _handle_redact_error(_request, exc):
      return JSONResponse(status_code=_PROCESS_STATUS.get(exc.code, 422),
                          content={"detail": {"code": exc.code, "message": exc.message}})
  # router registration block (lines 25, 36-38):
  from .api import pages, process, sessions
  app.include_router(process.router)

api.js sole-seam helpers to MIRROR (web/js/api.js:95-123):
  export const API_BASE = (window.PDFTOOL_API_BASE) || "";
  async function toApiError(response) { ... }  # already defined in file
  export function pageImageURL(id, n, dpi) { return API_BASE + "/sessions/" + encodeURIComponent(id) + ... }
  export async function pageMeta(id, n) { const r = await fetch(...); if (!r.ok) throw await toApiError(r); return r.json(); }

regions.js stale machinery to REUSE (web/js/regions.js:440-452 onRegionsEdited):
  function onRegionsEdited() {
    if (resultFresh) { resultFresh = false; if (viewMode === "result") setViewMode("original");
                       setActionStatus(COPY.staleNotice); }
    renderList(); renderOverlay(); updateActionGroup();
  }
  # initRegions/resetRegions exported (regions.js:675-717); app.js wires them (app.js:140, 155, 199).

index.html mount point (web/index.html:299-336): aside#side-panel > .side-panel__inner contains
the region-panel header + #region-empty + #region-list + #region-notice. Append the logo picker
section AFTER #region-notice, inside the same .side-panel__inner. Module-load order (lines 360-364):
api.js, theme.js, viewer.js, regions.js, app.js — insert logos.js after regions.js, before app.js.

conftest.py fixtures to MIRROR (tests/conftest.py:18-74): _build_pdf builds PDFs in-memory via
fitz (no committed binaries); isolated_data_dir autouse monkeypatches config.DATA_DIR at tmp_path;
client() returns TestClient(app).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: logo.py service + config.LOGOS_DIR + LogoError wiring (Wave 0 tests for LOGO-01 list + path-traversal)</name>
  <read_first>
    - app/storage.py (the validate_session_id allowlist + subdir containment assert + _data_dir lazy read + read_session_meta graceful-None — the EXACT patterns to mirror)
    - app/services/render.py (RenderError typed-error module shape + lazy config read)
    - app/config.py (DATA_DIR .resolve() idiom + _env_int limit constants)
    - app/main.py (RedactError/PipelineError exception handlers + _PROCESS_STATUS table + router registration block)
    - tests/conftest.py (isolated_data_dir autouse monkeypatch + _build_pdf in-memory fixture style + client())
    - tests/test_process_api.py (TestClient structured-4xx assertion style; test_process_crafted_session_id_is_404 at lines 243-247)
    - .planning/phases/03-logo-placement/03-PATTERNS.md (logo.py section — Analogs A/B/C/D; the manifest-allowlist 3-step resolve)
    - .planning/phases/03-logo-placement/03-RESEARCH.md (§Security T-03-01..03; §Validation Architecture LOGO-01 test map; Pitfall 3 & 6)
  </read_first>
  <behavior>
    - Test (LOGO-01 list): `GET /logos` on a tmp library with a valid manifest+PNG returns 200 `{logos: [...]}` where each entry has `id` and `name` and NO filesystem path/key (no `file`, no absolute path leaked).
    - Test (LOGO-01 empty/absent): with LOGOS_DIR pointing at an empty/absent dir, `GET /logos` returns 200 `{logos: []}` (NOT 500) — picker empty-state (A2).
    - Test (T-03-01 traversal): `GET /logos/{id}/image` with `id="../../app/config.py"` (URL-encoded) and with an unknown id BOTH return 404 `{detail:{code:"logo_not_found"}}`, never 500, never reading outside LOGOS_DIR.
    - Test (LOGO-01 image happy): `GET /logos/{valid_id}/image` returns 200 `image/png` whose bytes start with the PNG signature.
    - Test (T-03-02/03 bad asset): a manifest entry whose `file` is missing/corrupt is SKIPPED from `list_logos()` (does not appear) rather than crashing the list.
  </behavior>
  <action>
    Add to `app/config.py` (mirror the `DATA_DIR` .resolve() + env-default idiom and the `_env_int` constants): `LOGOS_DIR: Path = Path(os.environ.get("LOGOS_DIR", "./logos")).resolve()` and `MAX_LOGO_BYTES: int = _env_int("MAX_LOGO_BYTES", 10 * 1024 * 1024)` (T-03-02 per-asset size cap).

    Create `app/services/logo.py` — fitz-free (import ONLY `json`, `re`, `pathlib`, `PIL`, and `from .. import config`; mirror the redact.py purity docstring note). Expose:
      - `class LogoError(Exception)` with `code`/`message` (mirror RenderError). Codes: `logo_not_found` (unknown id → 404), `logo_invalid`/`logo_unreadable` (corrupt asset → 422).
      - `_logos_dir() -> Path` returning `Path(config.LOGOS_DIR)` (lazy read so the test fixture can monkeypatch — mirror `_data_dir`).
      - `_load_manifest() -> dict` reading `_logos_dir()/"manifest.json"` keyed by `id`; an absent/empty/unparseable manifest returns `{}` (graceful — mirror `read_session_meta` try/except over `(FileNotFoundError, ValueError, OSError)`).
      - `list_logos() -> list[dict]` returning `[{id, name, ...}]` for entries whose `file` exists, is ≤ `MAX_LOGO_BYTES`, and passes Pillow validation; SKIP (try/except per asset) a bad entry rather than failing the whole list (T-03-02/03 / Pitfall 6). NEVER include the filesystem path or the raw `file` key in the returned dicts.
      - `resolve(logo_id) -> bytes`: (1) `entry = manifest_dict.get(logo_id)`; `None` → `raise LogoError("logo_not_found", "找不到所選商標。")` (no oracle, mirrors `session_not_found`). (2) build `path = _logos_dir() / entry["file"]`; defense-in-depth assert `path.resolve().is_relative_to(_logos_dir().resolve())` (T-03-01), else `LogoError("logo_not_found", ...)`. (3) validate bytes via Pillow (`Image.open(...).verify()`, confirm `mode in {"RGBA","LA"}` or palette transparency per D-03) — on decode failure `raise LogoError("logo_unreadable", ...)`. Return the raw PNG bytes. NEVER `LOGOS_DIR / logo_id` or `LOGOS_DIR / f"{logo_id}.png"` (Anti-Pattern / Pitfall 3, per D-04).

    Create `app/api/logos.py` (mirror `process.py` router skeleton + `pages.py` `Response(content=, media_type=)` bytes pattern + the `except <Error>` → 404 map). `router = APIRouter(tags=["logos"])`. NO `_require_session` (the library is shared, not session-scoped). `GET /logos` → `{"logos": logo.list_logos()}`. `GET /logos/{logo_id}/image` → `data = await run_in_threadpool(logo.resolve, logo_id)` wrapped in `except LogoError as err: raise HTTPException(status_code=<map>, detail={"code": err.code, "message": err.message})`, else `Response(content=data, media_type="image/png")`.

    Wire `app/main.py` (mirror `_handle_redact_error` byte-for-byte in shape): add `from .services.logo import LogoError` to the imports block; add `_LOGO_STATUS = {"logo_not_found": 404, "logo_invalid": 422, "logo_unreadable": 422}`; add `@app.exception_handler(LogoError)` returning `JSONResponse(status_code=_LOGO_STATUS.get(exc.code, 422), content={"detail": {"code": exc.code, "message": exc.message}})`; register `app.include_router(logos.router)` and add `logos` to the `from .api import ...` line. (This handler also covers a `LogoError` raised inside 03-02's `/process` path — never a bare 500, T-02-08.)

    Create `tests/test_logo.py` and add fixtures to `tests/conftest.py` (Wave 0 scaffold): a `logo_png_bytes` fixture building a transparent PNG in-memory via `PIL.Image.new("RGBA", (40, 20), (255,0,0,128))` saved to a `BytesIO` (no committed binary — mirror the in-memory `_build_pdf` philosophy); a `logo_library` fixture writing `manifest.json` + that PNG into a tmp dir and `monkeypatch.setattr(config, "LOGOS_DIR", tmp_logos)` (mirror `isolated_data_dir`). Write the five behavior tests above using the TestClient + structured-4xx assertion style of `test_process_api.py`. Manifest schema (RESEARCH Open Question 2): `{"id": "placeholder", "file": "placeholder.png", "name": "預設商標", "tags": []}`.

    Keep `logo.py` and `logos.py` fitz-free — the existing `test_fitz_import_confined_to_engine_seam` (test_redact.py:514-531) walks all `app/**/*.py` and will fail if either imports fitz. Do NOT add a new grep test.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_logo.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/services/logo.py` exists and contains `def list_logos(` and `def resolve(` and `class LogoError`.
    - `Grep "is_relative_to" app/services/logo.py` matches (containment assert present, T-03-01).
    - `Grep "import fitz" app/services/logo.py` returns NO match AND `Grep "import fitz" app/api/logos.py` returns NO match (AGPL seam, T-02-03).
    - `app/config.py` contains `LOGOS_DIR` and `MAX_LOGO_BYTES`.
    - `app/main.py` contains `LogoError` and `_LOGO_STATUS` and `logos.router`.
    - `pytest tests/test_logo.py::test_list_logos -x` exits 0 (GET /logos lists id+name, no paths).
    - `pytest tests/test_logo.py::test_logo_id_path_traversal_rejected -x` exits 0 (`../` and unknown id → 404 logo_not_found, never 500).
    - `pytest tests/test_logo.py -q` exits 0 with ≥ 5 tests collected and passing.
    - `.venv/Scripts/python.exe -m pytest tests/test_redact.py::test_fitz_import_confined_to_engine_seam -x` exits 0 (logo.py/logos.py did not leak fitz).
  </acceptance_criteria>
  <done>
    GET /logos lists the library (id+name, no paths) and degrades to {logos: []} when empty; GET /logos/{id}/image serves a valid PNG and maps a crafted/unknown id to a structured 404 logo_not_found (never 500); logo.py is fitz-free; all Wave 0 LOGO-01 tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Side-panel thumbnail picker UI (logos.js + api.js seam + index.html section + app.css grid) wired into Phase 2 stale machinery</name>
  <read_first>
    - web/js/regions.js (the END-TO-END analog: COPY object 34-58; createElement-only DOM build incl. makeTrashIcon NS pattern 218-238; setActiveRegion dataset-comparison single-select 301-317; onRegionsEdited stale path 440-452; initRegions/resetRegions exports 675-717; module-load wiring 736-764)
    - web/js/api.js (pageMeta fetch-or-throw 110-123 + pageImageURL builder 95-107 + processJob 136-149 + the module docstring sole-seam contract 1-31 + toApiError)
    - web/js/app.js (initRegions/resetRegions wiring at 140, 155, 199; setSidePanelExpanded loaded-state gate 80-83)
    - web/index.html (aside#side-panel > .side-panel__inner block 299-336; the #view-result segment label 157-162; module-load order 360-364)
    - web/styles/app.css (.region-panel__header, .region-empty, .region-list, .region-row:hover rules — and tokens.css vars referenced by 03-PATTERNS.md app.css section)
    - .planning/phases/03-logo-placement/03-UI-SPEC.md (§Component Inventory; §Color "Accent reserved for" + Thumbnail backing; §Copywriting Contract — verbatim 繁中 strings; §Layout & Interaction picker placement + logo-change→stale + conditional relabel)
    - .planning/phases/03-logo-placement/03-PATTERNS.md (logos.js / api.js / index.html / app.css sections — the 5 structural patterns + exact additions)
  </read_first>
  <action>
    Add to `web/js/api.js` (the SOLE server seam — mirror `pageMeta` + `pageImageURL`): `export async function listLogos()` doing `fetch(API_BASE + "/logos")`, `if (!response.ok) throw await toApiError(response)`, `return response.json()`; and `export function logoImageURL(id)` returning `API_BASE + "/logos/" + encodeURIComponent(id) + "/image"`. Update the JSDoc "Backend contract consumed" block (lines 8-27) to document `GET /logos`, `GET /logos/{id}/image`, and the new optional `logo_id` on `/process` (the 03-02 field). Do NOT change `processJob` — 03-02's caller adds `logo_id` to the spec.

    Add a logo-picker section to `web/index.html` AFTER `#region-notice`, INSIDE the same `.side-panel__inner` (UI-SPEC: below the region list, A3). Reuse the existing chrome classes: a `<section class="logo-picker" id="logo-picker">` with a `.region-panel__header` (heading `我司商標` via `.region-panel__heading`; one-line subtext `選擇要置入移除區域的商標(套用後置入所有框選區域)` via `.region-panel__scope`), a `<div class="logo-grid" id="logo-grid" role="group" hidden>` (rows via createElement), a `.region-empty`-styled `#logo-empty` empty-state block (heading `尚無可用的商標`, body per UI-SPEC), and reuse `.spinner` + a `.region-notice`-styled `#logo-notice` for loading/failed. Do NOT hardcode the `#view-result` label change here — leave it to JS (03-02 sets it conditionally; this plan leaves it as-is).

    Create `web/js/logos.js` (mirror regions.js end-to-end). Include a `COPY` object with the verbatim 繁中 strings from UI-SPEC §Copywriting (`heading`, `subtext`, `selectAria(name)`, `noLogo: "不置入商標"`, `emptyHeading`, `emptyBody`, `loading`, `loadFailed`, `staleNotice: "所選商標已變更,請重新套用以更新結果"`). Import `* as api`. Build the grid with createElement ONLY (NEVER innerHTML, T-03-04/T-02-11): each cell is a `<button type="button" class="logo-thumb">` carrying `dataset.logoId`, an `<img>` whose `src = api.logoImageURL(id)` and `alt=""`, a caption via `textContent`, and `aria-label = COPY.selectAria(name)` via setAttribute. Hold a single `selectedLogoId` client state; selecting toggles `.is-selected` + `aria-pressed` by dataset comparison (mirror setActiveRegion — NOT a CSS-selector string built from the id, WR-03). Optionally include a `不置入商標` clear cell that sets `selectedLogoId = null`. Export `initLogos({ session_id })` (call `api.listLogos()`; render grid, or empty/loading/failed state; reset `selectedLogoId`), `resetLogos()` (clear selection + grid), and `getSelectedLogoId()` (consumed by 03-02). Add `<script type="module" src="js/logos.js">` to index.html AFTER regions.js, BEFORE app.js. Wire `initLogos`/`resetLogos` in `app.js` alongside `initRegions`/`resetRegions` (in `handleFile` success, the catch reset, and the error-retry handler).

    Reuse ONE stale state machine (Pitfall 5 is load-bearing — D-05 carried-forward): add a small exported hook `export function notifyJobInputChanged()` to `web/js/regions.js` that runs the SAME block as `onRegionsEdited`'s stale branch (set `resultFresh = false`; if `viewMode === "result"` → `setViewMode("original")`; `setActionStatus(staleNotice)`; then `updateActionGroup()`). Call it from `logos.js` on every selection change (select/clear). Do NOT fork the action-group machine. The logo-specific stale notice (UI-SPEC default #7) may pass its own copy if `notifyJobInputChanged` accepts an optional message; otherwise reuse the Phase-2 notice.

    Add `web/styles/app.css` rules using EXISTING tokens (no new color tokens; only the two layout vars appended to tokens.css `:root`: `--logo-thumb-size: 72px;` and `--logo-grid-gap: var(--space-sm);`): `.logo-grid` = `display:grid; grid-template-columns: repeat(auto-fill, minmax(var(--logo-thumb-size), 1fr)); gap: var(--logo-grid-gap); padding: var(--space-md);`. `.logo-thumb` = `min-height: var(--control-hit); background: var(--color-surface)` (neutral backing behind transparent PNG, Pitfall 2); `border: 1px solid var(--color-border); border-radius: var(--radius-sm)`. `.logo-thumb:hover { background: var(--color-neutral-hover); }` (NOT accent — mirror `.region-row:hover`). `.logo-thumb.is-selected { border: 2px solid var(--color-accent); }` (the ONE new accent element, UI-SPEC §Color reserved-for). `.logo-thumb img { width:100%; height:100%; object-fit: contain; }` (matches D-02 contain). The picker is visible only in the `loaded` state (it lives inside the side-panel which app.js toggles via `.main--paneled`).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -c "import re,pathlib; src=pathlib.Path('web/js/logos.js').read_text(encoding='utf-8'); assert 'innerHTML' not in src, 'logos.js must never use innerHTML (T-03-04)'; assert 'api.listLogos' in src and 'api.logoImageURL' in src, 'must use the api.js seam'; assert re.search(r'fetch\\s*\\(', src) is None, 'logos.js must NOT fetch directly — api.js is the sole seam'; print('logos.js seam + XSS guards OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `web/js/logos.js` exists and contains `export function initLogos`, `export function resetLogos`, and `export function getSelectedLogoId`.
    - `Grep "innerHTML" web/js/logos.js` returns NO match (T-03-04 / T-02-11 XSS guard).
    - `Grep "fetch\(" web/js/logos.js` returns NO match; `Grep "api.listLogos" web/js/logos.js` AND `Grep "api.logoImageURL" web/js/logos.js` both match (api.js is the sole seam, T-02-12).
    - `web/js/api.js` contains `export async function listLogos` and `export function logoImageURL`.
    - `web/js/regions.js` contains `export function notifyJobInputChanged` (ONE stale machine; Pitfall 5).
    - `Grep "notifyJobInputChanged" web/js/logos.js` matches (logo change reuses the Phase-2 stale path).
    - `web/index.html` contains `id="logo-picker"` and `id="logo-grid"` inside the side-panel, and a `<script ... src="js/logos.js">` tag ordered after regions.js and before app.js.
    - `web/styles/app.css` contains `.logo-grid` and `.logo-thumb.is-selected` using `var(--color-accent)`; the only new tokens are `--logo-thumb-size` and `--logo-grid-gap` (Grep confirms no new `--color-*` token added for the picker).
    - `web/js/app.js` contains `initLogos` and `resetLogos` (wired alongside initRegions/resetRegions).
    - The picker uses the verbatim 繁中 strings `我司商標` and `尚無可用的商標` (Grep matches in logos.js or index.html).
  </acceptance_criteria>
  <done>
    A document-loaded user sees a side-panel thumbnail grid of the fixed logo library, can single-select a logo (accent ring marks the choice), can clear the selection via 不置入商標, and an empty/absent library shows the 繁中 empty state without blocking the flow; selecting/changing a logo invalidates a fresh Phase-2 result via the single shared stale machine; all logo strings are 繁中 and rendered via textContent; api.js is the sole server seam.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (`GET /logos`, `GET /logos/{id}/image`) | Untrusted `logo_id` path param crosses here; must never build a filesystem path |
| filesystem → API (`logos/` library) | Admin-placed PNG/JSON assets are decoded by Pillow; a malicious/oversized asset crosses into the worker |
| API → browser (logo `name`/manifest text) | Manifest display text is reflected into the picker DOM; must never become HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering / Info Disclosure | `logo.resolve(logo_id)` in `logo.py` + `GET /logos/{id}/image` | mitigate | Resolve `logo_id` ONLY as a manifest dict key (allowlist); join the admin-controlled `entry["file"]` to LOGOS_DIR; defense-in-depth `path.resolve().is_relative_to(LOGOS_DIR)` assert; unknown id → 404 `logo_not_found` (no oracle). NEVER `LOGOS_DIR / logo_id`. Mirrors `validate_session_id`/`subdir` (T-01-04). |
| T-03-02 | Denial of Service | Library PNG decode in `logo.py` (`list_logos`/`resolve`) | mitigate | `MAX_LOGO_BYTES` file-size cap before decode + Pillow dimension validation (Pillow raises `DecompressionBombError`); a bad asset is SKIPPED from the list / raises `logo_unreadable`, never crashes `GET /logos`. |
| T-03-03 | Denial of Service | Crafted PNG → parser crash in the image decode path | mitigate | Wrap per-asset load/decode in try/except → typed `LogoError`, mapped to a structured 4xx in `main.py` (mirror `PdfEngineError` handling, T-01-03); never a bare 500 that kills a worker. |
| T-03-04 | Tampering (stored XSS) | Logo `name`/manifest text reflected into the picker | mitigate | `logos.js` writes ALL logo text via `textContent`/`createElement`, never `innerHTML` (existing T-02-11 discipline); verified by the no-innerHTML grep. |
| T-02-12 (carried) | Tampering / architecture | `logos.js` could fetch the server directly | mitigate | `logos.js` calls ONLY `api.listLogos()` / `api.logoImageURL()`; the no-`fetch(` grep enforces api.js as the sole seam (embedding contract, Pattern 4). |
| T-02-03 (carried) | Licensing / architecture | `import fitz` leaking out of `pdf_engine.py` | mitigate | `logo.py` and `logos.py` are fitz-free (import only json/re/pathlib/PIL/config); the existing AST grep test (`test_fitz_import_confined_to_engine_seam`) enforces it. |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/test_logo.py -q` — LOGO-01 list + path-traversal + empty-library + bad-asset-skip all green.
- `.venv/Scripts/python.exe -m pytest -q` — full suite (140 existing + new) stays green; `test_fitz_import_confined_to_engine_seam` confirms the new modules did not leak fitz.
- Static (logos.js): no `innerHTML`, no direct `fetch(`, uses `api.listLogos`/`api.logoImageURL` (the Task 2 automated check).
- Manual visual (per-phase quality gate): load a doc → the 我司商標 side-panel section shows the seeded placeholder thumbnail on a neutral backing in BOTH light/dark themes; selecting it shows the accent ring; an empty `logos/` shows the 繁中 empty state and the rest of the flow still works.
</verification>

<success_criteria>
- LOGO-01 satisfied: a fixed logo library exists (`logos/` + `manifest.json` with ≥1 seeded PNG), `GET /logos` lists it (id+name, no paths), and the side-panel thumbnail grid lets the user browse and pick one.
- Unknown/crafted `logo_id` → 404 `logo_not_found` (never a path read, never a 500); empty/absent library → `{logos: []}` + picker empty-state (graceful degradation to pure removal).
- `logo.resolve(logo_id) -> bytes` contract and `selectedLogoId`/`getSelectedLogoId()` are in place for 03-02 to consume.
- All user-facing strings are 繁中, rendered via textContent; api.js is the sole server seam; logo.py/logos.js are fitz-free.
</success_criteria>

<output>
After completion, create `.planning/phases/03-logo-placement/03-01-SUMMARY.md`
</output>
