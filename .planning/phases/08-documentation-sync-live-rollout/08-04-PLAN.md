---
phase: 08-documentation-sync-live-rollout
plan: 04
type: execute
wave: 3
depends_on: ["08-03"]
files_modified: []
autonomous: false
requirements: [DEPLOY-01]

must_haves:
  truths:
    - "A final code-review/fix pass runs AFTER the push (D-02 step 4) and any findings are fixed + committed"
    - "All four Phase 8 requirement checkboxes (THREAT-02, DOC-01, DOC-02, DEPLOY-01) are checked before milestone close"
    - "All commits (including any post-push review fixes) are pushed to origin"
    - "/gsd-complete-milestone is run as the terminal step and creates the v1.1 tag itself (no separate/double tag task)"
  artifacts: []
  key_links:
    - from: "Phase 8 close"
      to: "/gsd-complete-milestone (archive ROADMAP/REQUIREMENTS, PROJECT.md Key Decisions audit, git tag v1.1)"
      via: "terminal milestone-close invocation; the workflow owns the tag"
      pattern: "complete-milestone"
---

<objective>
Close out v1.1: run the final code-review/fix pass (deliberately AFTER the LIVE push per D-02 step 4), ensure the requirement gate is satisfied, push everything to origin, and run /gsd-complete-milestone as the terminal step (D-05). The milestone-close workflow creates the `git tag v1.1` itself — this plan must NOT create it separately (Pitfall 2: double-tagging).

Purpose: one clean wrap-up, no loose ends (D-05). final review lands after the push because Phase 7 already passed three gates (D-02 accepted tradeoff).
Output: a reviewed, pushed v1.1 with the milestone archived and tagged by the close workflow.

KEY CONSTRAINTS:
- Do NOT add a `git tag v1.1` task — /gsd-complete-milestone's git_tag step owns it (RESEARCH section 7 + Pitfall 2). Mode is yolo so the milestone-scope confirmation auto-approves; the artifact audit still runs.
- The four Phase 8 requirement checkboxes must be checked BEFORE close or the workflow surfaces an "Unchecked Requirements" warning (RESEARCH Pitfall 4). Plan 02 Task 3 flipped THREAT-02/DOC-01/DOC-02 (and optionally DEPLOY-01); this plan verifies all four are checked and flips DEPLOY-01 if Plan 03 deferred it.
- /gsd-complete-milestone runs its OWN PROJECT.md Key Decisions audit + STATE.md rewrite (RESEARCH section 7) — let it reconcile; do not pre-duplicate the DOC-02 row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/workflows/complete-milestone.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-documentation-sync-live-rollout/08-CONTEXT.md
@.planning/phases/08-documentation-sync-live-rollout/08-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Final code-review/fix pass (D-02 step 4) + verify requirement gate</name>
  <read_first>
    - .planning/phases/08-documentation-sync-live-rollout/08-CONTEXT.md D-02 (final review AFTER push) + the carried-forward three-gate note (review/fix MUST run; validate Nyquist no-op; secure near no-op)
    - .planning/REQUIREMENTS.md (confirm THREAT-02/DOC-01/DOC-02/DEPLOY-01 checkbox + traceability state after Plans 01-03)
    - .planning/phases/08-documentation-sync-live-rollout/08-RESEARCH.md section 7 (close-workflow requirement gate) + Pitfall 4
  </read_first>
  <what-built>
    Plans 01-03 delivered the docstring/doc edits, the deploy, and the green LIVE-UAT. This step is the standard phase-boundary review/fix gate (per project memory: review/fix + validate + secure at every phase boundary). Because Phase 8 is docs + deploy, validate (Nyquist) is a config-disabled no-op and secure is expected near no-op (THREAT-02 is documentation honesty, not a new mitigation; the one genuine new risk — public-repo data exposure — was mitigated in Plan 03). The review/fix gate MUST run — it is success criterion 5's "final code-review pass".
  </what-built>
  <action>
    Run the final code-review/fix pass over the Phase 8 changes (the three docstring/comment edits + the doc edits + the .gitignore guard). Scope is intentionally narrow (minimum-change discipline / 5330290): confirm the diff touched ONLY the intended text blocks + .gitignore, that no production logic changed, that the three §1.4 non-target HONEST LIMITATION markers (pdf_engine.py:1173/1343/1497) were NOT touched, that `grep -rn "import fitz" app/` is still exactly one line, and that the three docstrings + HANDOFF 6.5 are factually accurate against the Option B implementation. Fix any finding and commit it (NEW commit, not amend). Per project memory's commit/push cadence: commit locally; the push of these fixes happens in Task 2.
    Then verify the milestone-close requirement gate is satisfied: all four Phase 8 requirement checkboxes (THREAT-02, DOC-01, DOC-02, DEPLOY-01) are `- [x]` in REQUIREMENTS.md with traceability Status = Complete. If Plan 03 deferred the DEPLOY-01 flip, flip it now (LIVE-UAT is green). Leave the ROADMAP Phase 8 line to the close workflow.
  </action>
  <how-to-verify>
    1. `git diff <pre-phase-8-ref>..HEAD --stat` shows changes confined to: app/services/pdf_engine.py + app/services/redact.py (comment/docstring regions only), HANDOFF.md, .planning/PROJECT.md, .planning/STATE.md, .planning/REQUIREMENTS.md, .gitignore — and nothing else.
    2. `grep -rn "import fitz" app/` returns exactly one line (app/services/pdf_engine.py).
    3. No diff lines at pdf_engine.py >1000 (the three non-target markers untouched).
    4. Any review finding is fixed and committed as a new commit.
    5. REQUIREMENTS.md: all four Phase 8 reqs `- [x]`; traceability rows show Complete.
  </how-to-verify>
  <acceptance_criteria>
    - Final code-review/fix pass completed; findings (if any) fixed + committed as new commits (no amend).
    - Phase 8 diff confined to the intended files; no production logic changed; three §1.4 markers untouched; AGPL seam = one import line.
    - REQUIREMENTS.md THREAT-02/DOC-01/DOC-02/DEPLOY-01 all begin `- [x]`; traceability Status Complete for all four.
    - ROADMAP Phase 8 line still `- [ ]` (left for close workflow).
  </acceptance_criteria>
  <resume-signal>Reply "review clean, four reqs checked" (note any fix commits), or describe findings.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Push origin + run /gsd-complete-milestone (terminal, owns the v1.1 tag)</name>
  <read_first>
    - $HOME/.claude/get-shit-done/workflows/complete-milestone.md (the close workflow: artifact audit, archive ROADMAP+REQUIREMENTS, PROJECT.md evolution review, ROADMAP reorg, git rm REQUIREMENTS.md, STATE/MILESTONES/RETROSPECTIVE update, git_tag v1.1, push-tag prompt)
    - .planning/phases/08-documentation-sync-live-rollout/08-CONTEXT.md D-05 (scope extends through milestone close) + D-02 step 5
    - .planning/phases/08-documentation-sync-live-rollout/08-RESEARCH.md section 7 (mode yolo auto-approves scope; tag owned by workflow; commit_docs true; branching none) + Pitfall 2 (no double-tag)
  </read_first>
  <what-built>
    Task 1 finished the final review/fix with all commits local. This step pushes everything to origin and runs the milestone-close workflow as the terminal action of the phase (D-05).
  </what-built>
  <action>
    PUSH (D-02 step 5 / success criterion 5): push master to origin so all commits (including any Task-1 review fixes made after the Plan-03 deploy push) are on the remote. `git push origin master` (fast-forward; NO force-push). If the Task-1 fixes were trivial/none and master was already fully pushed in Plan 03, this is a no-op or a small fast-forward — confirm `git status` shows "up to date with origin/master" afterward.
    RUN MILESTONE CLOSE (D-05, terminal): run `/gsd-complete-milestone`. Mode is yolo so the milestone-scope confirmation auto-approves; the pre-close artifact audit still runs and shows output. The workflow will: audit open artifacts, archive ROADMAP.md + REQUIREMENTS.md to `.planning/milestones/v1.1-*.md`, run a full PROJECT.md Key Decisions evolution review (it will reconcile the DOC-02 Option B row added in Plan 02 — do NOT add a duplicate), reorganize ROADMAP.md with milestone grouping (preserving Backlog), `git rm REQUIREMENTS.md`, update STATE/MILESTONES/RETROSPECTIVE, and create `git tag -a v1.1` (the workflow OWNS this tag — do NOT create it yourself; Pitfall 2). When it asks "Push tag to remote? (y/n)" answer yes so the v1.1 tag reaches origin.
    DO NOT create `git tag v1.1` in any task — only /gsd-complete-milestone creates it. There must be exactly one tag-creation point.
  </action>
  <how-to-verify>
    1. After `git push origin master`: `git status -sb` shows the branch up to date with origin/master (nothing ahead).
    2. /gsd-complete-milestone runs to completion: REQUIREMENTS.md archived (and git rm'd), ROADMAP.md reorganized with v1.1 grouped under Milestones, STATE.md/MILESTONES.md/RETROSPECTIVE.md updated.
    3. `git tag` lists `v1.1` (created by the workflow, alongside the existing v1.0) — and it was created exactly once.
    4. The v1.1 tag was pushed to origin (`git ls-remote --tags origin` shows v1.1).
  </how-to-verify>
  <acceptance_criteria>
    - `git status -sb` shows master up to date with origin/master after the push (all Phase 6+7+8 commits + any review fixes on the remote).
    - /gsd-complete-milestone completed: REQUIREMENTS.md git-rm'd + archived to `.planning/milestones/v1.1-*.md`; ROADMAP.md reorganized (v1.1 under Milestones, Backlog preserved); STATE/MILESTONES/RETROSPECTIVE updated.
    - `git tag` shows `v1.1` created exactly once BY the close workflow (no double-tag; no Phase 8 task created it).
    - `git ls-remote --tags origin` shows `v1.1` pushed.
  </acceptance_criteria>
  <resume-signal>Reply "milestone closed, v1.1 tagged + pushed" (confirm tag created once by the workflow), or describe any close-workflow issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo -> PUBLIC GitHub (final push + tag push) | Same boundary as Plan 03. Any post-review fix commits + the v1.1 tag cross to the public remote — must carry no sensitive data (Plan 03 already retired scratch/supplier artifacts; Task 1 re-confirmed diff scope). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Integrity (double-tag / divergent tag) | `git tag v1.1` | mitigate | Exactly one tag-creation point: /gsd-complete-milestone's git_tag step. No Phase 8 task creates the tag (Pitfall 2). Acceptance criterion verifies the tag was created once by the workflow. |
| T-08-09 | Information disclosure (late leak via review-fix commit) | post-review fix commits pushed to public origin | mitigate | Task 1 re-verifies the Phase 8 diff is confined to intended text/.gitignore (no sensitive data); Plan 03 already retired all scratch/supplier artifacts and proved `git ls-files` clean. The final push carries only reviewed doc/comment changes. |
| T-08-10 | Integrity (force-push to public master) | `git push origin master` (final) | accept | Fast-forward only; NO force-push (CLAUDE.md / git safety). master is strictly ahead of origin. Low risk. |
</threat_model>

<verification>
- Final code-review/fix pass ran; findings fixed + committed (new commits); Phase 8 diff confined to intended files; AGPL seam intact; three §1.4 markers untouched.
- All four Phase 8 requirement checkboxes checked before close.
- master pushed to origin (fast-forward); branch up to date.
- /gsd-complete-milestone run as terminal step: archives, reorganizes, updates tracking docs, and creates + pushes the v1.1 tag exactly once.
- No double-tag; no separate tag task.
</verification>

<success_criteria>
Maps to ROADMAP Phase 8 success criterion 5: v1.1 milestone close — final code-review pass completed, all pushes on origin, `git tag v1.1` set (by the close workflow). Completes D-05 (Phase 8 scope extends through milestone close, one clean wrap-up).
</success_criteria>

<output>
After completion, create `.planning/phases/08-documentation-sync-live-rollout/08-04-SUMMARY.md`
</output>
