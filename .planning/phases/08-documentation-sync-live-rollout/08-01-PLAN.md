---
phase: 08-documentation-sync-live-rollout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/pdf_engine.py
  - app/services/redact.py
autonomous: true
requirements: [THREAT-02]

must_haves:
  truths:
    - "pdf_engine.py:933 LIMITATION block no longer claims content-stream deletion is a future candidate"
    - "redact.py module-level TRUE_REMOVAL_LIMITATION prose no longer describes Option B as a future/candidate hotfix"
    - "redact.py dispatcher inline HONEST LIMITATION comment no longer says deletion is deferred to Option B"
    - "All three blocks now state Option B has truly deleted page-level zero-area sources upstream; overlay is last-mile defence only"
    - "AGPL fitz seam unchanged: import fitz appears in exactly one app/ file"
    - "No production logic changed — only docstring/comment text edited"
  artifacts:
    - path: "app/services/pdf_engine.py"
      provides: "replace_region_with_white_raster docstring rewritten (Option-B-accurate)"
      contains: "last-mile"
    - path: "app/services/redact.py"
      provides: "module TRUE_REMOVAL_LIMITATION prose + dispatcher HONEST LIMITATION comment rewritten"
      contains: "Option B"
  key_links:
    - from: "redact.py TRUE_REMOVAL_LIMITATION + dispatcher comment"
      to: "pdf_engine.log_xobject_intersect / delete_zero_area_type_f_fills_inside contract"
      via: "honest framing aligned to the SOURCE-OF-TRUTH markers at pdf_engine.py:1497/1343"
      pattern: "log_xobject_intersect|delete_zero_area_type_f_fills_inside"
---

<objective>
Rewrite the THREE — and only three — "honest limitation" docstring/comment blocks so they reflect the post-Phase-7 reality: Option B (`delete_zero_area_type_f_fills_inside`) now runs UPSTREAM and truly deletes page-level zero-area `type='f'` CAD-glyph sources from the content stream BEFORE any overlay fires. The overlay (Option A) is now a last-mile defence only — for form-XObject-internal residue (page-level-only strategy, logged not deleted) and regex-miss fail-safe cases.

Purpose: THREAT-02 — the three blocks currently contain a now-FALSE claim ("True deletion ... requires content-stream surgery — a candidate hotfix / future iteration / Option B / #07"). Option B shipped in Phase 7. Leaving the false framing misleads colleagues/legal/maintainers about the actual threat posture.
Output: Three edited text blocks in `pdf_engine.py` and `redact.py`. Zero logic change. AGPL seam untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-documentation-sync-live-rollout/08-CONTEXT.md
@.planning/phases/08-documentation-sync-live-rollout/08-RESEARCH.md

<interfaces>
<!-- The factual SOURCE OF TRUTH the three rewrites must align TO (do NOT edit these — already Option-B-accurate, see RESEARCH §1.4). -->

Option B behaviour the rewrites must describe accurately (RESEARCH §6):
- `pdf_engine.delete_zero_area_type_f_fills_inside(page, rect)` — page-level content-stream surgery; TRULY deletes fully-inside-rect zero-area type='f' paths; returns 0 on no-op (SEC-02) or cardinality fail-safe (logger.warning "option_b_parse_anomaly", stream UNTOUCHED).
- `pdf_engine.log_xobject_intersect(page, rect, logger)` — page-level-only strategy: does NOT descend into form-XObject internal streams; emits logger.warning "option_b_xobject_intersect" on intersect; never mutates. (This is the SOURCE-OF-TRUTH the targets align to.)
- Dispatcher wiring: redact.py:213 `deleted = pdf_engine.delete_zero_area_type_f_fills_inside(page, rect)` runs BEFORE the dense/sparse dispatcher (which is downstream, 0 deletions, unchanged).
- `_DISALLOWED_IN_BLOCK` (pdf_engine.py:451): over-delete guard — co-located Do/BT/sh/BI → candidate skipped → fail-safe → Option A overlay last-mile.

DO-NOT-TOUCH (scope creep guard — RESEARCH §1.4, Pitfall 3): the OTHER three "HONEST LIMITATION" markers at pdf_engine.py:1173, 1343, 1497 are inside the Phase-7 Option B helpers and are ALREADY Option-B-accurate. A diff that touches pdf_engine.py lines >1000 is a red flag.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite pdf_engine.py replace_region_with_white_raster LIMITATION block (THREAT-02 target 1)</name>
  <read_first>
    - app/services/pdf_engine.py (read lines 903-949 — the function + the LIMITATION block at 933-948)
    - .planning/phases/08-documentation-sync-live-rollout/08-RESEARCH.md §1 target 1 (exact before-text + rewrite note) and §6 (Option B facts)
    - .planning/phases/08-documentation-sync-live-rollout/08-CONTEXT.md Claude's Discretion (rewrite DIRECTION is fixed by success criterion 1)
  </read_first>
  <action>
    In `replace_region_with_white_raster`'s docstring, rewrite ONLY the "LIMITATION (be honest)" block at lines 933-948. Replace the false closing framing — currently "True deletion of zero-area sources requires content-stream surgery (a candidate hotfix for a future iteration if higher assurance is required)" — with the Option-B-accurate framing per success criterion 1 (CONTEXT.md D Discretion): this overlay is now a LAST-MILE defence; Option B (`delete_zero_area_type_f_fills_inside`) runs UPSTREAM in `remove_region_vector` and has already truly deleted page-level zero-area sources from the content stream before this branch can fire; this overlay remains relevant only for form-XObject-internal residue (page-level-only strategy, NOT deleted — logged via `log_xobject_intersect`) and Option B regex-miss / `_DISALLOWED_IN_BLOCK` fail-safe cases. Keep the factually-still-true sentences (zero-area BLACK sources remain when THIS branch fires because it only fires when Option B could not reach them). Edit text only — do NOT change `q = fitz.Rect(...)` or any code below line 949. Follow the surrounding bilingual (English + zh-TW comment) convention. minimum-change discipline: touch only this block.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python -c "import ast,sys; ast.parse(open('app/services/pdf_engine.py',encoding='utf-8').read()); print('parse-ok')" && grep -c "candidate hotfix for a future iteration" app/services/pdf_engine.py</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import ast; ast.parse(...)"` on pdf_engine.py prints `parse-ok` (file still valid Python — docstring edit didn't break syntax).
    - `grep -c "candidate hotfix for a future iteration" app/services/pdf_engine.py` returns `0` (the false phrase is gone).
    - The rewritten block contains the words "last-mile" and references that Option B runs upstream / page-level (e.g. `grep -n "last-mile" app/services/pdf_engine.py` matches within the 933-949 range).
    - `git diff --stat app/services/pdf_engine.py` shows changes confined to the ~933-948 region; NO lines >1000 touched (the three §1.4 non-target markers untouched).
  </acceptance_criteria>
  <done>The replace_region_with_white_raster LIMITATION block honestly states Option B already deleted page-level sources upstream and the overlay is last-mile-only; no code changed; file parses.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite redact.py module TRUE_REMOVAL_LIMITATION prose + dispatcher HONEST LIMITATION comment (THREAT-02 targets 2 & 3)</name>
  <read_first>
    - app/services/redact.py (read lines 1-36 = module docstring TRUE_REMOVAL_LIMITATION prose; lines 205-260 = Option B wiring at 213 + dispatcher HONEST LIMITATION comment at 245-252)
    - .planning/phases/08-documentation-sync-live-rollout/08-RESEARCH.md §1 targets 2 & 3 (exact before-text + rewrite notes) and §6
    - .planning/phases/08-documentation-sync-live-rollout/08-CONTEXT.md Claude's Discretion
  </read_first>
  <action>
    Two edits in `redact.py`, text only:
    (a) Module docstring prose `TRUE_REMOVAL_LIMITATION` (lines 6-36 — this is DOCSTRING PROSE, NOT a Python string constant; do not turn it into an assignment): rewrite the closing claim "True deletion of zero-area sources requires content-stream surgery (a candidate hotfix #07 / Option B if higher assurance is required)" to state that Option B (`pdf_engine.delete_zero_area_type_f_fills_inside`) NOW runs in `remove_region_vector` BEFORE the dense/sparse dispatcher and truly deletes page-level zero-area `type='f'` sources from the content stream; the OVERLAY described above now remains a last-mile defence only for (1) form-XObject-internal residue Option B does not descend into (page-level-only, logged) and (2) regex-miss / `_DISALLOWED_IN_BLOCK` fail-safe cases. Keep the historical hotfix-#06 context accurate. May keep or update the cross-reference to the hotfix-06 analysis dir.
    (b) Dispatcher inline comment `HONEST LIMITATION` (lines 245-252): rewrite the last sentence "True content-stream deletion of zero-area sources is deferred to a future content-stream-surgery hotfix (Option B / #07)" to state that page-level zero-area sources have ALREADY been truly deleted upstream by Option B (`delete_zero_area_type_f_fills_inside`, redact.py:213); this dense branch's overlay is now a last-mile defence for form-XObject-internal residue + regex-miss fail-safe. Keep the existing "mirrors replace_region_with_white_raster's docstring and the module-level TRUE_REMOVAL_LIMITATION note" cross-reference (it stays accurate after Task 1 + (a)).
    Do NOT change any executable line (205-220 Option B wiring, 257+ dispatcher logic). minimum-change: edit only these two prose blocks.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python -c "import ast; ast.parse(open('app/services/redact.py',encoding='utf-8').read()); print('parse-ok')" && grep -c "deferred to a future" app/services/redact.py && grep -c "candidate\nhotfix #07" app/services/redact.py</automated>
  </verify>
  <acceptance_criteria>
    - redact.py parses (`parse-ok`).
    - `grep -c "deferred to a future" app/services/redact.py` returns `0` (dispatcher comment false framing gone).
    - `grep -c "a candidate" app/services/redact.py` returns `0` (module prose "candidate hotfix #07 / Option B" framing gone).
    - Both rewritten blocks reference Option B as SHIPPED/upstream (e.g. `grep -n "upstream\|last-mile\|truly delete" app/services/redact.py` matches in both the 6-36 and 245-252 regions).
    - `git diff app/services/redact.py` shows NO change to executable lines 205-220 or to the dispatcher logic at 257+ (only comment/docstring prose changed).
    - `grep -rn "import fitz" app/` returns exactly one line (`app/services/pdf_engine.py`) — AGPL seam intact, this plan added no fitz import to redact.py.
  </acceptance_criteria>
  <done>Both redact.py honesty blocks state page-level deletion already happened upstream via Option B; overlay is last-mile-only; no logic changed; AGPL seam intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new this plan) | Docstring/comment text edits only. No new input surface, no data flow, no crypto, no auth change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information disclosure (documentation integrity) | The three honesty docstrings | mitigate | This plan IS the mitigation: the false "deletion is a future candidate" framing under-states the shipped defence; rewriting to Option-B-accurate prevents a maintainer from re-deferring already-shipped protection. No code-level threat surface change (THREAT-02 is documentation honesty, not a new mitigation — RESEARCH Security Domain). |
| T-08-02 | Tampering (accidental over-edit / scope creep) | pdf_engine.py lines >1000 (the 3 non-target HONEST LIMITATION markers) | accept | Guarded by acceptance criterion: `git diff --stat` must show no lines >1000 touched. Editing the §1.4 markers would mis-state the Option B helper's own fail-safe contract. Low risk given the explicit guard. |
</threat_model>

<verification>
- pdf_engine.py and redact.py both parse as valid Python (no syntax break from docstring edits).
- All three false-framing phrases removed (the three greps return 0).
- AGPL seam: `grep -rn "import fitz" app/` returns exactly one line.
- No executable line changed in either file (diff confined to comment/docstring regions).
- The three §1.4 non-target markers (pdf_engine.py:1173/1343/1497) untouched.
</verification>

<success_criteria>
Maps to ROADMAP Phase 8 success criterion 1: three LIMITATION blocks synchronized from "needs delete-image-XObject + per-path bbox surgery attack" framing → "Option B closed the page-level zero-area source path; form-XObject internals remain Option A overlay-only (logged)". Exactly three blocks edited; zero logic change; AGPL seam intact.
</success_criteria>

<output>
After completion, create `.planning/phases/08-documentation-sync-live-rollout/08-01-SUMMARY.md`
</output>
