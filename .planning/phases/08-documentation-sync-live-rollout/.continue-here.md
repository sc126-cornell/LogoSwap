---
context: phase
phase: 08-documentation-sync-live-rollout
plan: 3
task: 2
total_tasks: 3
status: paused
last_updated: 2026-05-28T17:20:23.703Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These gate resumption. Acknowledge before proceeding.

- [ ] CONSTRAINT: **UAT-pass is NOT yet confirmed** — it is gated on the colleague's Adobe Illustrator verification of all 4 output files (F1–F4), scheduled ~2026-05-30. Do NOT declare UAT passed, do NOT start Plan 03 Task 3, and do NOT run milestone-close based on my scripted content-stream checks alone. Illustrator is the authoritative truth-test; my `_deep_check.py` + render crops are supporting evidence only.

**Do not proceed past the wait until the colleague's Illustrator result is in hand.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Auto-advancing past UAT on script/render results | Declaring PASS or running Plan 03 Task 3 / milestone-close before the colleague's Illustrator recovery test. My checks predict what Illustrator will see; they are not the verdict. | blocking | Resuming agent MUST obtain the colleague's per-file Illustrator result on F1–F4 before any further plan step. |
| Misreading deep_check region numbers on raster/multi-change files | deep_check's auto-region is a single bbox of ALL changed pixels; on F4 (raster) it spanned the page, making the region look 93% white / 4% dark — that 4% was adjacent RETAINED legit content (title-block text + borders), not residual mark. | advisory | For raster / multi-change files, isolate "orig-dark → out-light" pixels and measure the supplier-logo band specifically (see `_f4_crop.py`), not the whole bbox. |
| deep_check cp950 crash on non-cp950 region text | `_deep_check.py` step 5 (`print(get_textbox)`) raised `UnicodeEncodeError` on F4's garbage OCR text layer under Windows cp950 stdout. | advisory | Wrap stdout as utf-8 (`io.TextIOWrapper(..., encoding='utf-8', errors='replace')`, as in `_f4_struct.py` / `_f4_crop.py`) or set `PYTHONIOENCODING=utf-8`. |

<current_state>
Paused mid **Plan 03 → Task 2** (the LIVE-UAT substep), at the user's request, to wait for the colleague's Illustrator check before confirming UAT.

LIVE is up (Zeabur, /health 200, footer = sc126-cornell). master is pushed. Content-stream `_deep_check.py` PASSED on all four UAT files this far (F1, F2 prior session; F3, F4 this session). The user explicitly wants the colleague to verify F1–F4 in Illustrator (~2026-05-30) before UAT is declared passed.
</current_state>

<completed_work>

Completed Tasks:
- Plan 03 Task 1: harden .gitignore against scratch/supplier-PDF leak — Done (commit 946da28). Verified: `08-live-uat/` scratch + `*-supplier-raw.pdf` are git-ignored anywhere under the scratch tree; 0 tracked under `08-live-uat/`; archived forensic evidence dir stays tracked.
- Plan 03 Task 2: single push + LIVE deploy + LIVE-UAT — In progress. Push done (master on origin); Zeabur LIVE confirmed (/health 200, section-13 footer = sc126-cornell). LIVE-UAT per-file content-stream verification:
  - F1, F2: PASS (prior session). NB: F2 top-left red stamp was left unframed (still present).
  - F3 `B-3012IP-WM02-T430`: PASS — dCt/DAN-CHIEF logo truly removed (vector; region empty 255.0 white; 0 images, 0 white fills, zero-area type='f' ==0). No white cover at all.
  - F4 `3013A-19-C3-W4-3D02-A01-00071`: PASS — DCE/NINGBO DAN-CHIEF NETWORK truly removed (RASTER; 7566 logo px → 100.0% white 255.0; single page image, no overlay → pixels rewritten not covered).
</completed_work>

<remaining_work>

- WAIT (blocking): colleague's Illustrator verification of F1–F4 (~2026-05-30). UAT-pass gated on this.
- Plan 03 Task 3: D-04 one-off attack-sim scratch script (formal two-gate: render ≥98% white AND zero-area type='f' ==0) → RETIRE scratch (delete supplier PDFs/scripts/attacked.pdf, prove `git ls-files .planning/debug/scratch/` clean) → flip DEPLOY-01 checkbox → write `08-03-SUMMARY.md`.
- Plan 04 Task 1: final code-review/fix (narrow scope: only doc/comment/.gitignore changed; `grep -rn "import fitz" app/` still 1 line; the 3 §1.4 HONEST-LIMITATION markers at pdf_engine.py:1173/1343/1497 untouched) + confirm 4 req checkboxes (THREAT-02/DOC-01/DOC-02/DEPLOY-01).
- Plan 04 Task 2: `git push origin master` + run `/gsd-complete-milestone` (archives ROADMAP/REQUIREMENTS, creates + pushes `git tag v1.1`; the workflow OWNS the tag — do NOT create it separately).
</remaining_work>

<decisions_made>

- Gate UAT-pass on the colleague's Illustrator batch verification, not on my scripted checks alone (Illustrator = authoritative truth-test).
- F3 truly removed (vector, region empty); F4 truly removed (raster, pixel-level, single image no overlay).
- Unframed marks (red stamps on F2/F3/F4, bottom-right approval stamps on F3/F4, "All Copy Rights Reserved") are OUT OF SCOPE for the UAT verdict — they were not framed. Illustrator finding them is NOT a tool failure; to remove them, re-frame and re-process.
</decisions_made>

<blockers>
- UAT-pass confirmation blocked on colleague Illustrator verification of F1–F4 (~2026-05-30). No workaround — this is the authoritative gate by design.
</blockers>

## Required Reading (in order)
1. `.planning/phases/08-documentation-sync-live-rollout/08-03-PLAN.md` — current plan; Task 3 (D-04 attack-sim + retire scratch + flip DEPLOY-01) is the next executable step once UAT is confirmed.
2. `.planning/phases/08-documentation-sync-live-rollout/08-04-PLAN.md` — milestone close (final review + `/gsd-complete-milestone` owns the v1.1 tag; no double-tag).
3. `.planning/phases/08-documentation-sync-live-rollout/08-CONTEXT.md` — D-03 (real un-sanitized supplier PDF, not a fixture) + D-04 (one-off scratch, import existing attack helpers, retire after) + D-05 (phase scope extends through milestone close).
4. memory `feedback_illustrator_verification` (`~/.claude/.../memory/`) — Illustrator is the authoritative UAT gate; the colleague performs it on the batch.
5. `.planning/STATE.md` — current position + accumulated decisions.

## Critical Anti-Patterns (do NOT repeat these)
- [ANTI-PATTERN]: Declaring UAT PASS / advancing plans on scripted checks alone → structural mitigation: wait for the colleague's Illustrator result on all 4 files first.
- [ANTI-PATTERN]: Trusting deep_check's single-bbox region on raster/multi-change files → measure the supplier-logo band specifically (`_f4_crop.py` isolation approach).
- [ANTI-PATTERN]: Running deep_check with default cp950 stdout → wrap stdout utf-8 / set `PYTHONIOENCODING=utf-8`.

## Infrastructure State
- LIVE: Zeabur — https://logoswap.scottchen0622.com (deployed; /health 200; section-13 footer renders sc126-cornell). master pushed to origin (Plan 03 Task 2).
- Scratch: `.planning/debug/scratch/08-live-uat/` — **gitignored**; holds UAT diagnostics (`_deep_check.py`, `_f3_*`, `_f4_*` scripts + crops, `_attacked*.pdf`). MUST NOT be committed; Task 3 retires them. Verified 0 tracked under `08-live-uat/`.
- UAT supplier PDFs live OUTSIDE the repo: F3 `C:\Users\scott\Downloads\test 3\`, F4 `C:\Users\scott\Downloads\test 4\` (output = `*_logoswap*.pdf`, original = same name without the suffix). Real un-sanitized supplier files — keep out of repo.
- No local background processes running (LIVE runs on Zeabur, not a local server).
- Uncommitted (NOT part of this WIP commit; owned by milestone-close reconciliation): `.planning/STATE.md`, `.planning/config.json`.

<context>
Per-file UAT loop: user processes a file on LIVE (upload → 框選 → process → download), hands me output + original paths, I run `python .planning/debug/scratch/08-live-uat/_deep_check.py <ORIG> <OUT> <page> <tag>` from repo root and report PASS/FAIL on the content-stream gates + flag any unframed supplier marks. Four files done (all PASS on my side). Now waiting on the human Illustrator gate. When that's green, the path to done is short: Plan 03 Task 3 → Plan 04 (final review → /gsd-complete-milestone tags v1.1). Phase 8 is the last phase of milestone v1.1.
</context>

<next_action>
Start with: ask the user for the colleague's Illustrator verification result on F1–F4.
- If ALL pass (no supplier logo recoverable in the framed regions) → proceed to Plan 03 Task 3 (write the D-04 attack-sim two-gate script, run it green, retire scratch, flip DEPLOY-01, write 08-03-SUMMARY.md).
- If ANY file fails → `/gsd-debug` that file (likely an Option B blind spot, e.g. a zero-area source inside a form XObject — SEC-03 deferred).
Do NOT auto-advance to milestone close before the Illustrator sign-off.
</next_action>
