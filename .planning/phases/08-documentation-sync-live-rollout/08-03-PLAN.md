---
phase: 08-documentation-sync-live-rollout
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - .gitignore
autonomous: false
requirements: [DEPLOY-01]
user_setup:
  - service: zeabur
    why: "LIVE deploy target — Zeabur watches the GitHub repo and auto-builds the Dockerfile on push to master"
    dashboard_config:
      - task: "Confirm the Zeabur project is connected to github.com/sc126-cornell/LogoSwap and auto-deploy on push is enabled (fallback: local `docker compose up` if Zeabur is down — DEPLOY-01 allows it)"
        location: "Zeabur dashboard -> LogoSwap project -> Deployments"

must_haves:
  truths:
    - "Before the push, no un-sanitized supplier PDF and no D-04 scratch script are staged/committed to the public repo"
    - "master is pushed once to origin (D-01 scoped push exception; D-02 step 2), triggering a Zeabur build of the Dockerfile (python:3.12-slim-bookworm)"
    - "The LIVE site serves the new build (GET /health returns 200) and the AGPL section-13 footer renders the real owner (sc126-cornell), not the <OWNER> literal"
    - "A real un-sanitized CAD-glyph supplier PDF (D-03 — engineer-provided, NOT a repo fixture) completes upload -> 框選 -> process -> download end-to-end on the LIVE site"
    - "The D-04 one-off attack-sim scratch script (importing tests/_illustrator_attack.py) runs against the downloaded file and passes BOTH gates: render region >= 98.0% white AND zero-area type='f' count == 0 in the selected region"
    - "After the run, the scratch script + the real supplier PDF + any attacked.pdf are NOT in the repo (deleted or gitignored)"
  artifacts:
    - path: ".gitignore"
      provides: "scratch-dir-anchored ignore guarding the D-04 script + supplier PDF from the public repo"
      contains: ".planning/debug/scratch"
  key_links:
    - from: "D-04 scratch script"
      to: "tests/_illustrator_attack.py (delete_image_xobjects_intersecting + render_region_white_pct + count_zero_area_fills_in_region)"
      via: "import of the existing battle-tested attack helpers"
      pattern: "from tests._illustrator_attack import"
    - from: "git push origin master"
      to: "Zeabur build (Dockerfile autodetect)"
      via: "Zeabur watches the repo; push triggers build"
      pattern: "push.*origin.*master"
---

<objective>
Deploy v1.1 to LIVE and prove the Option B protection end-to-end on a real supplier sample — without leaking sensitive/transient artifacts to the PUBLIC GitHub repo.

This is DEPLOY-01 and follows the D-02 fixed order strictly: it runs ONLY after all doc/docstring edits (Plans 01 + 02) are committed locally. Then: (1) guard the repo against the privacy landmine, (2) ONE push deploys to Zeabur, (3) LIVE-UAT with the engineer's ORIGINAL un-sanitized supplier PDF, (4) a one-off attack-sim scratch script verifies the downloaded file, then retires.

Purpose: prove on a real CAD-glyph PDF, on the actual production runtime, that an Illustrator-class attacker who deletes the image XObject overlay can NO LONGER recover the supplier mark (Option B truly deleted the page-level sources upstream).
Output: a verified LIVE deploy + a green attack-sim run. The ONLY committed artifact is a .gitignore hardening line. The scratch script, supplier PDF, and attacked.pdf never enter the repo.

CRITICAL PRIVACY LANDMINE (RESEARCH finding #2 / Runtime State Inventory / Pitfall 1): `.planning/debug/scratch/**` IS git-tracked — the underscore-prefix ignores (`/_*.py` etc.) are ROOT-anchored only and do NOT apply inside the scratch dir. This repo is PUBLIC (AGPL section 13). A scratch script or a real supplier PDF left in the scratch dir WILL be pushed publicly. This plan makes the exclusion a BLOCKING acceptance criterion, not a footnote.

D-01 is a SCOPED cadence exception: this phase deliberately pushes during UAT (overriding the normal "commit local, never push" rule). It does not spill to other phases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-documentation-sync-live-rollout/08-CONTEXT.md
@.planning/phases/08-documentation-sync-live-rollout/08-RESEARCH.md
@.gitignore
@Dockerfile

<interfaces>
<!-- The D-04 scratch script imports these existing helpers (RESEARCH section 5). Do NOT re-implement attack logic. -->

from tests._illustrator_attack import (
    delete_image_xobjects_intersecting,   # (doc: fitz.Document, page_index: int, rect: (x0,y0,x1,y1) PDF-pts) -> int  (simulate Illustrator: delete intersecting image XObjects)
    render_region_white_pct,              # (pdf_path, page_index: int, rect) -> float [0..100]  (gate a: >= 98.0)
    count_zero_area_fills_in_region,      # (pdf_path, page_index: int, rect) -> int  (gate b: == 0; delegates to production count_zero_area_fills_fully_inside)
)
# import fitz is allowed in tests/ + scratch (AGPL AST guard scopes only app/**/*.py — _illustrator_attack.py:9-14).

# The two gates (test_illustrator_attack_regression.py:174-185):
#   assert white_pct >= 98.0
#   assert zero_area_count == 0

# D-04 scratch flow (RESEARCH section 5, skipping ingest/process which happen on LIVE via browser):
#   doc = fitz.open(downloaded.pdf)
#   delete_image_xobjects_intersecting(doc, page_index, region_pdf_pts)
#   doc.save(attacked.pdf, garbage=4, deflate=True)
#   render_region_white_pct(attacked.pdf, page_index, region_pdf_pts)        # gate a
#   count_zero_area_fills_in_region(attacked.pdf, page_index, region_pdf_pts) # gate b

# Deploy facts (RESEARCH section 4): no zeabur.json/zbpack.json -> Zeabur autodetects Dockerfile.
#   ARG GITHUB_OWNER=sc126-cornell (matches remote; sed bakes section-13 <OWNER> -> correct URL, no override needed).
#   Both Dockerfile stages: python:3.12-slim-bookworm. $PORT injected by Zeabur. GET /health = 5-field health check.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden .gitignore against the scratch / supplier-PDF leak (privacy landmine guard)</name>
  <read_first>
    - .gitignore (read lines 28-75 — note the root-anchored underscore guards at 36-38 do NOT cover .planning/debug/scratch/**, and the already-working non-root-anchored *-supplier-raw.pdf at line 75)
    - .planning/phases/08-documentation-sync-live-rollout/08-RESEARCH.md Runtime State Inventory (last row) + Pitfall 1 + finding #2
  </read_first>
  <action>
    Add a working ignore so the D-04 scratch script (and any transient PDF) under `.planning/debug/scratch/` for THIS phase's UAT session cannot be committed to the public repo. Add a scratch-session-anchored pattern (e.g. ignore the specific Phase-8 UAT scratch session dir such as `.planning/debug/scratch/08-live-uat/` and/or `.planning/debug/scratch/**/_*.py` and `.planning/debug/scratch/**/*-supplier-raw.pdf`) — the goal is that the D-04 script + supplier PDF + attacked.pdf are ignored ANYWHERE under the scratch tree, while preserving the existing tracked archived forensic evidence dir (`.planning/debug/scratch/illustrator-attack-2026-05-28-archived/`) which is intentionally committed. Verify the chosen pattern actually matches with `git check-ignore`. The existing `*-supplier-raw.pdf` (line 75, non-root-anchored) already ignores any file with that suffix anywhere — instruct (in this plan + Task 2's operator notes) that the LIVE-UAT input PDF be named with the `-supplier-raw.pdf` suffix and the scratch script be placed in the gitignored session dir.
    minimum-change: add only the needed ignore lines with a brief zh-TW comment matching the file's style; do NOT remove or restructure existing patterns or untrack the archived evidence dir.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && touch .planning/debug/scratch/08-live-uat/_probe.py 2>/dev/null; mkdir -p .planning/debug/scratch/08-live-uat && touch .planning/debug/scratch/08-live-uat/_probe.py .planning/debug/scratch/08-live-uat/x-supplier-raw.pdf && git check-ignore .planning/debug/scratch/08-live-uat/_probe.py .planning/debug/scratch/08-live-uat/x-supplier-raw.pdf; rm -f .planning/debug/scratch/08-live-uat/_probe.py .planning/debug/scratch/08-live-uat/x-supplier-raw.pdf</automated>
  </verify>
  <acceptance_criteria>
    - `git check-ignore .planning/debug/scratch/08-live-uat/_probe.py` prints the path (the probe scratch script IS ignored).
    - `git check-ignore .planning/debug/scratch/08-live-uat/x-supplier-raw.pdf` prints the path (a supplier-raw PDF IS ignored).
    - `git status --porcelain .planning/debug/scratch/illustrator-attack-2026-05-28-archived/` shows the archived forensic README/PNGs are STILL tracked (the new pattern did not untrack the intentional evidence dir).
    - `git ls-files .planning/debug/scratch/ | grep -c "_probe\|supplier-raw"` returns 0 (no probe artifact tracked).
  </acceptance_criteria>
  <done>The scratch session dir + supplier-raw PDFs are reliably gitignored anywhere under the scratch tree, the existing archived evidence dir stays tracked, and the guard is proven with git check-ignore.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: BLOCKING prerequisite + single push deploy + LIVE-UAT end-to-end (D-02 step 2-3, D-03)</name>
  <read_first>
    - .planning/phases/08-documentation-sync-live-rollout/08-CONTEXT.md D-01/D-02/D-03 (scoped push exception; fixed order; original un-sanitized supplier PDF, NOT a repo fixture; sample acquisition is a blocking prerequisite)
    - .planning/phases/08-documentation-sync-live-rollout/08-RESEARCH.md section 4 (deploy mechanics, 3.12 verification, /health, section-13 footer), Environment Availability (engineer's real PDF = no fallback; Zeabur fallback = local Docker), Open Question 1 (capture the region PDF-point rect)
  </read_first>
  <what-built>
    Plans 01 + 02 produced all doc/docstring edits, committed locally. Task 1 hardened .gitignore. This step performs the real-world deploy + manual LIVE-UAT — work only a human can do (supply the sensitive PDF, push, drive the live browser UI, capture the region rect).
  </what-built>
  <action>
    BLOCKING PREREQUISITE (D-03, no fallback): obtain a fresh REAL un-sanitized supplier CAD-glyph PDF from the engineer. A repo fixture is explicitly NOT acceptable. Do not proceed past pre-push verification until this file is in hand. Keep it OUTSIDE the repo tree, or if placed under the scratch session dir name it `*-supplier-raw.pdf` so Task 1's guard ignores it. If the engineer cannot supply it, STOP and report blocked.

    PRE-PUSH SAFETY VERIFICATION (operator, blocking): run `git status` and `git ls-files .planning/debug/scratch/`. Confirm NO un-sanitized supplier PDF and NO D-04 scratch script are staged or tracked. `master` is ~62 commits ahead of origin — this single push carries Phase 6 fixtures + Phase 7 Option B + Phase 8 docs and is v1.1's FIRST production deploy. Eyeball that nothing sensitive (real supplier data, fixtures-that-should-not-ship) is about to go public.

    PUSH (D-02 step 2, D-01 scoped exception): `git push origin master` (fast-forward; NO force-push). This triggers Zeabur to autodetect + build the Dockerfile.

    POST-DEPLOY VERIFY: wait for the Zeabur build to finish, then confirm the LIVE site serves the new build:
      - `GET https://logoswap.scottchen0622.com/health` returns 200 (5-field check). Inspect the payload shape; if it exposes a Python/runtime version, confirm 3.12. If not, the 3.12 guarantee is structural (Dockerfile pin both stages) + confirmed by the Zeabur build log showing a Dockerfile build (NOT a buildpack) — RESEARCH A1/A2.
      - Load the page and confirm the AGPL section-13 footer link renders `https://github.com/sc126-cornell/LogoSwap` (real owner), NOT the `<OWNER>` literal (RESEARCH Open Question 2).
      Fallback (RESEARCH Environment Availability): if Zeabur is down, run the new build locally via `docker compose up` and perform the same /health + footer checks against the local container — DEPLOY-01 explicitly allows local Docker.

    LIVE-UAT END-TO-END (D-03): on the LIVE site, upload the real supplier PDF -> draw the framing rectangle (框選) over the supplier mark -> process (pure-removal or with logo) -> download the output PDF. Keep the download OUTSIDE the repo (or in the gitignored scratch session dir).

    CAPTURE THE REGION RECT (RESEARCH Open Question 1 — the one non-trivial D-04 input): record the chosen region's PDF-point rect (x0,y0,x1,y1) + the page_index. Capture it from the `/process` POST request body in the browser Network tab (the frontend sends PDF-point coords), or re-derive from the px rect + dpi via the pipeline's coords mapping. Note these values down — Task 3's scratch script needs them.
  </action>
  <how-to-verify>
    1. `git status` before push shows no sensitive/scratch files staged; `git ls-files .planning/debug/scratch/` shows only the intentional archived evidence dir.
    2. After push, the Zeabur build log shows a Dockerfile build (not buildpack).
    3. `curl -fsS https://logoswap.scottchen0622.com/health` returns HTTP 200 (or local container if fallback).
    4. The LIVE footer link is `https://github.com/sc126-cornell/LogoSwap` (no `<OWNER>` literal).
    5. The supplier PDF completed upload -> 框選 -> process -> download; the output PDF is saved outside the repo / in the gitignored scratch dir.
    6. The region PDF-point rect (x0,y0,x1,y1) + page_index are recorded for Task 3.
  </how-to-verify>
  <acceptance_criteria>
    - Real un-sanitized supplier CAD-glyph PDF obtained and kept out of the repo (or named `*-supplier-raw.pdf` under the gitignored scratch dir).
    - Pre-push: `git ls-files .planning/debug/scratch/` shows no supplier PDF and no `_*`-scratch script (only the archived evidence dir).
    - `git push origin master` succeeded as a fast-forward (no force).
    - LIVE `/health` returns 200 (or local Docker fallback returns 200).
    - LIVE section-13 footer renders `sc126-cornell` owner, not `<OWNER>`.
    - End-to-end upload -> 框選 -> process -> download completed on a real supplier PDF.
    - Region PDF-point rect + page_index captured and recorded (handed to Task 3).
  </acceptance_criteria>
  <resume-signal>Reply with the recorded region rect (x0,y0,x1,y1) + page_index and the path to the downloaded output PDF (kept outside the repo), or describe any deploy/UAT issue.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: D-04 one-off attack-sim scratch script — verify the LIVE output, then retire (DEPLOY-01 gate)</name>
  <read_first>
    - tests/_illustrator_attack.py (read lines 9-14 AGPL test exception + 105-263 the three public functions + signatures)
    - tests/test_illustrator_attack_regression.py (read lines 46-185 — the fixture-wiring template + the two gate assertions at 174-185 the scratch must mirror)
    - .planning/phases/08-documentation-sync-live-rollout/08-CONTEXT.md D-04 (one-off scratch in .planning/debug/scratch/, import existing logic, retire after; standalone CLI runner REJECTED)
    - .planning/phases/08-documentation-sync-live-rollout/08-RESEARCH.md section 5 (scratch flow + what to import) + Pitfall 1 (retire / never commit)
  </read_first>
  <what-built>
    Task 2 produced a LIVE-downloaded output PDF + the region PDF-point rect + page_index. This step writes a ONE-OFF scratch script (in the gitignored scratch session dir from Task 1) that imports the existing `tests/_illustrator_attack.py` helpers, simulates the Illustrator attack on the downloaded file, and asserts the two gates. No new production or test surface (D-04 minimum-change).
  </what-built>
  <action>
    In the gitignored scratch session dir (e.g. `.planning/debug/scratch/08-live-uat/_attack_live_uat.py` — must match Task 1's ignore so it cannot be committed), write a one-off script that:
      1. `from tests._illustrator_attack import delete_image_xobjects_intersecting, render_region_white_pct, count_zero_area_fills_in_region` (import fitz is allowed here under the test-harness exception).
      2. Opens the LIVE-downloaded output PDF with `fitz.open(<downloaded.pdf>)`.
      3. Calls `delete_image_xobjects_intersecting(doc, page_index, region_pdf_pts)` to simulate the Illustrator attack (delete the overlay image XObject).
      4. Saves the attacked copy: `doc.save(<attacked.pdf>, garbage=4, deflate=True)` (also inside the gitignored scratch dir).
      5. Asserts gate (a): `render_region_white_pct(attacked.pdf, page_index, region_pdf_pts) >= 98.0`.
      6. Asserts gate (b): `count_zero_area_fills_in_region(attacked.pdf, page_index, region_pdf_pts) == 0`.
      Use the page_index + region_pdf_pts captured in Task 2. The script skips ingest/process (those happened on the LIVE site through the browser) — it operates on the already-processed downloaded file.
    Run it. BOTH gates must pass (render region >= 98% white AND zero-area type='f' count == 0 in the selected region) — that is the proof Option B truly deleted the page-level sources upstream (an attacker pulling the image XObject reveals nothing).
    RETIRE (D-04 + Pitfall 1, BLOCKING): after a green run, delete the scratch script, the attacked.pdf, and the downloaded supplier output PDF from the scratch dir (or confirm they are all gitignored). Then re-run `git status` + `git ls-files .planning/debug/scratch/` to PROVE nothing sensitive/transient is tracked or staged. The standalone-CLI-runner-into-scripts/ option was explicitly REJECTED — do NOT add anything under scripts/.
  </action>
  <how-to-verify>
    1. The scratch script lives in the gitignored session dir; `git check-ignore <script path>` prints it.
    2. Running the script prints both gate results and asserts pass: white_pct >= 98.0 AND zero_area_count == 0.
    3. After retirement, `git ls-files .planning/debug/scratch/` shows no `_attack_live_uat.py`, no attacked.pdf, no supplier output PDF.
    4. `git status --porcelain` shows nothing sensitive staged/untracked-and-about-to-be-added under the scratch dir.
  </how-to-verify>
  <acceptance_criteria>
    - The one-off scratch script imports the three existing `tests/_illustrator_attack.py` functions (no re-implemented attack logic) and lives in the gitignored scratch session dir.
    - The attack-sim run PASSES BOTH gates on the LIVE-downloaded file: render region >= 98.0% white AND zero-area type='f' count == 0 in the selected region.
    - After the run: `git ls-files .planning/debug/scratch/` contains NO scratch script, NO attacked.pdf, NO supplier PDF (only the intentional archived evidence dir).
    - `git status` is clean of any sensitive/transient artifact before this plan is considered done.
    - Nothing added under scripts/ (standalone CLI runner stays rejected per D-04).
    - (If chosen in Plan 02 Task 3) flip REQUIREMENTS.md DEPLOY-01 to `- [x]` now that LIVE-UAT is green — otherwise confirm it is already checked.
  </acceptance_criteria>
  <resume-signal>Reply "attack-sim green, scratch retired" with the two gate numbers (white_pct, zero_area_count), or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo -> PUBLIC GitHub (push) | The single push exposes all staged content publicly (AGPL section 13). Un-sanitized supplier IP or transient scratch artifacts must NOT cross this boundary. |
| engineer's machine -> LIVE site (browser upload) | The real supplier PDF is uploaded to the LIVE server (transient, 1h TTL, no datastore) — acceptable for internal-LAN-style use; the file is not persisted server-side and never committed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Information disclosure (sensitive data exposure on public repo) | `.planning/debug/scratch/**` (git-tracked) + the D-03 real supplier PDF + the D-04 scratch script + attacked.pdf | mitigate | Task 1 adds a proven (`git check-ignore`) scratch-dir-anchored ignore; Task 2 keeps the supplier PDF outside the repo / named `*-supplier-raw.pdf`; pre-push `git status` + `git ls-files` review is a BLOCKING gate; Task 3 retires (deletes) the scratch script + attacked.pdf + output PDF and re-proves `git ls-files` is clean. This is the one genuinely NEW risk this phase introduces (RESEARCH security_threat_model) — modeled explicitly and tied to acceptance criteria. |
| T-08-06 | Tampering (deploying wrong runtime / wrong owner URL) | Zeabur build (buildpack vs Dockerfile; `<OWNER>` substitution) | mitigate | Verify the Zeabur build log shows a Dockerfile build (3.12 pin both stages) and the LIVE footer renders `sc126-cornell` not `<OWNER>` (RESEARCH A1/A2 + Open Question 2). Fallback: local Docker. |
| T-08-07 | Spoofing / integrity (force-push clobbering remote history) | `git push origin master` (62 commits ahead) | accept | Push is a fast-forward (master strictly ahead of origin); NO force-push needed or used. Acceptance criterion records "fast-forward, no force". Low risk. |
</threat_model>

<verification>
- .gitignore reliably ignores the Phase-8 scratch session dir + supplier-raw PDFs (proven by git check-ignore); archived evidence dir stays tracked.
- Pre-push git status/ls-files confirm no sensitive or scratch artifact is staged.
- master pushed once (fast-forward) to origin -> Zeabur Dockerfile build.
- LIVE /health 200 (or local Docker fallback); section-13 footer shows sc126-cornell; runtime is 3.12 (structural via Dockerfile pin + build log).
- End-to-end upload -> 框選 -> process -> download on a real supplier PDF.
- D-04 scratch script (importing the existing helpers) passes BOTH gates on the downloaded file, then is retired; git ls-files proves nothing sensitive/transient committed.
</verification>

<success_criteria>
Maps to ROADMAP Phase 8 success criterion 4: LIVE environment (Zeabur or local Docker) runs the new build; >= 1 CAD-glyph sample completes upload -> 框選 -> process -> download; the LIVE file re-runs the attack-simulation with render result >= 98% white (LIVE-UAT green). Plus the phase-specific NEW security requirement: no supplier PDF / scratch script leaks to the public repo.
</success_criteria>

<output>
After completion, create `.planning/phases/08-documentation-sync-live-rollout/08-03-SUMMARY.md`
</output>
