---
phase: 08-documentation-sync-live-rollout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - HANDOFF.md
  - .planning/PROJECT.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [DOC-01, DOC-02]

must_haves:
  truths:
    - "HANDOFF.md section 6 gains a 6.5 subsection describing the three-layer defence (apply_redactions + Option A + Option B) and CAD-glyph vs general-vector handling"
    - "HANDOFF.md section 6 Option A reference adjusted to reflect Option A + B dual-layer defence"
    - "PROJECT.md Key Decisions gains a Hotfix v1.1 Option B row whose Rationale cites the 2026-05-28 forensic attack evidence"
    - "STATE.md Promoted-from-Deferred Option B note final-cleaned to DONE/shipped (Phase 7 complete + heading toward v1.1 LIVE), not merely active"
    - "All four Phase 8 requirement checkboxes (THREAT-02, DOC-01, DOC-02, DEPLOY-01) flipped to checked in REQUIREMENTS.md + traceability Status set Complete"
  artifacts:
    - path: "HANDOFF.md"
      provides: "section 6.5 Option B content-stream surgery"
      contains: "6.5"
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions v1.1 Option B row"
      contains: "Option B 落地"
    - path: ".planning/STATE.md"
      provides: "final-cleaned Option B promoted note"
      contains: "Option B"
    - path: ".planning/REQUIREMENTS.md"
      provides: "four Phase 8 reqs checked + traceability Complete"
      contains: "[x] **DEPLOY-01**"
  key_links:
    - from: "PROJECT.md Key Decisions Option B row Rationale"
      to: ".planning/debug/scratch/illustrator-attack-2026-05-28-archived/"
      via: "forensic evidence citation"
      pattern: "illustrator-attack-2026-05-28-archived"
---

<objective>
Synchronise the decision docs to the post-Phase-7 reality and pre-stage the milestone-close requirement check:
- DOC-01: add HANDOFF.md section 6.5 "Option B content-stream surgery" (three-layer defence + CAD-glyph vs general-vector handling) and adjust the section-6 Option A reference to "Option A + B 雙層防線".
- DOC-02: add a PROJECT.md Key Decisions "Hotfix v1.1 — Option B 落地" row (Rationale citing the 2026-05-28 forensic attack evidence) and final-clean the STATE.md Promoted-from-Deferred Option B note from "active" to "DONE/shipped".
- Flip the four Phase 8 requirement checkboxes (THREAT-02, DOC-01, DOC-02, DEPLOY-01) to checked and set their traceability Status to Complete, so /gsd-complete-milestone (Plan 04) does not surface an "Unchecked Requirements" warning.

Purpose: colleagues/legal/maintainers read these docs as the source of truth; they must say "deleted upstream, not just overlaid" for the page-level case. The checkbox flip removes a known milestone-close snag (RESEARCH Pitfall 4).
Output: edited HANDOFF.md, PROJECT.md, STATE.md, REQUIREMENTS.md, ROADMAP.md (checkbox). Docs only — no code.

NOTE on the milestone-close second pass (RESEARCH section 7): /gsd-complete-milestone runs its OWN full PROJECT.md Key Decisions audit and rewrites STATE.md at close. Treat THIS plan's DOC-02 edits as the PRIMARY authoritative write; do not duplicate the row. The close-workflow reconciles. The checkbox flip MUST happen here (before close) because the close workflow reads it as a gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-documentation-sync-live-rollout/08-CONTEXT.md
@.planning/phases/08-documentation-sync-live-rollout/08-RESEARCH.md
@HANDOFF.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add HANDOFF.md section 6.5 + adjust section 6 Option A reference (DOC-01)</name>
  <read_first>
    - HANDOFF.md (read section 6 in full — subsections 6.1 line 146, 6.2 line 156 incl. the Option A reference at line 172, 6.3 line 175, 6.4 line 187, the section-6 close before the separator at line 196, section 7 at line 198)
    - .planning/phases/08-documentation-sync-live-rollout/08-RESEARCH.md section 2 (insertion point, three-layer facts for 6.5, CAD-glyph vs general-vector, the line-172 Option A reference text + flag A3) and section 6
    - .planning/phases/08-documentation-sync-live-rollout/08-CONTEXT.md specifics (6.5 must describe three-layer division + CAD-glyph vs general-vector)
  </read_first>
  <action>
    Two edits in HANDOFF.md, zh-TW to match the file's language:
    (a) Insert a new "### 6.5 Option B content-stream surgery" subsection as the LAST subsection of section 6 — after 6.4's body and BEFORE the separator at line 196 (so it sits inside section 6, before section 7). Content per success criterion 2 + RESEARCH section 2 three-layer facts: (1) apply_redactions is the baseline (truly removes normal-area text + fully-covered vectors + image pixels); (2) Option B (delete_zero_area_type_f_fills_inside, page-level content-stream surgery) runs UPSTREAM and truly DELETES page-level zero-area type='f' CAD-glyph fills that apply_redactions cannot reach; (3) Option A (replace_region_with_white_raster dense / cover_zero_area_artefacts sparse) is the last-mile OVERLAY defence for form-XObject-internal residue (Option B is page-level-only, logged via log_xobject_intersect) + Option B regex-miss fail-safe. Then state the CAD-glyph vs general-vector difference: general/normal-area vector logos are truly removed by apply_redactions (Option B is a no-op for them — SEC-02); CAD-glyph supplier marks decompose into many zero-area type='f' fills that apply_redactions leaves in the stream so Option B targets exactly these.
    (b) Adjust the only Option A reference in section 6 — line 172's "(dCt-residue Option A)" inside 6.2 — to reflect "Option A + B 雙層防線" (e.g. note Option A is now one layer of a two-layer defence and cross-reference section 6.5). Per RESEARCH flag A3 this is the only "Option A description" in section 6; success criterion 2 calls for "Option A 描述同步調整". Keep the surrounding 6.2 sentence (the REMOVE_IF_TOUCHED vs REMOVE_IF_COVERED guidance + commit-history caution) intact.
    minimum-change: add 6.5 + adjust the one line-172 reference; do NOT rewrite 6.1-6.4 prose or polish unrelated sections.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -c "6.5" HANDOFF.md && grep -c "雙層防線" HANDOFF.md && grep -c "delete_zero_area_type_f_fills_inside" HANDOFF.md</automated>
  </verify>
  <acceptance_criteria>
    - HANDOFF.md contains a `6.5` heading (grep -n "6.5" HANDOFF.md matches a markdown subsection heading inside section 6, before section 7).
    - Section 6.5 names all three layers: grep -n "apply_redactions" HANDOFF.md and grep -n "delete_zero_area_type_f_fills_inside" HANDOFF.md and a reference to the overlay (replace_region_with_white_raster or cover_zero_area_artefacts) all match in the 6.5 region.
    - Section 6.5 describes the CAD-glyph vs general-vector distinction (mentions SEC-02 no-op for normal-area vectors).
    - The section-6 Option A reference reflects dual-layer: grep -c "雙層防線" HANDOFF.md returns >= 1.
    - The new 6.5 is positioned before section 7 ("接手後第一週建議") and after 6.4.
  </acceptance_criteria>
  <done>HANDOFF.md section 6.5 documents the three-layer defence + CAD-glyph vs general-vector handling; section 6 Option A reference reflects Option A + B dual-layer; no unrelated section-6 prose touched.</done>
</task>

<task type="auto">
  <name>Task 2: PROJECT.md Key Decisions v1.1 row + STATE.md Option B note final-clean (DOC-02)</name>
  <read_first>
    - .planning/PROJECT.md (read Key Decisions table 88-112; note the 3-column shape | Decision | Rationale | Outcome |, header at line 92, existing rows at 104/105 that the new row relates to; Outcome markers like "✓ Validated (Phase N)")
    - .planning/STATE.md (read Deferred Items table 74-86 + the "Promoted from Deferred to Active" note at lines 88-90 — the final-clean target)
    - .planning/phases/08-documentation-sync-live-rollout/08-RESEARCH.md section 3 (table format, exact STATE.md note text, flag A4: the final-clean target is the Promoted NOTE not a table row) and the forensic-evidence dir citation point
    - .planning/phases/08-documentation-sync-live-rollout/08-CONTEXT.md specifics (Rationale must cite the 2026-05-28 forensic attack evidence)
  </read_first>
  <action>
    (a) PROJECT.md Key Decisions: ADD one new 3-column row matching the existing | Decision | Rationale | Outcome | shape — Decision = "Hotfix v1.1 — Option B 落地 (content-stream surgery 真正刪除 page-level 零面積 type='f' sources)"; Rationale MUST cite the 2026-05-28 forensic attack evidence (the v1.0 deferral assumption "Option A 對使用者實質不可恢復" was disproven — Illustrator can pull the image XObject overlay; reference .planning/debug/scratch/illustrator-attack-2026-05-28-archived/); Outcome = a Validated-style marker for Phase 7 (e.g. "✓ Validated (Phase 7, T-06-01 + T-02-07 CLOSED via Option B)"). Do NOT edit the existing line-105 row (historical record stays; success criterion 3 only requires ADDING the new row). Add exactly one row — do NOT duplicate.
    (b) STATE.md: final-clean the "Promoted from Deferred to Active (2026-05-28)" note at lines 88-90. It currently shows Option B as struck-through then "v1.1 active (Phase 7 核心)". Update it to reflect Option B is now DONE/shipped: Phase 7 complete (baseline 338 passed + 3 skipped + 0 xfailed; three gates green; T-06-01 + T-02-07 CLOSED), heading into v1.1 LIVE rollout (Phase 8). Keep the disproven-assumption explanation (forensic attack) for the record. Do NOT touch the two genuinely-deferred-onward table rows at lines 85-86 (Form-XObject recursive surgery; type='s' stroke surgery) — those stay deferred.
    minimum-change discipline. Note (RESEARCH section 7): /gsd-complete-milestone will run a second Key Decisions audit + rewrite STATE.md at close — these edits are the primary write; do not pre-empt the close-workflow's reorganization.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -c "Option B 落地" .planning/PROJECT.md && grep -c "illustrator-attack-2026-05-28-archived" .planning/PROJECT.md && grep -c "遞迴 surgery" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Option B 落地" .planning/PROJECT.md returns >= 1 (new Key Decisions row added).
    - The new PROJECT.md row Rationale cites the forensic evidence: grep -c "illustrator-attack-2026-05-28-archived" .planning/PROJECT.md returns >= 1.
    - Exactly ONE new Key Decisions row added (git diff .planning/PROJECT.md shows a single added table row, not a duplicate; line-105 historical row unchanged).
    - STATE.md Promoted note (88-90) now states Option B is DONE/shipped (Phase 7 complete), not merely "active": grep -n "shipped\|完成\|DONE\|Phase 7 complete\|已落地" .planning/STATE.md matches in the promoted-note region.
    - The two deferred-onward STATE.md table rows survive: grep -c "遞迴 surgery" .planning/STATE.md returns >= 1 (Form-XObject row intact); the type='s' stroke row also intact.
  </acceptance_criteria>
  <done>PROJECT.md has one new Option B Key Decisions row citing forensic evidence; STATE.md Option B promoted note reads DONE/shipped; deferred-onward rows untouched; no duplicate rows.</done>
</task>

<task type="auto">
  <name>Task 3: Flip the four Phase 8 requirement checkboxes + traceability Status (milestone-close gate pre-stage)</name>
  <read_first>
    - .planning/REQUIREMENTS.md (read lines 26 THREAT-02, 40 DOC-01, 41 DOC-02, 47 DEPLOY-01, and the Traceability table rows 84-87)
    - .planning/ROADMAP.md (read the Phase 8 entry at line 36 — `- [ ] **Phase 8: ...**` — and Phase Details section 90-101)
    - .planning/phases/08-documentation-sync-live-rollout/08-RESEARCH.md section 7 (the close-workflow's "Unchecked Requirements" gate) + Pitfall 4
  </read_first>
  <action>
    Flip all four Phase 8 requirement checkboxes from `- [ ]` to `- [x]` in REQUIREMENTS.md: THREAT-02 (line 26), DOC-01 (line 40), DOC-02 (line 41), DEPLOY-01 (line 47). Update the Traceability table (rows 84-87): set the Status column for THREAT-02 / DOC-01 / DOC-02 / DEPLOY-01 from "Pending" to "Complete". This pre-stages the /gsd-complete-milestone requirement gate so it does not surface an "Unchecked Requirements" warning at close (Plan 04).
    SEQUENCING CAVEAT: DEPLOY-01's actual work (LIVE deploy + attack-sim green) happens in Plan 03, and THREAT-02/DOC-01/DOC-02 land in Plans 01 + 02. This checkbox flip is a docs-only edit; it asserts completion. Because this plan is in Wave 1 and Plan 03 (DEPLOY-01) is Wave 2, the executor of THIS plan should flip THREAT-02/DOC-01/DOC-02 now and MAY also flip DEPLOY-01 now (the close gate just needs them checked before close in Plan 04). If the executor prefers strict honesty, leave DEPLOY-01 unchecked here and flip it as the FINAL sub-step of Plan 03 once LIVE-UAT is green — Plan 04's milestone-close task carries a verification that all four are checked regardless. Either path satisfies the gate; pick one and note it in the SUMMARY.
    Do NOT also flip the ROADMAP Phase 8 line `- [ ]` here — that is the phase-completion checkbox owned by phase close / milestone close, not a requirement checkbox. (Leave ROADMAP line 36 to the close workflow.)
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -c "^- \[x\] \*\*THREAT-02\*\*" .planning/REQUIREMENTS.md && grep -c "^- \[x\] \*\*DOC-01\*\*" .planning/REQUIREMENTS.md && grep -c "^- \[x\] \*\*DOC-02\*\*" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md THREAT-02 line begins with `- [x] **THREAT-02**` (grep returns 1).
    - REQUIREMENTS.md DOC-01 and DOC-02 lines begin with `- [x]` (grep returns 1 each).
    - REQUIREMENTS.md DEPLOY-01 line begins with `- [x]` IF this plan chose to flip it now; otherwise it remains `- [ ]` and Plan 03 / Plan 04 carry it (document the choice in SUMMARY).
    - Traceability table Status column shows "Complete" for the flipped reqs (grep -c "Pending" .planning/REQUIREMENTS.md reflects only still-pending rows — at minimum THREAT-02/DOC-01/DOC-02 no longer Pending).
    - ROADMAP.md Phase 8 line 36 still begins `- [ ]` (not flipped by this plan).
  </acceptance_criteria>
  <done>THREAT-02, DOC-01, DOC-02 (and optionally DEPLOY-01) requirement checkboxes are checked with traceability Status Complete; the close-workflow requirement gate is pre-staged; ROADMAP phase checkbox left to close.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new this plan) | Markdown documentation edits only. No code, no input surface, no data flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Repudiation / Integrity (decision-log accuracy) | PROJECT.md Key Decisions + STATE.md Deferred note | mitigate | The new row + final-clean ensure the decision log truthfully records that the v1.0 deferral assumption was disproven by forensic evidence and Option B shipped — prevents future re-deferral of an already-shipped protection. Rationale cites the concrete evidence dir (auditable). |
| T-08-04 | Tampering (premature/false completion assertion) | REQUIREMENTS.md DEPLOY-01 checkbox | accept | DEPLOY-01 work is in Wave 2 (Plan 03); flipping its checkbox in Wave 1 asserts completion before LIVE-UAT runs. Accepted because Plan 04's milestone-close task independently verifies all four are checked AND that LIVE-UAT was green before close; the action gives the executor the honest option to defer the DEPLOY-01 flip to end of Plan 03. Low risk. |
</threat_model>

<verification>
- HANDOFF.md has a 6.5 subsection (three layers + CAD-glyph vs general-vector) and the section-6 Option A reference reflects Option A + B dual-layer.
- PROJECT.md has exactly one new Option B Key Decisions row citing the 2026-05-28 forensic evidence dir; line-105 historical row unchanged.
- STATE.md Option B promoted note final-cleaned to DONE/shipped; deferred-onward table rows intact.
- REQUIREMENTS.md THREAT-02/DOC-01/DOC-02 (and optionally DEPLOY-01) checkboxes checked + traceability Complete.
- ROADMAP phase-8 checkbox NOT flipped (owned by close).
- No code files touched.
</verification>

<success_criteria>
Maps to ROADMAP Phase 8 success criteria 2 (HANDOFF 6.5 + Option A description) and 3 (PROJECT Key Decisions row + STATE.md Option B final-clean). Also pre-stages the milestone-close requirement gate (RESEARCH Pitfall 4) so Plan 04 closes cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/08-documentation-sync-live-rollout/08-02-SUMMARY.md`
</output>
