---
phase: 04-raster-image-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/ingest.py
  - app/services/pdf_engine.py
  - app/storage.py
  - app/services/pipeline.py
  - app/config.py
  - app/main.py
  - app/api/sessions.py
  - web/index.html
  - web/js/app.js
  - web/js/api.js
  - tests/conftest.py
  - tests/test_ingest.py
autonomous: true
requirements:
  - UPLOAD-03
tags:
  - phase-04
  - ingest
  - image-normalization
  - pillow
  - mvp-vertical-slice
must_haves:
  truths:
    - "使用者可在 dropzone 上傳 PNG / JPG / TIFF 單頁影像檔,看到預覽並走完既有 vector 框選 / 套用 / 下載流程,輸出 {stem}_logoswap.pdf"
    - "後端以 magic-header 嗅探拒絕假冒副檔名(.png 內容是 ELF / GIF / WebP / SVG 一律 unsupported_type 415,不洩漏為 500)"
    - "多頁 TIFF 上傳被以結構化 4xx(multi_page_tiff_unsupported)拒絕,訊息為繁中『暫不支援多頁 TIFF,請先將 TIFF 拆成單頁後再上傳。』"
    - "CMYK 影像在 ingest 階段被強制轉 RGB 後嵌入 A4 PDF(Pitfall 5 黑框防護)"
    - "originals/{sid}/ 保留使用者上傳的真實 image bytes(SHA-256 不變);work/{sid}/doc.pdf 是正規化後 A4 PDF;pristine/{sid}/doc.pdf 同樣是正規化後 A4 PDF(供 pipeline reset 用)"
    - "Phase 1–3 既有 PDF 路徑零迴歸:現有 PDF 上傳、預覽、Phase 2 移除、Phase 3 logo 置入測試全數 pass"
    - "AGPL seam 不破:import fitz / import pymupdf 只在 pdf_engine.py(既有 test_fitz_import_confined_to_engine_seam 仍 pass)"
  artifacts:
    - path: "app/services/pdf_engine.py"
      provides: "image_to_a4_pdf(image_bytes) -> bytes + A4_WIDTH_PT/A4_HEIGHT_PT 常數"
      contains: "def image_to_a4_pdf"
    - path: "app/services/ingest.py"
      provides: "_sniff_kind 四 magic dispatch + _ingest_image Pillow 驗證鏈 + _ingest_image_to_pdf"
      contains: "def _sniff_kind"
    - path: "app/storage.py"
      provides: "pristine_path(session_id) + write_pristine_copy(session_id, data)"
      contains: "def pristine_path"
    - path: "app/main.py"
      provides: "_INGEST_STATUS 新增三條(unsupported_image_format=415, multi_page_tiff_unsupported=415, corrupt_image=422)"
      contains: "multi_page_tiff_unsupported"
    - path: "web/index.html"
      provides: "dropzone accept 擴張 + 五處字串更新(D-11)"
      contains: "image/png,image/jpeg,image/tiff"
    - path: "web/js/app.js"
      provides: "COPY 新增三條 + messageForError 新增三個 case"
      contains: "unsupportedImageFormat"
    - path: "tests/conftest.py"
      provides: "in-memory image fixture builders(PNG / JPEG / TIFF / multi-page TIFF / CMYK / fake-PNG)"
      contains: "def _build_image_bytes"
    - path: "tests/test_ingest.py"
      provides: "image ingest 路徑整合測試"
      contains: "test_png_upload_normalizes_to_a4_pdf"
  key_links:
    - from: "app/services/ingest.py::ingest_upload"
      to: "app/services/pdf_engine.py::image_to_a4_pdf"
      via: "image dispatch 後呼叫 wrapper(不直接 import fitz)"
      pattern: "pdf_engine\\.image_to_a4_pdf"
    - from: "app/services/ingest.py::_ingest_image_to_pdf"
      to: "app/storage.py::write_pristine_copy"
      via: "ingest 同時寫 originals/ work/ pristine/ 三處"
      pattern: "storage\\.write_pristine_copy"
    - from: "app/main.py::_INGEST_STATUS"
      to: "app/api/sessions.py::_CODE_STATUS"
      via: "兩份 dict 同步擴張"
      pattern: "multi_page_tiff_unsupported"
    - from: "web/js/app.js::messageForError"
      to: "web/js/app.js::COPY"
      via: "switch case 對應後端 IngestError code"
      pattern: "case \"multi_page_tiff_unsupported\""
---

<objective>
**Plan 04-01 — 影像 ingest 垂直切片(PNG/JPG/TIFF → A4 PDF → 走完既有預覽 / 框選 / 套用 / 下載流程)**

Purpose:
為 Phase 4 第一條 vertical slice。本 plan 結束後,使用者能:在 dropzone 上傳 PNG / JPG / TIFF 單頁影像 → 系統在 ingest 階段以 magic-header 嗅探並驗證 → 強制轉 RGB 後正規化為 A4 portrait 單頁 PDF → 此 PDF 進入既有 Phase 1–3 流程(預覽 / 框選 / 套用 / 下載)。對影像型內容的框選,本 plan **仍走既有 vector 分支**(images=IMAGE_NONE);raster 分支會在 04-02 接上。這保證 04-01 完成後即是端到端可運作的最薄切片(REQ UPLOAD-03 字面達成,部分覆蓋 REMOVE-02 — 待 04-02 完成 raster 分支才完全達成)。

同時建立 04-02 需要的 storage 基礎設施:`pristine/{sid}/doc.pdf` 第三目錄,供 `pipeline.process_job` 從 pristine PDF 而非 originals(可能為 PNG bytes)reset work copy — 解決 PATTERNS.md 標出的 deferred-mutation invariant 衝突(planner 拍板採「雙寫 pristine_pdf」方案)。

Output:
- `app/services/pdf_engine.py`:新增 `image_to_a4_pdf(image_bytes) -> bytes`、`A4_WIDTH_PT`、`A4_HEIGHT_PT` 常數(AGPL seam,fitz 操作在此檔)
- `app/services/ingest.py`:`_sniff_kind` 四 magic dispatch + `_ingest_image` Pillow 驗證鏈(verify → reopen → n_frames 檢查 → 格式檢查 → CMYK→RGB convert → load) + `_ingest_image_to_pdf` 整合 + 新增三個 IngestError code
- `app/storage.py`:新增 `pristine_path` / `write_pristine_copy` + `_KINDS` 擴為四個目錄
- `app/services/pipeline.py`:`process_job` reset-from-pristine 從 originals/ 改為 pristine/(僅這一處改動,本 plan 完成此基礎,實際 raster 分支落在 04-02)
- `app/config.py`:新增 `MAX_INGEST_IMAGE_PIXELS = 89_478_485` + `JPEG_REENCODE_QUALITY = 90` 顯式化常數
- `app/main.py` + `app/api/sessions.py`:兩處 `_INGEST_STATUS` / `_CODE_STATUS` dict 同步新增三條映射
- `web/index.html`:dropzone 六處字串 / 屬性更新(D-11)
- `web/js/app.js`:COPY 字典新增三條繁中文案 + `messageForError` switch 新增三個 case;更新既有 `unsupported_type` 文案("請改用 PDF、PNG、JPG 或 TIFF 檔案")
- `web/js/api.js`:docstring comment 補三個新 error code(transport 層程式碼不動)
- `tests/conftest.py`:新增 in-memory image fixture builders(沿用 `_build_pdf` 模式,絕不 commit 二進位)
- `tests/test_ingest.py`:新增 image 上傳整合測試
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-raster-image-support/04-CONTEXT.md
@.planning/phases/04-raster-image-support/04-RESEARCH.md
@.planning/phases/04-raster-image-support/04-PATTERNS.md
@.planning/phases/04-raster-image-support/04-UI-SPEC.md
@.planning/research/PITFALLS.md
@app/services/ingest.py
@app/services/pdf_engine.py
@app/services/pipeline.py
@app/services/logo.py
@app/storage.py
@app/main.py
@app/api/sessions.py
@app/config.py
@web/index.html
@web/js/app.js
@web/js/api.js
@tests/conftest.py
@tests/test_ingest.py

<interfaces>
<!-- Key interfaces / identifiers the executor needs. Extracted from canonical refs.
     Executor should treat these as the contract — no codebase exploration needed. -->

**From `app/services/ingest.py` (既有,本 plan 擴張):**
```python
class IngestError(Exception):
    def __init__(self, code: str, message: str) -> None:
        super().__init__(message)
        self.code = code
        self.message = message

_PDF_MAGIC = b"%PDF-"
_PDF_MAGIC_MAX_OFFSET = 8

def _looks_like_pdf(data: bytes) -> bool: ...
def ingest_upload(filename: str, data: bytes) -> SessionInfo: ...
```

**From `app/services/pdf_engine.py` (既有,本 plan 擴張):**
```python
def open_pdf(path_or_bytes) -> "fitz.Document": ...
def close(doc: "fitz.Document") -> None: ...
def save_doc(doc, path, *, garbage=4, deflate=True, clean=True) -> None: ...
def page_count(doc) -> int: ...
# 既有 import:import fitz  ← AGPL seam,唯一允許 import fitz / pymupdf 的檔
```

**From `app/storage.py` (既有,本 plan 擴張):**
```python
_KINDS = ("originals", "work", "outputs")          # 本 plan 改為 ("originals", "work", "outputs", "pristine")
_ORIGINAL_NAME = "source.pdf"
_WORK_NAME = "doc.pdf"

def original_path(session_id: str) -> Path: ...
def work_path(session_id: str) -> Path: ...
def outputs_dir(session_id: str) -> Path: ...
def write_original(session_id: str, filename: str, data: bytes) -> Path: ...
def write_work_copy(session_id: str, data: bytes) -> Path: ...
```

**From `app/services/pipeline.py::process_job` 第 127–129 行(既有,本 plan 修改 1 行):**
```python
if not Path(original).is_file():
    raise PipelineError("work_copy_misconfigured", "...找不到原始檔...")
Path(work).parent.mkdir(parents=True, exist_ok=True)
shutil.copyfile(original, work)   # ← 本 plan 改為從 pristine 取代 original 作 reset source
```

**From `app/main.py` 第 43–58 行(既有,本 plan 擴張):**
```python
_INGEST_STATUS: dict[str, int] = {
    "unsupported_type": 415,
    "file_too_large": 413,
    "too_many_pages": 413,
    "corrupt_pdf": 422,
    "empty_file": 400,
}

@app.exception_handler(IngestError)
async def _handle_ingest_error(_request: Request, exc: IngestError) -> JSONResponse: ...
```

**From `app/api/sessions.py` 第 21–27 行(既有,本 plan 擴張同步):**
```python
_CODE_STATUS: dict[str, int] = {
    "unsupported_type": 415,
    "file_too_large": 413,
    "too_many_pages": 413,
    "corrupt_pdf": 422,
    "empty_file": 400,
}
```

**From `web/index.html` 第 280–298 行(既有,本 plan 更新六處):**
```html
<div id="dropzone" class="dropzone" role="button" tabindex="0"
     aria-label="選擇 PDF 檔案">
  <h2 class="dropzone__heading">上傳 PDF 以開始</h2>
  <p class="dropzone__body">選擇或拖曳一個供應商 PDF 檔案,即可在此預覽各頁內容。原始檔案不會被更動。</p>
  <button type="button" id="choose-file" class="primary-btn">選擇 PDF 檔案</button>
  <p class="dropzone__secondary">或將檔案拖曳到這裡</p>
  <p class="dropzone__hint">支援 PDF 檔(單一檔案)</p>
  <input type="file" id="file-input" class="visually-hidden" accept="application/pdf,.pdf" />
</div>
```

**From `web/js/app.js` 第 18–33 行 + 第 96–113 行(既有,本 plan 擴張):**
```javascript
const COPY = {
  uploading: "正在上傳檔案…",
  processing: "正在處理檔案,準備預覽…",
  errorHeading: "無法開啟此檔案",
  unsupportedType: "此檔案格式不支援。請改用 PDF 檔案後再試一次。",
  corruptPdf: "這個 PDF 檔案無法讀取,可能已損毀。請確認檔案後再試一次。",
  fileTooLarge: (limit) => ...,
  networkFailure: "上傳失敗,請檢查網路連線後再試一次。",
};
function messageForError(err) { switch(code) { case "unsupported_type": ... } }
```

**繁中文案(UI-SPEC 04 鎖定,不得改字):**
- dropzone heading: `上傳 PDF 或影像以開始`
- dropzone body: `選擇或拖曳一個 PDF 或影像檔案(PNG、JPG、TIFF),即可在此預覽並框選要替換的商標。原始檔案不會被更動。`
- choose-file button: `選擇檔案`
- dropzone__secondary: `或將檔案拖曳到這裡`(不變)
- dropzone__hint: `支援 PDF、PNG、JPG、TIFF(單一檔案)`
- dropzone aria-label: `選擇 PDF 或影像檔案`
- accept 屬性: `application/pdf,.pdf,image/png,image/jpeg,image/tiff,.png,.jpg,.jpeg,.tif,.tiff`
- IngestError 訊息(空檔案):`檔案是空的,請選擇有內容的 PDF 或影像。`
- IngestError 訊息(unsupported_type 沿用 code,文案更新):`不支援的檔案類型,僅接受 PDF、PNG、JPG、TIFF。`
- IngestError 訊息(unsupported_image_format):`此影像格式不支援。`(message 短版;對外文案由前端 COPY.unsupportedImageFormat 提供完整句)
- IngestError 訊息(multi_page_tiff_unsupported):`暫不支援多頁 TIFF,請先拆成單頁 TIFF 再上傳。`
- IngestError 訊息(corrupt_image):`影像檔案損壞或無法解析。`
- COPY.unsupportedType(更新): `此檔案格式不支援。請改用 PDF、PNG、JPG 或 TIFF 檔案後再試一次。`
- COPY.unsupportedImageFormat(新): `此影像格式不支援。請改用 PDF、PNG、JPG 或 TIFF 檔案後再試一次。`
- COPY.multiPageTiffUnsupported(新): `暫不支援多頁 TIFF。請先將 TIFF 拆成單頁後再上傳。`
- COPY.corruptImage(新): `這個影像檔案無法讀取,可能已損毀。請確認檔案後再試一次。`

**Magic bytes(D-12,RESEARCH Pattern 1):**
- PDF: `b"%PDF-"`(現有,容許 leading offset ≤ 8)
- PNG: `b"\x89PNG\r\n\x1a\n"`(8 bytes,必須 `startswith` offset 0)
- JPEG: `b"\xff\xd8\xff"`(3 bytes,必須 `startswith` offset 0)
- TIFF LE: `b"II*\x00"`(4 bytes,必須 `startswith` offset 0)
- TIFF BE: `b"MM\x00*"`(4 bytes,必須 `startswith` offset 0)

**A4 常數(D-01):**
- A4_WIDTH_PT = 595.0
- A4_HEIGHT_PT = 842.0
</interfaces>
</context>

## Phase Goal

**As a** 內部使用者(貿易公司業務),**I want to** 在工具的 dropzone 直接上傳供應商寄來的 PNG / JPG / TIFF 影像檔(不必先自己轉成 PDF),**so that** 系統會把它正規化成 A4 單頁 PDF,我可以走完跟 PDF 一樣的預覽 / 框選 / 套用 / 下載流程拿到 {原檔名}_logoswap.pdf。

## Multi-Source Coverage Audit(04-01 對應項目)

| Source | Item | Covered by Task | Notes |
|--------|------|-----------------|-------|
| **GOAL** | 獨立影像檔(PNG/JPG/TIFF)→ 正規化為單頁 PDF → 後續流程一致 | T-04-01-02 + T-04-01-03 | UPLOAD-03 字面 |
| **REQ** | UPLOAD-03(使用者可上傳獨立影像檔,系統將其正規化為可處理的單頁文件) | T-04-01-02 + T-04-01-03 | 完整覆蓋 |
| **RESEARCH** | Pattern 1: 四 magic dispatch in ingest | T-04-01-02 | _sniff_kind |
| **RESEARCH** | Pattern 2: Pillow verify / reopen / n_frames / convert("RGB") / load 鏈 | T-04-01-02 | _ingest_image |
| **RESEARCH** | Pattern 3: image_to_a4_pdf in fitz seam | T-04-01-01 | AGPL seam |
| **RESEARCH** | Pitfall A(fill=1,1,1 留 fs drawing)| 04-02 處理 | (Not in scope this plan)|
| **RESEARCH** | Pitfall C(multi-page TIFF silent skip)| T-04-01-02 | n_frames check |
| **RESEARCH** | Pitfall D(CMYK black-box)| T-04-01-02 | convert("RGB") |
| **RESEARCH** | Pitfall G(transparent PNG segfault)| T-04-01-02 | RGBA→RGB drop alpha |
| **RESEARCH** | Pitfall H(extension trust)| T-04-01-02 | content-sniff |
| **CONTEXT** | D-01(A4 portrait + fit + center + 白底) | T-04-01-01 | image_to_a4_pdf 內 keep_proportion=True |
| **CONTEXT** | D-02(多頁 TIFF 拒絕) | T-04-01-02 | n_frames > 1 → multi_page_tiff_unsupported |
| **CONTEXT** | D-03(CMYK→RGB)| T-04-01-02 | img.convert("RGB") |
| **CONTEXT** | D-04(沿用 50MB / 30 頁 / 40MP 上限) | T-04-01-02 | 沿用 config 既有 |
| **CONTEXT** | D-07(UI 不揭露分類) | T-04-01-04 | dropzone 不加 badge |
| **CONTEXT** | D-11(單一 dropzone + accept 擴張 + 五處字串)| T-04-01-04 | web/index.html |
| **CONTEXT** | D-12(四 magic sniff,image 強制 offset 0) | T-04-01-02 | _sniff_kind startswith vs find |
| **CONTEXT** | D-13(輸出檔名 stem + `_logoswap.pdf`)| 沿用既有 `_logoswap_name`,no change needed | pipeline.output_filename 已正確 |
| **CONTEXT** | Planner 拍板「雙寫 pristine_pdf」 | T-04-01-01(storage)+ T-04-01-03(ingest)+ T-04-01-03(pipeline reset 改源)| pristine_path + write_pristine_copy + shutil.copyfile(pristine, work)|
| **UI-SPEC** | dropzone 六處字串 / accept 擴張 | T-04-01-04 | UI-SPEC 04 鎖定文案 |
| **UI-SPEC** | 三個新錯誤碼 COPY 文案 + switch case | T-04-01-04 | UI-SPEC Inline error block 表 |
| **UI-SPEC** | unsupported_type 文案更新 | T-04-01-04 | UI-SPEC Sniff 失敗段 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 04-01-01: pdf_engine 新增 image_to_a4_pdf + A4 常數;storage 新增 pristine 目錄與寫入 helper</name>
  <files>app/services/pdf_engine.py, app/storage.py, app/config.py, tests/conftest.py, tests/test_ingest.py</files>

  <read_first>
    - app/services/pdf_engine.py(整檔,理解既有 wrapper 命名 / docstring 風格 / try-finally close pattern,line 30–43 open_pdf、line 221–223 IMAGE_NONE export、line 281–315 place_logo(已用 insert_image keep_proportion=True)、line 433–447 save_doc;新增的 helper 必須沿用同型)
    - app/storage.py(整檔,理解 `_KINDS` tuple / `subdir` 函式 / `validate_session_id` token allowlist / `new_session` 一次建多目錄的 pattern;新增 pristine 目錄必須加進 `_KINDS` 才會在 `new_session` 中一起建立)
    - app/services/logo.py(line 90–110 `_validate_png` Pillow 鏈 — 沿用 `img.format` 在 `verify()` 前讀的順序;這個 pattern 在 04-01-02 也會用)
    - app/config.py(整檔,理解 `_env_int` helper 與常數命名 convention `MAX_X_Y`;新增 MAX_INGEST_IMAGE_PIXELS / JPEG_REENCODE_QUALITY 沿用)
    - tests/conftest.py(整檔,理解 `_build_pdf` in-memory fixture builder pattern 與 `isolated_data_dir` autouse fixture;新增 image fixture builders 必須沿用 in-memory 風格,絕不 commit 二進位)
    - .planning/phases/04-raster-image-support/04-CONTEXT.md(D-01、D-04、Planner 拍板的「雙寫 pristine_pdf」段)
    - .planning/phases/04-raster-image-support/04-RESEARCH.md(Pattern 3 image_to_a4_pdf + 驗證事實 1–4 段;Architectural Responsibility Map 中 image_to_a4_pdf 屬 pdf_engine.py 的論證)
    - .planning/phases/04-raster-image-support/04-PATTERNS.md(pdf_engine.py 段 line 104–166,確認 sibling helper 命名與 docstring 風格)
  </read_first>

  <behavior>
    - Behavior 1(image_to_a4_pdf 基本功能):給定一張正規化過的 RGB 影像 bytes(任何長寬比),呼叫 `pdf_engine.image_to_a4_pdf(img_bytes)` 回傳的 bytes,經 `fitz.open(stream=..., filetype='pdf')` 開啟後 `page_count == 1`、`page.rect.width == 595.0`、`page.rect.height == 842.0`。
    - Behavior 2(image_to_a4_pdf 留白 + fit + center):上傳一個寬 800 × 高 600 的橫向影像,產出的 A4 PDF 在 `page.get_image_rects(xref)` 應回傳一個 `Rect` 其 `width / height` 與原影像 aspect 一致(±1pt),且 x0 / x1 對稱於 A4 中線(297.5 ± 5pt)或 y0 / y1 對稱於 A4 中線(421.0 ± 5pt)— 表示 keep_proportion=True 真有置中+contain。
    - Behavior 3(image_to_a4_pdf 檔大小 sane):一張 50KB 的 JPEG byte 經 image_to_a4_pdf 後 PDF bytes 應 < 200KB(garbage=4 + deflate=True 已啟用,JPEG byte-exact passthrough,實測 RESEARCH 驗證 8229→8229)。
    - Behavior 4(storage pristine 目錄存在):呼叫 `storage.new_session()` 回傳的 session_id 之下,`<DATA_DIR>/pristine/<sid>/` 目錄存在;`storage.pristine_path(sid)` 回傳 `<DATA_DIR>/pristine/<sid>/doc.pdf`。
    - Behavior 5(storage write_pristine_copy 寫檔 + 路徑與 work 不同):呼叫 `storage.write_pristine_copy(sid, b"%PDF-test")` 後 pristine_path(sid).read_bytes() == b"%PDF-test",且 pristine_path(sid) != work_path(sid) ≠ original_path(sid) 三路徑各異。
    - Behavior 6(AGPL seam invariant 仍維持):新增的 `image_to_a4_pdf` 在 `pdf_engine.py` 內(import fitz 仍只在這檔);既有 `tests/test_api.py::test_fitz_import_confined_to_engine_seam` 或等效 grep 仍 pass。
  </behavior>

  <action>
    Step 1(pdf_engine.py 新增 image_to_a4_pdf + A4 常數):
    1. 在 `app/services/pdf_engine.py` 既有 `IMAGE_NONE = fitz.PDF_REDACT_IMAGE_NONE` 常數段(line 221–223)**正下方**新增:
       ```
       # A4 page dimensions in PDF points (1 pt = 1/72"). D-01 of Phase 4:
       # all standalone image uploads (PNG/JPG/TIFF) normalize to a single-page
       # portrait A4 PDF with the image fit-in-page + centered + keep_proportion.
       A4_WIDTH_PT = 595.0
       A4_HEIGHT_PT = 842.0
       ```
    2. 在檔案合適位置(例如 `save_doc` 之前 line ~433)新增 `image_to_a4_pdf(image_bytes: bytes) -> bytes`:
       - 內部 `fitz.open()`(空 PDF)→ `doc.new_page(width=A4_WIDTH_PT, height=A4_HEIGHT_PT)` → `page.insert_image(page.rect, stream=image_bytes, keep_proportion=True)` → `return doc.tobytes(garbage=4, deflate=True, clean=True)` → `finally: doc.close()`。
       - Docstring 引用 D-01、Pitfall D(此 helper 只接收已 normalize 過的 RGB bytes,CMYK→RGB 在 ingest 完成)、AGPL seam 註解、`[VERIFIED: RESEARCH Pattern 3 本機實測 keep_proportion=True 留白置中]`。
       - 與既有 `place_logo`(line 281–315)同樣呼叫 `insert_image(keep_proportion=True)`,證明此參數行為已驗證;不再重複跑驗證。
    3. 沿用既有 wrapper 命名 convention(動詞 + 名詞):`image_to_a4_pdf` 已符合(transform name,input → output)。

    Step 2(storage.py 新增 pristine 目錄):
    1. 在 `app/storage.py` 第 26 行 `_KINDS` 從 `("originals", "work", "outputs")` 改為 `("originals", "work", "outputs", "pristine")`。**此一字串變更會讓 `new_session()` 自動為新建 session 多建一個 `pristine/<sid>/` 目錄,不需改 `new_session` 函式本體**。
    2. 在第 60 行 `_WORK_NAME = "doc.pdf"` 之後新增 `_PRISTINE_NAME = "doc.pdf"`(同名 doc.pdf,但放不同 subdir;這是 pipeline reset-from-pristine 的真實單一信任點,內容一律為 PDF bytes)。
    3. 在第 127 行 `work_path` 之後 / 第 132 行 `outputs_dir` 之前新增:
       ```
       def pristine_path(session_id: str) -> Path:
           """Path to the immutable post-ingest PDF snapshot used as the reset-from-pristine
           source by pipeline.process_job.

           For a PDF upload, this is byte-identical to original_path. For an image upload
           (Phase 4), originals/ stores the user's raw image bytes (PNG/JPG/TIFF) — these
           are NOT a PDF, so pipeline cannot reset work from them directly. We therefore
           write a normalized A4 PDF here at ingest time and pipeline reads THIS path for
           reset (D-05 SHA-256 invariant on originals/ stays intact).
           """
           return subdir("pristine", session_id) / _PRISTINE_NAME
       ```
    4. 在第 197 行 `write_work_copy` 之後新增 `write_pristine_copy(session_id: str, data: bytes) -> Path`(沿用 `write_work_copy` body — mkdir parents + open wb + write + return dest)。
    5. **不要**動 `write_original` 的 chmod 0o444(originals 仍 read-only,保 SHA-256 invariant)。

    Step 3(config.py 新增兩個常數):
    1. 在 `app/config.py` 第 56 行 `MAX_RENDER_PIXELS` 之後新增:
       ```
       # Phase 4 ingest:Pillow decompression-bomb 預設(89,478,485 px,~89 MP)。
       # 預設值來自 Pillow 12.x 的 Image.MAX_IMAGE_PIXELS;此處顯式化是為了讓 ops 可以調整
       # 而不需要 monkey-patch Pillow 全域狀態。沒對齊到 MAX_RENDER_PIXELS(40 MP)是刻意:
       # ingest 階段不展開像素,只嵌入到 PDF stream;render 階段 fit_dpi_to_pixel_budget 再縮。
       MAX_INGEST_IMAGE_PIXELS: int = _env_int("MAX_INGEST_IMAGE_PIXELS", 89_478_485)

       # Phase 4 image ingest:JPEG 重壓品質(D-01 Claude's discretion + RESEARCH Open Q2)。
       # 90 = 視覺無損上限(下游可能列印 / 簽核);85 = web 標準。預設 90 偏視覺品質;
       # 對 JPEG 直通路徑無影響(byte-exact passthrough);只在 PNG / TIFF 轉 RGB 後重新編碼
       # 為 JPEG 時生效。
       JPEG_REENCODE_QUALITY: int = _env_int("JPEG_REENCODE_QUALITY", 90)
       ```

    Step 4(conftest.py 新增 in-memory image fixture builders):
    1. 在 `tests/conftest.py` 第 32 行 `_build_pdf` 函式正下方新增 `_build_image_bytes` 與相關 fixtures。沿用 `_build_pdf` 的設計(in-memory 建立 bytes、return bytes、不 commit 二進位)。**所有 fixtures 必須以 Python 函式建構,絕不引用 `tests/fixtures/*.png` 之類靜態檔案**(目前 `tests/fixtures/` 目錄並不存在,且 conftest pattern 是 in-memory)。
    2. 新增 helper:`_build_png(width=400, height=300, mode="RGB", color=(200, 100, 50)) -> bytes`(用 `PIL.Image.new(mode, (w,h), color)` → `img.save(BytesIO, format="PNG")` → return bytes)。
    3. 新增 helper:`_build_jpeg(width=400, height=300, color=(200, 100, 50), quality=90) -> bytes`(同上,format="JPEG", quality=quality)。
    4. 新增 helper:`_build_tiff(width=400, height=300, mode="RGB", color=(200, 100, 50), num_frames=1) -> bytes`(format="TIFF";對 num_frames > 1,用 `img.save(buf, format="TIFF", save_all=True, append_images=[img2, img3])` 製造多頁 TIFF)。
    5. 新增 helper:`_build_cmyk_tiff(width=400, height=300) -> bytes`(`Image.new("CMYK", (w,h), (100, 100, 100, 50))` → save format="TIFF")。**注意:CMYK PNG 在 Pillow 12 不支援存 PNG,所以 CMYK 必須用 TIFF 或 JPEG 媒介**。
    6. 新增 fixture:`@pytest.fixture def png_bytes() -> bytes: return _build_png()`;同樣為 jpeg_bytes / tiff_bytes / multipage_tiff_bytes / cmyk_tiff_bytes。
    7. 新增 fixture:`@pytest.fixture def fake_png_bytes() -> bytes: return b"NOT_A_REAL_PNG_AT_ALL_DEFINITELY_BYTES"`(用於 sniff 失敗測試 — 副檔名上傳時叫 `.png` 但內容是隨機 bytes)。

    Step 5(tests/test_ingest.py 新增本 task 相關的 4 個 storage / engine 單元測試):
    1. `test_image_to_a4_pdf_produces_single_a4_page(png_bytes)` — 呼叫 `pdf_engine.image_to_a4_pdf(png_bytes)`,assert 結果 bytes 能用 `pdf_engine.open_pdf(result)` 開啟、`page_count(doc) == 1`、第一頁 `page.rect.width == 595.0` 且 `page.rect.height == 842.0`。
    2. `test_image_to_a4_pdf_jpeg_passthrough_is_compact(jpeg_bytes)` — 一張 50KB 量級 JPEG,結果 PDF bytes 應 < 200KB(實測 RESEARCH 8KB JPEG → 8KB roundtrip)。
    3. `test_storage_pristine_directory_exists_after_new_session()` — 呼叫 `storage.new_session()`,assert `storage.pristine_path(sid).parent.is_dir()`。
    4. `test_storage_write_pristine_copy(tmp_path)` — `storage.new_session()` → `storage.write_pristine_copy(sid, b"%PDF-fake")` → `pristine_path(sid).read_bytes() == b"%PDF-fake"`,且 `pristine_path(sid) != work_path(sid) != original_path(sid)` 三路徑各異。

    Step 6(documentation):
    在 `pdf_engine.py` 既有 module docstring(line 1–13)**不需修改** — AGPL 註解已涵蓋。新增的 `image_to_a4_pdf` 自己的 docstring 補上 "lives in pdf_engine.py because it must call `fitz.open() + new_page + insert_image` — AGPL seam invariant" 一句。
  </action>

  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_ingest.py::test_image_to_a4_pdf_produces_single_a4_page tests/test_ingest.py::test_image_to_a4_pdf_jpeg_passthrough_is_compact tests/test_ingest.py::test_storage_pristine_directory_exists_after_new_session tests/test_ingest.py::test_storage_write_pristine_copy -x -v</automated>
    
    additional check(grep gates,人工 sanity):
    1. `grep -c "import fitz" app/services/*.py` — must be exactly 1(只在 pdf_engine.py)。
    2. `grep -c "import pymupdf" app/services/*.py` — must be exactly 0(沒有人用 pymupdf 名)。
    3. `grep -n "A4_WIDTH_PT\|A4_HEIGHT_PT\|image_to_a4_pdf" app/services/pdf_engine.py` — 三個 identifier 都在(常數定義 + 函式定義)。
    4. `grep -n "pristine_path\|write_pristine_copy" app/storage.py` — 兩個函式都在。
    5. `grep -n '"pristine"' app/storage.py` — `_KINDS` tuple 內有 "pristine"。
  </verify>

  <done>
    - `app/services/pdf_engine.py` 新增 `A4_WIDTH_PT = 595.0`、`A4_HEIGHT_PT = 842.0` 與 `image_to_a4_pdf(image_bytes: bytes) -> bytes` 函式;沒有任何新的 `import` line(已有 `import fitz`)。
    - `app/storage.py` `_KINDS` 擴為 4 個 kind;新增 `pristine_path` + `write_pristine_copy` + `_PRISTINE_NAME` 常數;`new_session()` 自動為新 session 建立 4 個目錄。
    - `app/config.py` 新增 `MAX_INGEST_IMAGE_PIXELS = 89_478_485` 與 `JPEG_REENCODE_QUALITY = 90` 兩個 `_env_int` 常數。
    - `tests/conftest.py` 新增 in-memory image fixture builders(_build_png / _build_jpeg / _build_tiff / _build_cmyk_tiff)+ 6 個 pytest fixtures(png_bytes / jpeg_bytes / tiff_bytes / multipage_tiff_bytes / cmyk_tiff_bytes / fake_png_bytes);**沒有任何 `tests/fixtures/*.png` 之類 commit 進 repo**。
    - `tests/test_ingest.py` 新增 4 個本 task 單元測試,全部 pass。
    - `import fitz` 仍只在 `app/services/pdf_engine.py`(grep 確認)。
    - 既有 Phase 1–3 測試 zero-regression(可選擇執行 `pytest tests/ -x -q` 確認)。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-01-02: ingest.py magic-header 四 dispatch + Pillow 驗證鏈 + 三個新 IngestError code + main.py / sessions.py dict 同步</name>
  <files>app/services/ingest.py, app/main.py, app/api/sessions.py, tests/test_ingest.py</files>

  <read_first>
    - app/services/ingest.py(整檔,line 25–53 `_PDF_MAGIC` + `_looks_like_pdf`、line 35–41 `IngestError` class、line 56–118 `ingest_upload` validation 順序 — 新增的 sniff dispatch 必須嵌入這個順序)
    - app/services/logo.py(line 90–150,理解 `_validate_png` Pillow 驗證鏈:`img.format` 在 verify() 之前讀、`img.verify()`、re-open、`Image.DecompressionBombError` 與 `UnidentifiedImageError` 一起 except 的順序;_ingest_image 在 ingest.py 內必須 1:1 對齊此 pattern)
    - app/main.py(line 43–58 `_INGEST_STATUS` dict + line 52–58 `_handle_ingest_error` exception handler;**新增三條 code 映射,不動 handler**)
    - app/api/sessions.py(line 21–35 `_CODE_STATUS` mirror dict + `_ingest_http_error` 函式;**同步擴張 _CODE_STATUS**,planner 拍板暫不重構共用,優先穩定 phase 交付)
    - .planning/phases/04-raster-image-support/04-CONTEXT.md(D-02、D-03、D-04、D-11、D-12 段;Pitfall C / D / G / H)
    - .planning/phases/04-raster-image-support/04-RESEARCH.md(Pattern 1 + Pattern 2 完整程式碼;Example 1 _ingest_image_to_pdf 整合)
    - .planning/phases/04-raster-image-support/04-PATTERNS.md(ingest.py 段 line 26–101;app/main.py 段 line 324–356;app/api/sessions.py 同步段)
    - .planning/phases/04-raster-image-support/04-UI-SPEC.md(Inline error block 表 — 三個新後端 code 對應的繁中文案;Sniff 失敗段 unsupported_type 文案更新)
  </read_first>

  <behavior>
    - Behavior 1(_sniff_kind 四 magic dispatch):呼叫 `ingest._sniff_kind(b"%PDF-1.7\n...")` 回 `"pdf"`;`_sniff_kind(b"\x89PNG\r\n\x1a\n...")` 回 `"png"`;`_sniff_kind(b"\xff\xd8\xff\xe0...")` 回 `"jpeg"`;`_sniff_kind(b"II*\x00...")` 回 `"tiff"`;`_sniff_kind(b"MM\x00*...")` 回 `"tiff"`;`_sniff_kind(b"random_bytes_no_magic_at_all")` 回 `None`。
    - Behavior 2(PDF magic 容許 leading offset,image magic 必須 startswith):`_sniff_kind(b"\xef\xbb\xbf%PDF-")`(BOM + PDF)回 `"pdf"`;`_sniff_kind(b"\xef\xbb\xbf\x89PNG\r\n\x1a\n")`(BOM + PNG)回 `None`(image magic 不容許 leading offset);`_sniff_kind(b"X" * 9 + b"%PDF-")` 回 `None`(超過 8-byte offset)。
    - Behavior 3(假冒副檔名拒絕):上傳 `client.post("/sessions", files={"file": ("evil.png", fake_png_bytes, "image/png")})`,response status == 415,response.json()["detail"]["code"] == "unsupported_type"。
    - Behavior 4(空檔案文案更新):上傳 0-byte 檔案,response.json()["detail"]["message"] 包含「PDF 或影像」(更新文案,不再只說 PDF)。
    - Behavior 5(Pillow verify 鏈拒絕假 PNG):上傳一個檔案,其前 8 bytes 是 PNG magic(`\x89PNG\r\n\x1a\n`)但後續是隨機 bytes(`+ b"random_garbage" * 100`),sniff 通過(回 "png")但 Pillow `verify()` 失敗 → 4xx,code == "corrupt_image",status 422。
    - Behavior 6(多頁 TIFF 拒絕):上傳一個 3-page TIFF(`multipage_tiff_bytes` fixture),response status == 415,code == "multi_page_tiff_unsupported",message 含「暫不支援多頁 TIFF」。
    - Behavior 7(unsupported image format 拒絕):若 Pillow 解開後 format 不在 {"PNG","JPEG","TIFF"}(例如把 GIF / WebP / BMP magic 構造但 sniff 已濾掉 → 此 case 主要用「sniff 通過為 png 但 Pillow 認為 image.format == 'GIF'」的人造 case 觸發,或直接以 mock 路徑),code == "unsupported_image_format",status 415。本 task 至少測試 sniff 路徑;Pillow format mismatch 路徑可在 04-01-03 完整整合中覆蓋。
    - Behavior 8(_INGEST_STATUS dict mirror 同步):`main._INGEST_STATUS` 與 `api.sessions._CODE_STATUS` 兩個 dict 都包含三條新 key:`unsupported_image_format=415`、`multi_page_tiff_unsupported=415`、`corrupt_image=422`(可用 import 後 in-line assert,或新增一個 `test_ingest_status_dicts_in_sync` 對 keys 取 symmetric_difference 應為空集合)。
    - Behavior 9(既有 PDF 上傳零迴歸):`tests/test_ingest.py` 既有測試(`test_upload_valid_pdf` 之類)全 pass。
  </behavior>

  <action>
    Step 1(ingest.py 新增四 magic 常數 + `_sniff_kind`):
    1. 在 `app/services/ingest.py` 第 30 行 `_PDF_MAGIC = b"%PDF-"` 之後 / 第 32 行 `_PDF_MAGIC_MAX_OFFSET = 8` 之後新增:
       ```
       # Image magics MUST match at offset 0 — PNG/JPEG/TIFF spec do NOT permit leading
       # junk bytes before the header, unlike PDF which tolerates a BOM (~8 bytes).
       _PNG_MAGIC = b"\x89PNG\r\n\x1a\n"
       _JPEG_MAGIC = b"\xff\xd8\xff"
       _TIFF_LE_MAGIC = b"II*\x00"
       _TIFF_BE_MAGIC = b"MM\x00*"
       ```
    2. 在第 53 行 `_looks_like_pdf` 之後 / 第 56 行 `ingest_upload` 之前新增 `_sniff_kind(data: bytes) -> str | None`:
       - 讀 head = data[:1024];
       - PDF 路徑:沿用既有邏輯,`0 <= head.find(_PDF_MAGIC) <= _PDF_MAGIC_MAX_OFFSET` → `return "pdf"`;
       - PNG:`head.startswith(_PNG_MAGIC)` → `return "png"`;
       - JPEG:`head.startswith(_JPEG_MAGIC)` → `return "jpeg"`;
       - TIFF:`head.startswith(_TIFF_LE_MAGIC) or head.startswith(_TIFF_BE_MAGIC)` → `return "tiff"`;
       - else `return None`。
       - Docstring 引用 D-12 + Pitfall H(extension 不可信)+ RESEARCH Pattern 1 + 「Image magic 必須 offset 0,PDF magic 容許 offset ≤ 8」的差異。

    Step 2(ingest.py 新增 `_ingest_image` Pillow 驗證鏈):
    1. 在檔首 import 區(line 19–23)**新增**(沿用 logo.py 第 26 行的 import 形式):
       ```
       import io
       from PIL import Image, UnidentifiedImageError
       ```
       **不可 import fitz**(AGPL seam)。
    2. 在 `_sniff_kind` 之後新增 `_ingest_image(data: bytes, sniff_kind: str) -> bytes`:
       - 對應 RESEARCH Pattern 2 的完整鏈:
         - Step (a):`with Image.open(io.BytesIO(data)) as img:` → 讀 `fmt = img.format`(**必須在 verify 之前**)→ `img.verify()`;except `(UnidentifiedImageError, OSError, ValueError, Image.DecompressionBombError)` → raise `IngestError("corrupt_image", "影像檔案損壞或無法解析。")`。
         - Step (b):重新 `with Image.open(io.BytesIO(data)) as img:`(verify 後物件無效)→ 檢查 `n_frames = getattr(img, "n_frames", 1); if n_frames > 1: raise IngestError("multi_page_tiff_unsupported", "暫不支援多頁 TIFF,請先拆成單頁 TIFF 再上傳。")`。
         - Step (c):`if fmt not in ("PNG", "JPEG", "TIFF"): raise IngestError("unsupported_image_format", f"不支援的影像格式:{fmt}。")`。
         - Step (d):D-03 強制轉 RGB:`if img.mode != "RGB": img = img.convert("RGB")`;**alpha 在此被合成到白底**(RGBA → RGB 丟 alpha,合成 White A4 背景 — Pitfall G transparent PNG segfault 防護)。
         - Step (e):`img.load()` 強迫像素解碼(verify() 只 header sanity,真實 corrupt-payload 在 load() 才會 raise → 同 except 鏈處理)。
         - Step (f):重新編碼為 bytes — `if sniff_kind == "jpeg": img.save(buf, format="JPEG", quality=config.JPEG_REENCODE_QUALITY)` else `img.save(buf, format="PNG")`(PNG 給 PNG/TIFF 路徑,確保無 alpha 之後的 lossless roundtrip;JPEG 直通省檔肥)→ `return buf.getvalue()`。
       - 整段 docstring 引用 D-02 / D-03 / Pitfall C/D/G 與 logo.py 第 90–110 行的 sibling pattern。

    Step 3(ingest.py 把 `ingest_upload` 改為 dispatch + 新增 `_ingest_image_to_pdf` + 把既有 PDF 路徑重構為 `_ingest_pdf`):
    1. 把現有 `ingest_upload` 第 80–112 行 PDF 邏輯(從 `# 4. Parse` 到 `return SessionInfo(...)`)抽成新函式 `_ingest_pdf(filename: str, data: bytes) -> SessionInfo`(body 完全照搬,除了 storage 寫入路徑見 Step 4)。
    2. 把 `ingest_upload` 改為:
       ```python
       def ingest_upload(filename: str, data: bytes) -> SessionInfo:
           if not data:
               raise IngestError("empty_file", "檔案是空的,請選擇有內容的 PDF 或影像。")
           if len(data) > config.MAX_UPLOAD_BYTES:
               raise IngestError(
                   "file_too_large",
                   f"檔案過大,超過大小上限 {config.MAX_UPLOAD_MB} MB。",
               )
           kind = _sniff_kind(data)
           if kind is None:
               raise IngestError(
                   "unsupported_type",
                   "不支援的檔案類型,僅接受 PDF、PNG、JPG、TIFF。",
               )
           if kind == "pdf":
               return _ingest_pdf(filename, data)
           return _ingest_image_to_pdf(filename, data, kind)
       ```
       **訊息變更注意**:既有「不支援的檔案類型,本階段僅接受向量 PDF。」→ 改為「不支援的檔案類型,僅接受 PDF、PNG、JPG、TIFF。」(UI-SPEC 04 Sniff 失敗段鎖定)。既有「檔案是空的,請選擇有內容的 PDF。」→ 改為「檔案是空的,請選擇有內容的 PDF 或影像。」。
    3. 新增 `_ingest_image_to_pdf(filename: str, data: bytes, kind: str) -> SessionInfo`:
       - Step (a):`normalized_bytes = _ingest_image(data, kind)`(走 Pillow 鏈)。
       - Step (b):`pdf_bytes = pdf_engine.image_to_a4_pdf(normalized_bytes)`(透過 fitz seam wrapper 包成 A4 PDF)。
       - Step (c):驗證產出的 PDF 可開啟、page_count == 1(defensive check):`doc = pdf_engine.open_pdf(pdf_bytes); try: n_pages = pdf_engine.page_count(doc); if n_pages != 1: raise IngestError("corrupt_pdf", "正規化失敗,影像無法產出有效 PDF。") finally: pdf_engine.close(doc)`。
       - Step (d):session 建立 + 三寫(originals = raw image bytes,work = normalized PDF bytes,**pristine = normalized PDF bytes**):
         ```python
         session_id = storage.new_session()
         safe_name = storage.sanitize_filename(filename)
         storage.write_original(session_id, safe_name, data)          # raw image bytes(SHA-256 invariant)
         storage.write_work_copy(session_id, pdf_bytes)               # A4 PDF for downstream processing
         storage.write_pristine_copy(session_id, pdf_bytes)           # pipeline reset source
         storage.write_session_meta(session_id, page_count=1, filename=safe_name)
         return SessionInfo(session_id=session_id, page_count=1, filename=safe_name)
         ```

    Step 4(`_ingest_pdf` 在 PDF 路徑也補寫 pristine_copy):
    1. 在 `_ingest_pdf` body 結尾 `storage.write_work_copy(session_id, data)` 之後新增 `storage.write_pristine_copy(session_id, data)`(PDF 路徑下 originals == work == pristine 三者 byte-identical,但 pristine 是 pipeline 的真實 reset source — 這保證未來無論 originals 是 PDF 還是 image,pipeline 都從 pristine 取)。

    Step 5(pipeline.py 改 reset source 從 originals 為 pristine):
    1. 在 `app/services/pipeline.py` 第 105 行 `original = storage.original_path(session_id)` 之後新增 `pristine = storage.pristine_path(session_id)`。
    2. 把第 110 行的 `if Path(work).resolve() == Path(original).resolve()` invariant 從「work vs original」改為「work vs pristine」(reset source 是 pristine,不是 original)— 但 D-05 SHA-256 invariant 仍針對 originals/(originals 是 user-supplied raw bytes,SHA-256 不變;pipeline 不再依賴 originals/ 為 PDF)。實際改:
       ```python
       if Path(work).resolve() == Path(pristine).resolve():
           raise PipelineError("work_copy_misconfigured", "內部錯誤:工作副本路徑與初始 PDF 副本相同,已中止以保護工作流程。")
       ```
    3. 把第 123 行的 `if not Path(original).is_file()` 改為 `if not Path(pristine).is_file()`,訊息「找不到原始檔,無法重設工作副本。」→「找不到初始 PDF 副本,無法重設工作副本。」。
    4. 把第 129 行 `shutil.copyfile(original, work)` 改為 `shutil.copyfile(pristine, work)`。
    5. **保留**「originals/ 的 SHA-256 不變」測試保證(在 `test_pipeline.py` 既有 test_original_unchanged_*  測試 → pipeline 不再 touch originals,所以保證更強)。

    Step 6(main.py + api/sessions.py 兩處 dict 同步擴張):
    1. 在 `app/main.py` 第 43–49 行 `_INGEST_STATUS` dict 內新增三條:
       ```
       "unsupported_image_format": 415,
       "multi_page_tiff_unsupported": 415,
       "corrupt_image": 422,
       ```
    2. 在 `app/api/sessions.py` 第 21–27 行 `_CODE_STATUS` dict 同樣新增三條(保持兩 dict 同步;雖然 planner 拍板暫不重構共用,但**新增一個 sanity test 確保兩 dict 永遠同步**)。
    3. 在 `tests/test_ingest.py` 新增 `test_ingest_status_dicts_in_sync`:
       ```
       from app.main import _INGEST_STATUS as main_dict
       from app.api.sessions import _CODE_STATUS as api_dict
       assert set(main_dict.keys()) == set(api_dict.keys())
       for k in main_dict:
           assert main_dict[k] == api_dict[k]
       ```

    Step 7(tests/test_ingest.py 新增本 task 6 個整合 + 單元測試):
    1. `test_sniff_kind_dispatches_four_magics()` — 直接呼叫 `ingest._sniff_kind` 對 5 個 magic byte 字串各回正確 kind(pdf/png/jpeg/tiff/tiff)。
    2. `test_sniff_kind_pdf_tolerates_leading_offset()` — `_sniff_kind(b"\xef\xbb\xbf%PDF-1.7")` 回 "pdf";`_sniff_kind(b"\xef\xbb\xbf\x89PNG\r\n\x1a\n")` 回 None;`_sniff_kind(b"X"*9 + b"%PDF-")` 回 None。
    3. `test_extension_not_trusted_fake_png(fake_png_bytes, client)` — `client.post("/sessions", files={"file": ("evil.png", fake_png_bytes, "image/png")})` → 415,detail.code == "unsupported_type"。
    4. `test_multi_page_tiff_rejected(multipage_tiff_bytes, client)` — 上傳 3-page TIFF → 415,detail.code == "multi_page_tiff_unsupported",detail.message 含「暫不支援多頁 TIFF」。
    5. `test_corrupt_image_truncated_png(client)` — 上傳 `_PNG_MAGIC + b"garbage" * 100` → 422,detail.code == "corrupt_image"。
    6. `test_ingest_status_dicts_in_sync()` — 見 Step 6.3。
    7. `test_empty_file_message_mentions_image(client)` — 上傳空檔案 → 400,detail.message 含「PDF 或影像」。
  </action>

  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_ingest.py -x -v</automated>
    
    additional check:
    1. `grep -c "import fitz" app/services/ingest.py` — must be 0(ingest.py 不可 import fitz,AGPL seam)。
    2. `grep -c "from PIL import" app/services/ingest.py` — must be 1(Pillow OK)。
    3. `grep -n "unsupported_image_format\|multi_page_tiff_unsupported\|corrupt_image" app/main.py app/api/sessions.py` — 三個 code 在兩個檔內都至少各 1 處。
    4. `grep -n "_sniff_kind\|_ingest_image\|_ingest_image_to_pdf\|_ingest_pdf" app/services/ingest.py` — 四個函式都已定義。
    5. `grep -n "shutil.copyfile(pristine, work)" app/services/pipeline.py` — pipeline reset 改源已落地。
  </verify>

  <done>
    - `app/services/ingest.py` 含 `_PNG_MAGIC` / `_JPEG_MAGIC` / `_TIFF_LE_MAGIC` / `_TIFF_BE_MAGIC` 四個 magic byte 常數;`_sniff_kind` / `_ingest_image` / `_ingest_image_to_pdf` 三個新函式;`_ingest_pdf`(從原 PDF 路徑抽出);`ingest_upload` 為 dispatch shell;繁中 message 更新。
    - `import fitz` 仍只在 `pdf_engine.py`;ingest.py import 新增 `io` + `PIL.Image, UnidentifiedImageError`。
    - `app/main.py::_INGEST_STATUS` 與 `app/api/sessions.py::_CODE_STATUS` 兩 dict 同步新增三條 key,值對齊。
    - `app/services/pipeline.py::process_job` reset-from-pristine 改為從 `pristine_path` 而非 `original_path` 取得 source(line 105 / 110 / 123 / 129 共 4 處更新)。
    - `tests/test_ingest.py` 既有測試 zero-regression,新增 7 個測試全 pass。
    - 既有 Phase 1–3 全測試 `pytest tests/ -x -q` 仍 pass(尤其 `tests/test_pipeline.py::test_original_unchanged_*`、`tests/test_redact.py::test_remove_region_removes_text_and_vector` 等 pipeline 相關測試)。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-01-03: 前端 dropzone + 三個錯誤 COPY + switch case + 影像端到端 integration test(走完 vector 分支)</name>
  <files>web/index.html, web/js/app.js, web/js/api.js, tests/test_ingest.py, tests/test_process_api.py</files>

  <read_first>
    - web/index.html(line 277–299 dropzone block)
    - web/js/app.js(整檔,line 18–33 COPY 字典 + line 86–113 messageForError + line 115–119 showError + line 121–145 handleFile)
    - web/js/api.js(整檔,理解 errors -> 4xx { detail: { code, message } } transport contract)
    - .planning/phases/04-raster-image-support/04-UI-SPEC.md(Copywriting Contract 完整段 + Inline error block 表 + What Phase 4 Deliberately Does NOT Touch 段)
    - .planning/phases/04-raster-image-support/04-PATTERNS.md(web/index.html 與 web/js/app.js 段)
    - tests/test_process_api.py(整檔,理解 FastAPI TestClient 走完 /sessions → /sessions/{id}/process → /sessions/{id}/result 端到端 pattern;影像上傳整合測試沿用此 pattern,不用真實瀏覽器)
    - tests/test_ingest.py(理解 既有上傳整合測試 pattern,確認與 client fixture 相容)
  </read_first>

  <behavior>
    - Behavior 1(dropzone 文案 + accept 屬性更新):`web/index.html` 渲染後,`#dropzone[aria-label]` == "選擇 PDF 或影像檔案";`.dropzone__heading` text == "上傳 PDF 或影像以開始";`.dropzone__body` 含「PNG、JPG、TIFF」;`#choose-file` text == "選擇檔案";`.dropzone__hint` text == "支援 PDF、PNG、JPG、TIFF(單一檔案)";`#file-input[accept]` == "application/pdf,.pdf,image/png,image/jpeg,image/tiff,.png,.jpg,.jpeg,.tif,.tiff";`.dropzone__secondary` text 不變(仍為「或將檔案拖曳到這裡」)。
    - Behavior 2(app.js COPY 字典新增三條):`COPY.unsupportedImageFormat` 字串等於 UI-SPEC 鎖定的「此影像格式不支援。請改用 PDF、PNG、JPG 或 TIFF 檔案後再試一次。」;`COPY.multiPageTiffUnsupported` 等於「暫不支援多頁 TIFF。請先將 TIFF 拆成單頁後再上傳。」;`COPY.corruptImage` 等於「這個影像檔案無法讀取,可能已損毀。請確認檔案後再試一次。」。
    - Behavior 3(messageForError switch 新增三個 case):呼叫 `messageForError({code: "unsupported_image_format"})` 回 `COPY.unsupportedImageFormat`;`{code: "multi_page_tiff_unsupported"}` 回 `COPY.multiPageTiffUnsupported`;`{code: "corrupt_image"}` 回 `COPY.corruptImage`;既有 case(unsupported_type / corrupt_pdf / file_too_large / too_many_pages / empty_file)行為不變。
    - Behavior 4(unsupported_type 文案更新):`COPY.unsupportedType` 從「此檔案格式不支援。請改用 PDF 檔案後再試一次。」改為「此檔案格式不支援。請改用 PDF、PNG、JPG 或 TIFF 檔案後再試一次。」。
    - Behavior 5(PNG 端到端 integration):`client.post("/sessions", files={"file": ("scan.png", png_bytes, "image/png")})` → 201;`response.json()["page_count"] == 1`;`response.json()["filename"] == "scan.png"`(原檔名保留);`/sessions/{id}/pages/0/image` GET 回 PNG image。
    - Behavior 6(JPEG / TIFF 端到端):同 Behavior 5,filename 分別為 "drawing.jpg" / "scan.tiff",page_count == 1。
    - Behavior 7(影像 + 走完 vector 分支套用流程):上傳 PNG → 對 page 0 構造一個 px_rect 區域 → POST /sessions/{id}/process(JobSpec dpi=200, regions=[{page:0, px_rect:[x0,y0,x1,y1]}])→ 200 result.regions[0]["removed"] in (True, False)(會走既有 vector 分支因 04-02 尚未上線,所以對 image-only PDF 中的 image 框選會「無聲 no-op」— 這在 04-02 才修),但**至少不 raise**;`/sessions/{id}/result` GET 回 PDF bytes(以 `%PDF-` 開頭)。
    - Behavior 8(SHA-256 originals 不動性):上傳 PNG → 跑 process → originals/ 內的 PNG bytes SHA-256 與上傳時一致(D-05 不動性對 image 路徑同樣成立 — 因為 pipeline 已改為從 pristine reset,不再 touch originals)。
    - Behavior 9(下載檔名 stem 命名):上傳 "scan.png" → process 完成 → `/sessions/{id}/result` 的 Content-Disposition 含 `scan_logoswap.pdf`(沿用 D-13 / Phase 2 `_logoswap_name`)。
  </behavior>

  <action>
    Step 1(web/index.html 六處字串 + accept 屬性更新):
    1. 將 `#dropzone[aria-label]` 從 `"選擇 PDF 檔案"` 改為 `"選擇 PDF 或影像檔案"`。
    2. 將 `<h2 class="dropzone__heading">` text 從 `上傳 PDF 以開始` 改為 `上傳 PDF 或影像以開始`。
    3. 將 `<p class="dropzone__body">` text 從 `選擇或拖曳一個供應商 PDF 檔案,即可在此預覽各頁內容。原始檔案不會被更動。` 改為 `選擇或拖曳一個 PDF 或影像檔案(PNG、JPG、TIFF),即可在此預覽並框選要替換的商標。原始檔案不會被更動。`。
    4. 將 `<button id="choose-file">` text 從 `選擇 PDF 檔案` 改為 `選擇檔案`。
    5. **不動** `<p class="dropzone__secondary">或將檔案拖曳到這裡</p>`(UI-SPEC 鎖定不變)。
    6. 將 `<p class="dropzone__hint">` text 從 `支援 PDF 檔(單一檔案)` 改為 `支援 PDF、PNG、JPG、TIFF(單一檔案)`。
    7. 將 `<input id="file-input" accept>` 從 `"application/pdf,.pdf"` 改為 `"application/pdf,.pdf,image/png,image/jpeg,image/tiff,.png,.jpg,.jpeg,.tif,.tiff"`。
    8. **不動**:dropzone 的 class、id、role、tabindex、結構標籤、`<input type="file">` 的 id / class / 隱藏方式 — 全部 zero-change(UI-SPEC「What Phase 4 Deliberately Does NOT Touch」段)。

    Step 2(web/js/app.js COPY 字典擴張 + unsupportedType 文案更新):
    1. 將 `COPY.unsupportedType` 從 `"此檔案格式不支援。請改用 PDF 檔案後再試一次。"` 改為 `"此檔案格式不支援。請改用 PDF、PNG、JPG 或 TIFF 檔案後再試一次。"`。
    2. 在 `COPY` 物件內(networkFailure 之後)新增三個 key:
       ```javascript
       unsupportedImageFormat: "此影像格式不支援。請改用 PDF、PNG、JPG 或 TIFF 檔案後再試一次。",
       multiPageTiffUnsupported: "暫不支援多頁 TIFF。請先將 TIFF 拆成單頁後再上傳。",
       corruptImage: "這個影像檔案無法讀取,可能已損毀。請確認檔案後再試一次。",
       ```

    Step 3(web/js/app.js messageForError switch 三個 case):
    1. 在 `messageForError` 函式內(現有 `case "corrupt_pdf":` 之後 / `case "file_too_large":` 之前)新增三個 case,group 在「PDF family」與「image family」之間 visually contiguous:
       ```javascript
       case "unsupported_image_format":
         return COPY.unsupportedImageFormat;
       case "multi_page_tiff_unsupported":
         return COPY.multiPageTiffUnsupported;
       case "corrupt_image":
         return COPY.corruptImage;
       ```
    2. **不動**:`showError` / `errorBody.textContent` 注入路徑(沿用 Phase 1 T-01-14 / T-01-15 XSS 防護)。

    Step 4(web/js/api.js docstring comment 同步):
    1. 找到 api.js 第 10–11 行附近「errors -> 4xx { detail: { code, message } } code in: ...」comment,把 code 列表從 `unsupported_type | file_too_large | too_many_pages | corrupt_pdf | empty_file` 擴為 `unsupported_type | file_too_large | too_many_pages | corrupt_pdf | empty_file | unsupported_image_format | multi_page_tiff_unsupported | corrupt_image`。
    2. **api.js 的 JS 程式碼本身不動**(它只 transport,不 code-specific 處理)。

    Step 5(tests/test_ingest.py 新增影像 ingest 端到端整合測試):
    1. `test_png_upload_normalizes_to_a4_pdf(png_bytes, client)`:
       ```python
       resp = client.post("/sessions", files={"file": ("scan.png", png_bytes, "image/png")})
       assert resp.status_code == 201
       body = resp.json()
       assert body["page_count"] == 1
       assert body["filename"] == "scan.png"
       sid = body["session_id"]
       # work copy is a PDF; originals is the raw PNG bytes
       from app import storage
       assert storage.work_path(sid).read_bytes().startswith(b"%PDF-")
       assert storage.original_path(sid).parent.is_dir()
       # /pages/0/image must return a PNG (server-rendered preview of the normalized A4 page)
       img_resp = client.get(f"/sessions/{sid}/pages/0/image")
       assert img_resp.status_code == 200
       assert img_resp.content.startswith(b"\x89PNG")
       ```
    2. `test_jpeg_upload_normalizes_to_a4_pdf(jpeg_bytes, client)`:同上,filename "drawing.jpg",mime "image/jpeg"。
    3. `test_tiff_upload_normalizes_to_a4_pdf(tiff_bytes, client)`:同上,filename "scan.tiff",mime "image/tiff"。
    4. `test_cmyk_tiff_normalized_to_rgb(cmyk_tiff_bytes, client)`:上傳 CMYK TIFF → 201;進一步開啟 work_path PDF,確認 `page.get_images()` 回的 entry colorspace 是 RGB / ICCBased(實作可呼叫 `pdf_engine.open_pdf(work_path)` → `page = doc[0]` → `imgs = page.get_images()` → assert len(imgs) >= 1)。本測試的 sanity 重點是「沒 crash + 產出有效 PDF」,深度檢查 colorspace 可在 04-02 一併處理。
    5. `test_originals_sha256_unchanged_after_image_run(png_bytes, client, tmp_path)`:
       ```python
       import hashlib
       expected_sha = hashlib.sha256(png_bytes).hexdigest()
       resp = client.post("/sessions", files={"file": ("scan.png", png_bytes, "image/png")})
       sid = resp.json()["session_id"]
       # 跑一個 trivial process job(no logo, simple region — pipeline 沿用 vector 分支)
       job = {"dpi": 200, "regions": [{"page": 0, "px_rect": [50.0, 50.0, 250.0, 200.0]}]}
       proc = client.post(f"/sessions/{sid}/process", json=job)
       assert proc.status_code == 200
       # originals 仍是原始 PNG bytes,SHA-256 不變
       from app import storage
       actual_sha = hashlib.sha256(storage.original_path(sid).parent.glob("*")[0].read_bytes()).hexdigest()
       # 注意:write_original 用 fixed _ORIGINAL_NAME = "source.pdf",但對 image 路徑寫入的是 PNG bytes,
       # filename 是 fixed,bytes 是原 PNG — SHA-256 對比 png_bytes hash 應一致。
       assert hashlib.sha256(storage.original_path(sid).read_bytes()).hexdigest() == expected_sha
       ```
       **注意 storage.original_path 仍回 `<DATA_DIR>/originals/<sid>/source.pdf`** — 即便 image 路徑寫入的是 PNG bytes,檔名仍是 `source.pdf`(_ORIGINAL_NAME)。這是既有不變行為,SHA-256 對 bytes 不對 path,所以仍能驗證 invariant。**若 executor 認為這樣命名怪異,可在 04-01-02 storage.write_original 將檔名改為 ext-aware,但本 plan 不要求 — 保守起見維持 fixed 名,bytes 對比即可**。
    6. `test_image_upload_download_filename_uses_stem(png_bytes, client)`:
       ```python
       resp = client.post("/sessions", files={"file": ("scan.png", png_bytes, "image/png")})
       sid = resp.json()["session_id"]
       job = {"dpi": 200, "regions": [{"page": 0, "px_rect": [50.0, 50.0, 250.0, 200.0]}]}
       proc = client.post(f"/sessions/{sid}/process", json=job)
       result = client.get(f"/sessions/{sid}/result")
       cd = result.headers.get("content-disposition", "")
       assert "scan_logoswap.pdf" in cd or "scan_logoswap.pdf" in cd.lower() or "filename*" in cd  # D-13,沿用 Phase 2 `_logoswap_name`
       ```

    Step 6(tests/test_process_api.py 加 1 個影像 + logo placement integration — 部分覆蓋 Phase 4 success criteria #3):
    1. `test_png_upload_with_logo_placement(png_bytes, client, logo_library)`:
       ```python
       resp = client.post("/sessions", files={"file": ("scan.png", png_bytes, "image/png")})
       sid = resp.json()["session_id"]
       # Phase 3 logo 庫的 logo_library fixture 在 conftest.py 已存在
       logos_resp = client.get("/logos")
       assert logos_resp.status_code == 200
       logos_list = logos_resp.json()
       if logos_list:  # 容錯:空 library 直接 skip(graceful degradation)
           job = {
               "dpi": 200,
               "regions": [{"page": 0, "px_rect": [100.0, 100.0, 400.0, 300.0]}],
               "logo_id": logos_list[0]["id"],
           }
           proc = client.post(f"/sessions/{sid}/process", json=job)
           assert proc.status_code == 200
           assert proc.json()["logo_skipped"] is False
       ```

  </action>

  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_ingest.py tests/test_process_api.py -x -v</automated>
    
    additional check:
    1. `grep -c "image/png" web/index.html` — 至少 1(accept 屬性內)。
    2. `grep -c "上傳 PDF 或影像以開始" web/index.html` — exactly 1(dropzone heading)。
    3. `grep -n "unsupportedImageFormat\|multiPageTiffUnsupported\|corruptImage" web/js/app.js` — 三個 key 都在 COPY 物件內,並且各有 1 個 switch case。
    4. `grep -c "支援 PDF 檔(單一檔案)" web/index.html` — must be 0(舊文案已移除)。
    5. `grep -c "選擇 PDF 檔案" web/index.html web/js/app.js` — 應為 0(舊 aria-label 與 button text 都已移除;若 grep 命中要核對是否為註解內 reference,如為 active text 必須移除)。
    6. 既有 Phase 1 dropzone 視覺(虛線框、drag-over accent、focus ring)不變:`grep -c "dropzone__heading\|dropzone__body\|dropzone__hint\|dropzone__secondary" web/index.html` 仍各為 1(結構不變)。
  </verify>

  <done>
    - `web/index.html` 六處字串/屬性更新到 UI-SPEC 04 鎖定文案;`accept` 屬性擴張為 8 個格式 token。
    - `web/js/app.js` `COPY` 字典新增 3 個 key、更新 1 個既有 key 文案;`messageForError` switch 新增 3 個 case;順序在 corrupt_pdf 之後 / file_too_large 之前。
    - `web/js/api.js` docstring comment 同步補上 3 個新 error code。
    - `tests/test_ingest.py` 新增 6 個影像 ingest + 端到端 integration 測試,全部 pass。
    - `tests/test_process_api.py` 新增 1 個影像 + logo placement integration 測試,pass(或在空 logo library 時 graceful skip)。
    - 既有 Phase 1–3 全測試 `pytest tests/ -x -q` 仍 pass(零迴歸)。
    - **使用者實際可運作流程(端到端):** 開瀏覽器 → 上傳 scan.png → dropzone 顯示新文案 → 預覽 A4 page → 框選一塊區域 → 套用 → 下載 scan_logoswap.pdf(走 vector 分支,框內 image pixel 不變 — 04-02 才補,但端到端不 crash、檔名正確、無 regression)。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /sessions multipart upload | 未受信任的 image bytes + filename 跨進系統 — 假冒副檔名、超大檔、decompression bomb、CMYK / alpha mask、ELF polyglot 都可能進入 |
| ingest.py `_sniff_kind` → ingest.py `_ingest_image` | sniff 是粗濾(magic byte),Pillow `verify()` + reopen + `n_frames` + `convert("RGB")` + `load()` 是細濾(真實格式驗證 + 像素解碼)|
| ingest.py `_ingest_image` → pdf_engine.image_to_a4_pdf | 已 normalize 過的 RGB bytes 進入 AGPL seam,fitz.open + new_page + insert_image — fitz 接受的 stream 已知為 valid Pillow-encoded PNG/JPEG |
| Server filesystem | originals/ = user raw bytes(chmod 0o444、SHA-256 invariant);work/ = normalized A4 PDF(可寫);pristine/ = normalized A4 PDF(reset source,不可寫只可被 copy)|

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Spoofing | ingest._sniff_kind | mitigate | 不信任副檔名;PNG/JPEG/TIFF magic 強制 `startswith` offset 0;PDF magic 沿用 ≤ 8 offset(historical reason WR-05);sniff 失敗 → unsupported_type 415(test_extension_not_trusted_fake_png 證實) |
| T-04-01-02 | Tampering | ingest._ingest_image Pillow chain | mitigate | `verify()` 是 header sanity + decompression bomb 防護;`load()` 是真實像素解碼(catch corrupt payload);任何 Pillow 失敗 → corrupt_image 422 typed error,絕不 escape 為 500(test_corrupt_image_truncated_png 證實) |
| T-04-01-03 | Repudiation | originals/ SHA-256 invariant | mitigate | `storage.write_original` 沿用 chmod 0o444 + write-once;pipeline.process_job reset source 改為 pristine/,不再 touch originals/;test_originals_sha256_unchanged_after_image_run 證實 |
| T-04-01-04 | Information Disclosure | UI badge / mode disclosure | mitigate | UI-SPEC D-07 contract 明令禁止任何「raster/vector/scan」badge / icon / tooltip;dropzone 五處字串 / 三個錯誤碼文案 / accept 屬性 都不揭露分類;PATTERNS.md「What Phase 4 Deliberately Does NOT Touch」段保證下游元件不動 |
| T-04-01-05 | Denial of Service(decompression bomb)| ingest._ingest_image | mitigate | Pillow `MAX_IMAGE_PIXELS = 89,478,485`(config.MAX_INGEST_IMAGE_PIXELS 顯式化)+ `verify()` 觸發 `DecompressionBombError`;multi-page TIFF 也是 DoS 載體 → n_frames > 1 立即 415 拒絕(test_multi_page_tiff_rejected) |
| T-04-01-06 | Denial of Service(超大檔案) | api.sessions.create_session 上傳串流 | mitigate | 沿用 Phase 1 既有早期 size cap(每 1MB chunk + 累計 > MAX_UPLOAD_BYTES 立即 413 ),image 路徑不改變上限(D-04 沿用 50 MB)|
| T-04-01-07 | Tampering(image 路徑 reset-from-pristine 漏洞) | pipeline.process_job | mitigate | Planner 拍板「雙寫 pristine_pdf」;ingest 階段 originals=raw bytes、work=PDF、pristine=PDF;pipeline 從 pristine copy 進 work — 對 PDF 路徑 originals==pristine==work bytes-identical,對 image 路徑 originals 為 PNG/JPG/TIFF、work 與 pristine 都是 normalized A4 PDF;**沒有任何路徑會把 PNG bytes 當 PDF 開啟造成 crash** |
| T-04-01-08 | Elevation(path traversal)| storage.subdir("pristine", session_id) | accept | 沿用既有 `validate_session_id` token allowlist + `subdir` defense-in-depth 容器內檢查(`is_relative_to(DATA_DIR)`);新增 "pristine" kind 走相同路徑,不引入新 traversal sink |
| T-04-01-09 | Information Disclosure(XSS via error message) | web/js/app.js showError | mitigate | 沿用 Phase 1 既有 `errorBody.textContent` 注入(T-01-14 / T-01-15);新增的繁中錯誤訊息與 COPY 字典都是靜態字串,不含使用者輸入 |

</threat_model>

<verification>
**Plan-level verification(所有 task 完成後一次跑):**

1. **單元 + 整合測試**:
   ```
   .venv/Scripts/python.exe -m pytest tests/ -x -q
   ```
   全綠;新增約 17 個測試,既有約 ~80 個測試零迴歸。

2. **AGPL seam grep gate**(防回歸):
   ```
   grep -l "import fitz" app/services/ app/api/ app/*.py
   ```
   結果只有 `app/services/pdf_engine.py` 一行。

3. **繁中文案 invariant grep**:
   ```
   grep -c "上傳 PDF 或影像以開始" web/index.html
   ```
   == 1(dropzone heading 已更新)。

4. **既有 PDF flow 零迴歸 sanity**:
   人工或既有 `tests/test_process_api.py::test_process_endpoint_*` 跑 PDF 上傳→框選→套用→下載,結果 PDF 與 Phase 3 結尾時 bit-identical(除了允許的時間戳之外)。

5. **影像端到端使用者驗證**(可由 user 手動或 integration test):
   - 上傳 scan.png(任何 800x600 RGB PNG)→ 預覽顯示 A4 portrait 頁面,影像置中、上下留白白色。
   - 上傳 drawing.jpg → 預覽顯示 A4 portrait;檔肥檢查:work_path(sid).stat().st_size 應接近原 JPEG bytes(JPEG 直通)。
   - 上傳 scan.tiff(單頁) → 預覽顯示;TIFF 經 PNG/JPEG 重編碼後嵌入,檔肥不超過原 TIFF 2x。
   - 上傳 multipage.tiff(3 頁) → inline error block 顯示「暫不支援多頁 TIFF。請先將 TIFF 拆成單頁後再上傳。」並有 重試 按鈕。
   - 上傳 evil.png(內容是 ELF / 隨機 bytes) → inline error block 顯示「此檔案格式不支援。請改用 PDF、PNG、JPG 或 TIFF 檔案後再試一次。」。
   - 上傳 truncated.png(magic 對但 payload 截斷) → inline error block 顯示「這個影像檔案無法讀取,可能已損毀。請確認檔案後再試一次。」。
</verification>

<success_criteria>
**Plan 04-01 完成的可驗證標準:**

1. **REQ UPLOAD-03 字面達成**:使用者上傳 PNG / JPG / TIFF(單頁) → 系統正規化為 A4 portrait 單頁 PDF → 走完既有預覽 / 框選 / 套用 / 下載流程 → 下載得到 `{stem}_logoswap.pdf`。(test_png/jpeg/tiff_upload_normalizes_to_a4_pdf 一系列證實)
2. **REQ UPLOAD-02 部分覆蓋**:image-only PDF 上傳走既有 PDF 路徑(本 plan 不需特殊改動,vector PDF 上傳已 work);**REMOVE-02 raster 分支完整實作落在 04-02**。
3. **三道防護鏈完整**:Magic sniff(粗) → Pillow verify(細) → Pillow load(像素解碼);任一層失敗都映射到 typed 4xx,絕不 500。(test_extension_not_trusted_fake_png + test_corrupt_image_truncated_png 證實)
4. **deferred-mutation D-05 invariant 仍成立且更強**:originals/ SHA-256 對 user-supplied bytes 不變;pipeline 從 pristine/ reset,不再 touch originals/。(test_originals_sha256_unchanged_after_image_run 證實)
5. **AGPL seam invariant 仍成立**:`import fitz` / `import pymupdf` 只在 `app/services/pdf_engine.py`(grep 確認;既有 test_fitz_import_confined_to_engine_seam 仍 pass)。
6. **UI-SPEC 04 鎖定文案逐字落地**:dropzone 五處字串 + 三個新錯誤訊息 + unsupported_type 更新文案 + accept 屬性都 byte-exact 對齊 UI-SPEC 04 Copywriting Contract 表。
7. **D-07 contract 嚴守**:UI 任何位置沒有 "raster" / "vector" / "scan" / "點陣" / "向量" / "掃描" 字眼(grep 確認)。
8. **Phase 1–3 零迴歸**:`pytest tests/ -x -q` 全綠,既有所有測試 pass。
</success_criteria>

<output>
After completion, create `.planning/phases/04-raster-image-support/04-01-SUMMARY.md` summarizing:
- 實際修改的檔案 + 行數
- 新增的 fixtures / 測試清單
- pristine_path 雙寫機制對 pipeline.process_job 帶來的契約變化
- 對 04-02 的 hand-off:`pdf_engine.image_to_a4_pdf` 已就位、`pristine` 目錄已就位、ingest dispatch 已就位、UI 已接受 image — 04-02 需要做的剩下「pdf_engine.rect_overlaps_image」「pdf_engine.IMAGE_PIXELS 常數」「redact.remove_region_vector / _raster 拆分」「pipeline 每框 dispatch」
- 任何發現需要在 04-02 重新審視的邊角(尤其 CMYK colorspace 深度檢查 / EXIF orientation 等 deferred 議題)
</output>
</content>
</invoke>