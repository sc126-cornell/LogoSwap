---
phase: 07-option-b-implementation-content-stream-surgery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/pdf_engine.py
  - tests/test_pdf_engine.py
autonomous: true
requirements:
  - SEC-02
  - SEC-03
  - TEST-03
tags:
  - pdf
  - content-stream
  - regex
  - redaction
  - pymupdf
  - security
must_haves:
  truths:
    - "新 helper `delete_zero_area_type_f_fills_inside(page, user_rect, tolerance)` 存在於 `app/services/pdf_engine.py`,簽名與 D-A3 一致"
    - "新 helper `log_xobject_intersect(page, user_rect, logger)` 存在於 `app/services/pdf_engine.py`,接受 logger 注入"
    - "helper 對「無零面積 type='f' fill」的正常向量 PDF 為 no-op(SEC-02 — return 0、page.read_contents() bytes 不變)"
    - "helper 對 form-XObject 內巢狀的零面積 fill 不下鑽(SEC-03 — page-level only;XObject xref stream bytes 不變)"
    - "user_rect 與 form-XObject bbox intersect 時,log_xobject_intersect 觸發 `option_b_xobject_intersect` warning + 結構化 extra dict"
    - "Cardinality 不一致時(ranges_to_delete count != zafs count)→ helper return 0 + 觸發 `option_b_parse_anomaly` warning,絕不寫入 stream(D-A5 fail-safe)"
    - "Helper 在同一 page 上 re-entrant safe — 連續呼叫第二次回傳 0(Open Q3)"
    - "TEST-03 14 個 case 全綠;safe-skip 5 context 各一 test 通過;密度梯度 0/1/100/1742 各一 test 通過"
    - "AGPL seam 不破 — `grep -rn 'import fitz' app/` 仍只回 `app/services/pdf_engine.py:19`"
    - "Production code 改動範圍只在 `app/services/pdf_engine.py`(`redact.py` 本 plan 不動 — Plan 07-02 才動)"
    - "Phase 6 紅燈 regression test 在本 plan close 時 **仍然 XFAIL**(integration 還未接,本 plan close 不能讓它 XPASS)"
  artifacts:
    - path: "app/services/pdf_engine.py"
      provides: "delete_zero_area_type_f_fills_inside + log_xobject_intersect helpers + module-level _SAFE_SKIP_REGIONS_RE / _RE_FILL_RECT_RE / _Q_BLOCK_RE compiled patterns + logger"
      contains: "def delete_zero_area_type_f_fills_inside"
    - path: "tests/test_pdf_engine.py"
      provides: "TEST-03 unit tests — 14 cases (4 density gradient parametrized + 2 SEC-02 no-op + 5 safe-skip + 3 SEC-03 form-XObject)"
      contains: "def test_option_b_density_gradient"
  key_links:
    - from: "tests/test_pdf_engine.py"
      to: "app/services/pdf_engine.delete_zero_area_type_f_fills_inside"
      via: "from app.services import pdf_engine; pdf_engine.delete_zero_area_type_f_fills_inside(page, user_rect)"
      pattern: "delete_zero_area_type_f_fills_inside\\("
    - from: "app/services/pdf_engine.delete_zero_area_type_f_fills_inside"
      to: "page.get_drawings() + page.read_contents() + doc.update_stream"
      via: "STEP A pre-screen + STEP B mask + STEP C anchor regex + STEP D cardinality + STEP E PATTERNS S1 multi-stream write-back"
      pattern: "page\\.read_contents\\(\\)|doc\\.update_stream\\("
    - from: "app/services/pdf_engine.log_xobject_intersect"
      to: "page.get_xobjects()"
      via: "iterate (xref, name, invoker, bbox) tuples; bbox.intersects(user_rect)"
      pattern: "page\\.get_xobjects\\(\\)"
---

<objective>
落地 Phase 7 Option B 核心 helper —— **page-level content-stream surgery** + form-XObject intersect 透明化 log + TEST-03 完整單元測試覆蓋(密度梯度 + safe-skip 5 context + SEC-02 no-op + SEC-03 form-XObject)。

Purpose: 把「真正刪除 fully-inside-rect 零面積 type='f' fill」這件事的 mechanic 鎖死在 AGPL seam(`pdf_engine.py`)內,並用 14 個 TEST-03 case 把 helper 的「safe-skip 邊界、密度梯度、fail-safe、re-entrancy、form-XObject 不下鑽」這 5 條 invariants 變成可重現 pytest gate。Plan 07-01 close 時 helper 已存在且 unit-tested,**但尚未串到 redact.py dispatcher** — Phase 6 紅燈 regression test 仍 XFAIL(integration handoff 在 Plan 07-02)。

Output:
- `app/services/pdf_engine.py` 新增兩個 helper(`delete_zero_area_type_f_fills_inside`、`log_xobject_intersect`)+ module-level compiled regex patterns + `import logging` + `import re` + `logger = logging.getLogger(__name__)`
- `tests/test_pdf_engine.py`(新檔)— 14 個 TEST-03 case,沿用 `tests/conftest.py::_build_pdf` + `Shape.draw_rect(W=0).finish(fill=).commit()` in-memory 構造 pattern
- Production code 改動 audit:`git diff --stat app/` 應只命中 `app/services/pdf_engine.py`
- AGPL guard 仍綠:`python -m pytest tests/test_redact.py::test_fitz_import_confined_to_engine_seam -v` 1 PASSED
- 既有 baseline 不退步:`python -m pytest 2>&1 | tail -3` 仍 `301 passed + 3 skipped + 3 xfailed`(Phase 6 xfail 保留 — 本 plan 不動);加上 TEST-03 14 個 PASSED → 升級為 `315 passed + 3 skipped + 3 xfailed`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-option-b-implementation-content-stream-surgery/07-CONTEXT.md
@.planning/phases/07-option-b-implementation-content-stream-surgery/07-RESEARCH.md
@.planning/phases/07-option-b-implementation-content-stream-surgery/07-PATTERNS.md
@.planning/phases/06-regression-foundation-threat-model-re-evaluation/06-PATTERNS.md
@.planning/phases/06-regression-foundation-threat-model-re-evaluation/06-SECURITY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from existing codebase. -->
<!-- DO NOT re-explore. These are the contracts. -->

From app/services/pdf_engine.py (existing — DO NOT modify; new helpers append):
- `_DEGENERATE_BBOX_EPS = 0.01`  (line 261) — IN-01 epsilon shared with get_drawings_fully_inside + cover_zero_area_artefacts; new helper's `tolerance` default MUST equal this constant (no new constant)
- `ZERO_AREA_RASTER_THRESHOLD = 100`  (line 294) — dispatcher threshold; Plan 07-01 does NOT use this
- `def _rect_contains(query, candidate) -> bool`  (line 508) — fully-inside test (reuse, do not reimplement)
- `def count_zero_area_fills_fully_inside(page: "fitz.Page", rect: tuple[float, float, float, float]) -> int`  (line 699) — sanity dependency for TEST-03 pre/post assertions
- `def replace_region_with_white_raster(page: "fitz.Page", rect)`  (line 746) — DO NOT touch (Option A overlay; last-mile defense in Phase 4-6 dispatcher)
- `def cover_zero_area_artefacts(page, rect)`  (line 635 区段) — DO NOT touch (sparse-residue last-mile defense)
- Existing module imports at lines 15-19: `from __future__ import annotations`, `from pathlib import Path`, `import fitz` (sole `import fitz` in app/**/*.py — AGPL seam)

From PyMuPDF 1.27.2.3 (verified via Context7 + WebFetch in 07-RESEARCH § Standard Stack):
- `page.read_contents() -> bytes` — concatenation of all `/Contents` xrefs, decompressed; **page-level only — does NOT include Form XObject internal streams**
- `page.get_contents() -> list[int]` — content xref list (multi-stream pages return ≥ 2)
- `doc.update_stream(xref: int, data: bytes, *, compress: bool = True)` — replaces stream of object; auto-deflate
- `page.get_xobjects() -> list[tuple]` — each tuple = `(xref, name, invoker, bbox)`; **Form XObjects only** (NOT image XObjects); `bbox` is `fitz.Rect` already in page user-space (no CTM math)
- `page.get_drawings() -> list[dict]` — each dict has `rect` (`fitz.Rect`), `type` (`'f'`/`'s'`/`'fs'`), `fill` (tuple), `items` (list of tuples like `('l', point)` or `('re', rect)`), `seqno` (int monotonic byte-order)
- `fitz.Rect.intersects(other) -> bool` — standard AABB intersect
- `page.parent` — returns the owning `fitz.Document` (Pitfall 7 — do NOT add `doc` arg to helper signature)
- `page.number` — int page index (for log `extra={"page_index": page.number, ...}`)

From tests/conftest.py:12 (verbatim license comment idiom):
```python
import fitz  # only the test harness may use fitz directly to BUILD fixtures
```

From tests/test_redact.py:722-728 (Shape.draw_rect(W=0) zero-area injection pattern — VERBATIM precedent):
```python
shape = page.new_shape()
shape.draw_rect(fitz.Rect(x, 110.0, x, 190.0))  # W=0 (x0 == x1) → zero-area type='f'
shape.finish(fill=(0.0, 0.0, 0.0), color=None, width=0)
shape.commit()
```

From app/services/integrity.py:26-32 (logging idiom precedent — DOES exist in sibling service file):
```python
import logging
logger = logging.getLogger(__name__)
```

From tests/_illustrator_attack.py:180-189 (PATTERNS S1 multi-stream write-back VERBATIM — LOAD-BEARING; do NOT collapse the two branches into one loop, do NOT remove `compress=True`):
```python
new_bytes = stream_text.encode("latin-1")
content_xrefs = page.get_contents()
if len(content_xrefs) == 1:
    doc.update_stream(content_xrefs[0], new_bytes, compress=True)
else:
    doc.update_stream(content_xrefs[0], new_bytes, compress=True)
    for xref in content_xrefs[1:]:
        doc.update_stream(xref, b"", compress=True)
```

From tests/test_redact.py:1190-1207 (AGPL guard — MUST stay green):
- Scope: `glob.glob(os.path.join(app_dir, "**", "*.py"), recursive=True)`
- `app_dir = os.path.dirname(os.path.dirname(os.path.abspath(redact.__file__)))` → resolves to `app/`
- `tests/` is OUTSIDE scope by construction → `tests/test_pdf_engine.py` can `import fitz` without breaking guard

ISO 32000-1 §8.5.3 / Table 60 — Fill operators (ALL 7 must be matched per Pitfall 2):
- `f`, `F`, `f*`, `B`, `b`, `B*`, `b*`  — produce a fill
- `S`, `s`, `n` — stroke-only / no-paint — NOT matched
- Operator alternation order in regex MUST place `f*` BEFORE `f` (regex greedy left-to-right; bare `f` shadows `f*`)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `delete_zero_area_type_f_fills_inside` + `log_xobject_intersect` helpers to `pdf_engine.py`</name>

  <files>app/services/pdf_engine.py</files>

  <read_first>
    - app/services/pdf_engine.py (FULL — especially lines 15-19 import block, 255-294 `_DEGENERATE_BBOX_EPS` + `ZERO_AREA_RASTER_THRESHOLD`, 500-545 `_rect_contains`, 630-700 existing `cover_zero_area_artefacts` style precedent, 695-745 `count_zero_area_fills_fully_inside` 4-gate filter pattern, 746-792 `replace_region_with_white_raster` HONEST LIMITATION docstring style)
    - app/services/integrity.py:20-35 (logging import idiom precedent)
    - tests/_illustrator_attack.py:180-189 (PATTERNS S1 multi-stream write-back verbatim — LOAD-BEARING)
    - .planning/phases/07-option-b-implementation-content-stream-surgery/07-CONTEXT.md (§ decisions D-A1..D-A5 + § canonical_refs + § code_context — full file)
    - .planning/phases/07-option-b-implementation-content-stream-surgery/07-RESEARCH.md (§ Architecture Patterns Pattern 1 STEP A-E + § Code Examples 1, 2, 3 + § Common Pitfalls 1-9 — full file)
    - .planning/phases/07-option-b-implementation-content-stream-surgery/07-PATTERNS.md (§ Pattern Assignments A1-A6 + § Shared Patterns S1-S6 + § Risk Callouts #1-#8 — full file)
    - .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-PATTERNS.md (§ Shared Pattern S1 multi-stream write-back + § Risk Callout #4 verbatim preservation — confirmation that asymmetric `[0]` write + empty `[1:]` is empirically verified)
  </read_first>

  <behavior>
    - delete_zero_area_type_f_fills_inside(page, user_rect, tolerance=_DEGENERATE_BBOX_EPS) — Test 1: PDF with N=1 PScript5 m/l-based zero-area type='f' fully inside user_rect → return 1; post-call page.read_contents() bytes shorter; count_zero_area_fills_fully_inside returns 0 post-call.
    - Test 2: PDF with N=1 `re`-style zero-area (Shape 2: `x y 0 h re f`) fully inside user_rect → return 1; post-call count 0.
    - Test 3: PDF with normal-area filled rect (300×100, NOT zero-area) → return 0 (STEP A pre-screen short-circuit before reading stream); page.read_contents() bytes UNCHANGED (SEC-02).
    - Test 4: Empty page (no drawings at all) → return 0; bytes unchanged.
    - Test 5: PDF with zero-area fill OUTSIDE user_rect (not fully-inside) → return 0 (STEP A 4-gate filter rejects it); bytes unchanged.
    - Test 6: Negative-w/-h `re` operator (e.g. `365.76 416.658 -1.019989 -1.019989 re f` → abs area = 1.04 pt² NOT zero) — STEP A `abs(w) < tolerance or abs(h) < tolerance` check → NOT a ZAF → NOT counted (Pitfall 5).
    - Test 7: Re-entrancy — call helper twice; 2nd call returns 0; page bytes after 2nd call == page bytes after 1st call (Open Q3).
    - Test 8: Cardinality mismatch synthesis — if STEP C produces fewer/more byte ranges than zafs count, STEP D logger.warning("option_b_parse_anomaly") fires + return 0 + page.read_contents() unchanged (D-A5 — no destructive write).
    - log_xobject_intersect(page, user_rect, logger) — Test 9: page with form-XObject bbox intersecting user_rect → return 1 + logger.warning("option_b_xobject_intersect", extra={"page_index", "user_rect", "xobject_count"}) fires once.
    - Test 10: page with NO form-XObjects → return 0 + no log emitted.
    - Test 11: form-XObject exists but bbox does NOT intersect user_rect → return 0 + no log emitted.
  </behavior>

  <action>
    在 `app/services/pdf_engine.py` 既有 imports (line 15-19) 中加入新 imports — 按字母順序插入 `import logging` 與 `import re`(stdlib;non-fitz,不破 AGPL seam,per Risk Callout #5)。在 `import fitz` 之後加入 `logger = logging.getLogger(__name__)`(rule from `app/services/integrity.py:26-32` — sibling service idiom)。

    在模組層級(建議放在 `_DEGENERATE_BBOX_EPS` 常數附近、所有 helper 函式之前)加入三個 compiled regex 常數(理由:Pitfall 8 perf — 不可在 hot path 重 compile):
    - `_SAFE_SKIP_REGIONS_RE` — 5 個 alternation:`BT...ET` text block / `BI...ID...EI` inline image / `(...)` paren string with `\(` `\)` escape / `<...>` hex string / `%...\n` comment(D-A2 強制 — 從 07-RESEARCH § Code Examples Example 1 / lines 748-757 verbatim;用 `re.VERBOSE | re.DOTALL`)
    - `_RE_FILL_RECT_RE` — Shape 2 detector with named groups `<x> <y> <w> <h> re \s+ (?P<fillop>f\*|f|F|B\*|b\*|B|b) \b`(`f*` 在 alternation 中**先於** `f`,per Pitfall 2 — 從 07-RESEARCH lines 763-775 verbatim)
    - `_Q_BLOCK_RE` — Shape 1 detector `\bq\b (?P<body>[^Q]{0,2048}?) \bQ\b`(tight bound 防 catastrophic backtracking,per Pitfall 8;這個 regex 只作 SHAPE DETECTOR — 實際 ZAF 識別仍透過 `get_drawings()` — 從 07-RESEARCH lines 782-789 verbatim)

    加入第一個 helper:`def delete_zero_area_type_f_fills_inside(page: "fitz.Page", user_rect: "fitz.Rect", tolerance: float = _DEGENERATE_BBOX_EPS) -> int:`(D-A3 verbatim signature — **不可加 doc arg**,per Pitfall 7 Risk Callout #7;改用 `doc = page.parent` 取 Document handle)。函式體實作 STEP A-E pipeline per 07-RESEARCH § Architecture Patterns Pattern 1(lines 222-270):

    - **STEP A — pre-screen via `page.get_drawings()`**(SEC-02 fast no-op):與 `count_zero_area_fills_fully_inside`(line 699-743)同樣 4-gate filter:`type == 'f'` + bbox `width < tolerance or height < tolerance`(注意 `abs()` per Pitfall 5)+ rect fully inside `user_rect_tuple` via 既有 `_rect_contains(query, candidate)`(line 508)+ drop None rects。zafs list 為空 → return 0(SEC-02:typical v1.0 vector logo PDF goes here, no stream mutation)
    - **STEP B — read stream + build safe-skip mask O(N)**:`stream = page.read_contents()`;`mask = _build_safe_skip_mask(stream)` —— 沿用 07-RESEARCH Pattern 1 STEP B / lines 353-359 verbatim — 新增 `_build_safe_skip_mask(stream: bytes) -> bytearray` helper:同長度 bytearray,初值 `\x01`(searchable),對每個 `_SAFE_SKIP_REGIONS_RE.finditer(stream)` match,把 `mask[m.start():m.end()]` 置 `\x00`。再加 `_is_unmasked(mask, start, end) -> bool` 檢查 range 全 `\x01`。
    - **STEP C — anchor-based byte-range discovery**:逐 zaf inspect `zaf.get("items", [])`,分派至 Shape 1 / Shape 2 byte-range locator(per Risk Callout #1 — **兩種 shape 都必須處理**;若 items 全 `('l', ...)` / `('m', ...)` → Shape 1;若 items 全 `('re', ...)` → Shape 2;否則 None — cardinality 會 fail-safe)。Shape 1 locator(`_locate_shape1_byte_range`)實作見 07-RESEARCH § Code Examples Example 2 / lines 797-867:遍歷 `_Q_BLOCK_RE.finditer(stream)`,對每個 q...Q block parse `cm` matrix(預設 identity)+ 收集所有 `m`/`l` 操作元 points → local_rect transform via `fitz.Matrix * ctm` → 與 `zaf.rect` 比對(tolerance per 座標),`re.search(rb"\b(?:f\*|f|F|B\*|b\*|B|b)\b", body)` 確認 fill operator 存在 → 回傳 `(start, end)`。Shape 2 locator 採 dict-lookup pattern(per 07-RESEARCH § Code Examples Example 3 / lines 873-911 + Pitfall 8 perf):**single pass** `_build_shape2_candidate_index(stream, mask, tolerance)` 對全 stream 跑一次 `_RE_FILL_RECT_RE.finditer`,建 `dict[(round(x0,3), round(y0,3), round(x1,3), round(y1,3)) → [byte_ranges]]`(只收 `abs(w) < tolerance or abs(h) < tolerance` 的 candidate);然後逐 Shape 2 zaf 用 zaf.rect rounded key 查 dict — `len(candidates)==1` 回傳 range,否則 None。對所有 zaf 收集 `(start, end)` 至 `ranges_to_delete: list[tuple[int,int]]`。
    - **STEP D — cardinality assertion(D-A5 fail-safe)**:`if len(ranges_to_delete) != len(zafs):` → `logger.warning("option_b_parse_anomaly", extra={"page_index": page.number, "user_rect": list(user_rect_tuple), "expected": len(zafs), "matched": len(ranges_to_delete)})` → `return 0`。**絕不**呼叫 `doc.update_stream` / `_splice_out`;**絕不** raise(per Pattern S3 + Risk Callout #2)
    - **STEP E — splice + multi-stream write-back(PATTERNS S1 VERBATIM)**:加 helper `_splice_out(stream: bytes, ranges: list[tuple[int,int]]) -> bytes`(per 07-RESEARCH lines 446-455 — 將 ranges sorted-by-start 後逐段切掉)。`doc = page.parent`;`content_xrefs = page.get_contents()`;依 `len(content_xrefs) == 1` 分兩個 branch(**不可** collapse 為單 loop,per Risk Callout #3):single branch 寫 `[0]`;multi branch 寫 `[0]` 並對 `[1:]` 寫 `b""`,**全部 `compress=True`**(per Pitfall 6)。最後 `return len(zafs)`。

    Docstring 必須含繁中 `HONEST LIMITATION` 區段(per Pattern A3 + Pattern S6 — memory `feedback_language`),verbatim from 07-RESEARCH § Code Examples Example 1 lines 380-387:
    ```
    HONEST LIMITATION
    -----------------
    本 helper 採 regex anchor matching;PDF 內容流的 byte-level 表達細節(operator
    間任意 whitespace、CTM nested q/Q stack、PScript5 vs Acrobat 寫法差異)可能讓
    某些 zero-area path 的 byte 範圍 regex 漏抓。漏抓時 cardinality assertion 失敗
    → return 0 + logger.warning("option_b_parse_anomaly") → 既有 dispatcher
    (Phase 4-6 Option A overlay + cover_zero_area_artefacts) 接 last-mile defense。
    詳見 06-PATTERNS Risk Callout #4 + 07-RESEARCH § Common Pitfalls Pitfall 1。
    ```

    加入第二個 helper:`def log_xobject_intersect(page: "fitz.Page", user_rect: "fitz.Rect", logger=None) -> int:`(per Pattern A5 + 07-RESEARCH § Pattern 2 lines 489-518 verbatim)。若 `logger is None` → `logger = logging.getLogger(__name__)`(允許 caller 注入,per CONTEXT D-B2 + Claude's Discretion)。遍歷 `page.get_xobjects()` 解包 `(xref, name, invoker, bbox)`;`if bbox.intersects(user_rect):` → `n += 1`。**只在 `n > 0` 時** emit `logger.warning("option_b_xobject_intersect", extra={"page_index": page.number, "user_rect": [user_rect.x0, user_rect.y0, user_rect.x1, user_rect.y1], "xobject_count": n})`(per D-B3 — 每次 Option B 跑時都檢查 + log,SEC-03 透明化)。Return `n`。

    Docstring 同樣含繁中 HONEST LIMITATION 區段說明「page-level Option B 不下鑽 Form XObject 內部 stream;若 XObject 內含 ZAF,既有 dispatcher dense/sparse branch 接 last-mile defense」(per Pattern A5)。

    **絕對禁止(per Risk Callout #3 + Anti-Patterns):**
    - 將 STEP E 兩個 branch collapse 為單一 `for xref in content_xrefs: update_stream(xref, slice)` loop(asymmetric `[0]` write + empty `[1:]` 是 empirically verified pattern)
    - 在 helper 內呼叫 `doc.save(...)`(caller 擁有 save lifecycle,per Pitfall 9 + Phase 4 既有 `save_doc` 寫到 NEW path)
    - 加入 `compress=False`(per Pitfall 6 — 輸出 PDF 體積 +30-50%)
    - 改 helper signature 加 `doc` arg(per Pitfall 7 + Risk Callout #7 — Plan 07-02 已用 3-arg 簽名)
    - 重新定義新的 `_DEGENERATE_BBOX_EPS` 常數 / shadow 既有的(per Pattern A6 IN-01)
    - 加 `import fitz` 到 `redact.py`(per Risk Callout #5 — 本 plan 不動 redact.py,但 strict invariant 提早記下)
  </action>

  <verify>
    <automated>
    # 1. AGPL seam — `import fitz` 仍只在 pdf_engine.py 一處
    grep -rn "import fitz" app/  # expect: app/services/pdf_engine.py:19
    # 2. 新 helper 存在 + signature 正確
    grep -n "def delete_zero_area_type_f_fills_inside" app/services/pdf_engine.py
    grep -n "def log_xobject_intersect" app/services/pdf_engine.py
    # 3. 三個 module-level regex 常數存在
    grep -n "_SAFE_SKIP_REGIONS_RE\|_RE_FILL_RECT_RE\|_Q_BLOCK_RE" app/services/pdf_engine.py
    # 4. AGPL guard test 仍綠
    python -m pytest tests/test_redact.py::test_fitz_import_confined_to_engine_seam -v
    # 5. 既有 baseline 不退步(本 Task 結束後 Task 2 未跑 — 新 helper 還未被測,但 import + def 應該 collectable)
    python -c "from app.services import pdf_engine; assert callable(pdf_engine.delete_zero_area_type_f_fills_inside); assert callable(pdf_engine.log_xobject_intersect); print('helpers loaded OK')"
    # 6. Pre-existing pytest baseline confirmation
    python -m pytest 2>&1 | tail -3  # expect ≥ 301 passed + 3 skipped + 3 xfailed (no regressions from new module-level code)
    </automated>
  </verify>

  <done>
    - `app/services/pdf_engine.py` 含兩個新 helper(`delete_zero_area_type_f_fills_inside` + `log_xobject_intersect`)+ 三個 module-level compiled regex 常數 + `import logging` + `import re` + `logger = logging.getLogger(__name__)` + 新增的內部 helpers (`_build_safe_skip_mask` / `_is_unmasked` / `_splice_out` / `_locate_shape1_byte_range` / `_locate_shape2_byte_range` / `_build_shape2_candidate_index`)
    - 兩個 public helper docstring 含繁中 HONEST LIMITATION 區段
    - `grep -rn "import fitz" app/` 仍只回 `app/services/pdf_engine.py:19` 一行
    - `python -m pytest tests/test_redact.py::test_fitz_import_confined_to_engine_seam -v` 1 PASSED
    - `python -m pytest 2>&1 | tail -3` 仍顯示 `301 passed + 3 skipped + 3 xfailed`(本 Task 不該 break baseline;新 helper 還沒被 TEST-03 unit test 覆蓋,Task 2 才測)
    - Production code 改動範圍 audit:`git diff --stat app/` 只命中 `app/services/pdf_engine.py`(redact.py 不動)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author `tests/test_pdf_engine.py` with 14 TEST-03 cases — density gradient + safe-skip + SEC-02 + SEC-03 + re-entrancy + cardinality fail-safe</name>

  <files>tests/test_pdf_engine.py</files>

  <read_first>
    - tests/test_pdf_engine.py (檔案不存在 — 確認後新建;Pattern T2 fitz license header 必加)
    - app/services/pdf_engine.py (剛由 Task 1 添加的兩個 helper + module-level regex + helper signatures)
    - tests/test_redact.py:691-794 (test_remove_region_vector_dense_real_zero_area_paths_end_to_end — end-to-end build-PDF→run-pipeline→assert pattern;特別 lines 722-728 Shape.draw_rect(W=0) zero-area injection 為 Pattern T1 verbatim source)
    - tests/test_redact.py:1190-1207 (AGPL guard scope confirmation — tests/ 在 scope 外,可 import fitz)
    - tests/conftest.py:1-80 (_build_pdf + fitz license header `# only the test harness may use fitz directly to BUILD fixtures`)
    - tests/_illustrator_attack.py (Phase 6 attack helper — 可作為對未來 Plan 07-02 integration 的參考,本 plan 不動此檔)
    - .planning/phases/07-option-b-implementation-content-stream-surgery/07-RESEARCH.md (§ Code Examples Examples 4, 5, 6, 7 全部讀完;§ Validation Architecture Phase Requirements → Test Map 14 cases 逐條對應)
    - .planning/phases/07-option-b-implementation-content-stream-surgery/07-PATTERNS.md (§ Pattern T1, T2, T3, T4 — Pattern T4 14-case coverage map 為本 task 的權威 case list)
    - .planning/phases/07-option-b-implementation-content-stream-surgery/07-CONTEXT.md (§ decisions D-D3 — TEST-03 5-category 覆蓋 verbatim;§ decisions D-A5 fail-safe 行為定義)
  </read_first>

  <behavior>
    14 個 unit test cases,沿用 Pattern T4 coverage map:
    
    **Density gradient (4 parametrized cases via `@pytest.mark.parametrize("n_zaf", [0, 1, 100, 1742])`):**
    1. `test_option_b_density_gradient[0]` — 0 ZAFs → `delete_zero_area_type_f_fills_inside` return 0 + page bytes unchanged
    2. `test_option_b_density_gradient[1]` — 1 ZAF → return 1 + post-call count == 0 + page bytes shorter
    3. `test_option_b_density_gradient[100]` — 100 ZAFs(`ZERO_AREA_RASTER_THRESHOLD` boundary)→ return 100 + post-call count == 0
    4. `test_option_b_density_gradient[1742]` — 1742 ZAFs(對齊 `mixed-glyph-01.pdf` density)→ return 1742 + post-call count == 0
    
    **SEC-02 no-op (2 cases):**
    5. `test_option_b_no_op_on_normal_vector_pdf` — page 含正常面積 filled rect(300×100)+ 文字 → STEP A pre-screen short-circuit;return 0 + page.read_contents() unchanged
    6. `test_option_b_reentrant` — 連續呼叫兩次,第一次 return N、第二次 return 0;第二次呼叫後 page.read_contents() bytes 與第一次呼叫後相同(Open Q3 / Risk Callout #8)
    
    **Safe-skip 5 context (5 cases):**
    7. `test_safe_skip_bt_et` — page 含 BT/ET text block 內含 `m l f` 字元的文字(透過 `page.insert_text((x, y), "Quality m l f")`)+ 1 個 user_rect 外的真 ZAF + 1 個 user_rect 內的真 ZAF → helper 只刪 user_rect 內那一個;文字內的 m/l/f 字元未被誤刪(`"Quality" in page.get_text()` 後仍可讀)
    8. `test_safe_skip_paren_string` — 構造 page 含 `(Quality m l f)` PDF literal string(透過 PDF text-show 模擬)+ 1 個 真 ZAF in user_rect → 只刪 ZAF;literal string 內字元 untouched
    9. `test_safe_skip_hex_string` — page 構造含 `<6d6c66>` hex string(`m`/`l`/`f` 的 hex)+ 1 ZAF → 只刪 ZAF
    10. `test_safe_skip_comment` — page 構造含 `% m l f\n` comment(insert via low-level stream manipulation 或測試 `_SAFE_SKIP_REGIONS_RE.finditer` 對 comment 模式 mask 為 0)+ 1 ZAF → 只刪 ZAF
    11. `test_safe_skip_inline_image` — page 構造含 BI/ID/EI inline image block(透過 `page.insert_image` 或低層 stream manipulation)+ 1 ZAF → 只刪 ZAF
    
    **SEC-03 form-XObject (3 cases):**
    12. `test_option_b_form_xobject_intersect_logged` — 透過 `page.show_pdf_page(host_rect, nested_doc, page_no)` 在 host page 嵌入一個 nested doc(其內含 zero-area fill)→ `page.get_xobjects()` 至少有 1 個 form-XObject;呼叫 `log_xobject_intersect(page, fitz.Rect(40, 90, 260, 260), logger=None)` → return ≥ 1 + `caplog.records` 含 `"option_b_xobject_intersect"` warning + `rec.xobject_count >= 1` + `rec.page_index == 0`(per Pattern T3 / 07-RESEARCH Example 5)
    13. `test_option_b_form_xobject_internal_stream_untouched` — 同 12 構造,呼叫 `delete_zero_area_type_f_fills_inside(page, user_rect)` → page-level read_contents() 可能不變(若 page 上無 page-level ZAF;只在 XObject 內);Form XObject 的 xref `doc.xref_stream(nested_xref)` bytes 在呼叫前後 **不變**(per D-B1 — fitz API contract)
    14. `test_option_b_no_xobject_no_log` — page 完全無 form-XObject → `log_xobject_intersect` return 0 + 無 `caplog.records` 含 `"option_b_xobject_intersect"`(reverse of 12)
  </behavior>

  <action>
    建立新檔 `tests/test_pdf_engine.py`(per CONTEXT Claude's Discretion "Test 檔案放置" 推薦)— 第一行 module docstring 簡述「TEST-03 Phase 7 Option B helper unit tests」+ 繁中說明。第二段加 fitz license header(per Pattern T2 verbatim,從 `tests/conftest.py:12`):
    ```python
    import fitz  # only the test harness may use fitz directly to BUILD fixtures
    ```
    其他 imports:`import logging`、`import pytest`、`from app.services import pdf_engine`。

    **14 個 case 完整實作,逐項按 Pattern T4 coverage table:**

    1-4. **Density gradient(`@pytest.mark.parametrize("n_zaf", [0, 1, 100, 1742])`)**:採 07-RESEARCH § Code Examples Example 6 / lines 1015-1051 verbatim 結構 — `doc = fitz.open()` → `page = doc.new_page(width=400, height=300)` → `user_rect = fitz.Rect(50.0, 100.0, 350.0, 200.0)` → `for i in range(n_zaf): x = 55.0 + (i % 290) * 1.0; y_off = (i // 290) * 1.0; shape = page.new_shape(); shape.draw_rect(fitz.Rect(x, 110.0 + y_off, x, 190.0 + y_off)); shape.finish(fill=(0.0, 0.0, 0.0), color=None, width=0); shape.commit()`(W=0 zero-area injection per Pattern T1 — 沿用 `tests/test_redact.py:722-728` 既有 pattern)→ pre-condition `count_zero_area_fills_fully_inside(page, tuple(user_rect)) == n_zaf` → `deleted = pdf_engine.delete_zero_area_type_f_fills_inside(page, user_rect); assert deleted == n_zaf` → post-condition `count_zero_area_fills_fully_inside(page, tuple(user_rect)) == 0` → `finally: doc.close()`

    5. **`test_option_b_no_op_on_normal_vector_pdf`** — 07-RESEARCH Example 4 / lines 923-962 verbatim:正常面積 `shape.draw_rect(fitz.Rect(50, 100, 350, 200))`(300×100,非 zero-area)+ `page.insert_text((100, 150), "SUPPLIER LOGO", fontsize=10)`;capture `bytes_before = page.read_contents()`;`assert pdf_engine.count_zero_area_fills_fully_inside(page, (50.0, 100.0, 350.0, 200.0)) == 0`(precondition);`deleted = pdf_engine.delete_zero_area_type_f_fills_inside(page, fitz.Rect(50, 100, 350, 200)); assert deleted == 0`;`bytes_after = page.read_contents(); assert bytes_after == bytes_before, "content stream MUST be unchanged in no-op case"`(SEC-02 strict invariant)

    6. **`test_option_b_reentrant`** — 同 4 構造(`n_zaf = 100`)但兩次呼叫:`first = pdf_engine.delete_zero_area_type_f_fills_inside(page, user_rect); assert first == 100`;`bytes_after_first = page.read_contents()`;`second = pdf_engine.delete_zero_area_type_f_fills_inside(page, user_rect); assert second == 0, "2nd call must be no-op"`;`bytes_after_second = page.read_contents(); assert bytes_after_second == bytes_after_first, "2nd call must not mutate stream"`(per Risk Callout #8)

    7. **`test_safe_skip_bt_et`** — 07-RESEARCH Example 7 / lines 1056-1087 verbatim:`page.insert_text((50, 50), "Quality m l f", fontsize=10)` 引入文字(會由 fitz 編入 BT...ET block)+ `shape.draw_rect(fitz.Rect(100, 150, 100, 180))` ZAF + `shape.finish(fill=(0.0, 0.0, 0.0), color=None, width=0); shape.commit()`;`user_rect = fitz.Rect(80, 140, 200, 200)` 涵蓋 ZAF 但不涵蓋文字;`assert count_zero_area_fills_fully_inside(page, tuple(user_rect)) == 1`(precondition);`assert pdf_engine.delete_zero_area_type_f_fills_inside(page, user_rect) == 1`;**critical assertion:** `assert "Quality" in page.get_text(), "text block content must NOT be touched"`(BT/ET safe-skip 生效;Pitfall 1)

    8-11. **safe-skip paren / hex / comment / inline image** — 沿用相同 pattern,測試框架:`page.insert_text((50, 50), "some string with m l f")` 模擬 paren literal(fitz 內部會 encode 為 PDF string `(some string with m l f)`);hex/comment/BI/ID/EI 因 fitz public API 較難直接注入 raw stream content,**改採以下穩定策略**:在每個 test 內(a)構造 1 個 user_rect 內的真 ZAF + (b) 確保 page.read_contents() bytes 含對應 context pattern(透過 inserted text / image / annotation 觸發);(c) assert delete count == 1;(d) for hex/comment/BI variants,額外 directly test `_build_safe_skip_mask(crafted_stream)` 對 crafted byte sequence 的 mask 行為 — `crafted = b"BT (test) Tj ET m l f"; mask = pdf_engine._build_safe_skip_mask(crafted); assert mask[:len("BT (test) Tj ET")] == bytearray(b"\x00" * len("BT (test) Tj ET"))` 之類精確 byte-mask assertion。Comment test:`crafted = b"% m l f\n"; mask = _build_safe_skip_mask(crafted); assert all(m == 0 for m in mask[:7])`。Inline image test:`crafted = b"BI /W 1 /H 1 ID some bytes EI"; mask = _build_safe_skip_mask(crafted); assert all(m == 0 for m in mask)`. Hex test:`crafted = b"<6d6c66> Tj"; mask = _build_safe_skip_mask(crafted); assert all(m == 0 for m in mask[:9])`. **這個雙閘策略(end-to-end no-misparse + unit-level mask correctness)是 D-A2 + Pitfall 1 的最 robust validation**。

    12. **`test_option_b_form_xobject_intersect_logged(caplog)`** — 07-RESEARCH Example 5 / lines 968-1009 verbatim:`doc = fitz.open()`;`page = doc.new_page(width=400, height=300)`;`nested_doc = fitz.open()`;`nested_page = nested_doc.new_page(width=200, height=150)`;在 nested_page 加 ZAF(W=0 draw_rect + finish + commit);`page.show_pdf_page(fitz.Rect(50, 100, 250, 250), nested_doc, 0)`;`nested_doc.close()`;`assert len(page.get_xobjects()) >= 1`(precondition);`with caplog.at_level(logging.WARNING, logger="app.services.pdf_engine"): n = pdf_engine.log_xobject_intersect(page, fitz.Rect(40, 90, 260, 260), logger=None)`;`assert n >= 1`;`matching = [r for r in caplog.records if "option_b_xobject_intersect" in r.message]`;`assert matching`;`rec = matching[0]`;`assert rec.xobject_count >= 1`(extra dict 透過 caplog 為 record attribute,per Assumption A5);`assert rec.page_index == 0`

    13. **`test_option_b_form_xobject_internal_stream_untouched`** — 同 12 構造,但先記錄 nested xref 與其 stream:在 `page.show_pdf_page` 之後 `nested_xref = page.get_xobjects()[0][0]; stream_before = doc.xref_stream(nested_xref)`;呼叫 `pdf_engine.delete_zero_area_type_f_fills_inside(page, fitz.Rect(40, 90, 260, 260))`;`stream_after = doc.xref_stream(nested_xref); assert stream_after == stream_before, "Form XObject internal stream must NOT be touched (SEC-03)"`(per D-B1 — fitz `read_contents()` 不下鑽 XObject;Plan 07-01 helper 對 read_contents() 跑 regex,天然不會誤改 XObject xref stream)

    14. **`test_option_b_no_xobject_no_log(caplog)`** — page 完全無 form-XObject(空白 page + 一個普通 filled rect 非 zero-area)→ `assert len(page.get_xobjects()) == 0`;`with caplog.at_level(logging.WARNING, logger="app.services.pdf_engine"): n = pdf_engine.log_xobject_intersect(page, fitz.Rect(0, 0, 400, 300), logger=None)`;`assert n == 0`;`matching = [r for r in caplog.records if "option_b_xobject_intersect" in r.message]; assert not matching, "no log when no XObject"`(per D-B3 — only emit when n > 0;reverse of 12)

    **每個 test 都用 `try: ... finally: doc.close()` 確保資源釋放**(per 07-RESEARCH § Don't Hand-Roll table — explicit cleanup in test body)。

    **絕對不能做(per Risk Callout #5 + Pattern S4):**
    - 在 `tests/test_pdf_engine.py` 中 import `app.services.redact`(避免假性依賴 — Plan 07-02 才會動 redact.py)
    - 在 test body 內 monkeypatch `pdf_engine.delete_zero_area_type_f_fills_inside`(會破壞 unit test 的 isolation)
    - 添加 binary fixture 到 `tests/fixtures/`(per Pattern S2 — Phase 7 zero new committed binary;全部 in-memory)
    - 修改 `tests/test_redact.py::test_fitz_import_confined_to_engine_seam`(AGPL guard 為 hard invariant)
  </action>

  <verify>
    <automated>
    # 1. 新 test 檔存在
    test -f tests/test_pdf_engine.py
    # 2. fitz license header 存在
    grep -n "only the test harness may use fitz directly to BUILD fixtures" tests/test_pdf_engine.py
    # 3. 14 個 test cases 全綠
    python -m pytest tests/test_pdf_engine.py -v 2>&1 | tail -30
    # expect: 14 PASSED (4 density parametrized + 2 SEC-02 + 5 safe-skip + 3 SEC-03)
    # 4. Full baseline 升級為 (301 + 14) passed + 3 skipped + 3 xfailed (Phase 6 xfail 仍紅 — 本 Plan 不動)
    python -m pytest 2>&1 | tail -3  # expect: 315 passed, 3 skipped, 3 xfailed
    # 5. AGPL guard 仍綠
    python -m pytest tests/test_redact.py::test_fitz_import_confined_to_engine_seam -v
    # 6. Phase 6 紅燈 regression 仍 XFAIL(Plan 07-01 不動 — Plan 07-02 才 flip)
    python -m pytest -k illustrator_attack -v 2>&1 | tail -5  # expect: 3 xfailed
    # 7. Production code 改動範圍 audit(本 Plan 結束)
    git diff --stat app/  # expect: ONLY app/services/pdf_engine.py
    </automated>
  </verify>

  <done>
    - `tests/test_pdf_engine.py` 存在;fitz license header (`# only the test harness may use fitz directly to BUILD fixtures`) 存在於 import 行
    - `python -m pytest tests/test_pdf_engine.py -v` 顯示 14 PASSED(4 density + 2 SEC-02 + 5 safe-skip + 3 SEC-03)
    - `python -m pytest 2>&1 | tail -3` 顯示 `315 passed, 3 skipped, 3 xfailed`(Phase 6 xfail 保留 — handoff signal 留給 Plan 07-02)
    - `grep -rn "import fitz" app/` 仍只回 `app/services/pdf_engine.py:19`
    - `git diff --stat app/` 只命中 `app/services/pdf_engine.py`(redact.py 不動 — Plan 07-02 才動)
    - Plan close gate:helper 已實作 + 14 個 TEST-03 unit test 全綠 + AGPL guard 仍綠 + Phase 6 紅燈仍紅(integration 還未接,Plan 07-02 才 flip)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller → `delete_zero_area_type_f_fills_inside` helper | Plan 07-02 dispatcher will pass `user_rect` (already `fitz.Rect`, upstream-validated by `pdf_engine.map_tuple_to_rect`); Plan 07-01 helper trusts upstream validation per ASVS V5 "input already validated" (CONTEXT § Security Domain). |
| Helper → `page.read_contents()` bytes | Untrusted PDF content stream byte sequence — could contain crafted operator bytes inside literal strings / hex strings / inline images / BT/ET / comments; safe-skip mask (D-A2) is the trust-boundary enforcement. |
| Helper → `doc.update_stream` | Only crossed when cardinality assertion PASSES; mismatch path returns 0 + warns + does NOT cross (D-A5 fail-safe). |
| `tests/test_pdf_engine.py` → `pdf_engine` helpers | Tests import fitz directly per `tests/conftest.py:12` license carve-out — only the test harness may use fitz to BUILD fixtures (AGPL seam preserved by AST guard scope `app/**/*.py`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | T (Tampering) | `delete_zero_area_type_f_fills_inside` regex pre-pass | mitigate | D-A2 5-context safe-skip mask via `_build_safe_skip_mask` runs O(N) before any operator regex; prevents regex from matching `m`/`l`/`f`/`B` bytes inside `(...)` / `<...>` / BT/ET / BI/ID/EI / `%...\n`. Verified by 5 `test_safe_skip_*` cases (Task 2) — both end-to-end (real PDF with text + ZAF) and unit-level (`_build_safe_skip_mask(crafted_bytes)` assertion). |
| T-07-02 | I (Information disclosure) / partial removal | Cardinality drift — Shape regex finds fewer/more byte ranges than `get_drawings()` ZAFs | mitigate | D-A5 fail-safe: `len(ranges_to_delete) != len(zafs)` → `logger.warning("option_b_parse_anomaly", extra={...})` + `return 0` + NEVER call `update_stream`. Last-mile defense: existing dispatcher's Option A overlay (`replace_region_with_white_raster`) catches residue. Phase 6 regression test surfaces the failure naturally (Option B miss → count > 0 → dispatcher → XFAIL stays XFAIL → implementer debugs). |
| T-07-03 | I (Information disclosure) | Multi-stream content streams not all updated → orphaned operators emit zero-area paths in `[1:]` streams | mitigate | PATTERNS S1 VERBATIM port: write splice to `[0]`, empty `[1:]` with `b""` (asymmetric pattern is empirically verified on Phase 6 `3013A-13A-C6-...pdf` forensic). Risk Callout #3 forbids collapsing the two branches into one loop. Density gradient test (1742 ZAFs in `test_option_b_density_gradient[1742]`) exercises multi-stream behavior. |
| T-07-04 | I (Information disclosure) | Form XObject internal zero-area fills silently ignored — supplier brand recoverable inside XObject | accept (page-level only) | D-B1 + SEC-03 page-level only strategy — `page.read_contents()` API contract guarantees XObject internal streams are NOT included (verified via Context7 + WebFetch). `log_xobject_intersect` helper emits `logger.warning("option_b_xobject_intersect", extra={...})` for SEC-03 transparency. Test 13 `test_option_b_form_xobject_internal_stream_untouched` asserts `doc.xref_stream(nested_xref)` bytes unchanged before/after Option B call. Form XObject recursive surgery is **explicitly Deferred** per `.planning/REQUIREMENTS.md` Out of Scope + STATE.md Deferred table. |
| T-07-05 | A (Availability / DoS) | Helper raises on parse anomaly → 500 to user → pipeline disrupted | mitigate | D-A5 fail-safe — helper NEVER raises `RedactError` / no `raise` inside the helper. All anomaly paths emit `logger.warning` + return 0. Verified by Task 2 cardinality fail-safe assertion (synthesized mismatch case in unit test — `_build_shape2_candidate_index` returning ambiguous candidates triggers fail-safe). |
| T-07-06 | T (Tampering) | `update_stream` writes reach disk before `doc.save()` called by caller → original file mutated | mitigate | Plan 07-01 helper does NOT call `doc.save()` (Anti-Pattern in 07-RESEARCH). Caller (`redact.py` → `pipeline.py::save_doc`) owns save lifecycle; existing pipeline writes output to NEW path (not back to upload) per v1.0 Pitfall 9. Verified by Test 6 re-entrancy — second call's read_contents() bytes match first call's (in-memory state coherent;disk untouched until caller's save). |
| T-07-07 | T (Tampering) | Performance pitfall — 13,962 ZAFs in `B-3012IP-WM02-T430.pdf` × O(stream) regex per ZAF → catastrophic backtracking / timeout | mitigate | Pitfall 8 strategy: build safe-skip mask ONCE (O(N)), build Shape 2 dict-index ONCE (O(N + M)), Shape 1 regex bounded by `[^Q]{0,2048}?` to prevent unbounded backtracking. Live spike on `mixed-glyph-01.pdf` (1.3MB stream, 3396 ZAFs) completes < 1 sec. Test 4 density `[1742]` provides regression coverage. |
| T-07-08 | (AGPL operational risk — not STRIDE-classical) | New `import fitz` accidentally added to `redact.py` or other `app/**/*.py` during Plan 07-01 changes | mitigate | Plan 07-01 file scope is **only `app/services/pdf_engine.py`** + `tests/test_pdf_engine.py`. AGPL guard test `tests/test_redact.py::test_fitz_import_confined_to_engine_seam` (line 1190-1207) continues to enforce — Plan 07-01 verify block grep-asserts `import fitz` only in `app/services/pdf_engine.py:19`. Test file `tests/test_pdf_engine.py` is OUTSIDE guard scope (`app/**/*.py` glob) per Pitfall 9. |

## Threat closures inherited from Phase 6 (Plan 07-02 closes, not 07-01)

| Source threat | Disposition status after Plan 07-01 | Reason |
|--------------|--------------------------------------|--------|
| T-06-01 (Phase 6 06-SECURITY.md — Illustrator pulls image XObject overlay → supplier brand re-rendered) | **STILL ACCEPT (P0, transition-pending)** | Plan 07-01 lands the helper but does NOT integrate into `redact.py`; Phase 6 regression test stays XFAIL. T-06-01 closure happens in Plan 07-02 when dispatcher integration + xfail flip = 3 PASSED. |
| T-02-07 (TRUE REMOVAL vs cover gap) | **STILL ACCEPT (P0, transition-pending)** | Same as above — Plan 07-01 produces the mechanism, Plan 07-02 wires + verifies closure. |

After Phase 7 closes (Plan 07-02), `07-SECURITY.md` (if `gsd-secure-phase` runs) should mark T-06-01 + T-02-07 as `CLOSED via Option B` with `supersedes: [.planning/phases/06-regression-foundation-threat-model-re-evaluation/06-SECURITY.md]` cross-reference.
</threat_model>

<verification>
**Plan-level invariants (must all hold at Plan 07-01 close):**

1. **AGPL seam unbroken** — `grep -rn "import fitz" app/` returns exactly one line: `app/services/pdf_engine.py:19`
2. **AGPL guard test green** — `python -m pytest tests/test_redact.py::test_fitz_import_confined_to_engine_seam -v` → 1 PASSED
3. **Production code scope** — `git diff --stat app/` only shows `app/services/pdf_engine.py` modified (NO `app/services/redact.py` change in this plan)
4. **New helpers callable** — `python -c "from app.services import pdf_engine; assert callable(pdf_engine.delete_zero_area_type_f_fills_inside); assert callable(pdf_engine.log_xobject_intersect)"` succeeds
5. **14 TEST-03 cases all green** — `python -m pytest tests/test_pdf_engine.py -v` → 14 PASSED
6. **Full baseline upgraded** — `python -m pytest 2>&1 | tail -3` shows `315 passed, 3 skipped, 3 xfailed` (301 existing + 14 new TEST-03; Phase 6 xfail preserved for Plan 07-02 handoff)
7. **Phase 6 regression remains XFAIL** — `python -m pytest -k illustrator_attack -v` shows 3 xfailed (NOT 3 passed — integration not yet wired in this plan)
8. **No new committed binaries** — `git status` shows no new files under `tests/fixtures/`
9. **HONEST LIMITATION docstring present** — `grep -A 8 "def delete_zero_area_type_f_fills_inside" app/services/pdf_engine.py | grep -E "HONEST LIMITATION|本 helper"` finds the 繁中 limitation section
10. **IN-01 alignment preserved** — `grep -c "_DEGENERATE_BBOX_EPS = 0.01" app/services/pdf_engine.py` returns 1 (constant defined exactly once, no shadowing per Pattern A6)
</verification>

<success_criteria>
- [ ] `delete_zero_area_type_f_fills_inside` 與 `log_xobject_intersect` 已存在於 `app/services/pdf_engine.py`,signatures 與 D-A3 / Pattern A5 一致
- [ ] 兩 helper 對外可用(`from app.services import pdf_engine; pdf_engine.delete_zero_area_type_f_fills_inside`)
- [ ] 兩 helper docstring 含繁中 HONEST LIMITATION 區段
- [ ] STEP A pre-screen / STEP B safe-skip mask / STEP C anchor-based byte-range discovery / STEP D cardinality fail-safe / STEP E PATTERNS S1 multi-stream write-back 五段全部實作
- [ ] `tests/test_pdf_engine.py` 含 14 個 TEST-03 case(4 density gradient parametrized + 2 SEC-02 + 5 safe-skip + 3 SEC-03)全綠
- [ ] AGPL guard 仍綠;production code 改動範圍嚴格限縮在 `pdf_engine.py`
- [ ] Phase 6 紅燈 regression test 仍 3 XFAIL(integration 還未接 — handoff signal 留給 Plan 07-02)
- [ ] Baseline 升級為 `315 passed + 3 skipped + 3 xfailed`(原 301 + 14 TEST-03)
- [ ] STRIDE threat register T-07-01..T-07-08 + T-06-01 / T-02-07 inherit 已記錄於本 PLAN 的 `<threat_model>` 區塊
</success_criteria>

<output>
After completion, create `.planning/phases/07-option-b-implementation-content-stream-surgery/07-01-SUMMARY.md` 包含:

- 兩個新 helper 的 file:line 位置與 signature 確認
- 14 個 TEST-03 case 名稱 + 通過證據(pytest output tail)
- `git diff --stat app/` 輸出證明只動 `pdf_engine.py`
- AGPL guard test 通過證據
- Baseline 升級證據(`python -m pytest 2>&1 | tail -3`)
- Phase 6 regression test 仍 3 XFAIL 證據(handoff invariant 維持)
- 任何 Open Q1-Q4(07-RESEARCH § Open Questions)的 spike 觀察結果(若 Plan 07-01 implementer 在 5-min spike 中驗證了 A1-A6 assumption,記入 SUMMARY 作 Phase 8 cross-reference 資料)
- 為 Plan 07-02 dispatcher 整合的 ready signal:helper 簽名、回傳值、log event 名稱
</output>
