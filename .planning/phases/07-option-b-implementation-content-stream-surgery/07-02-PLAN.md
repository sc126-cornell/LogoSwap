---
phase: 07-option-b-implementation-content-stream-surgery
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - app/services/redact.py
  - tests/test_illustrator_attack_regression.py
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
tags:
  - pdf
  - redaction
  - dispatcher
  - regression
  - security
  - integration
must_haves:
  truths:
    - "`app/services/redact.py` 含 `import logging` + `logger = logging.getLogger(__name__)`(模組層級 stdlib import,non-fitz)"
    - "`app/services/redact.py` 在 line 195 `residual_content` raise 之後、line 232 `zero_area_count` 之前插入 6-10 LOC Option B dispatcher block"
    - "插入塊呼叫 `pdf_engine.delete_zero_area_type_f_fills_inside(page, user_rect)` 並在 deleted > 0 時 `logger.info('option_b_deleted', extra=...)`"
    - "插入塊呼叫 `pdf_engine.log_xobject_intersect(page, user_rect, logger=logger)` 作 SEC-03 透明化"
    - "既有 line 232-256 dispatcher(dense Option A overlay + sparse cover_zero_area_artefacts)**完整保留**為 last-mile defense"
    - "`tests/test_illustrator_attack_regression.py` lines 74-82 的 `@pytest.mark.xfail(strict=True, reason=...)` decorator 完全刪除;`@pytest.mark.parametrize` decorator 保留"
    - "`python -m pytest -k illustrator_attack -v` 顯示 3 PASSED(原 3 XFAIL → flip,SEC-01 acceptance gate 通過)"
    - "`python -m pytest 2>&1 | tail -3` 顯示 `≥ 318 passed + 3 skipped + 0 xfailed`(315 from 07-01 baseline + 3 flipped from XFAIL)"
    - "AGPL seam 不破 — `grep -rn 'import fitz' app/` 仍只回 `app/services/pdf_engine.py:19`(`redact.py` 加的是 `import logging`,non-fitz)"
    - "AGPL guard test `tests/test_redact.py::test_fitz_import_confined_to_engine_seam` 仍 PASSED"
    - "Production code 改動範圍:本 plan 結束時 `git diff --stat app/`(對 Phase 7 開始 base)只命中 `app/services/pdf_engine.py`(從 Plan 07-01)+ `app/services/redact.py`(從本 Plan)兩檔"
  artifacts:
    - path: "app/services/redact.py"
      provides: "Option B dispatcher 插入塊 (~6-10 LOC) + `import logging` + `logger`;既有 dispatcher block 完整保留"
      contains: "pdf_engine.delete_zero_area_type_f_fills_inside"
    - path: "tests/test_illustrator_attack_regression.py"
      provides: "xfail decorator 已移除 — Phase 6 紅燈 regression test 升級為 3 PASSED(handoff completion 動作)"
      contains: "@pytest.mark.parametrize"
  key_links:
    - from: "app/services/redact.py::remove_region_vector"
      to: "app/services/pdf_engine.delete_zero_area_type_f_fills_inside"
      via: "插入 line 195/232 之間 + 既有 `from . import pdf_engine` import"
      pattern: "pdf_engine\\.delete_zero_area_type_f_fills_inside\\("
    - from: "app/services/redact.py::remove_region_vector"
      to: "app/services/pdf_engine.log_xobject_intersect"
      via: "插入塊 + 同 import"
      pattern: "pdf_engine\\.log_xobject_intersect\\("
    - from: "tests/test_illustrator_attack_regression.py::test_illustrator_attack_residual_supplier_revealed"
      to: "Plan 07-02 dispatcher integration"
      via: "xfail decorator 移除 → 真實執行 → 3 PASSED 為 SEC-01 acceptance gate"
      pattern: "@pytest\\.mark\\.parametrize"
---

<objective>
完成 Phase 7 Option B 的 **integration handoff** —— 在 `app/services/redact.py` 既有 dispatcher 流程的 line 195 / line 232 boundary 之間,插入 6-10 LOC 呼叫 Plan 07-01 落地的 helper,並翻轉 Phase 6 留下的 3 個 `@pytest.mark.xfail(strict=True)` 紅燈 regression test 為 3 PASSED。

Purpose: 把 Plan 07-01 落地的 helper 真正接到生產 pipeline(`pipeline.process_job` → `redact.remove_region_vector` → 新插入的 Option B 呼叫);Phase 6 binding handoff signal —— 3 個 XFAIL 在 xfail decorator 拔掉後一旦真正 run 都得 PASSED,代表 Option B 對真實 supplier CAD-glyph PDF 有效:Illustrator-class editor 拔掉 image XObject overlay 後, render 框選區仍 ≥98% 白 + zero-area `type='f'` count 在框選區內 == 0(content stream 真的被刪)。**5330290 minimum-change 紀律:redact.py 改動 ≤ 12 LOC(2 LOC import + ~10 LOC dispatcher block);既有 dispatcher line 232-256 一字不改保留為 last-mile defense。**

Output:
- `app/services/redact.py` —— 加 `import logging` + `logger = logging.getLogger(__name__)` + line 195/232 boundary 插入 Option B dispatcher block;**既有 dispatcher line 232-256 不動**
- `tests/test_illustrator_attack_regression.py` —— lines 74-82 的 `@pytest.mark.xfail(strict=True, reason=...)` decorator 完全刪除;`@pytest.mark.parametrize` decorator 保留
- Production code 改動範圍 audit(Phase 7 整體):`git diff --stat <phase-7-base>..HEAD -- app/` 只命中 `app/services/pdf_engine.py`(Plan 07-01)+ `app/services/redact.py`(本 Plan)兩檔
- Baseline 最終升級:`python -m pytest 2>&1 | tail -3` → `≥ 318 passed + 3 skipped + 0 xfailed`(315 from Plan 07-01 + 3 flipped XFAIL → PASSED;若 TEST-03 case 數有調整,N 可變動,acceptance 採「≥ 318 + 3 skipped + 0 xfailed」)
- SEC-01 acceptance gate 通過:`python -m pytest -k illustrator_attack -v` 顯示 3 PASSED
- Phase 6 SECURITY.md 的 T-06-01 + T-02-07 兩條 `accept (P0, transition-pending)` 在本 Plan close 後可由 Phase 7 自身 07-SECURITY.md(若 `gsd-secure-phase` 跑)markedown 為 `CLOSED via Option B`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-option-b-implementation-content-stream-surgery/07-CONTEXT.md
@.planning/phases/07-option-b-implementation-content-stream-surgery/07-RESEARCH.md
@.planning/phases/07-option-b-implementation-content-stream-surgery/07-PATTERNS.md
@.planning/phases/07-option-b-implementation-content-stream-surgery/07-01-PLAN.md
@.planning/phases/06-regression-foundation-threat-model-re-evaluation/06-PATTERNS.md
@.planning/phases/06-regression-foundation-threat-model-re-evaluation/06-SECURITY.md
@.planning/phases/06-regression-foundation-threat-model-re-evaluation/06-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from existing codebase. -->
<!-- DO NOT re-explore. Plan 07-01 produced the helpers; this plan wires them in. -->

From app/services/pdf_engine.py (Plan 07-01 added — DO NOT modify here):
- `def delete_zero_area_type_f_fills_inside(page: "fitz.Page", user_rect: "fitz.Rect", tolerance: float = _DEGENERATE_BBOX_EPS) -> int:` — Returns count deleted (0 on no-op / fail-safe parse anomaly)
- `def log_xobject_intersect(page: "fitz.Page", user_rect: "fitz.Rect", logger=None) -> int:` — Returns intersect count;emits `option_b_xobject_intersect` warning iff count > 0

From app/services/redact.py (existing — Plan 07-02 inserts at line 195/232 boundary):
- Line 1-80: module docstring (TRUE_REMOVAL_LIMITATION) — do NOT modify in this plan (Phase 8 DOC-01 will update)
- Line 82-84: `from __future__ import annotations` + blank + `from . import pdf_engine` — Plan 07-02 ADDS `import logging` here (alphabetical between `__future__` and `pdf_engine`)
- Line 86-90: `REDACT_PAD_PT = 5.0` constant + comment — unchanged
- Line 174-179: `apply_redactions` call — unchanged
- Line 189-195: `residual_content` raise — Plan 07-02 inserts AFTER line 195
- Line 197-256: existing zero_area_count dispatcher block (Phase 4-6 Hotfix 06 logic, dense Option A overlay / sparse cover_zero_area_artefacts) — **DO NOT MODIFY ANY LINE**
- Line 258: `return True` — unchanged

From app/services/integrity.py:26-32 (logging idiom precedent — same package sibling):
```python
import logging
logger = logging.getLogger(__name__)
```

From tests/test_illustrator_attack_regression.py (Phase 6 produced — Plan 07-02 modifies lines 74-82 only):
- Lines 1-72: docstring + `_load_fixtures` helper + decorator order comment — DO NOT MODIFY
- Line 73: `@pytest.mark.parametrize("fixture_pdf,manifest", _load_fixtures())` — KEEP (decorator order matters; parametrize stays as the OUTER decorator)
- Lines 74-82: `@pytest.mark.xfail(strict=True, reason=("Option B 尚未實作..."))` — **DELETE entire decorator** (lines 74 through 82 inclusive, including the closing `)`)
- Line 83+: function definition `def test_illustrator_attack_residual_supplier_revealed(...)` — DO NOT MODIFY
- Lines 89-159: test body — DO NOT MODIFY

From tests/test_redact.py:1190-1207 (AGPL guard — MUST stay green):
- AST guard scope: `app/**/*.py`
- Plan 07-02 ADDS `import logging` to `redact.py` (stdlib, NOT fitz → passes guard)
- Plan 07-02 MUST NOT add `import fitz` to redact.py — `user_rect` is already `fitz.Rect` (passed in from `pipeline.py` via `pdf_engine.map_tuple_to_rect`); pdf_engine helpers consume it. Use STRING type annotation `"fitz.Rect"` if any function signature in redact.py needs to reference the type (existing codebase already uses this style at line 84-86 of pdf_engine.py).

Existing redact.py — context after Plan 07-01 (which DID NOT modify redact.py):
```python
# Lines 82-84 (current):
from __future__ import annotations

from . import pdf_engine

# Lines 189-195 (insertion-point reference — DO NOT change):
    residual_words = pdf_engine.get_text_words_in_rect(page, user_rect)
    residual_covered_drawings = pdf_engine.get_drawings_fully_inside(page, user_rect)
    if residual_words or residual_covered_drawings:
        raise RedactError(
            "residual_content",
            "移除後仍偵測到殘留內容(文字或向量),無法保證真正移除。",
        )

# ◄── PLAN 07-02 INSERTS ~10 LOC HERE (between line 195 raise and line 197 comment)

# Lines 197-258 (existing dispatcher — DO NOT change a single line):
    # Zero-area artefact cleanup — dispatched on residue DENSITY (hotfix #06, dCt-residue).
    # ... [verbatim comment block] ...
    zero_area_count = pdf_engine.count_zero_area_fills_fully_inside(page, user_rect)
    if zero_area_count >= pdf_engine.ZERO_AREA_RASTER_THRESHOLD:
        pdf_engine.replace_region_with_white_raster(page, user_rect)
        whitepaint = pdf_engine.get_white_fill_drawings_intersecting(page, user_rect)
        if whitepaint:
            raise RedactError("residual_whitepaint", "raster fallback 後仍偵測到 ...")
    else:
        pdf_engine.cover_zero_area_artefacts(page, user_rect)
    return True
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 插入 Option B dispatcher block 到 `app/services/redact.py`(line 195/232 boundary)</name>

  <files>app/services/redact.py</files>

  <read_first>
    - app/services/redact.py (FULL — 特別 line 1-80 模組 docstring + TRUE_REMOVAL_LIMITATION;line 82-90 imports;line 122-258 `remove_region_vector` 函式體含 line 174-179 apply_redactions、line 189-195 residual_content raise、line 197-256 既有 dispatcher、line 258 return True)
    - app/services/pdf_engine.py (從 Plan 07-01 留下的 `delete_zero_area_type_f_fills_inside` + `log_xobject_intersect` 完整 signature + docstring)
    - app/services/integrity.py:20-35 (logging import idiom — `import logging` + `logger = logging.getLogger(__name__)` 位置 + 順序)
    - tests/test_redact.py:1190-1207 (AGPL guard scope confirmation — adding `import logging` 到 redact.py 不破 guard,但加 `import fitz` 會破)
    - .planning/phases/07-option-b-implementation-content-stream-surgery/07-CONTEXT.md (§ decisions D-C1, D-C2, D-C3 — 插入點 + 既有 dispatcher 保留 + logger import strategy)
    - .planning/phases/07-option-b-implementation-content-stream-surgery/07-RESEARCH.md (§ Architecture Patterns 系統架構圖 lines 200-218 — dispatcher 6 步驟視覺化 + § Code Examples Example 8 lines 1093-1127 verbatim insertion block)
    - .planning/phases/07-option-b-implementation-content-stream-surgery/07-PATTERNS.md (§ Pattern B1 既有 dispatcher STYLE only DO NOT REWRITE + § Pattern B2 插入 block verbatim + § Risk Callout #5 AGPL seam + § Risk Callout #6 production code scope)
  </read_first>

  <action>
    在 `app/services/redact.py` 的 imports 區段(目前 line 82-84:`from __future__ import annotations`、空白行、`from . import pdf_engine`),於 `from __future__ import annotations` 之後、`from . import pdf_engine` 之前,加入 stdlib import:
    - `import logging`(stdlib;non-fitz,**不**破 AGPL seam,per Risk Callout #5 + Pattern S4)
    
    在所有 imports 之後(`from . import pdf_engine` 之後、`REDACT_PAD_PT = 5.0` 之前)加入 `logger = logging.getLogger(__name__)`(per Pattern A1 — 沿用 `app/services/integrity.py:26-32` 既有同包 sibling 慣例;per CONTEXT D-C3)。

    在 `remove_region_vector` 函式體內 line 195 `raise RedactError("residual_content", ...)` 結束之後(即 line 196 應該是空白行)、line 197 既有 `# Zero-area artefact cleanup ...` 註解開始之前,插入 Option B dispatcher block(per 07-RESEARCH § Code Examples Example 8 lines 1117-1127 verbatim + Pattern B2 verbatim,~10 LOC 含註解):
    - 一段繁中 inline comment block 標記為「Phase 7 Option B — page-level content-stream surgery (SEC-01)」,說明「真正刪除 fully-inside-rect 零面積 type='f' fills,upstream defense before 既有 Phase 5 Hotfix 06 dispatcher(form-XObject 內巢狀殘留時才會走 dense/sparse last-mile defense)」(per CONTEXT D-C1 verbatim)
    - 呼叫 `deleted = pdf_engine.delete_zero_area_type_f_fills_inside(page, user_rect)`(注意:不是 `tuple(user_rect)` — Plan 07-01 helper signature 是 `(page: "fitz.Page", user_rect: "fitz.Rect", tolerance=...)`,接受 `fitz.Rect` 物件)
    - `if deleted > 0:` →  `logger.info("option_b_deleted", extra={"page_index": page.number, "count": deleted})`(per Pattern S5 structured logging)
    - 呼叫 `pdf_engine.log_xobject_intersect(page, user_rect, logger=logger)`(注入 redact.py 的 logger,讓 event 在 caller namespace 中 surface,per Pattern A5)

    **既有 dispatcher 完整保留(per CONTEXT D-C2 + Pattern B1 + Risk Callout #6):**
    - line 197-258 的所有內容一字不改 — `# Zero-area artefact cleanup ...` 整段註解、`zero_area_count = ...` assignment、`if zero_area_count >= ... / else` dispatcher、`replace_region_with_white_raster` 呼叫、`whitepaint` 後置檢查、`cover_zero_area_artefacts` 呼叫、`return True`
    - 既有 `# HONEST LIMITATION` 區段註解(line 220-227)**保留**(Phase 8 THREAT-02 + DOC-01 才會 update,本 Plan 不動)
    - Phase 7 接到 dispatcher 後行為:Option B 跑完通常會把 page-level zero-area count 歸 0 → 既有 dispatcher 的 `if zero_area_count >= 100` 走 `replace_region_with_white_raster` 路徑成為 form-XObject 殘留時才觸發的 last-mile defense;`cover_zero_area_artefacts` 同理

    **絕對禁止(per Risk Callout #5 + Risk Callout #6 + 5330290 minimum-change 紀律):**
    - 加 `import fitz` 到 redact.py(AGPL seam 破裂 — guard test 立刻 fail)
    - 修改 line 197-258 既有 dispatcher 的**任何一行**(包括註解、空白、含 `HONEST LIMITATION` 區段 — Phase 8 才動)
    - 修改 line 1-80 模組 docstring 的 `TRUE_REMOVAL_LIMITATION` 區段(Phase 8 THREAT-02 才動)
    - 修改 line 261+ `remove_region_raster` 函式(raster branch 與 Option B 無關 — Phase 4 既有邏輯)
    - 加新 RedactError code(per CONTEXT D-A5 fail-safe — Option B parse anomaly 走 logger.warning,不 raise)
    - 把 helper 呼叫包在 try/except 內(per D-A5 fail-safe — Plan 07-01 helper 本身就 fail-safe,caller 不需額外包 try)
    - 修改 redact.py 任何其他函式(本 plan scope 嚴格限縮在 `remove_region_vector` 的 line 195/197 boundary)

    **最終 redact.py 改動 audit:** `git diff app/services/redact.py | grep -c '^[+-][^+-]'` 應顯示新增行數 12-14 行(2 LOC import + ~10 LOC dispatcher block + 0 LOC 刪除);**絕無**任何 `-` 開頭的 diff line(本 plan 只新增不刪除既有 code)。
  </action>

  <verify>
    <automated>
    # 1. AGPL seam unbroken — `import fitz` 仍只在 pdf_engine.py
    grep -rn "import fitz" app/  # expect: ONLY app/services/pdf_engine.py:19
    # 2. 新加 import logging 與 logger 存在
    grep -n "^import logging$" app/services/redact.py
    grep -n "^logger = logging.getLogger(__name__)$" app/services/redact.py
    # 3. Option B 呼叫已插入 + logger.info + log_xobject_intersect 呼叫存在
    grep -n "pdf_engine.delete_zero_area_type_f_fills_inside" app/services/redact.py
    grep -n "option_b_deleted" app/services/redact.py
    grep -n "pdf_engine.log_xobject_intersect" app/services/redact.py
    # 4. 既有 dispatcher line 197-256 完整保留(spot-check 既有 dispatcher 既存)
    grep -n "zero_area_count = pdf_engine.count_zero_area_fills_fully_inside" app/services/redact.py
    grep -n "if zero_area_count >= pdf_engine.ZERO_AREA_RASTER_THRESHOLD" app/services/redact.py
    grep -n "pdf_engine.cover_zero_area_artefacts" app/services/redact.py
    # 5. 既有 dispatcher 無 - diff(本 plan 只新增不刪除既有 code)
    git diff app/services/redact.py | awk '/^-[^-]/{c++} END {print "deletions:", c+0}'  # expect: deletions: 0
    # 6. AGPL guard test 仍綠
    python -m pytest tests/test_redact.py::test_fitz_import_confined_to_engine_seam -v
    # 7. Pre-existing tests 不退步(本 Task 完成、Task 2 xfail flip 還未跑)
    # Baseline 應該還是 315 passed + 3 skipped + 3 xfailed
    python -m pytest 2>&1 | tail -3  # expect: 315 passed, 3 skipped, 3 xfailed
    </automated>
  </verify>

  <done>
    - `app/services/redact.py` 含 `import logging`(stdlib)+ `logger = logging.getLogger(__name__)`
    - `app/services/redact.py::remove_region_vector` 在 line 195(residual_content raise)之後、line 197(既有 dispatcher 註解)之前插入 ~10 LOC Option B dispatcher block(包含繁中 inline comment + `delete_zero_area_type_f_fills_inside` 呼叫 + `option_b_deleted` info log + `log_xobject_intersect` 呼叫)
    - 既有 line 197-258 dispatcher 一字不改(`git diff app/services/redact.py | grep '^-[^-]' | wc -l` == 0)
    - AGPL seam 完整 — `grep -rn "import fitz" app/` 仍只回 `app/services/pdf_engine.py:19`
    - AGPL guard test PASSED
    - 既有 baseline 不退步 — `python -m pytest 2>&1 | tail -3` 仍顯示 `315 passed + 3 skipped + 3 xfailed`(本 Task 結束、Task 2 xfail flip 還未跑;Phase 6 紅燈仍紅但**現在的紅是因為 xfail decorator 還在,而非實際失敗** — 確認 integration 行為正確的 sanity step 留到 Task 2)
  </done>
</task>

<task type="auto">
  <name>Task 2: 拔 `tests/test_illustrator_attack_regression.py` line 74-82 的 `@pytest.mark.xfail(strict=True)` decorator,驗證 3 PASSED + 全 baseline 升級</name>

  <files>tests/test_illustrator_attack_regression.py</files>

  <read_first>
    - tests/test_illustrator_attack_regression.py (FULL — 特別 line 66-72 decorator 順序註解、line 73 `@pytest.mark.parametrize`、lines 74-82 `@pytest.mark.xfail(strict=True, reason=...)` decorator 區塊、line 83+ test function definition、lines 89-159 test body)
    - tests/_illustrator_attack.py (Phase 6 attack helper — 本 plan 不動,但確認 import path `from tests._illustrator_attack import delete_image_xobjects_intersecting` 仍有效)
    - tests/fixtures/cad-glyph/ (3 個 real-supplier sanitized fixture + sidecar manifests — `_load_fixtures()` 在 line 50-64 讀此目錄;本 plan 不動 fixtures)
    - app/services/redact.py(Task 1 已修改後的狀態 — Plan 07-02 Task 1 已落地)
    - app/services/pdf_engine.py(Plan 07-01 落地的 helper — Plan 07-02 Task 1 dispatcher 已呼叫)
    - .planning/phases/07-option-b-implementation-content-stream-surgery/07-CONTEXT.md (§ decisions D-D4 — xfail flip 機制 + Claude's Discretion "完全拔" 推薦)
    - .planning/phases/07-option-b-implementation-content-stream-surgery/07-RESEARCH.md (§ Code Examples Example 9 lines 1130-1158 — BEFORE/AFTER diff verbatim + 驗證指令序列)
    - .planning/phases/07-option-b-implementation-content-stream-surgery/07-PATTERNS.md (`tests/test_illustrator_attack_regression.py` 區段 lines 472-514 — DELETE 8 lines verbatim guidance + DO NOT MODIFY 邊界)
  </read_first>

  <action>
    刪除 `tests/test_illustrator_attack_regression.py` lines 74-82(含 line 74 `@pytest.mark.xfail(` 至 line 82 `)` 為止整個 decorator)。**保留**:
    - lines 1-72 全部(模組 docstring、imports、`_load_fixtures` helper、decorator 順序解釋註解)
    - line 73 `@pytest.mark.parametrize("fixture_pdf,manifest", _load_fixtures())`(此 decorator **必須保留** — 它是 outer decorator,parametrize 行為依賴它在最外層;per Phase 6 06-PATTERNS Risk Callout #3 解釋 decorator 順序的 load-bearing 性)
    - line 83+ test function definition `def test_illustrator_attack_residual_supplier_revealed(...)` 與 test body lines 89-159 全部

    **刪除動作精確指示**(per 07-RESEARCH § Code Examples Example 9 lines 1131-1153 verbatim):
    - BEFORE 狀態(verbatim 引自 Phase 6 produced 檔案,當前 line 73-83):
      ```
      @pytest.mark.parametrize("fixture_pdf,manifest", _load_fixtures())
      @pytest.mark.xfail(
          strict=True,
          reason=(
              "Option B 尚未實作(Phase 7 SEC-01 待落地)— "
              "Illustrator-class editor 拔 image XObject 後 page content stream 內的零面積 "
              "type='f' 路徑仍會 render 出供應商商標。Phase 7 落地後請拔掉本 marker。"
              "參 .planning/REQUIREMENTS.md SEC-01。"
          ),
      )
      def test_illustrator_attack_residual_supplier_revealed(
      ```
    - AFTER 狀態:
      ```
      @pytest.mark.parametrize("fixture_pdf,manifest", _load_fixtures())
      def test_illustrator_attack_residual_supplier_revealed(
      ```

    執行流程:
    1. `grep -n "@pytest.mark.xfail" tests/test_illustrator_attack_regression.py` 定位 marker(sanity check;應只命中 line 74)
    2. Edit:精確刪除 line 74 至 line 82 inclusive(9 行 — `@pytest.mark.xfail(` 開頭、`)` 結尾連同 4 行 reason 字串)
    3. 跑驗證指令序列:`python -m pytest -k illustrator_attack -v 2>&1 | tail -10` 應顯示 `3 passed`(原 3 xfailed)
    4. 跑全 baseline:`python -m pytest 2>&1 | tail -3` 應顯示 `≥ 318 passed + 3 skipped + 0 xfailed`(315 from Plan 07-01 + 3 flipped)

    **絕對禁止(per CONTEXT Claude's Discretion + 07-PATTERNS DO NOT MODIFY 邊界):**
    - 修改 lines 1-72(docstring、imports、`_load_fixtures` helper、decorator 順序註解)— 全部 load-bearing
    - 修改 line 73 `@pytest.mark.parametrize` decorator(必須是最外層 outer decorator)
    - 修改 lines 83+ test body(test logic 不動 — 只拔 decorator)
    - 在 commit message 之外保留 xfail reason 字串作 in-code historical note(per Claude's Discretion — 完全拔,reason 字串本來只在 marker 存在時有意義;若要保留歷史可在 commit message 加 cross-reference,但**不**在 code 內)
    - 改成 `@pytest.mark.xpass(strict=True)` 之類 alternative marker(無此語意 — 直接拔 decorator 是 Phase 6 設計的 binding handoff signal)
    - 改 reason 字串改為「已 closed by Plan 07-02」(留 marker 會繼續 mark 為 xfail — 必須完全拔才會真的 run + PASS)
    - 啟動性質改變(如 `@pytest.mark.skip`)— 必須真的 run 才能驗收 Option B 對真實 fixture 有效

    **xfail-strict 攔截邏輯確認**:Phase 6 設計的 xfail strict 在 Option B 落地後 test 真的 pass 時,marker 還在 → pytest 報 `XPASS(strict)` 為 FAILED(非 PASSED)。所以 marker 必須拔掉,不能留;這是 Phase 6 → Phase 7 的 binding handoff signal(per 06-PATTERNS Risk Callout #3 解釋 + 07-CONTEXT Claude's Discretion 確認)。
  </action>

  <verify>
    <automated>
    # 1. `@pytest.mark.xfail` 已被拔掉 — grep 不到此 pattern
    if grep -n "@pytest.mark.xfail" tests/test_illustrator_attack_regression.py; then echo "FAIL: xfail decorator still present"; exit 1; fi
    # 2. `@pytest.mark.parametrize` 仍保留(緊鄰 function definition 上方)
    grep -n "@pytest.mark.parametrize" tests/test_illustrator_attack_regression.py
    # 3. test function 仍存在
    grep -n "def test_illustrator_attack_residual_supplier_revealed" tests/test_illustrator_attack_regression.py
    # 4. SEC-01 acceptance gate — 3 個 parametrized case 全綠
    python -m pytest -k illustrator_attack -v 2>&1 | tail -10
    # expect: 3 passed in N.NNs(was 3 xfailed in Phase 6 baseline + Plan 07-01)
    # 5. Full baseline 最終升級
    python -m pytest 2>&1 | tail -3
    # expect: ≥ 318 passed + 3 skipped + 0 xfailed
    # 6. AGPL guard 仍綠
    python -m pytest tests/test_redact.py::test_fitz_import_confined_to_engine_seam -v
    # 7. AGPL seam(再次確認 Plan 整體結束時的 invariant)
    grep -rn "import fitz" app/  # expect: ONLY app/services/pdf_engine.py:19
    # 8. Phase 7 production code 改動範圍 audit(整體 Phase 7 從 base 到本 plan close)
    git diff --stat $(git log --oneline --grep="Phase 7" | tail -1 | awk '{print $1}')^..HEAD -- app/ 2>&1 || \
      git diff --stat HEAD~6..HEAD -- app/  # fallback 比對近 6 commits 範圍
    # expect: ONLY app/services/pdf_engine.py + app/services/redact.py 兩檔 modified
    # 9. TEST-03 14 cases(Plan 07-01)仍綠
    python -m pytest tests/test_pdf_engine.py -v 2>&1 | tail -5
    # expect: 14 passed
    </automated>
  </verify>

  <done>
    - `tests/test_illustrator_attack_regression.py` lines 74-82 `@pytest.mark.xfail(strict=True, reason=...)` decorator 完全刪除
    - `@pytest.mark.parametrize` decorator(原 line 73)保留,緊鄰 test function definition 上方
    - test body(原 lines 83-159)未修改
    - `python -m pytest -k illustrator_attack -v` 顯示 **3 PASSED**(原 3 XFAIL — Phase 6 → Phase 7 binding handoff signal 通過,SEC-01 acceptance gate 通過)
    - `python -m pytest 2>&1 | tail -3` 顯示 `≥ 318 passed + 3 skipped + 0 xfailed`(315 from Plan 07-01 + 3 flipped XFAIL → PASSED;原 Phase 6 baseline 301 + 14 TEST-03 + 3 flipped = 318 minimum;若 Plan 07-01 final N 有調整,acceptance 採 ≥ 318)
    - AGPL guard test PASSED;`grep -rn "import fitz" app/` 仍只回 `app/services/pdf_engine.py:19`
    - Phase 7 整體 production code 改動 audit:`git diff --stat $(phase-7-base-commit)..HEAD -- app/` 只命中 `app/services/pdf_engine.py` + `app/services/redact.py` 兩檔
    - Phase 7 close ready —— Phase 6 SECURITY.md 的 T-06-01 + T-02-07 兩條 `accept (P0, transition-pending)` 從生產證據面 closed(Option B 真正刪除 page-level 零面積 type='f' source,Illustrator 拔 image XObject 後 render 區仍 ≥98% 白 + count == 0)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `pipeline.process_job` → `redact.remove_region_vector` | Pipeline-validated `RegionMark`(via `pdf_engine.map_tuple_to_rect`)passes `fitz.Rect` user_rect downstream. Plan 07-02 dispatcher block trusts upstream validation per ASVS V5. |
| `redact.remove_region_vector` → `pdf_engine.delete_zero_area_type_f_fills_inside` | Plan 07-01 helper is fail-safe(D-A5)— never raises;all error paths return 0 + logger.warning. Plan 07-02 dispatcher call site does NOT wrap in try/except(per D-A5 design). |
| `redact.remove_region_vector` → existing dispatcher line 232-256 | Phase 4-6 Hotfix 06 last-mile defense untouched. Option B is upstream defense; when Option B leaves residual count > 0(form-XObject scenarios per SEC-03), existing dispatcher's Option A overlay catches. |
| `tests/test_illustrator_attack_regression.py` xfail flip | Phase 6 → Phase 7 binding handoff signal. Once decorator is removed and test really runs, MUST be 3 PASSED — that's the SEC-01 acceptance gate. If 3 cases somehow FAIL after flip(unexpected — would mean Plan 07-01 helper有bug或Plan 07-02 dispatcher integration有error),implementer MUST debug rather than re-add xfail marker. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | T (Tampering) / S (Spoofing) | `redact.py` dispatcher block accidentally adds `import fitz` during refactor / "tidiness" | mitigate | AGPL guard test enforces;Plan 07-02 verify gate `grep -rn "import fitz" app/` MUST return single line; explicit forbidden in Task 1 action list. `user_rect` arrives as `fitz.Rect` from upstream pipeline — no need to construct `fitz.Rect` in redact.py;all fitz ops delegate to pdf_engine helpers. |
| T-07-10 | T (Tampering) | Plan 07-02 修改既有 dispatcher block(line 197-258)— breaks last-mile defense for form-XObject residue | mitigate | Task 1 action explicitly forbids modifying line 197-258 any line;`git diff app/services/redact.py | grep '^-[^-]' | wc -l == 0` verify gate enforces;既有 dispatcher 字面保留為 form-XObject 殘留(per D-B4 / D-C2)last-mile defense。 |
| T-07-11 | I (Information disclosure) | xfail decorator 改成 `@pytest.mark.skip` 之類其他 marker → test 仍然不真實執行 → SEC-01 acceptance gate 形同虛設 | mitigate | Task 2 action 明列禁止 — 「啟動性質改變(如 `@pytest.mark.skip`)— 必須真的 run 才能驗收 Option B 對真實 fixture 有效」。`python -m pytest -k illustrator_attack -v` 應顯示 3 PASSED(NOT 3 skipped);driven by xfail decorator 完全刪除而非替換。 |
| T-07-12 | I (Information disclosure) | Plan 07-02 dispatcher block 把 Option B 呼叫包在 try/except 內 → 抓掉 helper 的 warning log → operational visibility lost | mitigate | Task 1 action 明列禁止 try/except wrap — Plan 07-01 helper 本身 fail-safe(D-A5)— caller 不需額外包 try;若包 try 會抓到 helper logger 內 warning(misclassified as caller error),失去 SEC-03 透明化 + cardinality 異常診斷能力。 |
| T-07-13 | A (Availability) | Plan 07-02 整合後 Option B 在某 fixture 上 hit edge case → helper raise(理論上 D-A5 已 fail-safe,但 import-time error 或 module 載入錯誤仍可能) → pipeline 中斷 | mitigate | Plan 07-01 Task 2 unit test 對 helper 的 import-time + fail-safe behavior 都有 verification(`from app.services import pdf_engine; callable(pdf_engine.delete_zero_area_type_f_fills_inside)`); Plan 07-02 Task 1 完成後跑 `python -m pytest 2>&1 | tail -3` baseline 應顯示 315 passed 不退步(若 import-time 或 module 載入錯誤,整個 test suite 會 ERROR 而非 passed)。 |

## Threat closures — Phase 7 close action(SEC-01 acceptance gate 通過後)

| Source threat | Disposition status after Plan 07-02 | Reason |
|--------------|--------------------------------------|--------|
| T-06-01 (Phase 6 06-SECURITY.md — Illustrator pulls image XObject overlay → supplier brand re-rendered) | **CLOSED via Option B** | Plan 07-02 Task 1 整合 Option B 到 redact.py dispatcher;Task 2 xfail decorator 拔除 → 3 PASSED 為生產證據面 closure。 Phase 7 自身 07-SECURITY.md(若 `gsd-secure-phase` 跑)應記為 `CLOSED via Option B` + `supersedes: [.planning/phases/06-regression-foundation-threat-model-re-evaluation/06-SECURITY.md]` cross-reference。 |
| T-02-07 (TRUE REMOVAL vs cover gap — v1.0 hotfix 06 close 為 `CLOSED with documented residual`,Phase 6 RE-OPENED) | **CLOSED via Option B** | 同上 — Phase 7 Option B 真正刪除 page-level 零面積 source,T-02-07 v1.0 hotfix 06 接受的「documented residual」現已 page-level only 殘留於 form-XObject 內(SEC-03 已透明化 via `log_xobject_intersect`);v1.0 archived `06-HOTFIX-SECURITY.md` 的 `CLOSED with documented residual` 由 Phase 7 升級為 `CLOSED via Option B`。 |

## New attack surface introduced by Phase 7 — covered by 07-01 / 07-02 plans

| New surface | Risk | Covered by |
|-------------|------|------------|
| Plan 07-01 helper bugs(regex 漏抓 / safe-skip 邊界誤判 / cardinality drift) | Helper 把 legit content 誤刪 / 漏刪 supplier source | D-A5 cardinality fail-safe(never destructive write on mismatch)+ TEST-03 14 cases(5 safe-skip context + 4 density gradient + 2 SEC-02 no-op + 3 SEC-03)+ Phase 6 regression test 3 PASSED gate(Plan 07-02 verify) |
| Plan 07-02 dispatcher integration 順序錯誤(若 Option B 插在 apply_redactions 之前或 residual_content assertion 之前,行為 undefined) | 既有 v1.0 baseline 被 break;Phase 4-6 invariants 不成立 | Task 1 action 明確插入點 = line 195 `residual_content` raise 之後、line 232 既有 dispatcher 之前(per D-C1 + 07-RESEARCH § Architecture Patterns dispatcher 流程圖);驗證 `python -m pytest 2>&1 | tail -3` baseline 不退步(若插入位置錯誤,既有 fixture test 立刻 fail)+ AGPL guard test 仍綠 |
</threat_model>

<verification>
**Plan-level invariants (must all hold at Plan 07-02 close — Phase 7 整體 close gate):**

1. **AGPL seam unbroken(Phase 7 整體)** — `grep -rn "import fitz" app/` returns exactly one line: `app/services/pdf_engine.py:19`
2. **AGPL guard test green** — `python -m pytest tests/test_redact.py::test_fitz_import_confined_to_engine_seam -v` → 1 PASSED
3. **Production code scope(Phase 7 整體)** — `git diff --stat <phase-7-base>..HEAD -- app/` only shows two files modified: `app/services/pdf_engine.py`(Plan 07-01)+ `app/services/redact.py`(Plan 07-02)
4. **既有 dispatcher line 197-258 untouched** — `git diff app/services/redact.py | grep '^-[^-]' | wc -l` returns `0`(本 plan 只新增不刪除既有 code)
5. **Plan 07-02 redact.py imports added** — `grep -n "^import logging$" app/services/redact.py` + `grep -n "^logger = logging.getLogger(__name__)$" app/services/redact.py` both find one line each
6. **Plan 07-02 dispatcher block inserted** — `grep -n "pdf_engine.delete_zero_area_type_f_fills_inside" app/services/redact.py` + `grep -n "pdf_engine.log_xobject_intersect" app/services/redact.py` + `grep -n "option_b_deleted" app/services/redact.py` 各找到一行
7. **xfail decorator 拔除** — `grep -n "@pytest.mark.xfail" tests/test_illustrator_attack_regression.py` 應**無**輸出(grep 退出 code 1)
8. **`@pytest.mark.parametrize` decorator 保留** — `grep -n "@pytest.mark.parametrize" tests/test_illustrator_attack_regression.py` 找到 1 行(line 73)
9. **SEC-01 acceptance gate — 3 PASSED** — `python -m pytest -k illustrator_attack -v 2>&1 | tail -10` 顯示 3 passed(原 3 xfailed)
10. **Final baseline upgraded** — `python -m pytest 2>&1 | tail -3` 顯示 `≥ 318 passed + 3 skipped + 0 xfailed`(原 Phase 6 baseline 301 + 14 TEST-03 + 3 flipped XFAIL = 318 minimum)
11. **TEST-03 14 cases 仍綠(Plan 07-01 work 未退步)** — `python -m pytest tests/test_pdf_engine.py -v 2>&1 | tail -5` 顯示 14 passed
12. **沒有 xpass(strict) failure** — Plan 07-02 結束後 `python -m pytest 2>&1 | tail -3` 應 `0 xfailed`(若 xfail 還留某 case,xpass(strict) 會報為 failed)
</verification>

<success_criteria>
- [ ] `app/services/redact.py` 含 `import logging` + `logger = logging.getLogger(__name__)`,既有 line 197-258 dispatcher 一字不改
- [ ] `app/services/redact.py::remove_region_vector` 在 line 195/197 boundary 插入 ~10 LOC Option B dispatcher block,呼叫 `delete_zero_area_type_f_fills_inside` 與 `log_xobject_intersect`,並在 deleted > 0 時 emit `option_b_deleted` info log
- [ ] `tests/test_illustrator_attack_regression.py` lines 74-82 `@pytest.mark.xfail` decorator 完全刪除;`@pytest.mark.parametrize` 保留
- [ ] `python -m pytest -k illustrator_attack -v` 顯示 3 PASSED — SEC-01 acceptance gate 通過
- [ ] `python -m pytest 2>&1 | tail -3` 顯示 `≥ 318 passed + 3 skipped + 0 xfailed` — final baseline 升級
- [ ] AGPL seam invariant 滿足:`grep -rn "import fitz" app/` 仍只回 `app/services/pdf_engine.py:19`;AGPL guard test PASSED
- [ ] Phase 7 整體 production code 改動範圍 audit 通過:`git diff --stat $(phase-7-base)..HEAD -- app/` 只命中 `pdf_engine.py` + `redact.py` 兩檔
- [ ] T-06-01 + T-02-07 兩條 Phase 6 accept (P0, transition-pending) 從生產證據面 closed —— Phase 7 自身 07-SECURITY.md 可記為 `CLOSED via Option B`(若 `gsd-secure-phase` 跑)
- [ ] STRIDE threat register T-07-09..T-07-13 + T-06-01 / T-02-07 closure 已記錄於本 PLAN 的 `<threat_model>` 區塊
</success_criteria>

<output>
After completion, create `.planning/phases/07-option-b-implementation-content-stream-surgery/07-02-SUMMARY.md` 包含:

- `app/services/redact.py` 改動 audit:`git diff app/services/redact.py` 輸出(僅新增,無刪除既有 code)
- Option B dispatcher block 位置確認(line 196 開始 insertion 點)
- 既有 dispatcher line 197-258 完整保留證據(無 `-` 開頭 diff lines)
- `tests/test_illustrator_attack_regression.py` xfail decorator 移除 diff
- SEC-01 acceptance gate 證據:`python -m pytest -k illustrator_attack -v` tail output 顯示 3 PASSED
- Final baseline 升級證據:`python -m pytest 2>&1 | tail -3` 顯示 `≥ 318 passed + 3 skipped + 0 xfailed`
- Phase 7 整體 production code 改動範圍 audit:`git diff --stat <phase-7-base>..HEAD -- app/` 只命中 2 檔
- AGPL seam invariant 確認:`grep -rn "import fitz" app/` 單行輸出
- T-06-01 + T-02-07 closure 記錄(cross-reference Phase 6 06-SECURITY.md 與本 plan)
- Phase 8 handoff signal:三處 LIMITATION docstring 區段(`pdf_engine.py::replace_region_with_white_raster`、`redact.py::TRUE_REMOVAL_LIMITATION` module-level、`redact.py` dispatcher inline `HONEST LIMITATION`)在 Phase 7 close 時 **未更新**(Phase 8 THREAT-02 + DOC-01 才動);Phase 8 implementer 接手須知:Option B 已 page-level closed,form-XObject 內仍 Option A overlay-only(已 log)
</output>
