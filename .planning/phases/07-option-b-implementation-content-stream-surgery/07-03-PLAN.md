---
phase: 07-option-b-implementation-content-stream-surgery
plan: 03
type: execute
wave: 1
depends_on: [01, 02]
gap_closure: true
autonomous: true
requirements: [SEC-01, SEC-02, SEC-03, TEST-03]
files_modified:
  - app/services/pdf_engine.py
  - tests/test_pdf_engine.py
  - tests/test_illustrator_attack_regression.py
  - tests/fixtures/cad-glyph/figure-glyph-01.pdf
  - tests/fixtures/cad-glyph/figure-glyph-01.json

must_haves:
  truths:
    - "python -m pytest -k illustrator_attack -v 顯示 3 PASSED(text-glyph / figure-glyph / mixed-glyph 全綠)"
    - "mixed-glyph-01.pdf 框選區(602,481,827,511)Option B 處理 < 5 秒(由 765s 降下),且 matched == 框選區 ZAF 數(高密度 Shape 1 不再 cardinality fail-safe 漏抓)"
    - "delete_zero_area_type_f_fills_inside 對重複 bbox 的 N 個 zaf(同一 logo 被分解為多筆同 bbox 描邊)全數刪除(攻擊 post-condition count==0 達成)"
    - "Shape 2(re 算子)路徑零改動 — _build_shape2_candidate_index / _locate_shape2_byte_range 一字未動"
    - "D-A5 fail-safe 語義保留 — 任一 zaf bbox 在 index 中對應 0 個 byte-range(真實漏抓)時 return 0 + logger.warning(option_b_parse_anomaly),絕不破壞性寫回"
    - "AGPL seam 不破 — grep -rn 'import fitz' app/ 仍只回 app/services/pdf_engine.py 一行"
    - "全測試套件 ≥318 passed + 3 skipped + 0 xfailed + 0 failed(既有 baseline 不退步)"
  artifacts:
    - path: "app/services/pdf_engine.py"
      provides: "_build_shape1_candidate_index 單次掃描索引 + 重設計的 bbox-keyed cardinality dispatch"
      contains: "_build_shape1_candidate_index"
    - path: "tests/test_pdf_engine.py"
      provides: "高密度 Shape 1 + 重複-bbox 單元測試"
      contains: "duplicate_bbox"
    - path: "tests/test_illustrator_attack_regression.py"
      provides: "重設計的 attack precondition(Option B 真刪→無 overlay 可拔=PASS)"
      contains: "white_pct"
    - path: "tests/fixtures/cad-glyph/figure-glyph-01.json"
      provides: "figure-glyph 重新 sanitize 後含真實零面積 type='f' 攻擊面的 region,或文件化的退化 fixture 說明"
  key_links:
    - from: "delete_zero_area_type_f_fills_inside dispatch loop"
      to: "_build_shape1_candidate_index"
      via: "Shape 1 zaf O(1) bbox-key 查表(取代 per-zaf 全串流 _Q_BLOCK_RE.finditer)"
      pattern: "_build_shape1_candidate_index"
    - from: "redact.remove_region_vector line 213"
      to: "delete_zero_area_type_f_fills_inside(page, rect)"
      via: "既有 07-02 wiring(本 plan 不動 redact.py)"
      pattern: "delete_zero_area_type_f_fills_inside"
---

<objective>
關閉 Plan 07-02 標記 FAILED 的 SEC-01 acceptance gate(3 個 illustrator-attack regression case)。診斷已查明三個獨立根因,本 plan 三管齊下修補:

1. **CORE — Shape 1 locator 重寫(SCOPE 1):** mixed-glyph-01.pdf 框選區內 1742 筆 Shape 1(`m/l` 算子,供應商 CAD glyph)只匹配 14%(249/1742)且耗時 765s。根因是 `_locate_shape1_byte_range`(pdf_engine.py:1019)對每個 zaf 做全串流 `_Q_BLOCK_RE.finditer` 掃描(O(1742 × 1.3MB))且強制 `len(matches) == 1` 唯一匹配(高密度下多筆同 bbox → 全部視為 ambiguous → None)。改為鏡像已正確運作的 Shape 2 single-pass 索引(`_build_shape2_candidate_index`,O(1) 查表),並重設計 cardinality 為 bbox-keyed 而非 1:1。目標:perf 765s→<5s、correctness 14%→~100%。

2. **text-glyph attack precondition 修補(SCOPE 2):** sparse fixture(zero-area count < 100)Option A overlay 不觸發,無 image XObject 可拔,但 Option B 已真刪 source → region 本已乾淨。test 須把「無 overlay AND region 乾淨(白≥98% + count==0)」視為 PASS,而非在 precondition(`n_deleted >= 1`)失敗。

3. **figure-glyph region 修補(SCOPE 3):** figure-glyph region(1950,80,2300,500)`original_supplier_zero_area_count: 0` — region 不含供應商零面積攻擊面,且 `process_job` 觸發既有 `residual_content` 斷言(region 落在真實非零面積向量幾何)。先調查 B-3012IP raw PDF 是否有可隔離的零面積 type='f' 攻擊區,有則重新 sanitize;無則文件化此 fixture 涵蓋的攻擊類別。

Purpose: SEC-01 是 v1.1 milestone「真正轉綠」的核心驗收閘。關閉後 Phase 6 繼承的 T-06-01 + T-02-07 才能 CLOSED via Option B。
Output: Shape 1 locator rework + 重設計的 attack test + figure-glyph fixture 修補,3 regression case 全綠。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

# Phase 7 lockfiles + diagnostic
@.planning/phases/07-option-b-implementation-content-stream-surgery/07-CONTEXT.md
@.planning/phases/07-option-b-implementation-content-stream-surgery/07-RESEARCH.md
@.planning/phases/07-option-b-implementation-content-stream-surgery/07-PATTERNS.md
@.planning/phases/07-option-b-implementation-content-stream-surgery/07-01-SUMMARY.md
@.planning/phases/07-option-b-implementation-content-stream-surgery/07-02-SUMMARY.md

# Source files to fix / read
@app/services/pdf_engine.py
@app/services/redact.py
@tests/test_pdf_engine.py
@tests/test_illustrator_attack_regression.py
@tests/_illustrator_attack.py
@tests/fixtures/cad-glyph/mixed-glyph-01.json
@tests/fixtures/cad-glyph/figure-glyph-01.json
@tests/fixtures/cad-glyph/text-glyph-01.json
@scripts/sanitize_fixture.py

<diagnostic_findings>
mixed-glyph-01.pdf region (602,481,827,511) 內部分流診斷(已執行,決定性):

STEP A: 3396 zafs in region; get_drawings+filter 0.29s
  Dispatch breakdown: re=1654  m/l=1742  mixed/other=0  empty=0
STEP B: stream=1,316,853 bytes; safe-skip mask 0.07s
STEP C: shape2 candidate index size=1654; built 0.23s
STEP C locate: 765.83s
  matched: re=1654  m/l=249  TOTAL=1903
  missed:  re=0  m/l=1466  other=0  dup-collision=27
  CARDINALITY: matched 1903 vs zafs 3396 → MISMATCH → D-A5 fail-safe → return 0

判讀:
- Shape 2(re 算子,TESTCO 注入):1654/1654 = 100% matched,0.23s O(1) dict index。此路徑正確 — 不要改。
- Shape 1(m/l 算子,供應商 CAD glyph):249/1742 = 14% matched,765s。兩個缺陷:
  1. PERF:_locate_shape1_byte_range 對每個 zaf 全串流 finditer = O(1742 × 1.3MB) = 765s。Shape 2 已用 single-pass index 解決,Shape 1 必須照做。
  2. CORRECTNESS:_locate_shape1_byte_range 要求 len(matches) == 1。1742 筆分解的供應商 logo 多筆共享近同 bbox → 多 q...Q block match 同 zaf bbox → return None(ambiguous)。1466 missed + 27 dup-collision。「唯一匹配」規則對「N 個 zaf 合法共享一 bbox 且應全刪」的高密度 glyph 根本錯誤。
</diagnostic_findings>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shape 1 locator 重寫 — single-pass 索引 + bbox-keyed cardinality(SCOPE 1 CORE)</name>
  <files>app/services/pdf_engine.py</files>

  <read_first>
  - app/services/pdf_engine.py:1108-1158 `_build_shape2_candidate_index` — 這是「鏡像的範本」。single-pass `_RE_FILL_RECT_RE.finditer(stream)`,逐 match 解析 → 算 user-space bbox → 以 round(bbox,3) 為 key、value 為 `list[(start,end)]`(setdefault 累加,非唯一)。只 index zero-area(`abs(w)<tol or abs(h)<tol`)候選。
  - app/services/pdf_engine.py:1019-1105 `_locate_shape1_byte_range` — 「被取代的舊函式」。注意它的 q...Q 解析:`_CM_RE`(cm 矩陣)、`_POINT_RE`(m/l 點)、`_FILL_OP_RE`(fill 算子驗證);bbox = local_rect * ctm * page_transform。這些解析子邏輯要保留搬進新索引函式,丟掉的只有「per-zaf 全串流掃描 + len(matches)==1 唯一規則」。
  - app/services/pdf_engine.py:1161-1183 `_locate_shape2_byte_range` — Shape 2 查表側(本 task 不動,僅作對照理解 dispatch 對稱性)。
  - app/services/pdf_engine.py:1186-1318 `delete_zero_area_type_f_fills_inside` — dispatch loop(1263-1301)+ cardinality(1287-1301)+ STEP E write-back(1303-1318)。本 task 改 dispatch + cardinality;STEP E PATTERNS S1 multi-stream write-back **一字不動**。
  - app/services/pdf_engine.py:384-391 `_Q_BLOCK_RE` 定義(`\bq\b (?P<body>[^Q]{0,2048}?) \bQ\b`);283-376 safe-skip + _RE_FILL_RECT_RE;964-988 `_build_safe_skip_mask` / `_is_unmasked` / `_splice_out`。
  - 07-PATTERNS.md Risk Callout #1(兩 shape 都要處理)+ #2(cardinality fail-safe 絕不破壞性寫回)+ #3(multi-stream write-back verbatim 不可 tidy)+ Pattern S3(D-A5 fail-safe)。
  - 07-CONTEXT.md D-A5(fail-safe 設計理由)+ D-A2(5-context safe-skip)。
  - diagnostic_findings(上方 context)— Shape 2 已 100% 正確,Shape 1 是唯一缺陷。
  </read_first>

  <action>
  在 pdf_engine.py 新增 `_build_shape1_candidate_index(stream, mask, tolerance, page_transform) -> dict`,並重設計 `delete_zero_area_type_f_fills_inside` 的 dispatch + cardinality。Shape 2 路徑(`_build_shape2_candidate_index` / `_locate_shape2_byte_range`)**一字不動**(diagnostic 證實 100% 正確)。STEP A pre-screen、STEP B mask、STEP E multi-stream write-back(PATTERNS S1 verbatim,asymmetric write-[0]+empty-[1:],compress=True)**一字不動**。AGPL seam 不破(全部在 pdf_engine.py 內,無新 fitz import 來源變更)。

  **1. 新增 `_build_shape1_candidate_index(stream, mask, tolerance, page_transform) -> dict`:**
  - single-pass over `_Q_BLOCK_RE.finditer(stream)`(取代舊函式的 per-zaf 重複全掃)。
  - 每個 q...Q block:`_is_unmasked(mask, start, end)` 先過 safe-skip;解析 `_CM_RE`(無則 fitz.Identity)、`_POINT_RE` 收集 m/l 點(無點則 skip);算 local bbox = min/max → `local_rect * ctm * page_transform` → `.normalize()` → user-space bbox;以 `_FILL_OP_RE.search(body)` 驗證至少一個 fill 算子(無則 skip)。
  - **只 index zero-area 候選**(鏡像 Shape 2):`if not (user_match.width < tolerance or user_match.height < tolerance): continue`(注意:user_match 已 normalize,width/height 恆正,用 `<` 即可;與 STEP A pre-screen 同 epsilon)。
  - key = `(round(x0,3), round(y0,3), round(x1,3), round(y1,3))`;value = `list[(start,end)]`,用 `index.setdefault(key, []).append((start, end))` **累加所有同 bbox 的 byte-range**(這是與舊「唯一匹配」的關鍵差異 — 多筆同 bbox 描邊全收)。
  - 把舊 `_locate_shape1_byte_range` 內的 `_NUMBER / _CM_RE / _POINT_RE / _FILL_OP_RE` 編譯搬到此新函式內(或提升為 module-level 與 _RE_FILL_RECT_RE 同層 — 推薦提升,避免每呼叫重編譯,呼應 pdf_engine.py:304-307 hot-path 註解;若提升須加同風格繁中註解)。
  - docstring 沿用 Pattern A3 風格 + 繁中 HONEST LIMITATION 段(說明 regex anchor 對高密度 stream 的本質侷限 + 漏抓走 D-A5 fail-safe);明確註記 value 是 list(非唯一)以支援合法重複-bbox glyph。

  **2. 刪除舊 `_locate_shape1_byte_range`**(pdf_engine.py:1019-1105 整段),由新索引函式取代。確認 grep 無其他呼叫點(僅 dispatch loop line 1272 呼叫,本 task 一併改)。

  **3. dispatch loop 改寫(pdf_engine.py:1263-1301):** 在 STEP C build Shape 2 index 之後、緊鄰新增 `shape1_index = _build_shape1_candidate_index(stream, mask, tolerance, page_transform)`。dispatch loop 改為 **bbox-keyed 分組** 而非 1:1 累加:
  - 先把 zafs 依 shape 分流並依 rounded bbox key 分組:`shape1_zaf_keys: dict[key → count]`、`shape2_zaf_keys: dict[key → count]`(re-only items → Shape 2;m/l-only items → Shape 1;mixed/empty → 走 fail-safe 標記)。bbox key 用與 index 相同的 `round(zaf['rect'].{x0,y0,x1,y1}, 3)`。
  - mixed/empty-item zaf(`items` 非全 re 且非全 m/l)→ 視為「無法定位」→ 必須觸發 fail-safe(見 cardinality 規則),不可靜默忽略。

  **4. cardinality 重設計（D-A5 fail-safe 保留,採 Option (ii) — 解析如下）:**
  舊規則 `len(ranges_to_delete) != len(zafs)` 在重複 bbox 下必失敗(M byte-ranges vs N zafs 不對稱)。新規則採 **per-zaf-bbox ≥1 覆蓋 + 刪除全部匹配 range**,理由與精確斷言:

  - **採 Option (ii)(每個 zaf-bbox 要求 index 有 ≥1 byte-range,刪除該 bbox 的全部 M 個 range),不採 Option (i)(M==N 精確)。** 理由:供應商把單一 logo 分解為多筆**同 bbox** 描邊(diagnostic 的 1466 missed + 27 dup-collision 即此),刪除該 bbox 的全部描邊才是正確語義;而 Phase 6 attack 的真實安全閘是 `count_zero_area_fills_in_region == 0`(框選區內零殘留)+ 白≥98%,不是「byte-range 數恰等於 get_drawings zaf 數」。要求 M==N 會在合法重複-bbox glyph 上偽失敗 → 退回 Option A overlay → attack 可拔 overlay → SEC-01 仍紅。
  - **精確判定式(寫進 code,逐字實作):**
    - 對 Shape 1:`missing_keys_1 = [k for k in shape1_zaf_keys if k not in shape1_index]`
    - 對 Shape 2:`missing_keys_2 = [k for k in shape2_zaf_keys if k not in shape2_index]`(Shape 2 dispatch 改為亦走 index `in` 檢查以對稱;但**不改 `_build_shape2_candidate_index` / `_locate_shape2_byte_range` 本體** — 可在 dispatch loop 直接用 `shape2_index.get(key, [])`,行為等價於既有 `_locate_shape2_byte_range` 但放寬「唯一匹配」為「≥1 匹配」以對齊 Shape 1 語義。若選擇保留呼叫 `_locate_shape2_byte_range`,則需確認其 `len==1` 規則不會在 mixed-glyph 的 1654 re zaf 上偽失敗 — diagnostic 顯示 re=1654 全 matched 故 Shape 2 目前唯一匹配成立;為求兩 shape 語義一致且 future-proof 重複-re-bbox,推薦 dispatch loop 統一用 index `.get(key)` ≥1 檢查,Shape 2 helper 函式體仍保留不動供既有測試引用)。
    - **fail-safe 觸發條件:** `if missing_keys_1 or missing_keys_2 or has_mixed_empty_zaf:` → `logger.warning("option_b_parse_anomaly", extra={page_index, user_rect, expected=len(zafs), matched=len(ranges_to_delete), missing_shape1=len(missing_keys_1), missing_shape2=len(missing_keys_2)})` → `return 0`(絕不寫回 — Risk Callout #2)。新增 missing_shape1/missing_shape2 欄位以利診斷。
    - **成功路徑:** 每個 zaf-bbox 都在對應 index 有 ≥1 range → 蒐集 `ranges_to_delete = 所有匹配 key 的全部 byte-range`(跨 Shape 1+2),用既有 `seen: set` 去重(同一 (start,end) 不重複加,已在 line 1282 有此邏輯,保留)。
  - **回傳值:** 成功時 `return len(zafs)`(維持「回傳偵測到的 zaf 數」既有契約 — redact.py:214 `if deleted > 0` 與 log count 仰賴此語義;刪除的 byte-range 數可能 > 或 < zaf 數,但對外契約是「成功刪除了這些 zaf」)。`_splice_out` 收的是去重後的 ranges_to_delete(可能多於 zaf 數,正確 — 一個 zaf bbox 對應多筆描邊 range)。

  **5. 保留不動的 invariant(逐項自檢):**
  - STEP A pre-screen(get_drawings 4-gate,IN-01 _DEGENERATE_BBOX_EPS)— 不動。
  - STEP B safe-skip mask(5-context,D-A2)— 不動。
  - STEP E PATTERNS S1 multi-stream write-back(asymmetric write-[0]+empty-[1:],compress=True,page.parent,不 collapse 兩 branch)— 一字不動。
  - Shape 2 helper 函式體(_build_shape2_candidate_index / _locate_shape2_byte_range)— 不動。
  - AGPL seam:無新增非 pdf_engine.py 的 fitz 來源。
  - 繁中 docstring / log message;English identifiers。

  禁止:不改 redact.py(07-02 已 wire,本 task 純 pdf_engine.py helper rework);不重寫 STEP E;不引入新 config / threshold;不放寬 zero-area epsilon。
  </action>

  <behavior>
  - 高密度 Shape 1:1742 筆 m/l zaf(含多筆同 bbox)→ 每個 zaf-bbox 在 shape1_index 有 ≥1 byte-range → 全數匹配(missing_keys_1 == []) → 不 fail-safe → 刪除全部匹配 range。
  - 重複 bbox:N 個 zaf 共享一 bbox 對應 index M 個 byte-range(M 可 ≠ N)→ 視為覆蓋成功(≥1)→ 刪除該 bbox 的全部 M 個 range。
  - 真實漏抓:某 zaf-bbox 在 index 完全找不到(0 byte-range)→ missing_keys 非空 → fail-safe return 0 + warning,不破壞性寫回。
  - mixed/empty-item zaf → has_mixed_empty_zaf True → fail-safe(維持保守)。
  - Shape 2 不退步:mixed-glyph 1654 re zaf 仍 100% 匹配。
  - perf:mixed-glyph 框選區 single-pass index → < 5s(舊 765s)。
  - re-entrancy:第二次呼叫 get_drawings 回 0 zaf → STEP A short-circuit return 0(既有,不退步)。
  </behavior>

  <verify>
    <automated>cd "C:/Users/scott/Dropbox/Working area/code PDF logo" && python -m pytest tests/test_pdf_engine.py -q 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
  - `python -m pytest tests/test_pdf_engine.py -q` 全綠(既有 14 case 不退步)。
  - `grep -rn "import fitz" app/` 仍只回 `app/services/pdf_engine.py` 一行。
  - `python -m pytest tests/test_redact.py::test_fitz_import_confined_to_engine_seam -v` PASSED。
  - `grep -n "_build_shape1_candidate_index" app/services/pdf_engine.py` 有定義 + dispatch 呼叫;`grep -n "_locate_shape1_byte_range" app/services/pdf_engine.py` 已移除(0 行)。
  - STEP E write-back block 與既有逐字相同(`git diff` STEP E 區段無 `-`/`+` 實質改動,僅 dispatch + cardinality 上方變更)。
  </acceptance_criteria>

  <done>
  _build_shape1_candidate_index 以 single-pass O(1)-lookup 取代舊 per-zaf 全掃;cardinality 改 bbox-keyed per-zaf ≥1 覆蓋(Option ii),保留 D-A5 fail-safe(真實漏抓 return 0 不寫回);Shape 2 + STEP A/B/E + AGPL seam 不動;test_pdf_engine.py 既有 14 case 全綠。
  </done>
</task>

<task type="auto">
  <name>Task 2: 高密度 Shape 1 + 重複-bbox 單元測試(SCOPE 1 驗證 + TEST-03 擴充)</name>
  <files>tests/test_pdf_engine.py</files>

  <read_first>
  - tests/test_pdf_engine.py:1-65 — module docstring + 既有 `test_option_b_density_gradient[0,1,100,1742]` 結構(Shape.draw_rect(W=0) 注入 pattern);本 task 新增 case 沿用此 in-memory builder 慣例。
  - tests/test_pdf_engine.py 全檔 — 既有 14 case(4 density + 2 SEC-02 + 5 safe-skip + 3 SEC-03)的 fitz license header(line 21)+ caplog 斷言風格(Pattern T3）。
  - 07-PATTERNS.md Pattern T1(Shape.draw_rect(W=0) 注入)+ Pattern T4(coverage map)+ Pattern S2(in-memory fixture,不 commit binary)。
  - app/services/pdf_engine.py Task 1 改後的 `_build_shape1_candidate_index` + dispatch(用於設計測試斷言)。
  - diagnostic_findings — 高密度 Shape 1 + 重複 bbox 是 Task 1 修補的目標,本 task 鎖死這個行為避免回歸。
  </read_first>

  <action>
  在 tests/test_pdf_engine.py 新增針對 Task 1 行為的單元測試(in-memory fixture,不 commit binary,沿用 Shape.draw_rect(W=0) + new_shape().finish(fill=).commit() 慣例)。所有測試用 m/l 算子型 zaf(Shape 1 路徑)— 注意 `Shape.draw_rect(W=0)` 產生的算子型別:若它產生 `re` 算子則改用 `Shape.draw_line` / `draw_polyline`(`p1→p2` 相同 x 或相同 y 的零面積線)以確保命中 Shape 1 m/l 路徑;executor 須先以小 spike 確認哪種 Shape API 產生 m/l items（`page.get_drawings()[i]['items'][0][0] == 'l' or 'm'`),再據以建 fixture。

  新增至少 3 個 case:

  1. **`test_option_b_shape1_high_density_all_matched`** — 合成 ~500 筆 Shape 1(m/l)零面積 fill 全在 user_rect 內(各不同 bbox),呼叫 `delete_zero_area_type_f_fills_inside` → assert `deleted == 500`(或實際合成數)+ `count_zero_area_fills_fully_inside` 後 == 0 + 處理時間合理(可加 `time.perf_counter` soft assert < 5s,但主斷言是正確性)。鎖死「single-pass 索引讓高密度 Shape 1 100% 匹配」。

  2. **`test_option_b_shape1_duplicate_bbox_all_deleted`** — 合成 N(如 5)筆**完全相同 bbox** 的 Shape 1 零面積 fill(同一 logo 分解為多筆同位置描邊),呼叫 helper → assert `deleted >= 1`(zaf 數)+ 刪除後 `count_zero_area_fills_fully_inside == 0`(全刪,不因「唯一匹配」漏掉)。鎖死 Option (ii) cardinality 語義 — 重複 bbox 不再偽失敗。

  3. **`test_option_b_shape1_genuine_miss_failsafe`** — 構造一個情境:get_drawings 偵測到一個 Shape 1 zaf,但其 byte-range 在 index 中找不到(例:把該 path 放進 safe-skip 區段如 `(...)` literal 內,或用 mixed items 的 zaf)→ assert `delete_zero_area_type_f_fills_inside == 0`(fail-safe)+ content stream `page.read_contents()` bytes 一字未改 + caplog 捕獲 `option_b_parse_anomaly` warning(用 `caplog.at_level(logging.WARNING, logger="app.services.pdf_engine")`,斷言 `rec.missing_shape1 >= 1` 或 expected/matched 欄位)。鎖死 D-A5 fail-safe 在新 cardinality 下仍成立 — 真實漏抓絕不破壞性寫回。

  測試命名用 English identifiers;docstring 用繁中說明驗證意圖 + 對應 Task 1 行為。若既有 `test_option_b_density_gradient[1742]` 因 Task 1 行為變更需微調(不應 — 它是不同 bbox 的密度測試,應仍綠),保留不動;若真需調整則在 docstring 標註 Rule-1 deviation 理由。
  </action>

  <behavior>
  - high_density:~500 不同-bbox Shape 1 zaf → deleted == 500,後 count == 0,< 5s。
  - duplicate_bbox:N 筆同 bbox zaf → 全刪,後 count == 0。
  - genuine_miss:zaf 存在但 index 找不到 → return 0 + bytes 不變 + option_b_parse_anomaly logged。
  </behavior>

  <verify>
    <automated>cd "C:/Users/scott/Dropbox/Working area/code PDF logo" && python -m pytest tests/test_pdf_engine.py -k "shape1" -v 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
  - 3 個新 case 全綠;`python -m pytest tests/test_pdf_engine.py -q` 總數 ≥17 case 全綠(原 14 + 新 3）。
  - genuine_miss case 確實捕獲 `option_b_parse_anomaly` 且 bytes 不變(證明 fail-safe 未破壞性寫回)。
  - 無 commit 新 binary fixture(全 in-memory)。
  </acceptance_criteria>

  <done>
  高密度 + 重複-bbox + genuine-miss-failsafe 三個單元測試鎖死 Task 1 的 Shape 1 rework 行為;test_pdf_engine.py 全綠。
  </done>
</task>

<task type="auto">
  <name>Task 3: attack precondition 重設計 + figure-glyph region 調查與修補(SCOPE 2 + SCOPE 3)</name>
  <files>tests/test_illustrator_attack_regression.py, tests/fixtures/cad-glyph/figure-glyph-01.pdf, tests/fixtures/cad-glyph/figure-glyph-01.json</files>

  <read_first>
  - tests/test_illustrator_attack_regression.py:131-168 — Step 3 attack + Step 4 雙閘 + line 146 `assert n_deleted >= 1` precondition(SCOPE 2 修補點)。整檔 docstring(line 80-109)說明四步流程與 Phase 6→7 handoff。
  - tests/_illustrator_attack.py:105-192 `delete_image_xobjects_intersecting`(回傳實際 subn count,0 = 無 overlay 可拔)+ render_region_white_pct(200-230)+ count_zero_area_fills_in_region(238-263)。
  - tests/fixtures/cad-glyph/figure-glyph-01.json — region(1950,80,2300,500)、`original_supplier_zero_area_count: 0`、`expected_zero_area_count_post_build: 1`(SCOPE 3 問題:region 不含供應商零面積攻擊面)。
  - tests/fixtures/cad-glyph/text-glyph-01.json + mixed-glyph-01.json — 對照(text region 440,80,580,200 count 1;mixed region 602,481,827,511 count 1742)。
  - scripts/sanitize_fixture.py:1-60 docstring + CLI(`--in --out --supplier-name --region-rect "x0,y0,x1,y1" --page-index --dpi`)+ Step 2 找 brand glyph union bbox + Step 5 self-assert(zero-area count ≥ 0.9× 原 count)。
  - 07-02-SUMMARY.md Issues Encountered 逐 fixture 分析(text-glyph attack precondition 過時 / figure-glyph residual_content 斷言)。
  - 07-CONTEXT.md「What Phase 7 does NOT touch」— 注意 scripts/sanitize_fixture.py 列為不動,但 SCOPE 3 例外:figure-glyph fixture 重新 sanitize 是 gap-closure 授權的 fixture 修補(非改 sanitize script 本體,只是用既有 CLI 重跑)。raw B-3012IP 在 .planning/debug/scratch/illustrator-attack-2026-05-28-archived/B-3012IP-WM02-T430.pdf(gitignored)。
  </read_first>

  <action>
  **SCOPE 2 — attack precondition 重設計(tests/test_illustrator_attack_regression.py):**
  把 line 146 `assert n_deleted >= 1` 的硬前提改為「承認 Option B 真刪 → 無 overlay 可拔 = 合法 PASS」。重設計 Step 3/4 邏輯:
  - 維持 Step 3 跑 `delete_image_xobjects_intersecting`(回傳 n_deleted)。
  - 移除「n_deleted == 0 即 precondition fail」的硬斷言。改為:無論 n_deleted 是否 > 0,**都跑兩道真實安全閘**:`white_pct = render_region_white_pct(...) >= 98.0` AND `zero_area_count = count_zero_area_fills_in_region(...) == 0`。
  - precondition 斷言**只在「無 overlay 可拔(n_deleted == 0)AND region 仍髒(白 < 98% 或 count > 0)」時才失敗** — 即 Option B 沒做事、又沒 overlay 防護 = 真實漏洞。若 n_deleted == 0 AND 兩閘都過 → region 本已乾淨(Option B upstream 已真刪)→ PASS。
  - 實作:先算兩閘值,再用組合邏輯斷言。給每個斷言繁中 descriptive message。docstring 加段說明:Phase 7 Option B 改變了防禦模型(true removal 取代 overlay cover),sparse fixture 可能無 overlay 但 region 已乾淨,故 precondition 改為「無 overlay 且 region 髒」才失敗。標註此為合法 test-design update(非降標準 — 兩道真實安全閘 white≥98 + count==0 一字不放鬆)。
  - 不動 line 1-72(docstring head + parametrize + _load_fixtures);不加 `--runxfail`;不動 _illustrator_attack.py。

  **SCOPE 3 — figure-glyph region 調查與修補(investigate-then-fix):**
  先調查,再擇一修補(executor 依調查結果決定 (a) 或 (b)):

  - **調查步驟(spike,throwaway script,完成後刪):** 開 raw `.planning/debug/scratch/illustrator-attack-2026-05-28-archived/B-3012IP-WM02-T430.pdf`,用 `page.get_drawings()` 掃全頁找零面積 type='f'(W<0.01 or H<0.01)path 群聚的 bbox union → 找出一個含真實供應商零面積 type='f' 攻擊面的 region(類似 mixed-glyph 的 CAD glyph 分解)。記錄該 region 的 PDF-point rect。
  - **(a) 若找到可隔離的零面積 type='f' 攻擊區:** 重跑既有 CLI `python scripts/sanitize_fixture.py --in <raw B-3012IP path> --out tests/fixtures/cad-glyph/figure-glyph-01.pdf --supplier-name "<從 raw 抓的供應商名>" --region-rect "<新 region x0,y0,x1,y1>" --page-index 0 --dpi 144`,重新產 figure-glyph-01.pdf + .json(含非零 `original_supplier_zero_area_count`)。確認 sanitize self-assert(Step 5)通過。確認新 fixture 跑 attack regression 後 region 不再觸發 `residual_content`(因 region 落在零面積攻擊面而非真實非零向量幾何)且兩閘過。
  - **(b) 若 B-3012IP 無可隔離的零面積 brand region(全是真實非零 CAD 幾何):** 文件化 figure-glyph 為涵蓋「不同攻擊類別 / 退化 fixture」。選項:或(b1)以另一 raw supplier PDF(archive 內 3013A-13A-C6 或 3013A-36A,已知有零面積攻擊面)重新 sanitize 成 figure-glyph;或(b2)若決定 figure-glyph 本質就是「無零面積攻擊面的 control fixture」,則調整其 manifest + 在 regression test 用 manifest 欄位(如 `original_supplier_zero_area_count == 0`)分支斷言 — 該 fixture 只驗「region 乾淨 + 無 residual」而非「攻擊拔 overlay 後仍乾淨」,並在 test docstring + manifest 註記其攻擊類別。**避免**靜默讓 figure-glyph 永遠 xfail/skip — 必須有明確的、會綠的斷言。

  **驗收(SCOPE 3 二擇一,但都須):** figure-glyph 在 `python -m pytest -k illustrator_attack -v` 中 PASS(不是 skip、不是 xfail)。若選 (b2) 分支斷言,該 case 仍須 assert 出實質安全屬性。

  **不動:** mixed-glyph / text-glyph 的 .pdf / .json(text-glyph 由 SCOPE 2 的 precondition 重設計修好,fixture 本身不重 sanitize);scripts/sanitize_fixture.py 本體;tests/_illustrator_attack.py。
  </action>

  <behavior>
  - text-glyph:Option B 真刪 → n_deleted==0(無 overlay)+ 白 99.59% + count==0 → 兩閘過 → PASS(不再因 precondition 失敗)。
  - mixed-glyph:Task 1 修好 Shape 1 後 Option B 真刪 1742+1654 → 框選區 count==0 + 白≥98% → PASS(可能仍有 Option A overlay 或無,皆 PASS)。
  - figure-glyph:(a) 重 sanitize 後含零面積攻擊面 → 攻擊拔 overlay 後仍 count==0 + 白≥98% → PASS;或 (b) 文件化為 control fixture,以 manifest 分支斷言 region 乾淨 + 無 residual → PASS。
  - 真實漏洞偵測能力保留:若未來某 fixture Option B 漏抓 AND 無 overlay AND region 髒 → precondition 斷言失敗(誠實 surface)。
  </behavior>

  <verify>
    <automated>cd "C:/Users/scott/Dropbox/Working area/code PDF logo" && python -m pytest -k illustrator_attack -v 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
  - `python -m pytest -k illustrator_attack -v` 顯示 **3 PASSED**(text-glyph / figure-glyph / mixed-glyph),0 failed / 0 xfailed / 0 skipped。
  - SCOPE 2:test 仍保留真實安全閘(白≥98% + count==0 一字未放鬆);precondition 只在「無 overlay 且 region 髒」才失敗。
  - SCOPE 3:figure-glyph PASS(非 skip/xfail);若重 sanitize 則 .json 含非零 `original_supplier_zero_area_count` 且 self-assert 通過;若文件化則 test docstring + manifest 明確記錄其攻擊類別與斷言。
  - 全套件:`python -m pytest 2>&1 | tail -3` 顯示 ≥318 passed + 3 skipped + 0 xfailed + 0 failed。
  </acceptance_criteria>

  <done>
  attack precondition 重設計(無 overlay + region 乾淨 = PASS,真實安全閘不放鬆);figure-glyph 調查後修補(重 sanitize 或文件化 control fixture)使其 PASS;3 個 illustrator-attack regression case 全綠;全套件 ≥318 passed + 3 skipped + 0 xfailed。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 供應商 PDF 內容流 → Option B regex surgery | 不可信的高密度供應商 content stream(1.3MB、3396 ZAF、PScript5 + Acrobat 混合算子)跨入 byte-range 定位與刪除 |
| Illustrator-class editor → 輸出 PDF | 攻擊者拔 image XObject overlay 試圖重現 page-level content stream 內的供應商零面積商標 |
| get_drawings (MuPDF top-left) ↔ content stream (PDF bottom-left) | 座標空間轉換經 page.transformation_matrix;bbox key 不一致會導致漏抓 → cardinality drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-14 | Tampering | `delete_zero_area_type_f_fills_inside` 重設計的 bbox-keyed cardinality | mitigate | 放寬 1:1 為 per-zaf-bbox ≥1 覆蓋(Option ii)後,風險是「刪到不該刪的 byte-range」。緩解:(a) byte-range 只來自通過 STEP A get_drawings 4-gate(type='f' + zero-area + fully-inside-rect)的 zaf bbox key;(b) 任一 zaf-bbox 在 index 找不到(missing_keys 非空)即 fail-safe return 0 不寫回;(c) 真實安全閘是 Phase 6 attack post-condition `count_zero_area_fills_in_region == 0` + 白≥98%(Task 3 鎖),不是 byte-range 計數;(d) Task 2 genuine_miss 測試證明 fail-safe 在新 cardinality 下仍不破壞性寫回。 |
| T-07-15 | Denial of Service | Shape 1 per-zaf 全串流 finditer(舊 765s) | mitigate | 改 single-pass `_build_shape1_candidate_index`(鏡像 Shape 2 O(1) 查表),mixed-glyph 框選區 765s → <5s。Task 2 high_density 測試含 perf soft-assert。 |
| T-07-16 | Information Disclosure | Illustrator 拔 overlay 後供應商零面積商標重現(SEC-01 核心威脅) | mitigate | Task 1 修好 Shape 1 後 Option B 真刪 page-level 零面積 source → 無 overlay 可拔即無內容可重現;Task 3 雙閘(白≥98% + count==0)為驗收。此即 T-06-01 + T-02-07 的 close 機制。 |
| T-07-17 | Tampering | figure-glyph 重新 sanitize 經既有 CLI(SCOPE 3a) | mitigate | 沿用既有 sanitize_fixture.py CLI + Step 5 self-assert(metadata 空 + supplier name 不在 get_text + zero-area count ≥0.9×原 count + out path 在 fixtures/cad-glyph);不改 sanitize script 本體;raw PDF 仍 gitignored(AGPL §13 lockfile 不破)。 |
| T-07-18 | Repudiation | D-A5 fail-safe 透明化 | accept(已緩解) | option_b_parse_anomaly 新增 missing_shape1/missing_shape2 欄位 → 漏抓可追溯;log_xobject_intersect(SEC-03)不變。既有 structured log 充分,無新攻擊面。 |

**繼承關閉:** Plan 07-03 close 後,mixed-glyph 通過 → Phase 6 繼承的 **T-06-01 + T-02-07** 由 `accept (P0, transition-pending)` 改為 **CLOSED via Option B**(07-SECURITY.md / verifier 記錄;本 plan 不寫 07-SECURITY,但提供 close evidence)。

**AGPL seam invariant(不變):** Shape 1 rework 全在 pdf_engine.py 內;redact.py 不動;`grep -rn "import fitz" app/` 仍單行。
</threat_model>

<verification>
## Phase-level checks (Plan 07-03 close gates)

```bash
cd "C:/Users/scott/Dropbox/Working area/code PDF logo"

# SEC-01 primary acceptance gate — 3 regression case 全綠
python -m pytest -k illustrator_attack -v 2>&1 | tail -10
# 期望:3 PASSED,0 failed / 0 xfailed / 0 skipped

# 全套件 baseline 不退步
python -m pytest 2>&1 | tail -3
# 期望:≥318 passed + 3 skipped + 0 xfailed + 0 failed

# TEST-03 Option B unit tests(含新高密度 + 重複-bbox + genuine-miss)
python -m pytest tests/test_pdf_engine.py -q 2>&1 | tail -5
# 期望:≥17 case 全綠

# AGPL seam 不破
grep -rn "import fitz" app/
# 期望:單行 app/services/pdf_engine.py

python -m pytest tests/test_redact.py::test_fitz_import_confined_to_engine_seam -v
# 期望:PASSED

# production scope:只動 pdf_engine.py(本 plan 不動 redact.py)
git diff --stat -- 'app/'
# 期望:僅 app/services/pdf_engine.py

# Shape 1 rework 落地驗證
grep -n "_build_shape1_candidate_index" app/services/pdf_engine.py   # 有定義 + 呼叫
grep -n "_locate_shape1_byte_range" app/services/pdf_engine.py       # 0 行(已移除)
```
</verification>

<success_criteria>
- SEC-01(primary)acceptance gate 通過:`python -m pytest -k illustrator_attack -v` → 3 PASSED。
- mixed-glyph 框選區 Option B 處理 < 5s(Shape 1 perf 765s→<5s)且 Shape 1 匹配率 14%→~100%。
- 重複-bbox glyph 全刪(Option ii cardinality);D-A5 fail-safe 在真實漏抓時仍 return 0 不破壞性寫回。
- Shape 2 路徑 + STEP A/B/E + AGPL seam 一字未動。
- 全套件 ≥318 passed + 3 skipped + 0 xfailed + 0 failed(既有 baseline 不退步)。
- SEC-02(no-op 安全)+ SEC-03(form-XObject 透明化)+ TEST-03(unit 覆蓋擴充)維持並擴充。
- T-06-01 + T-02-07 具備 CLOSED via Option B 的 evidence(mixed-glyph 通過)。
</success_criteria>

<output>
After completion, create `.planning/phases/07-option-b-implementation-content-stream-surgery/07-03-SUMMARY.md`

Summary 必須包含:
- Shape 1 rework 前後對照(perf 765s→實測秒數;匹配率 14%→實測%;cardinality 規則變更說明)
- `python -m pytest -k illustrator_attack -v` 的 3 PASSED 輸出(SEC-01 acceptance evidence)
- 全套件 final tally(≥318 passed + 3 skipped + 0 xfailed)
- SCOPE 3 figure-glyph 處置記錄((a) 重 sanitize 的 region + self-assert 結果,或 (b) 文件化的攻擊類別 + 分支斷言)
- T-06-01 + T-02-07 close-via-Option-B handoff signal(供 verifier / 07-SECURITY 記錄)
- AGPL seam + production scope(僅 pdf_engine.py)invariant 確認
</output>
