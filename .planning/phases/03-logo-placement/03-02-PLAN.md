---
phase: 03-logo-placement
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - app/services/pdf_engine.py
  - app/models.py
  - app/services/pipeline.py
  - app/api/process.py
  - web/js/regions.js
  - web/js/api.js
  - web/index.html
  - tests/test_logo.py
  - tests/test_process_api.py
autonomous: true
requirements:
  - LOGO-02
user_setup: []

must_haves:
  truths:
    - "選定的 logo 以維持長寬比的方式（keep_proportion）置入每個移除區域,在框內置中、完整顯示(contain),長寬比不符處自然留白（D-02 / LOGO-02）"
    - "輸出 PDF 中 logo 的 bbox 落在使用者框選的同一 pdf_rect 內,長寬比與來源相符（LOGO-02 / REMOVE-03 placement 繼承）"
    - "logo 在 apply_redactions 之後置入,移除後的文字/向量仍被真正移除而 logo 本身存活（D-02 / Pitfall 1）"
    - "全域單一 logo:JobSpec 帶選用 logo_id,所有移除區域置入同一 logo,跨多區重用單一 xref(無逐區重嵌)（D-01 / Pitfall 4）"
    - "未選 logo（logo_id 為 null/缺）時為純移除,與 Phase 2 行為完全一致；原始檔 SHA-256 在 remove+insert 後仍不變（D-01 / D-05）"
    - "結果預覽含 logo:現有 /result 端點渲染 work 副本(已含置入的 logo),前後對照標籤條件式改為「移除+置入結果」（D-06）"
  artifacts:
    - path: "app/services/pdf_engine.py"
      provides: "place_logo(page, rect, *, stream, xref) 包住 insert_image(keep_proportion=True, overlay=True)（fitz seam）+ get_image_rects 驗證包裝"
      contains: "def place_logo"
    - path: "app/models.py"
      provides: "JobSpec 擴充選用 logo_id（D-01 契約）"
      contains: "logo_id"
    - path: "app/services/pipeline.py"
      provides: "per-region 迴圈在 remove_region 之後對同一 pdf_rect 置入 logo,重用 xref"
      contains: "place_logo"
    - path: "tests/test_logo.py"
      provides: "LOGO-02 bbox/aspect + survives-redaction + single-xref 測試"
      contains: "def test_inserted_logo_bbox_within_rect_and_aspect_preserved"
  key_links:
    - from: "app/services/pipeline.py"
      to: "pdf_engine.place_logo(page, pdf_rect, ...)"
      via: "在 redact.remove_region 之後、同一 pdf_rect 上置入(logo_bytes 來自 logo.resolve)"
      pattern: "place_logo"
    - from: "app/services/pipeline.py"
      to: "logo.resolve(job_spec.logo_id)"
      via: "迴圈外解析一次 logo bytes（manifest 白名單,03-01 契約）"
      pattern: "logo\\.resolve"
    - from: "web/js/regions.js"
      to: "logos.getSelectedLogoId()"
      via: "applyRemoval 將 logo_id 併入 api.processJob 的 spec"
      pattern: "logo_id"
---

<objective>
完成「置入並在結果中看到 logo」的垂直切片（LOGO-02）:在 Phase 2 已證明的 per-region 移除迴圈中,於 `redact.remove_region` 之後對「同一 `pdf_rect`」呼叫新的 `pdf_engine.place_logo`(包住 `insert_image(keep_proportion=True, overlay=True)`),把 03-01 選定的 logo 置入每個移除區域、置中且維持長寬比;`JobSpec` 擴充選用 `logo_id`(全域單一 logo,D-01),跨多區重用單一 `xref`(避免檔案膨脹);前端 `applyRemoval` 把 `selectedLogoId` 併入 `/process`,並條件式把結果標籤改為「移除+置入結果」。結果預覽零新渲染——既有 `/result/.../image` 渲染的 work 副本現在已含 logo(D-06)。

此切片消費 03-01 的 `logo.resolve(logo_id)->bytes` 與 `getSelectedLogoId()`,交付完整可操作迴圈:框選 → 選 logo → 套用(移除+置入)→ 在前後對照看到 logo 已就定位 → 下載含 logo 的 PDF。

Purpose: 交付 LOGO-02(維持長寬比置入框選位置)的完整切片,並達成 ROADMAP 成功標準 3(移除與置入同一流程完成並下載)。
Output: `pdf_engine.place_logo`(唯一新 fitz 呼叫)+ `get_image_rects` 驗證包裝、`JobSpec.logo_id`、`pipeline.process_job` 置入掛鉤、`regions.js`/`api.js` 的 `logo_id` 串接 + 條件式標籤、`tests/test_logo.py` 的 LOGO-02 斷言、`test_process_api.py` 的 no-logo 純移除 + remove+insert SHA-256 擴充。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 3 source-of-truth artifacts (read before implementing)
@.planning/phases/03-logo-placement/03-CONTEXT.md
@.planning/phases/03-logo-placement/03-RESEARCH.md
@.planning/phases/03-logo-placement/03-PATTERNS.md
@.planning/phases/03-logo-placement/03-UI-SPEC.md

# 03-01 built the consumed contract — read its SUMMARY for logo.resolve + getSelectedLogoId
@.planning/phases/03-logo-placement/03-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase + 03-01. Use directly. -->

place_logo wrapper to ADD (pdf_engine.py — signature LIVE-VERIFIED against installed 1.27.2.3;
add it in the "Redaction seam" section after apply_redactions, ~line 230):
  def place_logo(page, rect, *, stream: bytes | None = None, xref: int = 0) -> int:
      return page.insert_image(rect, stream=stream, xref=xref,
                               keep_proportion=True,   # contain + center (LOGO-02)
                               overlay=True)           # paint ON TOP of cleaned content
  # First placement: pass stream=<png bytes>, returns the embedded image xref.
  # Subsequent placements of the SAME logo: pass xref=<that value> (omit stream) to dedup (Pitfall 4).
  # Also add get_image_rects(page, xref) -> list (thin wrapper, mirror get_text_words_in_rect:233-243)
  # so test_logo.py asserts the placed bbox without importing fitz.

The pipeline per-region loop to EDIT (app/services/pipeline.py:143-179). pdf_rect is ALREADY
computed at line 174; the logo target rect IS that same rect. Existing body:
  pdf_rect = coords.pixels_to_pdf_rect(clamped_px, effective_dpi, page)
  removed = redact.remove_region(page, pdf_rect)
  results.append({"page": page_no, "removed": removed, "clamped": was_clamped})
Add ABOVE the loop (after n_pages/results init, ~line 141):
  logo_bytes = logo.resolve(job_spec.logo_id) if getattr(job_spec, "logo_id", None) else None
  logo_xref = 0
Add INSIDE the loop, AFTER remove_region returns (CRITICAL ordering — Pitfall 1):
  if logo_bytes is not None:
      logo_xref = pdf_engine.place_logo(page, pdf_rect,
                  stream=(logo_bytes if logo_xref == 0 else None), xref=logo_xref)
Add `logo` to the imports line (pipeline.py:32 `from . import coords, pdf_engine, redact, render`).
pipeline.py STAYS fitz-free — the only new fitz call is inside place_logo (in pdf_engine).

remove_region returns False for a nothing-removable region (redact.py:97-99); place the logo
REGARDLESS of the `removed` flag when logo_id is set (A1 — the user framed it as a replacement
target; `removed` only gates the "沒有可移除的內容" notice). LogoError raised by logo.resolve
propagates out of process_job and is mapped to 4xx by the main.py handler 03-01 added (T-02-08).

JobSpec field to ADD (app/models.py:77-96 — mirror the existing optional-field + validator style):
  logo_id: str | None = Field(default=None, max_length=128,
      description="optional global logo id (D-01); resolved via manifest allowlist")
  # No charset validator needed for safety (resolution is a manifest-dict lookup, not a path);
  # max_length is cheap defense-in-depth (V5). Pydantic's RequestValidationError handler already
  # shapes a bad type into {code:"invalid_request"} 422.

process.py handler is UNCHANGED — it passes the whole JobSpec to process_job, so logo_id flows
through with zero handler change. Only update its docstring + the api.js JSDoc to mention the field.

Frontend wiring — regions.js applyRemoval (web/js/regions.js:570-576) currently posts:
  const result = await api.processJob(sessionId, { dpi: REQUESTED_DPI, regions: getJobRegions() });
Add logo_id to the spec: { dpi: REQUESTED_DPI, regions: getJobRegions(), logo_id: <selectedLogoId> }.
Source the selection from 03-01's logos.js export (import { getSelectedLogoId } from "./logos.js"
or read a shared accessor). processJob in api.js is UNCHANGED (it already JSON-stringifies the spec).

Conditional result relabel (UI-SPEC §Layout conditional relabel / D-06): the #view-result segment
(index.html:160-162, currently text "移除結果") shows "移除+置入結果" when a logo is selected and
"移除結果" when null. Accessible label stays "切換處理前後對照" (broadened). Set via textContent in
JS (regions.js setViewMode / updateActionGroup), NEVER hardcoded in HTML. The toggle MECHANICS are
unchanged — showResultImage(api.resultImageURL(...)) renders the work copy which now contains the
logo (ZERO new rendering).

LOGO-02 assertion recipe (RESEARCH §Validation Architecture, lines 459-466). Conftest page is
200x300pt; _REGION_PT = (10,40,190,120) (same constant as test_process_api.py:23 / test_redact.py).
Mirror test_process_api._exported_region_empty (39-50): open exported PDF, recompute pdf_rect via
coords.pixels_to_pdf_rect, then page.get_image_rects(<logo xref>) and assert each placed rect is
contained in target (± TOL) AND abs(width/height - source_aspect) < ASPECT_TOL.

Existing deferred-mutation SHA-256 assertion to EXTEND (test_process_api.py:62-79): the original
hash must stay unchanged across a remove+INSERT run too (D-05).

AST fitz-grep test (test_redact.py:514-531) ALREADY covers pipeline.py automatically (walks all
app/**/*.py) — just keep it green after place_logo lands. No new grep test needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: place_logo fitz wrapper + JobSpec.logo_id + pipeline integration (LOGO-02 bbox/aspect + survives-redaction + single-xref + no-logo + SHA-256 tests)</name>
  <read_first>
    - app/services/pdf_engine.py (the redaction wrappers add_redact_annot 193-208 / apply_redactions 211-230 / save_doc 338-352 — the EXACT wrapper shape to mirror; get_text_words_in_rect 233-243 — the verification-wrapper shape; the module docstring AGPL-seam note)
    - app/services/pipeline.py (the per-region loop 143-179; the imports line 32; the process_job docstring's RedactError-propagation note 99-101)
    - app/services/redact.py (remove_region returns False for nothing-removable 68-99 — confirms the A1 "place regardless of removed" decision; calls apply_redactions internally at 117 — confirms the Pitfall 1 ordering)
    - app/models.py (JobSpec + _cap_region_count validator 77-96 — the optional-field style to mirror)
    - tests/test_process_api.py (_exported_region_empty helper 39-50; _region_px_for 32-36; the deferred-mutation SHA-256 assertion 62-79; _REGION_PT constant 23)
    - tests/conftest.py (the logo_png_bytes + logo_library fixtures 03-01 added; _build_pdf 18-32)
    - .planning/phases/03-logo-placement/03-RESEARCH.md (Pattern 1 ordering + Pattern 2 xref dedup + §Code Examples place_logo + §Validation Architecture LOGO-02 recipe; Assumptions A1)
    - .planning/phases/03-logo-placement/03-PATTERNS.md (pdf_engine.py / pipeline.py / models.py / tests sections)
  </read_first>
  <behavior>
    - Test (LOGO-02 bbox + aspect): after process_job with a logo_id, open the exported PDF; for the logo xref, get_image_rects returns a bbox CONTAINED within the target pdf_rect (± TOL) AND abs(width/height - source_aspect) < ASPECT_TOL (contain + center + aspect-preserved, D-02).
    - Test (survives redaction): after a remove+insert run, the user rect has NO extractable text/vector (still truly removed, REMOVE-01 holds) AND the page has the logo image present (get_images non-empty) — logo inserted AFTER apply_redactions survives (Pitfall 1).
    - Test (single xref, D-01/Pitfall 4): with one logo_id across N>=2 regions (multi-region/multi-page job), page.get_images() shows the logo embedded ONCE (one shared xref reused), not N times.
    - Test (D-01 no-logo = pure removal): a /process with no logo_id (or null) produces NO embedded logo image — byte-behavior identical to Phase 2.
    - Test (D-05 SHA-256): the original's SHA-256 is unchanged across a remove+insert run (extend the existing assertion).
  </behavior>
  <action>
    Add `place_logo(page, rect, *, stream: bytes | None = None, xref: int = 0) -> int` to `app/services/pdf_engine.py` in the "Redaction seam" section (after `apply_redactions`, ~line 230) — a thin docstring'd wrapper around `page.insert_image(rect, stream=stream, xref=xref, keep_proportion=True, overlay=True)` returning the embedded image xref. `keep_proportion=True` gives contain+center (LOGO-02, verified default); `overlay=True` paints on top of the cleaned content. Also add a `get_image_rects(page, xref) -> list` thin wrapper (mirror `get_text_words_in_rect`) so the test asserts the placed bbox without importing fitz. Keep `import fitz` confined to this file.

    Extend `JobSpec` in `app/models.py` (mirror the existing optional-field style): `logo_id: str | None = Field(default=None, max_length=128, description="optional global logo id (D-01); resolved via manifest allowlist")`. No charset validator needed for safety (resolution is a manifest-dict lookup in 03-01's `logo.resolve`, never a path); `max_length` is cheap defense-in-depth (V5). Update the `JobSpec` docstring to note the new optional field. Update `app/api/process.py`'s `process_session` docstring to mention the field (the handler itself is UNCHANGED — it passes the whole JobSpec through).

    Edit `app/services/pipeline.py`'s `process_job` per-region loop. Add `logo` to the imports line (`from . import coords, logo, pdf_engine, redact, render`). ABOVE the loop (after `n_pages`/`results` init, ~line 141): `logo_bytes = logo.resolve(job_spec.logo_id) if getattr(job_spec, "logo_id", None) else None` and `logo_xref = 0`. INSIDE the loop, STRICTLY AFTER `removed = redact.remove_region(page, pdf_rect)` returns (Pitfall 1 — `remove_region` calls `apply_redactions` internally; inserting before would redact the logo away): `if logo_bytes is not None: logo_xref = pdf_engine.place_logo(page, pdf_rect, stream=(logo_bytes if logo_xref == 0 else None), xref=logo_xref)`. Place the logo on the SAME `pdf_rect` REGARDLESS of the `removed` flag (A1). `pipeline.py` stays fitz-free. A `LogoError` from `logo.resolve` propagates out of `process_job` and is mapped to a 4xx by the `main.py` handler 03-01 added (T-02-08) — do not catch it here; mirror the existing `RedactError` propagation.

    Add the LOGO-02 tests to `tests/test_logo.py` (mirror `_exported_region_empty` + `_region_px_for` + the SHA-256 assertion from `test_process_api.py`; use the `logo_library`/`logo_png_bytes` fixtures from 03-01; geometry 200x300pt, `_REGION_PT = (10,40,190,120)`): bbox-within-rect+aspect, survives-redaction, single-xref-across-N-regions. Extend `tests/test_process_api.py` with: a no-`logo_id` run produces no embedded image (D-01 pure removal unchanged), and the original-SHA-256 assertion covers a remove+insert run (D-05). Define `TOL`/`ASPECT_TOL` small constants per the RESEARCH recipe. Keep all new test modules fitz-free except via the pdf_engine wrappers (the existing AST grep test enforces it).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_logo.py tests/test_process_api.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/services/pdf_engine.py` contains `def place_logo(` and `def get_image_rects(`.
    - `Grep "keep_proportion=True" app/services/pdf_engine.py` matches AND `Grep "keep_proportion=False" app/services/pdf_engine.py` returns NO match (LOGO-02; the stretch anti-pattern is absent).
    - `app/models.py` contains `logo_id` on `JobSpec`.
    - `app/services/pipeline.py` contains `logo.resolve` and `place_logo`; `Grep "import fitz" app/services/pipeline.py` returns NO match (AGPL seam intact).
    - `pytest tests/test_logo.py::test_inserted_logo_bbox_within_rect_and_aspect_preserved -x` exits 0 (placed bbox subset of target rect AND aspect ~= source — D-02/LOGO-02).
    - `pytest tests/test_logo.py::test_logo_survives_redaction -x` exits 0 (text/vector removed AND logo image present — Pitfall 1).
    - `pytest tests/test_logo.py::test_global_logo_single_xref -x` exits 0 (one xref reused across N regions — D-01/Pitfall 4).
    - `pytest tests/test_process_api.py::test_process_without_logo_is_pure_removal -x` exits 0 (no logo_id -> no embedded image — D-01).
    - The deferred-mutation SHA-256 test passes for a remove+insert run (original bytes unchanged — D-05).
    - `.venv/Scripts/python.exe -m pytest tests/test_redact.py::test_fitz_import_confined_to_engine_seam -x` exits 0 (pipeline.py did not leak fitz after place_logo landed).
  </acceptance_criteria>
  <done>
    A logo selected via 03-01 is placed (centered, aspect-preserved) into every framed region on the SAME pdf_rect the removal used, inserted after apply_redactions so it survives while text/vector stay removed; one global logo reuses a single xref across all regions; a no-logo job is pure Phase-2 removal; the original is byte-for-byte unchanged; all LOGO-02 assertions pass and pipeline.py stays fitz-free.
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend — send logo_id on /process + conditional 移除+置入結果 relabel (D-06)</name>
  <read_first>
    - web/js/regions.js (applyRemoval 559-604 — the processJob call to extend; setViewMode 519-542 + updateActionGroup 458-490 — where the conditional label is set; the COPY object 34-58)
    - web/js/api.js (processJob 136-149 — UNCHANGED; the JSDoc contract block 8-27 to update)
    - web/js/logos.js (03-01's getSelectedLogoId export — the selection source)
    - web/index.html (the #view-result segment 157-162, current text 移除結果; the beforeafter-toggle aria-label 153)
    - .planning/phases/03-logo-placement/03-UI-SPEC.md (§Copywriting Contract Result/before-after relabel; §Layout & Interaction "Conditional result relabel" + "Single global logo, single flow"; default #6)
    - .planning/phases/03-logo-placement/03-PATTERNS.md (web/js/api.js + web/js/logos.js sections — the result-with-logo-needs-no-new-code note)
    - .planning/phases/03-logo-placement/03-01-SUMMARY.md (how 03-01 exposed the selection state)
  </read_first>
  <action>
    Wire the selected `logo_id` into the existing apply flow. In `web/js/regions.js` `applyRemoval`, add `logo_id` to the `api.processJob` spec: `{ dpi: REQUESTED_DPI, regions: getJobRegions(), logo_id: <selected> }`, sourcing the selection from 03-01's `getSelectedLogoId()` (import it from `./logos.js`, or use the shared accessor 03-01 established; pass `null` when nothing is selected). `api.processJob` is UNCHANGED (it already JSON-stringifies the whole spec). Update the `api.js` JSDoc "Backend contract consumed" block to document the optional `logo_id` on `/process` (if 03-01 did not already).

    Implement the conditional result relabel (D-06 / UI-SPEC default #6): the `#view-result` segment shows `移除+置入結果` when a logo is selected and `移除結果` when null — set via `textContent` in JS (in `setViewMode` and/or `updateActionGroup`, wherever the result state is refreshed), NEVER hardcoded in HTML. Add the two label strings to the `COPY` object (`resultLabelWithLogo: "移除+置入結果"`, `resultLabelNoLogo: "移除結果"`). Broaden the toggle's accessible label to `切換處理前後對照` (it reads correctly whether or not a logo is selected) — update the `aria-label` on `#beforeafter-toggle` (index.html:153) or via setAttribute in JS. The toggle MECHANICS are unchanged: `showResultImage(api.resultImageURL(...))` already renders the work copy which now contains the placed logo — ZERO new rendering. Verify the longer `移除+置入結果` string still fits the segmented control (it wraps gracefully per the Phase-2 responsive rule; no layout rework).

    Do NOT add or remove any action-group buttons (D-01: the logo rides the existing single flow; the action group stays the Phase-2 state machine verbatim). The logo-change->stale invalidation was wired in 03-01 via `notifyJobInputChanged`; this task only adds the apply-time payload + the label.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest -q && node -e "const fs=require('fs');const js=fs.readFileSync('web/js/regions.js','utf8');if(!js.includes('logo_id'))throw new Error('regions.js must send logo_id');if(!js.includes(String.fromCharCode(0x79fb,0x9664,0x002b,0x7f6e,0x5165,0x7d50,0x679c)))throw new Error('relabel string must live in JS');console.log('logo_id payload + relabel present');"</automated>
  </verify>
  <acceptance_criteria>
    - `Grep "logo_id" web/js/regions.js` matches inside `applyRemoval` (the `/process` spec carries the selected logo, D-01).
    - `web/js/regions.js` contains both `移除+置入結果` and `移除結果` (the conditional relabel strings live in the COPY object / JS, not hardcoded in HTML).
    - The `#view-result` segment in `web/index.html` is NOT hardcoded to `移除+置入結果` (it stays `移除結果` as the default; JS sets it conditionally).
    - `web/index.html` (or JS via setAttribute) carries the broadened toggle aria-label `切換處理前後對照`.
    - `Grep "getSelectedLogoId" web/js/regions.js` matches (selection sourced from 03-01's logos.js export).
    - No new action-group button added/removed in `regions.js` `updateActionGroup` (Grep shows `apply-removal`/`download-pdf` are the only action buttons — D-01 single flow unchanged).
    - `.venv/Scripts/python.exe -m pytest -q` exits 0 (full suite green — no backend regression from the frontend change).
  </acceptance_criteria>
  <done>
    Applying with a logo selected sends logo_id on /process, so the downloaded PDF and the after-image both contain the placed logo; the before/after "after" segment reads 移除+置入結果 when a logo is selected and 移除結果 when not, with the toggle mechanics and action group unchanged (zero new rendering — the work-copy render already contains the logo, D-06).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API (`POST /process` with `logo_id`) | Untrusted `logo_id` in the JobSpec crosses here; resolved server-side via the 03-01 manifest allowlist, never a path |
| service -> fitz (`pdf_engine.place_logo`) | The only new fitz call; the AGPL isolation seam must stay confined to pdf_engine.py |
| work copy -> original (deferred mutation) | Logo insertion mutates ONLY the work copy; the immutable original must stay byte-identical |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 (carried) | Tampering / Info Disclosure | `logo.resolve(job_spec.logo_id)` in the `/process` path | mitigate | `logo_id` resolved via the 03-01 manifest dict allowlist + `is_relative_to(LOGOS_DIR)` assert; an unknown id raises `LogoError("logo_not_found")` -> structured 404 via the main.py handler (never a path read, never a bare 500, T-02-08). |
| T-03-05 | Denial of Service (mild) | Logo image embedding across N regions | mitigate | Reuse the returned `xref=` for the 2nd+ placements of the one global logo (Pattern 2); `save_doc` compacts with `garbage=4,deflate=True,clean=True`. Asserted by the single-xref test. |
| T-02-04 (carried) | Denial of Service | Unbounded regions drive unbounded insert passes | mitigate | The existing `MAX_REGIONS=200` cap on `JobSpec.regions` already bounds the loop the insertion runs inside. |
| T-02-05 / D-05 (carried) | Tampering | Mutating the original instead of the work copy | mitigate | `process_job` resets+edits only the work copy (WR-01); logo insertion is on that same work copy; the original is chmod 0o444 and its SHA-256 is asserted unchanged across the remove+insert run. |
| T-02-03 / AGPL (carried) | Licensing / architecture | `import fitz` leaking out of `pdf_engine.py` | mitigate | `place_logo` is the only new fitz call and lives in `pdf_engine.py`; `pipeline.py` stays fitz-free; the existing AST grep test enforces it. |
| Pitfall 1 (integrity) | Tampering | Logo inserted before `apply_redactions` would be redacted away | mitigate | Insert STRICTLY after `redact.remove_region` returns; the survives-redaction test proves the logo persists while text/vector stay removed (REMOVE-01 holds). |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/test_logo.py tests/test_process_api.py -q` — LOGO-02 bbox/aspect + survives-redaction + single-xref + no-logo pure-removal + remove+insert SHA-256 all green.
- `.venv/Scripts/python.exe -m pytest -q` — full suite (140 existing + Phase-3 new) green; `test_fitz_import_confined_to_engine_seam` confirms pipeline.py stays fitz-free after place_logo lands.
- Static (regions.js): sends `logo_id`; conditional relabel strings in JS not hardcoded HTML (Task 2 automated check).
- Manual visual (per-phase quality gate): frame a region -> pick the placeholder logo -> 套用 -> the after-image (now labelled 移除+置入結果) shows the logo centered in the framed area over a colored region (Pitfall 2 transparency check); download -> the PDF contains the placed logo and the supplier content is gone; switching logo marks the result stale (重新套用).
</verification>

<success_criteria>
- LOGO-02 satisfied: the selected logo is placed into every framed region on the SAME pdf_rect the removal used, centered + aspect-preserved (keep_proportion), with the placed bbox contained in the target rect — proven by automated assertions.
- ROADMAP success criterion 3 met: remove + place happen in one /process flow and the result downloads as a single PDF; the after-image preview shows the logo (D-06, zero new rendering).
- D-01 (one global logo, no-logo = pure removal), D-05 (original SHA-256 unchanged), and the AGPL seam (fitz only in pdf_engine.py) all hold.
</success_criteria>

<output>
After completion, create `.planning/phases/03-logo-placement/03-02-SUMMARY.md`
</output>
