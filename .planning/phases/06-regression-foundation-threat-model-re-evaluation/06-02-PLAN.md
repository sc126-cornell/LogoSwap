---
phase: 06-regression-foundation-threat-model-re-evaluation
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - tests/_illustrator_attack.py
  - tests/test_illustrator_attack_regression.py
  - .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-SECURITY.md
  - .planning/debug/scratch/illustrator-attack-2026-05-28-archived/README.md
  - .planning/debug/scratch/illustrator-attack-2026-05-28-archived/_attack_proof_supplier_revealed.png
  - .planning/debug/scratch/illustrator-attack-2026-05-28-archived/_attack_target_pre.png
  - .planning/debug/scratch/illustrator-attack-2026-05-28-archived/_attack_orig_for_comparison.png
  - .planning/debug/scratch/illustrator-attack-2026-05-28-archived/_attack_image_xobject_deleted.pdf
  - samples/3013A-13A-C6-XX-3D02-A01-00040.pdf  # 此檔將由 Task 4 用 `git rm` 從 index 移除(per Blocker #2);實體 bytes 可同時物理移入 archived 目錄(.gitignore archived-anchored 屏蔽 — Plan 06-01 Step C 已加)
autonomous: true
requirements: [TEST-02, THREAT-01]
tags: [pytest, xfail-strict, illustrator-attack, stride, threat-model, scratch-retirement]
# scope_note: 4 tasks justified — testing(Tasks 1+2 helper + xfail regression)+ threat-doc(Task 3 06-SECURITY.md 是文件 deliverable,~300 行 prose 而非 implementation complexity)+ cleanup(Task 4 scratch retirement + samples/ git rm)tightly coupled within single phase deliverable;split into 06-03 considered (Warning #4) but rejected because Task 3 SECURITY.md cross-references Task 2 test file + Task 4 retirement,以及 Task 4 cleanup 對 Task 3 evidence path 有先後依賴(archived dir 必須先存在才能 cite),如拆 plan 將造成 wave 排序 + cross-file ref 增加複雜度。

must_haves:
  truths:
    - "`python -m pytest -k illustrator_attack -v` 顯示恰好 3 個 XFAIL(對應 text-glyph-01 / figure-glyph-01 / mixed-glyph-01 三個 fixture)"
    - "`python -m pytest --co -q tests/test_illustrator_attack_regression.py` 顯示恰好 3 個 parametrized cases collected"
    - "Full pytest suite(`python -m pytest`)顯示 `301 passed, 3 skipped, 3 xfailed`(baseline 301 + 3 不退步,新增 3 xfailed)"
    - "`tests/test_redact.py::test_fitz_import_confined_to_engine_seam` 仍綠燈(AGPL seam 未破)"
    - "`grep -rn 'import fitz' app/` 仍 == `app/services/pdf_engine.py` 一行"
    - "`.planning/phases/06-regression-foundation-threat-model-re-evaluation/06-SECURITY.md` 存在,frontmatter `threats_total: 2 / threats_closed: 0 / threats_open: 0 / threats_accepted: 2`,STRIDE 表含 T-02-07 RE-OPENED + T-06-01 OPEN 兩條,Accepted Risks Log 列出兩條皆 `accept (P0, transition-pending until Phase 7)`"
    - "`.planning/debug/scratch/illustrator-attack-2026-05-28/` 目錄已不存在(git mv 為 `…-archived/`),`_attack_delete_image_xobject.py` 與 `_check_supplier_removal.py` 已刪除,4 個 PNG/PDF 證據保留"
    - "`grep -rn 'xfail.*Option B' tests/` 至少 1 處 hit,reason 字串含 `.planning/REQUIREMENTS.md SEC-01` cross-reference"
    - "Phase 6 production-code invariant 不變:`git diff --stat HEAD app/` 顯示 0 changed files"
    - "**No raw supplier PDF tracked anywhere in repo**(per checker Blocker #2 + Warning #9):`git ls-files | grep -E '3013A-13A-C6-XX'` returns empty(`samples/3013A-...pdf` 由 Task 4 `git rm` 移除;repo root 副本本來 untracked + .gitignore 屏蔽;archived dir 內副本由 .gitignore archived-anchored 屏蔽)"
    - "**Scratch git history preserved**(per checker Blocker #3):`git log --follow .planning/debug/scratch/illustrator-attack-2026-05-28-archived/_attack_proof_supplier_revealed.png | head -1` returns ≥1 commit predating Phase 6"
  artifacts:
    - path: "tests/_illustrator_attack.py"
      provides: "Attack helper module — 3 exported functions per D-B2"
      contains: "def delete_image_xobjects_intersecting", "def render_region_white_pct", "def count_zero_area_fills_in_region", "import fitz"
      exports: ["delete_image_xobjects_intersecting", "render_region_white_pct", "count_zero_area_fills_in_region"]
      min_lines: 80
    - path: "tests/test_illustrator_attack_regression.py"
      provides: "Parametrized red-light regression test;@pytest.mark.parametrize ABOVE @pytest.mark.xfail(strict=True)"
      contains: "@pytest.mark.parametrize", "@pytest.mark.xfail(", "strict=True", "Option B", "SEC-01", "render_region_white_pct", "count_zero_area_fills_in_region", "JobSpec"
      min_lines: 90
    - path: ".planning/phases/06-regression-foundation-threat-model-re-evaluation/06-SECURITY.md"
      provides: "Pre-mortem STRIDE doc — Illustrator-class editor actor + T-02-07 RE-OPENED + T-06-01 OPEN,兩條皆 accept (P0, transition-pending until Phase 7);doc deliverable ~300 行 prose"
      contains: "Illustrator-class editor attacker", "T-02-07", "T-06-01", "RE-OPENED", "transition-pending", "Phase 7", "supersedes", "threats_open: 0", "threats_accepted: 2"
    - path: ".planning/debug/scratch/illustrator-attack-2026-05-28-archived/README.md"
      provides: "繁中 note 指向 tests/_illustrator_attack.py + tests/test_illustrator_attack_regression.py;說明 .py 腳本退役,保留 PNG/PDF 證據作審計 cite"
      contains: "tests/_illustrator_attack.py", "tests/test_illustrator_attack_regression.py", "06-SECURITY.md"
  key_links:
    - from: "tests/test_illustrator_attack_regression.py"
      to: "tests/fixtures/cad-glyph/*.pdf + *.json"
      via: "_load_fixtures() globs FIXTURES_DIR + json.loads sidecar"
      pattern: "sorted\\(FIXTURES_DIR\\.glob\\(.*\\.pdf"
    - from: "tests/test_illustrator_attack_regression.py"
      to: "app.services.pipeline.process_job + app.services.ingest.ingest_upload + app.models.JobSpec"
      via: "pipeline.process_job(session_id, JobSpec(dpi=manifest['dpi'], regions=[RegionMark(page=..., px_rect=manifest['region_rect_px'])]))"
      pattern: "JobSpec\\("
    - from: "tests/_illustrator_attack.py"
      to: "app.services.pdf_engine.count_zero_area_fills_fully_inside"
      via: "function-internal import then delegate"
      pattern: "from app.services import pdf_engine"
    - from: "tests/test_illustrator_attack_regression.py xfail reason"
      to: ".planning/REQUIREMENTS.md SEC-01"
      via: "繁中 reason string 含路徑 cross-ref,Phase 7 implementer 用 `grep -rn 'xfail.*Option B' tests/` 找回此處"
      pattern: "\\.planning/REQUIREMENTS\\.md SEC-01"
    - from: ".planning/phases/06-.../06-SECURITY.md"
      to: ".planning/milestones/v1.0-phases/05-ubuntu/hotfix-06-dct-residue/06-HOTFIX-SECURITY.md"
      via: "frontmatter supersedes: list(T-02-07 disposition only)"
      pattern: "supersedes:"
---

<objective>
建立 Phase 6 紅燈基線的 attack-simulation pytest + 重評威脅模型 + 退役 scratch 腳本。

Purpose: Phase 6 → Phase 7 的 handoff signal — 3 個 fixture × 1 個 attack regression test,期望紅燈(xfail strict);Phase 7 implementer 落地 Option B 後 strict=True 強迫拔 marker。同時 STRIDE 加入 Illustrator-class editor actor、T-02-07 RE-OPENED,文件化「v1.0 Option A 在 v1.1 威脅模型下不足」的客觀依據;scratch dir 改名 + .py 退役,以 pytest 為唯一邏輯落點。

Output:
- `tests/_illustrator_attack.py`(helper module,VERBATIM 搬入 scratch lines 40-115 q...Do...Q regex + multi-stream content-stream write-back)
- `tests/test_illustrator_attack_regression.py`(主測試;parametrize × xfail strict,3 cases)
- `.planning/phases/06-regression-foundation-threat-model-re-evaluation/06-SECURITY.md`(pre-mortem STRIDE,~300 行 doc deliverable)
- `.planning/debug/scratch/illustrator-attack-2026-05-28/` → `…-archived/`(git mv)+ 4 個 PNG/PDF 保留 + .py 刪除 + 新 README.md
- 處置 repo root + samples/ 既有 raw `3013A-...pdf`:samples/ 版用 `git rm` 移除(tracked),repo root 副本物理移到 archived 目錄(.gitignore archived-anchored 已屏蔽,不會被 track)

Scope boundaries:
- 不改 production code(`app/**/*.py` 0 動)
- 不改 conftest.py(per CONTEXT discretion + PATTERNS S3,沿用既有 `isolated_data_dir` autouse;若實作中發現需要新 fixture 才能 import_upload,儘量寫在 test 檔內部 helper,不污染 conftest)
- 不寫 Phase 7 unit tests(TEST-03 是 Phase 7)
- 不改 HANDOFF.md / PROJECT.md(DOC-01 / DOC-02 是 Phase 8)
- 不改 archived `.planning/milestones/v1.0-phases/.../06-HOTFIX-SECURITY.md`(只在新 06-SECURITY.md 的 supersedes 欄 cite,不編輯原檔)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-regression-foundation-threat-model-re-evaluation/06-CONTEXT.md
@.planning/phases/06-regression-foundation-threat-model-re-evaluation/06-RESEARCH.md
@.planning/phases/06-regression-foundation-threat-model-re-evaluation/06-PATTERNS.md
@.planning/phases/06-regression-foundation-threat-model-re-evaluation/06-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- 關鍵 type / API surfaces,executor 不需自行 grep。 -->

From .planning/debug/scratch/illustrator-attack-2026-05-28/_attack_delete_image_xobject.py (VERBATIM REUSE in tests/_illustrator_attack.py — same regex, same multi-stream fallback):

```python
# lines 21-30: render_region pixel-sample white check (used in helper export #2)
# lines 40-49: find_image_xrefs_intersecting (used inside helper export #1)
# lines 70-74: XObject resource name resolution (used inside helper export #1)
# lines 84-94: q...Do...Q content-stream regex strip (VERBATIM):
pattern = re.compile(
    r"q\b[^Q]*?/" + re.escape(name.lstrip("/")) + r"\s+Do\b[^Q]*?Q\b",
    re.DOTALL,
)
new_text, n = pattern.subn("", stream_text)
# lines 104-115: multi-stream update_stream write-back (VERBATIM):
new_bytes = stream_text.encode("latin-1")
content_xrefs = page.get_contents()
if len(content_xrefs) == 1:
    doc.update_stream(content_xrefs[0], new_bytes, compress=True)
else:
    doc.update_stream(content_xrefs[0], new_bytes, compress=True)
    for xref in content_xrefs[1:]:
        doc.update_stream(xref, b"", compress=True)
```

From app/services/pipeline.py:90 (actual signature — per PATTERNS Risk Callout #1):

```python
def process_job(session_id: str, job_spec) -> dict:
    """job_spec is a validated JobSpec (dpi + regions). Returns:
       {"output_filename": "...", "page_count": int, "regions": [...], "logo_skipped": bool}
    """
```

From app/services/pipeline.py:85 (output_path):

```python
def output_path(session_id: str) -> Path:
    """Absolute path of the exported PDF in the session's outputs dir."""
    return storage.outputs_dir(session_id) / output_filename(session_id)
```

From app/services/ingest.py (entrypoint):

```python
def ingest_upload(filename: str, raw_bytes: bytes) -> SessionInfo:
    """Returns SessionInfo with session_id attribute."""
```

From app/models.py:48-99 (CRITICAL — test must build JobSpec correctly):

```python
class RegionMark(BaseModel):
    page: int               # 0-based
    px_rect: List[float]    # (x0, y0, x1, y1) in IMAGE PIXELS at the job dpi (NOT pdf points)

class JobSpec(BaseModel):
    dpi: int                           # >= MIN_DPI, <= MAX_DPI
    regions: List[RegionMark]
    logo_id: str | None = None         # None => pure removal (D-01 contract)
    auto_logo: bool = False
    rotations: Dict[int, int] = {}     # default empty = no user rotation
```

**Important:** sidecar manifest's `region_rect_px` (built by Plan 06-01 sanitize script via `pdf_points × dpi / 72.0`) is what feeds `RegionMark.px_rect`. The same manifest's `region_rect_pdf_points` is what feeds fitz operations in the attack helper (`fitz.Rect(*region_rect_pdf_points)` for `get_pixmap(clip=...)` + `count_zero_area_fills_fully_inside(page, rect)`).

From app/services/pdf_engine.py (line 699 approx — production helper):

```python
def count_zero_area_fills_fully_inside(
    page: fitz.Page,
    rect: tuple[float, float, float, float] | fitz.Rect,
    tolerance: float = _DEGENERATE_BBOX_EPS,
) -> int: ...
```

From tests/conftest.py (relevant fixtures — autouse / available):

```python
@pytest.fixture(autouse=True)
def isolated_data_dir(tmp_path, monkeypatch):   # line ~298-307
    data_root = tmp_path / "data"
    monkeypatch.setattr(config, "DATA_DIR", data_root)
    yield data_root

# logo_library fixture (verbatim from CONTEXT canonical refs) — available if needed for logo path
```

From .planning/milestones/v1.0-phases/05-ubuntu/hotfix-06-dct-residue/06-HOTFIX-SECURITY.md (analog format — 06-SECURITY.md mirrors structure, adapted to pre-mortem variant; PATTERNS § 06-SECURITY.md Pattern Assignments has the full frontmatter + STRIDE row examples):

```yaml
---
phase: 6
phase_name: regression-foundation-threat-model-re-evaluation
milestone: v1.1
audit_scope: phase_06_pre_mortem
date: 2026-05-28
asvs_level: 1
diff_base: N/A
commits_audited: []
threats_total: 2
threats_closed: 0
threats_open: 0           # by design — both threats classified accept (P0, transition-pending)
threats_accepted: 2
register_authored_at_audit_time: true
supersedes:
  - .planning/milestones/v1.0-phases/05-ubuntu/hotfix-06-dct-residue/06-HOTFIX-SECURITY.md
---
```
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 tests/_illustrator_attack.py helper module(3 exports;搬入 scratch lines 40-115 VERBATIM)</name>
  <files>tests/_illustrator_attack.py</files>
  <read_first>
    - .planning/debug/scratch/illustrator-attack-2026-05-28/_attack_delete_image_xobject.py(全檔 — 邏輯來源;特別是 lines 21-30 render_region、40-49 find_image_xrefs、70-74 resource name、84-94 regex strip、104-115 multi-stream write-back)
    - .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-CONTEXT.md(D-B2 三個函式簽名 + D-C1 scratch retirement)
    - .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-RESEARCH.md(Code Examples Common Operations 1-3 + Pitfall 3 multi-stream + Pitfall 8 fitz auto-repair)
    - .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-PATTERNS.md § `tests/_illustrator_attack.py` Pattern Assignments + Shared Pattern S1(fitz license note)
    - tests/conftest.py(line 1-12 — fitz import license note 措辭)
    - app/services/pdf_engine.py(line 699 approx,`count_zero_area_fills_fully_inside` 簽名)
    - .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-01-SUMMARY.md(Plan 06-01 產出,確認 sidecar manifest schema 與 fixture coverage)
  </read_first>
  <behavior>
    - delete_image_xobjects_intersecting(doc: fitz.Document, page_index: int, rect: tuple[float,float,float,float]) -> int:
      * Input: 已開啟的 fitz.Document、page index、PDF-point rect。
      * Action: 找該 page 所有 image XObject 中 bbox 與 rect 有交集者,從 page Resources/XObject map 解析 resource names,用 scratch lines 84-94 verbatim 的 regex `r"q\b[^Q]*?/" + re.escape(name.lstrip("/")) + r"\s+Do\b[^Q]*?Q\b"` 在 latin-1 decoded content stream 上 `subn("")` 刪除;再用 bare pattern `r"/" + re.escape(name.lstrip("/")) + r"\s+Do\b"` 處理 stray invocations。Multi-stream write-back 用 scratch lines 104-115 verbatim(`update_stream([0], new_bytes)` + 其餘 `update_stream(xref, b"")`)。
      * Return: int — 刪除的 xref 數。**呼叫者尚未 doc.save();本函式只 mutate in-memory doc**。
      * Behavior tests(non-trivial 因為需要實 PDF — 留給 test_illustrator_attack_regression.py 的 step 3 隱性驗證:`assert n_deleted >= 1`)。
    - render_region_white_pct(pdf_path: Path | str, page_index: int, rect: tuple) -> float:
      * Input: PDF 檔案路徑、page index、PDF-point rect。
      * Action: `fitz.open(pdf_path)` → `page.get_pixmap(matrix=fitz.Matrix(4,4), clip=fitz.Rect(*rect), alpha=False)` → `np.frombuffer(pm.samples, dtype=np.uint8).reshape(pm.height, pm.width, pm.n)` → `100 * np.all(arr >= 250, axis=2).sum() / arr[..., 0].size`,round 到小數 2 位。
      * Return: float — 白佔比 percentage,0.0-100.0。
      * doc 在 try/finally 內 close。
    - count_zero_area_fills_in_region(pdf_path: Path | str, page_index: int, rect: tuple) -> int:
      * Input 同上。
      * Action: `fitz.open(pdf_path)` → function-internal `from app.services import pdf_engine`(避免 module-load-time side effects + clarify the call site) → `return pdf_engine.count_zero_area_fills_fully_inside(doc[page_index], rect)`。
      * Return: int。
      * doc 在 try/finally 內 close。
  </behavior>
  <action>

**Precondition — pytest 安裝檢查(per checker Warning #5):**

```
python -c "import pytest; print(pytest.__version__)" || pip install "pytest>=8.0"
```

(後續 task 1-4 所有 pytest 指令一律用 `python -m pytest` 形式以求 env 一致性。)

建立新檔 `tests/_illustrator_attack.py`(per D-B2)。

**檔案結構(目標 ~80-120 行):**

1. **Module docstring**(繁中,per memory feedback_language + PATTERNS S4):「Illustrator 拔 image XObject 攻擊邏輯,從 `.planning/debug/scratch/illustrator-attack-2026-05-28/_attack_delete_image_xobject.py` 搬入,改寫為可重用 helper。本模組 import fitz — AGPL guard scope = `app/**/*.py` only,`tests/` 在 scope 外(同 `tests/conftest.py:12` 既有 exception)。參 `.planning/phases/06-regression-foundation-threat-model-re-evaluation/06-PATTERNS.md` Shared Pattern S1。」
2. `from __future__ import annotations` + `import re, fitz, numpy as np` + `from pathlib import Path` + `from typing import Tuple` 或 `tuple[float, ...]` PEP 604(repo convention)。**`import fitz` 必須緊跟一行 comment** `# license: test harness exception (mirror tests/conftest.py:12)`。
3. **私有 helper `_find_image_xrefs_intersecting(page, rect)`** — copy scratch lines 40-49 VERBATIM(保留 `for img in page.get_images(full=True): xref = img[0]; for bbox in page.get_image_rects(xref): if fitz.Rect(bbox).intersects(rect):`)。簽名:`def _find_image_xrefs_intersecting(page: fitz.Page, rect: fitz.Rect) -> list[int]`。
4. **私有 helper `_resolve_resource_names(page, xrefs)`** — copy scratch lines 70-74 VERBATIM(`for img_info in page.get_images(full=True): xref, _, _, _, _, _, _, name = img_info[:8]; ...`)。
5. **Export 1: `delete_image_xobjects_intersecting(doc, page_index, rect) -> int`** —
   - call `_find_image_xrefs_intersecting(doc[page_index], fitz.Rect(*rect))` 拿 xrefs
   - call `_resolve_resource_names` 拿 names
   - `stream_bytes = page.read_contents(); stream_text = stream_bytes.decode("latin-1")`
   - 對每個 name 跑 scratch lines 84-94 regex VERBATIM(`q\b[^Q]*?/<name>\s+Do\b[^Q]*?Q\b` + bare fallback)
   - 用 scratch lines 104-115 multi-stream write-back VERBATIM
   - return `len(xrefs)`
6. **Export 2: `render_region_white_pct(pdf_path, page_index, rect) -> float`** — scratch lines 21-30 直譯,doc close 用 try/finally。
7. **Export 3: `count_zero_area_fills_in_region(pdf_path, page_index, rect) -> int`** — function-internal `from app.services import pdf_engine`(避免 module load-time fitz pollution 信號 — pdf_engine 已在 app/ 內,test 端 lazy import 是 既有 convention per PATTERNS)。Open doc → delegate → close。
8. **NO main()、NO `if __name__ == "__main__"`、NO 模組層 print 或 side effect**(per PATTERNS § Differences from analog — helper module 必須 silent for pytest collection cleanliness)。
9. **無新 dependency**,沿用 既有 PyMuPDF + numpy。

**特別注意 — PATTERNS Risk Callout #4(multi-stream pattern verbatim):**Plan 06-01 sanitize script 與本 helper **都**用同一 multi-stream write-back pattern(scratch lines 104-115);不可「整理」成 `for xref in content_xrefs: ...` 一行式,因為「write modified 到 [0],write `b""` 到 [1:]」的不對稱是 Pitfall 3 的關鍵(scratch script 已對 `3013A-...pdf` 證明這條 path 可行)。**保留條件分支 + 不對稱 pattern,verbatim**。

  </action>
  <verify>
    <automated>python -c "import pytest" || pip install "pytest>=8.0"</automated>
    <automated>python -c "import sys; sys.path.insert(0,'.'); from tests import _illustrator_attack as m; assert callable(m.delete_image_xobjects_intersecting), 'export 1 missing'; assert callable(m.render_region_white_pct), 'export 2 missing'; assert callable(m.count_zero_area_fills_in_region), 'export 3 missing'; print('all 3 exports present')"</automated>
    <automated>python -c "import ast; t=ast.parse(open('tests/_illustrator_attack.py',encoding='utf-8').read()); fn_names={n.name for n in t.body if isinstance(n,ast.FunctionDef)}; assert {'delete_image_xobjects_intersecting','render_region_white_pct','count_zero_area_fills_in_region'}.issubset(fn_names), f'missing: {fn_names}'; print('AST ok')"</automated>
    <automated>grep -v "^#" tests/_illustrator_attack.py | grep -c "import fitz"</automated>
    <automated>grep -rn "import fitz" app/</automated>
  </verify>
  <acceptance_criteria>
    - `from tests._illustrator_attack import delete_image_xobjects_intersecting, render_region_white_pct, count_zero_area_fills_in_region` 不 raise(3 exports 都存在且 callable)
    - `grep -v "^#" tests/_illustrator_attack.py | grep -c "import fitz"` == 1
    - `grep -rn "import fitz" app/` 仍 == `app/services/pdf_engine.py` 一行(AGPL seam 不破)
    - `python -m pytest --co tests/test_redact.py::test_fitz_import_confined_to_engine_seam` 顯示 1 collected;`python -m pytest tests/test_redact.py::test_fitz_import_confined_to_engine_seam` exits 0(AGPL guard test 仍綠)
    - `grep -c "q\\\\b\\[\\^Q\\]\\*\\?" tests/_illustrator_attack.py` ≥ 1(scratch line 89 verbatim regex 在檔內)
    - `grep -c "update_stream" tests/_illustrator_attack.py` ≥ 2(multi-stream pattern 兩個 branch 各一次)
    - `python -m pytest --collect-only tests/_illustrator_attack.py 2>&1 | grep -c "error\|Error"` == 0(module collectable without errors)
    - `git diff --stat HEAD app/` 顯示 0 changed files
  </acceptance_criteria>
  <done>
Helper module 就位、3 exports 可 import、scratch attack mechanics(regex + multi-stream)verbatim 保留、AGPL seam 不破、production code 0 動。Task 2 可直接 `from tests._illustrator_attack import ...` 寫 regression test。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 建立 tests/test_illustrator_attack_regression.py(parametrize × xfail strict,3 cases 紅燈)</name>
  <files>tests/test_illustrator_attack_regression.py</files>
  <read_first>
    - .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-CONTEXT.md(D-B1..D-B6 + Claude's Discretion xfail reason 字串文案)
    - .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-RESEARCH.md § Pattern 3 + Pitfall 4 (`--runxfail`)+ Pitfall 5 (decorator order)+ Pitfall 6 (sorted glob)+ Pitfall 8 (fitz auto-repair → 雙閘)
    - .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-PATTERNS.md § `tests/test_illustrator_attack_regression.py` Pattern Assignments + Shared Pattern S2(pipeline entrypoint caveat — JobSpec 不是 dict)+ S3(isolated_data_dir autouse)+ S4(繁中 strings)+ S5(SECURITY frontmatter)
    - tests/test_redact.py(lines 691-794 — end-to-end test analog;lines 1190-1207 — fitz seam guard 不破)
    - tests/conftest.py(line 1-12 fitz license + line 298-307 isolated_data_dir;logo_library fixture)
    - tests/test_process_api.py(line 270-316 — `test_process_without_logo_is_pure_removal` D-01 contract pattern,確認 `logo_id=None` 路徑)
    - app/models.py(class JobSpec + class RegionMark — 必須正確構造,Risk Callout #1)
    - app/services/pipeline.py(line 90 process_job 簽名 + line 85-87 output_path)
    - app/services/ingest.py(line 331-338 ingest_upload 簽名)
    - tests/_illustrator_attack.py(Task 1 產出,3 exports)
    - tests/fixtures/cad-glyph/*.json(Plan 06-01 產出,sidecar manifest schema)
    - **Sidecar schema decision note(per checker Warning #8):** sidecar manifest 採 split-coordinate schema(`region_rect_pdf_points` + `region_rect_px`)而非 CONTEXT D-B4 範例中的單一 `region_rect`(per Claude's Discretion on manifest schema in Plan 06-01 Task 1)。本 Phase 6 canonical schema;Phase 7 unit tests 對齊本 schema。consumer code(本 task)讀 `manifest["region_rect_pdf_points"]` 給 fitz / `manifest["region_rect_px"]` 給 RegionMark.px_rect。
  </read_first>
  <behavior>
    - test_illustrator_attack_residual_supplier_revealed parametrized over 3 fixtures (text-glyph-01 / figure-glyph-01 / mixed-glyph-01):
      * Test ID = fixture stem(per `pytest.param(..., id=pdf.stem)`),pytest -v 顯示 3 個 XFAIL 各帶 ID
      * Step 1: read fixture PDF bytes → ingest.ingest_upload(filename, bytes) → SessionInfo with session_id
      * Step 2: build JobSpec(dpi=manifest['dpi'], regions=[RegionMark(page=manifest['page_index'], px_rect=manifest['region_rect_px'])], logo_id=None) → pipeline.process_job(session_id, job_spec)
      * Step 3: open output_path(session_id) → delete_image_xobjects_intersecting(doc, page_index, region_rect_pdf_points) → assert n_deleted >= 1(attack precondition) → save attacked PDF beside output
      * Step 4 雙閘 assert(per D-B5):
        - (a) render_region_white_pct(attacked_pdf, page_index, region_rect_pdf_points) >= 98.0
        - (b) count_zero_area_fills_in_region(attacked_pdf, page_index, region_rect_pdf_points) == 0
      * Phase 6 期望:(a) 或 (b) 至少一個 fail → test FAILS → xfail strict marker 攔截 → 顯示 XFAIL
      * Phase 7 落地 Option B 後:(a) AND (b) 都 pass → test PASSES under xfail strict → XPASS(strict) → exit non-zero → implementer 必須拔 marker(handoff signal)
  </behavior>
  <action>

**Precondition — pytest 安裝檢查(per checker Warning #5):**

```
python -c "import pytest; print(pytest.__version__)" || pip install "pytest>=8.0"
```

建立新檔 `tests/test_illustrator_attack_regression.py`(per D-B1)。

**檔案結構(目標 ~120-160 行):**

1. **Module docstring(繁中,per S4)**:「Phase 6 Illustrator-class editor attacker 紅燈 regression test。每個 sanitized fixture(`tests/fixtures/cad-glyph/{text|figure|mixed}-glyph-01.pdf`)跑「ingest → process(logo_id=None pure removal)→ attack(拔 image XObject)→ assert region 仍 ≥98% 白 + zero-area type='f' count == 0」。**Phase 6 期望紅燈**(Option B 尚未實作);Phase 7 落地 Option B 後 xfail strict 會把這 3 個 test 變 XPASS error,強迫 implementer 拔 marker。**INVARIANT: 任何 pytest invocation 不可加 `--runxfail`(Pitfall 4)**;`pytest.ini` 不加 `strict_xfail` global default(Pitfall 5)。」
2. `from __future__ import annotations` + `import json, pathlib, pytest` + `import fitz`(test 檔內,scope 外,per PATTERNS S1)+ `from app.services import ingest, pipeline, storage` + `from app.models import JobSpec, RegionMark` + `from tests._illustrator_attack import delete_image_xobjects_intersecting, render_region_white_pct, count_zero_area_fills_in_region`。
3. **`FIXTURES_DIR = pathlib.Path(__file__).parent / "fixtures" / "cad-glyph"`**(module-level constant)。
4. **`_load_fixtures()`** — 自 PATTERNS § Fixture-discovery pattern verbatim:
   - `for pdf in sorted(FIXTURES_DIR.glob("*.pdf"))`(**sorted 必要 — Pitfall 6**)
   - 缺對應 `.json` → `pytest.fail` 帶清楚訊息
   - `data = json.loads(manifest.read_text(encoding="utf-8"))`
   - `pairs.append(pytest.param(pdf, data, id=pdf.stem))`
   - return pairs
5. **主 test function `test_illustrator_attack_residual_supplier_revealed(fixture_pdf, manifest, isolated_data_dir, logo_library)`** — `isolated_data_dir` 來自 conftest autouse(per S3 — 列在 signature 是顯式記錄而非必要,因為 autouse 已注入);`logo_library` 若需要(查 conftest 內定義,通常 logo_id=None 不需要,可選用省略)。
6. **Decorator stack 順序(per PATTERNS Risk Callout #3 + Pitfall 5 — `@pytest.mark.parametrize` ABOVE `@pytest.mark.xfail`):**
   ```
   @pytest.mark.parametrize("fixture_pdf,manifest", _load_fixtures())
   @pytest.mark.xfail(
       strict=True,
       reason=(
           "Option B 尚未實作(Phase 7 SEC-01 待落地)— "
           "Illustrator-class editor 拔 image XObject 後 page content stream 內的零面積 "
           "type='f' 路徑仍會 render 出供應商商標。Phase 7 落地後請拔掉本 marker。"
           "參 .planning/REQUIREMENTS.md SEC-01。"
       ),
   )
   ```
   **此順序為 Python decorator application(由下而上):xfail 先套用到 function,parametrize 再對 (function with xfail) 展開 3 cases,結果是 3 個帶獨立 xfail 的 case**。反過來會讓 parametrize 拿到 xfail-wrapped object 行為不可預期 — **嚴守 PATTERNS Risk Callout #3 順序**。
7. **Function body**(步驟 1-4 per behavior 區塊):
   - **Step 1**:`session = ingest.ingest_upload(fixture_pdf.name, fixture_pdf.read_bytes())`;`session_id = session.session_id`
   - **Step 2**:構造 JobSpec(per Risk Callout #1):
     ```
     job_spec = JobSpec(
         dpi=manifest["dpi"],
         regions=[RegionMark(page=manifest["page_index"], px_rect=manifest["region_rect_px"])],
         logo_id=None,   # pure removal — D-01 contract per test_process_api.py:270-316
     )
     result = pipeline.process_job(session_id, job_spec)
     output_pdf = pipeline.output_path(session_id)
     assert output_pdf.exists(), "process_job 未產出 output PDF"
     ```
   - **Step 3 — 攻擊步驟:**
     ```
     region_pdf_pts = manifest["region_rect_pdf_points"]   # tuple of 4 floats (PDF points) — split-coordinate schema per Plan 06-01
     page_index = manifest["page_index"]
     doc = fitz.open(output_pdf)
     try:
         n_deleted = delete_image_xobjects_intersecting(doc, page_index, region_pdf_pts)
         attacked_pdf = output_pdf.with_name(output_pdf.stem + "_attacked.pdf")
         doc.save(attacked_pdf, garbage=4, deflate=True)
     finally:
         doc.close()
     assert n_deleted >= 1, "attack precondition 不成立:框選區內無 image XObject overlay 可拔"
     ```
   - **Step 4 — 雙閘 assert(per D-B5):**
     ```
     white_pct = render_region_white_pct(attacked_pdf, page_index, region_pdf_pts)
     assert white_pct >= 98.0, (
         f"視覺乾淨閘失敗 — 框選區白佔比 {white_pct:.2f}% < 98% 門檻;"
         f"Illustrator 拔 image XObject 後供應商商標可能重現"
     )
     zero_area_count = count_zero_area_fills_in_region(attacked_pdf, page_index, region_pdf_pts)
     assert zero_area_count == 0, (
         f"content stream 乾淨閘失敗 — 框選區內仍有 {zero_area_count} 個 zero-area type='f' 路徑;"
         f"Option B content-stream surgery 未刪除"
     )
     ```
8. **NO main(), NO if __name__ block**。

**Important — PATTERNS Risk Callout #1(JobSpec shape):**RESEARCH § Pattern 3 範例用 `pipeline.process_job(session_id, regions=[{...}], logo_id=None)` 是**錯的**(D-B3 pseudocode 也走樣)— 實際簽名是 `(session_id: str, job_spec: JobSpec) -> dict`,executor 必須建 JobSpec instance。**`px_rect` 是 IMAGE PIXELS at the job dpi 不是 PDF points** — 直接吃 manifest['region_rect_px'](Plan 06-01 sanitize script 已 pre-computed `pdf_points × dpi / 72.0`);**`region_rect_pdf_points` 才是給 fitz 用的 clip + count_zero_area_fills 用的座標**。

**`logo_library` fixture 評估:**若 conftest.py 提供且 `process_job` 在 logo_id=None 下不訪問 manifest,可省略;若 process_job 必須能 resolve logo_id allowlist 才能 import logo module,則加入 fixture signature(test_process_api.py:270-316 D-01 contract pattern 是參考)。Executor 在實作時依實際 import 鏈決定。

  </action>
  <verify>
    <automated>python -c "import pytest" || pip install "pytest>=8.0"</automated>
    <automated>python -m pytest --co -q tests/test_illustrator_attack_regression.py 2>&1 | tail -5</automated>
    <automated>python -m pytest -k illustrator_attack -v 2>&1 | tail -20</automated>
    <automated>python -c "import re; t=open('tests/test_illustrator_attack_regression.py',encoding='utf-8').read(); idx_p=t.index('@pytest.mark.parametrize'); idx_x=t.index('@pytest.mark.xfail'); assert idx_p < idx_x, f'parametrize MUST be above xfail (Risk Callout #3): para={idx_p}, xfail={idx_x}'; print('decorator order ok')"</automated>
    <automated>grep -c "strict=True" tests/test_illustrator_attack_regression.py</automated>
    <automated>grep -c "SEC-01" tests/test_illustrator_attack_regression.py</automated>
    <automated>grep -c "JobSpec\|RegionMark" tests/test_illustrator_attack_regression.py</automated>
    <automated>python -m pytest tests/test_redact.py::test_fitz_import_confined_to_engine_seam -v 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest --co -q tests/test_illustrator_attack_regression.py` 收集到恰好 3 個 cases(對應 3 fixtures)
    - `python -m pytest -k illustrator_attack -v` 顯示恰好 3 個 `XFAIL`(每個 case 一個,test id = fixture stem)
    - `python -m pytest -k illustrator_attack -v` exit code == 0(XFAIL 視為通過)
    - `grep -c "@pytest.mark.parametrize" tests/test_illustrator_attack_regression.py` ≥ 1 且 line number < `@pytest.mark.xfail` line number(decorator order — Risk Callout #3)
    - `grep -c "strict=True" tests/test_illustrator_attack_regression.py` ≥ 1
    - `grep -c "SEC-01" tests/test_illustrator_attack_regression.py` ≥ 1(xfail reason cross-ref Phase 7)
    - `grep -c "JobSpec" tests/test_illustrator_attack_regression.py` ≥ 1(正確構造 — Risk Callout #1)
    - `python -m pytest tests/test_redact.py::test_fitz_import_confined_to_engine_seam -v` exits 0(AGPL guard test 仍綠)
    - `python -m pytest 2>&1 | tail -3` 結尾顯示包含 `301 passed`、`3 skipped`、`3 xfailed`(完整 baseline + 新 xfailed 3)
    - `grep -rn "import fitz" app/` 仍 == `app/services/pdf_engine.py` 一行
    - `git diff --stat HEAD app/` 顯示 0 changed files
  </acceptance_criteria>
  <done>
Regression test 就位、3 個 case xfail、Phase 7 implementer 拔 marker 即可變綠、baseline 301 + 3 不退步、AGPL seam 不破。Task 3 可開始寫 06-SECURITY.md(以 test 檔的存在 + xfail evidence 作 T-06-01 的 evidence cite)。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 建立 06-SECURITY.md(pre-mortem STRIDE — Illustrator-class editor actor + T-02-07 RE-OPENED + T-06-01 OPEN,皆 accept transition-pending)</name>
  <files>.planning/phases/06-regression-foundation-threat-model-re-evaluation/06-SECURITY.md</files>
  <read_first>
    - .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-CONTEXT.md(D-D1..D-D4 鎖定)
    - .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-RESEARCH.md § 5 (per-phase SECURITY.md schema)+ § STRIDE Pre-mortem 變體段(若 partial-read 中未見,grep `pre-mortem` 找)+ Assumptions A3(threats_open: 0 + threats_accepted: 2 framing)
    - .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-PATTERNS.md § `06-SECURITY.md` Pattern Assignments(frontmatter 範例 + STRIDE 表 row 範例 + Accepted Risks Log 範例 + Pre-mortem vs audit-time 差異表)+ Shared Pattern S5
    - .planning/milestones/v1.0-phases/05-ubuntu/hotfix-06-dct-residue/06-HOTFIX-SECURITY.md(全檔分析格式;**只讀不改**,06-SECURITY.md frontmatter `supersedes` 欄將 cite 此檔)
    - .planning/milestones/v1.0-phases/05-ubuntu/05-SECURITY.md(Phase 5 STRIDE actor 清單原始版本,Illustrator-class editor 即將以 D-D2 新增)
    - .planning/REQUIREMENTS.md(SEC-01 Phase 7 close 條件文字 — 06-SECURITY.md「closing condition」欄需 cross-ref)
    - tests/test_illustrator_attack_regression.py(Task 2 產出 — 06-SECURITY.md T-06-01 + T-02-07 的 evidence cite 指向此 test 檔的 xfail 紅燈)
    - .planning/debug/scratch/illustrator-attack-2026-05-28-archived/(Task 4 即將建立的 archived 目錄;`_attack_proof_supplier_revealed.png` 是 T-06-01 evidence cite — 注意 **Task 4 重命名是在本 Task 之後執行**,因此本 task 寫 06-SECURITY.md 時即使用 `…-archived/` path,執行順序由 plan 內 task 線性執行保證)
  </read_first>
  <action>
建立新檔 `.planning/phases/06-regression-foundation-threat-model-re-evaluation/06-SECURITY.md`(per D-D1)。

**檔案結構(目標 ~250-350 行,doc deliverable — prose 體量 ≠ implementation complexity,per checker Warning #4 scope_note 註記。沿用 PATTERNS § 06-SECURITY.md Pattern Assignments 的格式)。繁中 prose + 英文 identifiers / file paths / quoted code(per S4)。**

1. **Frontmatter**(VERBATIM 從 PATTERNS § 06-SECURITY.md Frontmatter pattern 取得,鎖死以下值 — `gsd-secure-phase` agent 讀此 schema):
   ```yaml
   ---
   phase: 6
   phase_name: regression-foundation-threat-model-re-evaluation
   milestone: v1.1
   audit_scope: phase_06_pre_mortem
   date: 2026-05-28
   asvs_level: 1
   diff_base: N/A
   commits_audited: []
   threats_total: 2
   threats_closed: 0
   threats_open: 0
   threats_accepted: 2
   register_authored_at_audit_time: true
   supersedes:
     - .planning/milestones/v1.0-phases/05-ubuntu/hotfix-06-dct-residue/06-HOTFIX-SECURITY.md
   ---
   ```
   **CRITICAL — Risk Callout #2:**`threats_open: 0` + `threats_accepted: 2`(非 `threats_open: 2`)以滿足 `gsd-secure-phase` non-block contract。理由(內文 prose 內解釋):兩條 threat 都是 `accept (P0, transition-pending until Phase 7)`,即「已決議接受 + 已預定下階段 close」,不算阻塞性 open。

2. **# 06-SECURITY.md — Phase 6 pre-mortem STRIDE(v1.1 milestone)**(H1 標題)

3. **## Phase 6 Pre-mortem Context**(繁中段落):
   - 說明 Phase 6 是 hardening / pre-mortem variant 而非 audit-time(無 production code commits to audit;diff_base: N/A)
   - 引用 2026-05-28 forensic attack script 作為威脅模型重評觸發點:cross-ref `.planning/debug/scratch/illustrator-attack-2026-05-28-archived/_attack_proof_supplier_revealed.png` + `tests/test_illustrator_attack_regression.py`(xfail 紅燈本身即威脅證據)
   - 明確指出兩條 threat 在 Phase 7 落地 Option B 後將在 `07-SECURITY.md` 重新 CLOSED(可預期的 transition,故 frontmatter 設為 accepted 而非 open)

4. **## STRIDE Actors**(append-only update 自 archived `05-SECURITY.md` 的清單):
   - 列出 既有 actors(從 v1.0 carry over;executor 在實作時 read archived 05-SECURITY.md 取得確切清單;若 archived 檔已詳列,06-SECURITY.md 用「沿用 archived `05-SECURITY.md` + 新增 1 條」措辭避免重複)
   - **新增 Actor: Illustrator-class editor attacker**(per D-D2)
     - Capabilities(繁中 + 英文 quoted):「擁有 Adobe Illustrator / Acrobat Pro / 等同 PDF editor 工具,能讀寫 PDF object stream / content stream」「能識別並刪除 page-level image XObjects(LogoSwap Hotfix-06 Option A 的 raster overlay)」「不需修改其他 content,單純拔 overlay 即觸發 zero-area `type='f'` source path 在 Acrobat / Illustrator render 出供應商商標」
     - Threat model upgrade rationale:cross-ref 2026-05-28 forensic evidence

5. **## STRIDE Threat Register**(per PATTERNS § STRIDE table pattern,VERBATIM 採其 row 格式):
   ```
   | Threat ID | Category | Disposition | Status | Evidence (file:line) |
   |-----------|----------|-------------|--------|----------------------|
   | T-02-07 | I — TRUE REMOVAL vs cover | mitigate(已 archive)+ RE-OPENED 2026-05-28 pending Option B(Phase 7)— accept (P0, transition-pending) | RE-OPENED | `.planning/debug/scratch/illustrator-attack-2026-05-28-archived/_attack_proof_supplier_revealed.png` + `tests/test_illustrator_attack_regression.py` xfail;closing condition: Phase 7 `07-SECURITY.md` 重新 CLOSED via Option B(content-stream surgery 刪除 zero-area type='f' source) |
   | T-06-01 | S + I — Illustrator-class editor pulls image XObject overlay → supplier brand re-rendered from zero-area type='f' source | mitigate (pending Option B Phase 7)— accept (P0, transition-pending) | OPEN | 同上 evidence;closing condition 同上 |
   ```

6. **## T-02-07 RE-OPENED — narrative**(per D-D3):
   - 原狀態(從 archived `06-HOTFIX-SECURITY.md` quote 引用):`CLOSED with documented residual`
   - 新狀態:`RE-OPENED 2026-05-28 (v1.1 Phase 6) — pending Option B`
   - 明文不撤銷 v1.0 LIVE mitigation:「此 RE-OPENED 不撤銷 v1.0 LIVE 的 mitigation 仍對 CLI-only 攻擊者 / 內網一般使用者威脅模型有效;但對 v1.1 升級後的 Illustrator-class editor 威脅模型不足。Phase 7 Option B 落地後在 `07-SECURITY.md` 重新 CLOSED via Option B。」

7. **## T-06-01 — narrative**(per D-D2):
   - Attacker capability + step-by-step:1) 用 Illustrator open LogoSwap output 2) 識別並 delete image XObject overlay(LogoSwap Option A raster cover)3) re-save → render → 供應商商標重現
   - Evidence cite:`_attack_proof_supplier_revealed.png` + `tests/test_illustrator_attack_regression.py` 三個 fixture 跑 xfail
   - Closing condition cross-ref:`.planning/REQUIREMENTS.md SEC-01`(Phase 7 SEC-01 落地後 close)

8. **## Accepted Risks Log**(per PATTERNS § Accepted Risks Log pattern + S5):
   - **T-02-07-r2** — v1.0 documented residual + v1.1 RE-OPENED transition
     - Disposition: accept (P0, transition-pending until Phase 7 closes)
     - Risk description(繁中)
     - Why accepted now: 「Phase 6 是 red-light-baseline by design — production-code mitigation 落於 Phase 7 Option B (SEC-01);regression test xfail markers 是該 handoff 的 binding contract」
     - Upgrade trigger:Phase 7 落地 Option B → `07-SECURITY.md` CLOSED via Option B
     - Documented at: `tests/test_illustrator_attack_regression.py` xfail reason, this file, `.planning/REQUIREMENTS.md SEC-01`
   - **T-06-01-r1** — Illustrator-class editor attack surface
     - 結構同上(disposition / risk / why / trigger / documented at)
     - Documented at: 三個位置同上

9. **## Pre-mortem vs Audit-time Variant**(PATTERNS § 06-SECURITY.md Pre-mortem vs audit-time variant table verbatim):
   - 表格欄位:`欄位` | `Audit-time 變體(eg. 06-HOTFIX-SECURITY.md)` | `Pre-mortem 變體(本檔)`
   - 列至少 6 行:`audit_scope`、`commits_audited`、`diff_base`、`threats_total/closed/open`、`live_uat_verified_at`、整體語義
   - 結語段:「本檔 `threats_open: 0` 因兩條 threat 皆分類為 `accept (P0, transition-pending)`,而非 `mitigate`;這滿足 `gsd-secure-phase` agent 對 `threats_open == 0` 的 non-block 要求(per RESEARCH § Assumptions A3)」

10. **## Open Threats**: 「`Open Threats: 0`(T-02-07 與 T-06-01 兩條皆 `accept (P0, transition-pending)` — 詳見 Accepted Risks Log)」一行明示。

11. **## Cross-references / Supersession Chain**:
    - Phase 7 expected: `.planning/phases/07-.../07-SECURITY.md` 將把 T-02-07 + T-06-01 CLOSED via Option B
    - Phase 8 expected: `.planning/phases/08-.../08-SECURITY.md` 將有 LIVE-UAT verifying note
    - This file supersedes(disposition only): archived `06-HOTFIX-SECURITY.md` T-02-07 row
    - Cross-ref REQUIREMENTS.md SEC-01 / SEC-02 / SEC-03(Phase 7 reqs)+ TEST-02 / THREAT-01(Phase 6 reqs)

**完全不編輯 archived `06-HOTFIX-SECURITY.md` 原檔**(只在 frontmatter `supersedes` 列出)。**完全不動 `app/**/*.py`**。
  </action>
  <verify>
    <automated>ls .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-SECURITY.md</automated>
    <automated>head -20 .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-SECURITY.md | grep -E "^(phase|threats_total|threats_open|threats_accepted|audit_scope|register_authored_at_audit_time|supersedes):"</automated>
    <automated>grep -c "Illustrator-class editor attacker" .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-SECURITY.md</automated>
    <automated>grep -c "T-02-07" .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-SECURITY.md</automated>
    <automated>grep -c "T-06-01" .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-SECURITY.md</automated>
    <automated>grep -c "RE-OPENED" .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-SECURITY.md</automated>
    <automated>grep -c "transition-pending" .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-SECURITY.md</automated>
    <automated>grep -c "SEC-01" .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-SECURITY.md</automated>
    <automated>grep -c "supersedes:" .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-SECURITY.md</automated>
  </verify>
  <acceptance_criteria>
    - 檔案存在於 `.planning/phases/06-regression-foundation-threat-model-re-evaluation/06-SECURITY.md`
    - Frontmatter 含 6 個 critical fields(`audit_scope: phase_06_pre_mortem`, `threats_total: 2`, `threats_closed: 0`, `threats_open: 0`, `threats_accepted: 2`, `register_authored_at_audit_time: true`)+ `supersedes:` list 含 `06-HOTFIX-SECURITY.md` 路徑
    - 內文包含 `Illustrator-class editor attacker`(≥1 處,actor 新增)
    - 內文包含 `T-02-07` ≥3 處(STRIDE table + RE-OPENED narrative + Accepted Risks Log + Cross-references)
    - 內文包含 `T-06-01` ≥3 處
    - 內文包含 `RE-OPENED` ≥2 處
    - 內文包含 `transition-pending` ≥2 處(STRIDE table + Accepted Risks Log)
    - 內文包含 `SEC-01` ≥1 處(closing condition cross-ref)
    - Pre-mortem vs Audit-time variant 表格存在(可用 grep 找 `Pre-mortem vs` 或 `Audit-time` 任一)
    - `git diff --stat HEAD app/` 顯示 0 changed files
    - Archived `.planning/milestones/v1.0-phases/05-ubuntu/hotfix-06-dct-residue/06-HOTFIX-SECURITY.md` 內容未被編輯(`git diff` 對 archived 路徑無變動)
  </acceptance_criteria>
  <done>
06-SECURITY.md 就位、STRIDE 表 + actor 更新 + T-02-07/T-06-01 雙 threat + Accepted Risks Log + Pre-mortem variant 完整。`gsd-secure-phase` agent 在 Phase 6 close 時可依此 frontmatter 通過 non-block(threats_open: 0 / threats_accepted: 2)。Phase 7 implementer + `gsd-secure-phase` 可在 07-SECURITY.md cross-ref 並 supersede。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Scratch 退役 — git mv 目錄重命名為 …-archived/、刪除 .py、新增 archived README、git rm samples/ + 物理移動 root 副本</name>
  <files>
    .planning/debug/scratch/illustrator-attack-2026-05-28-archived/README.md,
    .planning/debug/scratch/illustrator-attack-2026-05-28-archived/_attack_proof_supplier_revealed.png,
    .planning/debug/scratch/illustrator-attack-2026-05-28-archived/_attack_target_pre.png,
    .planning/debug/scratch/illustrator-attack-2026-05-28-archived/_attack_orig_for_comparison.png,
    .planning/debug/scratch/illustrator-attack-2026-05-28-archived/_attack_image_xobject_deleted.pdf,
    samples/3013A-13A-C6-XX-3D02-A01-00040.pdf
  </files>
  <read_first>
    - .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-CONTEXT.md(D-C1 退役 spec + Claude's Discretion samples/3013A-...pdf 處置)
    - .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-PATTERNS.md § Rename + cleanup(目標 archived 目錄狀態 + README 內容範例)
    - .planning/debug/scratch/illustrator-attack-2026-05-28/(目前狀態:**`git ls-files` 已驗證全部 6 個 entries tracked**,故 git mv 是乾淨單命令;不需 fallback)
    - .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-01-SUMMARY.md(Plan 06-01 對 root + samples + archived 三處 raw PDF 加了 .gitignore 防護,但實際 git rm + 物理移動由本 task 完成)
    - tests/_illustrator_attack.py(Task 1 產出 — archived README.md 將 cross-ref 此檔)
    - tests/test_illustrator_attack_regression.py(Task 2 產出 — archived README.md 將 cross-ref 此檔)
    - .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-SECURITY.md(Task 3 產出 — archived README.md 將 cross-ref 此檔)
    - .gitignore(Plan 06-01 已加入 root-anchored + samples-anchored + archived-anchored guards,本 task 不再改)
  </read_first>
  <action>

**Precondition — pytest 安裝檢查(per checker Warning #5):**

```
python -c "import pytest" || pip install "pytest>=8.0"
```

**Step A — 重命名 scratch 目錄(per D-C1 + PATTERNS § Rename + cleanup;per checker Blocker #3 — 全部 6 entries 已驗證 tracked,不需 fallback,單命令乾淨執行):**

```
git mv .planning/debug/scratch/illustrator-attack-2026-05-28 .planning/debug/scratch/illustrator-attack-2026-05-28-archived
```

(原 plan 中「若 git 對 untracked 部分回報問題, fall back to mv + git add ...-archived/」的 fallback wording **已移除** — `git ls-files .planning/debug/scratch/illustrator-attack-2026-05-28/` 在 plan 撰寫時已驗證全部 6 entries 都 tracked,git mv 為原子操作,無 split-mv state 風險。若意外 fail,executor 應視為環境問題並停手回報,而非走 fallback。)

執行後驗:
- `.planning/debug/scratch/illustrator-attack-2026-05-28/` 不存在
- `…-archived/` 存在且含原 6 個 entries(4 PNG/PDF + 2 .py)
- **History preserved**(per checker Blocker #3):`git log --follow .planning/debug/scratch/illustrator-attack-2026-05-28-archived/_attack_proof_supplier_revealed.png | head -1` returns ≥1 commit predating Phase 6(例如 commit b9aa005 archive 動作或更早的 forensic 取證 commit)

**Step B — 刪除退役 .py(per D-C1):**

```
git rm .planning/debug/scratch/illustrator-attack-2026-05-28-archived/_attack_delete_image_xobject.py
git rm .planning/debug/scratch/illustrator-attack-2026-05-28-archived/_check_supplier_removal.py
```

執行後驗:目錄內只剩 4 個 PNG/PDF entries(+ 即將新增的 README.md + 可能由 Step D 移入的 raw 3013A pdf,被 .gitignore archived-anchored 屏蔽不 track)。

**Step C — 新增 archived README.md(per D-C1 + PATTERNS § Rename + cleanup README example,繁中):**

寫入 `.planning/debug/scratch/illustrator-attack-2026-05-28-archived/README.md`,內容包含:

1. **H1 標題**:`# Illustrator attack — 2026-05-28 forensic evidence (archived)`
2. **Section 1 — 為何 archived**:此目錄保存 2026-05-28 forensic attack reproduction 的證據(PNG + PDF),v1.1 milestone 啟動的 ground-truth proof,將被 `.planning/PROJECT.md` Phase 8 Key Decisions + `.planning/STATE.md` milestone 啟動歷史 cite
3. **Section 2 — .py 退役說明**:`_attack_delete_image_xobject.py` 與 `_check_supplier_removal.py` 已退役;邏輯已搬入 `tests/_illustrator_attack.py` 並改寫為可重用 helper,主測試 `tests/test_illustrator_attack_regression.py` 對 3 個 sanitized fixture 跑同樣攻擊 + 雙閘 assert。Git history 仍可 `git log --all -- .planning/debug/scratch/illustrator-attack-2026-05-28-archived/_attack_delete_image_xobject.py` 查回 commit b9aa005 之前的版本。
4. **Section 3 — 4 個保留 artefact 的用途**:
   - `_attack_proof_supplier_revealed.png` — Illustrator 拔 image XObject 後供應商商標重現的視覺證據;`06-SECURITY.md` T-06-01 evidence cite
   - `_attack_target_pre.png` — 攻擊前 LogoSwap 輸出 render
   - `_attack_orig_for_comparison.png` — 原供應商 PDF render(原物保留 — 因為含原供應商 IP,目錄不可 public-release;但 `.planning/debug/scratch/` 不會自動同步進 npm/PyPI,僅 GitHub clone 可見,需於 AGPL §13 compliance review 時納入評估)
   - `_attack_image_xobject_deleted.pdf` — 攻擊輸出 PDF,可作為 Phase 6 regression test cross-verify reference
5. **Section 4 — Cross-references**:
   - 新 pytest:`tests/test_illustrator_attack_regression.py` + helper `tests/_illustrator_attack.py`
   - 新 fixtures:`tests/fixtures/cad-glyph/{text,figure,mixed}-glyph-01.pdf` + README.md
   - 威脅模型:`.planning/phases/06-regression-foundation-threat-model-re-evaluation/06-SECURITY.md`(T-02-07 RE-OPENED + T-06-01 OPEN)
   - 一次性 sanitize 工具:`scripts/sanitize_fixture.py`
   - Phase 7 對接:`.planning/REQUIREMENTS.md` SEC-01(Option B 落地後 close T-02-07 + T-06-01)
6. **Section 5 — 原始 raw supplier PDF 處置**:Step D 將 repo root `3013A-13A-C6-XX-3D02-A01-00040.pdf` 物理移到本目錄(被 .gitignore archived-anchored 屏蔽,不會被 git track);`samples/3013A-...pdf` 由 Step D 用 `git rm` 從 git index 移除(可選保留 working tree bytes 或一併刪除)。此舉確保 `git ls-files | grep -E '3013A-13A-C6-XX'` returns empty(per Blocker #2 + Warning #9 phase-level invariant)。本目錄內的 raw `3013A-...pdf` 是 mixed-glyph-01.pdf 的 raw source,僅 maintainer 本機可見。

**Step D — 處置 repo root + samples/ 既有 raw PDF(per CONTEXT § Claude's Discretion + checker Blocker #2 — 必須用 `git rm` 從 index 移除 tracked 副本,不能只靠 `mv`):**

當前 repo state(已驗證):
- Repo root `3013A-13A-C6-XX-3D02-A01-00040.pdf`(untracked per gitStatus + Plan 06-01 .gitignore root-anchored 已屏蔽 → 只需物理 `mv`)
- `samples/3013A-13A-C6-XX-3D02-A01-00040.pdf`(**已驗證 tracked** — **MUST 用 `git rm`,plain mv 不會從 git history / index 移除**)

執行(順序敏感,前置 `git rm`,後物理 `mv`):

```
# 1. 從 git index 移除 samples/ tracked 副本 — 此操作把檔案標記 staged-deleted;
#    physical bytes 仍在 working tree(若想完全清理可加 -f / 或 git rm 後再 rm)。
git rm samples/3013A-13A-C6-XX-3D02-A01-00040.pdf

# 2. 物理移動 repo root 副本到 archived 目錄 — .gitignore archived-anchored 已屏蔽,
#    移入後 untracked safe。
mv 3013A-13A-C6-XX-3D02-A01-00040.pdf .planning/debug/scratch/illustrator-attack-2026-05-28-archived/

# 3. 物理移動 samples/ 副本 working tree bytes(僅保留 raw source for maintainer reference;
#    git index 已由步驟 1 清理)— 加 _raw.pdf 後綴避免覆蓋已移入的 root 版本。
mv samples/3013A-13A-C6-XX-3D02-A01-00040.pdf .planning/debug/scratch/illustrator-attack-2026-05-28-archived/3013A-13A-C6-XX-3D02-A01-00040_raw.pdf 2>/dev/null || true
```

**驗證(checker Blocker #2 + Warning #9 phase-level invariant):**

```
# Phase-level invariant — 必須回傳 empty:
git ls-files | grep -E '3013A-13A-C6-XX' && echo "FAIL: still tracked" || echo "OK: no raw supplier PDF tracked"

# Repo root 應無此檔:
ls 3013A-13A-C6-XX-3D02-A01-00040.pdf 2>&1 | grep -q "No such" && echo "root clear" || echo "FAIL: root still has raw"
```

**重要(per Blocker #2 fix details):**
- `git rm samples/...` 之後 git status 顯示 `deleted: samples/...`(staged);commit 後 git ls-files 不再含此 path
- 物理 mv 進 archived 後,.gitignore line `/.planning/debug/scratch/illustrator-attack-2026-05-28-archived/3013A-*.pdf`(Plan 06-01 Step C 已加)讓 git 不會 track 這個新位置 — `git status` 不會顯示為 untracked 新增
- 結果:整個 repo `git ls-files` 不含任何 `3013A-13A-C6-XX` path;檔案 bytes 留在 maintainer 本機 archived 目錄供 forensic 對照

**完全不動 `app/**/*.py`**(production code invariant)。

  </action>
  <verify>
    <automated>ls .planning/debug/scratch/illustrator-attack-2026-05-28 2>&1 | head -3</automated>
    <automated>ls .planning/debug/scratch/illustrator-attack-2026-05-28-archived/</automated>
    <automated>ls .planning/debug/scratch/illustrator-attack-2026-05-28-archived/_attack_delete_image_xobject.py .planning/debug/scratch/illustrator-attack-2026-05-28-archived/_check_supplier_removal.py 2>&1 | head -3</automated>
    <automated>grep -c "tests/_illustrator_attack.py" .planning/debug/scratch/illustrator-attack-2026-05-28-archived/README.md</automated>
    <automated>grep -c "tests/test_illustrator_attack_regression.py" .planning/debug/scratch/illustrator-attack-2026-05-28-archived/README.md</automated>
    <automated>grep -c "06-SECURITY.md" .planning/debug/scratch/illustrator-attack-2026-05-28-archived/README.md</automated>
    <automated>ls 3013A-13A-C6-XX-3D02-A01-00040.pdf 2>&1 | head -3</automated>
    <automated>git ls-files | grep -E '3013A-13A-C6-XX' ; test $? -ne 0 && echo "OK: no raw supplier PDF tracked"</automated>
    <automated>git log --follow .planning/debug/scratch/illustrator-attack-2026-05-28-archived/_attack_proof_supplier_revealed.png 2>/dev/null | head -1</automated>
    <automated>python -m pytest -k illustrator_attack -v 2>&1 | tail -10</automated>
    <automated>python -m pytest 2>&1 | tail -3</automated>
    <automated>grep -rn "import fitz" app/</automated>
  </verify>
  <acceptance_criteria>
    - 舊目錄 `.planning/debug/scratch/illustrator-attack-2026-05-28/` 不存在(ls 應 fail 或 "No such file")
    - 新目錄 `.planning/debug/scratch/illustrator-attack-2026-05-28-archived/` 存在且含至少 5 個 git-tracked entries:4 PNG/PDF + 1 README.md(可能 +1 或 +2 raw 3013A pdf 若 Step D 物理移入 — 但這些 raw pdf 被 .gitignore archived-anchored 屏蔽,不算 tracked entries)
    - `.../illustrator-attack-2026-05-28-archived/_attack_delete_image_xobject.py` 不存在(`ls ... 2>&1 | grep -i "no such"` 應 match)
    - `.../illustrator-attack-2026-05-28-archived/_check_supplier_removal.py` 不存在
    - `…-archived/README.md` 包含字串 `tests/_illustrator_attack.py`(≥1 處)、`tests/test_illustrator_attack_regression.py`(≥1 處)、`06-SECURITY.md`(≥1 處)、`tests/fixtures/cad-glyph/`(≥1 處)
    - **Blocker #2 + Warning #9 phase-level invariant:** `git ls-files | grep -E '3013A-13A-C6-XX'` returns empty(no raw supplier PDF tracked anywhere in repo — `samples/` 版已由 `git rm` 移除,archived dir 內副本被 .gitignore 屏蔽)
    - Repo root `3013A-13A-C6-XX-3D02-A01-00040.pdf` 已物理移動(`ls 3013A-*.pdf` 顯示 "No such file")
    - **Blocker #3 history preservation:** `git log --follow .planning/debug/scratch/illustrator-attack-2026-05-28-archived/_attack_proof_supplier_revealed.png | head -1` returns ≥1 commit(history preserved through git mv)
    - `python -m pytest -k illustrator_attack -v` 仍顯示 3 個 XFAIL(scratch 退役不影響 test 行為)
    - `python -m pytest 2>&1 | tail -3` 仍顯示 `301 passed, 3 skipped, 3 xfailed`(baseline 完整)
    - `grep -rn "import fitz" app/` 仍 == `app/services/pdf_engine.py` 一行
    - `python -m pytest tests/test_redact.py::test_fitz_import_confined_to_engine_seam` exits 0
    - `git diff --stat HEAD app/` 顯示 0 changed files
  </acceptance_criteria>
  <done>
Scratch dir 已退役為 `-archived/` 命名(history preserved through git mv);`.py` 攻擊腳本刪除,邏輯活於 `tests/_illustrator_attack.py`;4 個 PNG/PDF 證據保留供審計 cite;README.md 文件化退役 + cross-ref 新落點;`samples/3013A-...pdf` 已從 git index 移除(`git rm`),repo root + samples 物理 bytes 已移到 archived(被 .gitignore archived-anchored 屏蔽);**整個 repo `git ls-files | grep -E '3013A-13A-C6-XX'` empty**(Blocker #2 + Warning #9 phase invariant 達成);git log --follow 仍可追到 forensic 證據 history(Blocker #3 達成);Phase 6 三項交付(fixtures from 06-01 + attack regression test + 06-SECURITY.md + scratch retirement)全數完成。pytest baseline 301 + 3 + 3 xfailed,AGPL seam 不破,production code 0 動。
  </done>
</task>

</tasks>

<threat_model>
## Phase 6 Canonical Threat Model

**Phase 6 的 STRIDE register 本身是這個 phase 的交付(THREAT-01 requirement)** — `.planning/phases/06-regression-foundation-threat-model-re-evaluation/06-SECURITY.md`(Task 3 產出)即 Phase 6 的 canonical threat-model 文件。本 PLAN.md 的 `<threat_model>` 段只是元層級摘要,完整 STRIDE register 在 06-SECURITY.md。

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LogoSwap output → Illustrator-class editor | 新威脅邊界(2026-05-28 forensic evidence 證實)— attacker 用 Adobe Illustrator/Acrobat Pro 讀寫 PDF object stream,能刪 image XObject overlay 觸發零面積 type='f' source path 重現供應商商標。**Phase 6 不修補此 boundary(production code 0 改動)**,只把它文件化 + 對齊紅燈 regression test;Phase 7 Option B 落地此 boundary 的 mitigation。 |
| Test fixture(committed binary)→ public repo | sanitization assert clauses + README.md immutability rule + .gitignore root-anchored + samples-anchored + archived-anchored guards(全部在 Plan 06-01 落地)+ Task 4 `git rm samples/...` + 物理 mv root → archived(本 plan)構成 legal exposure mitigation 全鏈。本 plan(06-02)Task 4 完成 phase-level invariant「`git ls-files | grep -E '3013A-13A-C6-XX'` empty」。 |
| Helper/test code → fitz API | `tests/_illustrator_attack.py` + `tests/test_illustrator_attack_regression.py` 都 import fitz;tests/ 不在 AGPL guard scope(`app/**/*.py` only)— seam 不破,per PATTERNS Shared Pattern S1 + tests/conftest.py:12 precedent。 |

## STRIDE Threat Register(Phase 6 全局)

Phase 6 整體只有 2 條 threat(都在 06-SECURITY.md 完整 register;本表為 PLAN.md 層級摘要):

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 (RE-OPENED) | I — Information disclosure | LogoSwap output PDF 內零面積 type='f' source path 仍存在於 page content stream | accept (P0, transition-pending until Phase 7) | Phase 7 SEC-01 落地 Option B content-stream surgery 刪除 zero-area source;**本 phase 的 mitigation 是「立紅燈 regression test 保證 Phase 7 落地後可客觀驗綠」+ 06-SECURITY.md 文件化此 transition + 沿用 v1.0 LIVE Option A overlay 對 CLI-only 威脅模型仍有效**;06-SECURITY.md `supersedes: [archived 06-HOTFIX-SECURITY.md]` 鎖定追溯 chain。 |
| T-06-01 (NEW) | S + I — Spoofing + Info disclosure | Illustrator-class editor attacker 拔 image XObject overlay 後 zero-area type='f' source 在 Acrobat/Illustrator render 出供應商商標 | accept (P0, transition-pending until Phase 7) | 同 T-02-07;evidence cite `_attack_proof_supplier_revealed.png` + `tests/test_illustrator_attack_regression.py` xfail。 |

**`gsd-secure-phase` non-block 條件**:`threats_open: 0` + `threats_accepted: 2`(per PATTERNS Risk Callout #2 + RESEARCH A3);Task 3 frontmatter 鎖定。

## Phase 6 production-code attack-surface delta: **0**(no production code change)

Plan 06-02 引入的所有 fitz import 均在 `tests/`,皆在 AGPL guard scope 外。`tests/test_redact.py::test_fitz_import_confined_to_engine_seam` 仍綠燈,每個 task 的 acceptance_criteria 都包含此驗證點。
</threat_model>

<verification>
**Phase 6 hardening invariants(Plan 06-02 段 — 收口驗證):**

1. **AGPL seam intact**:`grep -rn "import fitz" app/` 必須只回傳 `app/services/pdf_engine.py` 一行(全 phase 不變)。
2. **Production code 0 動**:`git diff --stat HEAD app/` 顯示 0 changed files。
3. **既有 baseline + 新 xfailed 對齊**:`python -m pytest 2>&1 | tail -3` 結尾顯示 `301 passed, 3 skipped, 3 xfailed`(無 failed、無 error)。
4. **3 個 xfail 各獨立 case**:`python -m pytest -k illustrator_attack -v 2>&1 | grep -c "XFAIL"` == 3。
5. **Phase 7 handoff signal 就位**:`grep -rn "xfail.*Option B" tests/` ≥ 1 處 hit;每處的 reason 字串包含 `.planning/REQUIREMENTS.md SEC-01` cross-ref(Phase 7 implementer grep-friendly)。
6. **AGPL guard test 仍綠**:`python -m pytest tests/test_redact.py::test_fitz_import_confined_to_engine_seam -v` exit 0。
7. **06-SECURITY.md frontmatter 滿足 gsd-secure-phase non-block 要求**:`threats_open: 0` + `threats_accepted: 2` + `audit_scope: phase_06_pre_mortem` + `supersedes:` 含 archived `06-HOTFIX-SECURITY.md` 路徑。
8. **Scratch 退役完整 + history preserved**:舊 `.../illustrator-attack-2026-05-28/` 目錄不存在;`…-archived/` 含 4 個 PNG/PDF + 1 README;`.py` 攻擊腳本已刪除;README cross-ref 三個新落點(tests/_illustrator_attack.py + tests/test_illustrator_attack_regression.py + 06-SECURITY.md);`git log --follow` 對任一保留 PNG 仍可追到 ≥1 commit(checker Blocker #3)。
9. **No raw supplier PDF tracked anywhere**(checker Blocker #2 + Warning #9 phase-level invariant):`git ls-files | grep -E '3013A-13A-C6-XX'` returns empty(samples/ 版已 `git rm`、root 副本本 untracked、archived dir 內副本被 .gitignore archived-anchored 屏蔽)。
10. **Archived `06-HOTFIX-SECURITY.md` 原檔未動**:`git diff --stat HEAD .planning/milestones/` 顯示 0 changed files(在 Plan 06-02 範圍內,不含 06-01 fixture 改動)。
</verification>

<success_criteria>
- `tests/_illustrator_attack.py` 含 3 個 exported callables(`delete_image_xobjects_intersecting`, `render_region_white_pct`, `count_zero_area_fills_in_region`),scratch lines 84-94 regex + 104-115 multi-stream write-back verbatim 保留
- `tests/test_illustrator_attack_regression.py` 含 1 個 parametrized + xfail-strict test,3 fixtures × 1 case = 3 XFAIL collected;`@pytest.mark.parametrize` 在 `@pytest.mark.xfail` 上方(Risk Callout #3);xfail reason 含繁中 + `SEC-01` cross-ref
- `python -m pytest 2>&1 | tail -3` 顯示 `301 passed, 3 skipped, 3 xfailed`(baseline + 新 3 xfailed)
- `.planning/phases/06-regression-foundation-threat-model-re-evaluation/06-SECURITY.md` 存在,frontmatter `threats_total: 2 / threats_closed: 0 / threats_open: 0 / threats_accepted: 2 / audit_scope: phase_06_pre_mortem / supersedes: [06-HOTFIX-SECURITY.md]`;STRIDE table 含 T-02-07 RE-OPENED + T-06-01 OPEN(兩條皆 `accept (P0, transition-pending)`)+ Accepted Risks Log + Pre-mortem variant section
- `.planning/debug/scratch/illustrator-attack-2026-05-28-archived/` 存在;原同名未-archived 目錄不存在;`.py` 攻擊腳本已刪除;4 個 PNG/PDF 證據保留;新 README.md cross-ref 三個新落點
- **Repo phase-level invariant:** `git ls-files | grep -E '3013A-13A-C6-XX'` returns empty(checker Blocker #2 + Warning #9);`samples/3013A-...pdf` 已 `git rm`;repo root + archived dir 內 raw 副本被 .gitignore 屏蔽
- **Forensic history preserved:** `git log --follow` 對任一 archived PNG 仍可追到 ≥1 commit(checker Blocker #3)
- `app/` 0 改動;`grep -rn "import fitz" app/` == `app/services/pdf_engine.py` 一行;`tests/test_redact.py::test_fitz_import_confined_to_engine_seam` 綠燈
- Phase 7 implementer 可用 `grep -rn "xfail.*Option B" tests/` 一鍵定位待拔的 marker;Phase 7 落地 Option B 後 `python -m pytest -k illustrator_attack` 會 XPASS(strict) 報錯,強迫拔 marker(handoff signal)
- 三個 requirement IDs 全部覆蓋:TEST-02(Tasks 1+2 attack regression test)、THREAT-01(Task 3 06-SECURITY.md)— TEST-01 由 Plan 06-01 覆蓋
</success_criteria>

<output>
After completion, create `.planning/phases/06-regression-foundation-threat-model-re-evaluation/06-02-SUMMARY.md` documenting:
- `tests/_illustrator_attack.py` 3 exports 的 final 簽名 + 是否任何 scratch 邏輯需 adapt(非 verbatim;若 100% verbatim 則明示)
- `tests/test_illustrator_attack_regression.py` 內 JobSpec 構造的精確 shape(dpi、regions、logo_id=None)+ 驗證的 `process_job` return dict 欄位(若任何欄位 unexpected 則 note)
- pytest baseline 確認:`301 passed, 3 skipped, 3 xfailed`(若數字不對則 STATE.md 同步更新並列為 phase blocker — 但本 phase 不修)
- 06-SECURITY.md frontmatter 確切值 + STRIDE table 兩條 threat 的 final disposition 文字
- Scratch retirement 結果:archived 目錄 final 內容清單 + .py 確認 deleted + README.md final 章節結構 + `git log --follow` 對 `_attack_proof_supplier_revealed.png` 的 first-commit output(證明 Blocker #3 history preserved)
- **Repo phase-level invariant 確認:** `git ls-files | grep -E '3013A-13A-C6-XX'` returns empty(Blocker #2 + Warning #9);列出 samples/ 移除 + root 物理移動 + archived dir untracked 三個狀態
- 任何 corner case(例:某 supplier PDF 的 multi-stream content stream 觸發 multi-stream fallback 路徑、`process_job` return dict 多了 unexpected key、conftest 的 logo_library fixture 在 logo_id=None 下是否仍被 import 鏈拉入)
- Phase 7 implementer 接手要點:`grep -rn "xfail.*Option B" tests/` 一鍵定位、Option B 落地後 `python -m pytest -k illustrator_attack` 預期變 XPASS error、拔 marker 為 handoff completion 動作
</output>
