---
phase: 06-regression-foundation-threat-model-re-evaluation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/sanitize_fixture.py
  - tests/fixtures/cad-glyph/README.md
  - tests/fixtures/cad-glyph/text-glyph-01.pdf
  - tests/fixtures/cad-glyph/text-glyph-01.json
  - tests/fixtures/cad-glyph/figure-glyph-01.pdf
  - tests/fixtures/cad-glyph/figure-glyph-01.json
  - tests/fixtures/cad-glyph/mixed-glyph-01.pdf
  - tests/fixtures/cad-glyph/mixed-glyph-01.json
  - .gitignore
autonomous: true
requirements: [TEST-01]
tags: [pdf-sanitization, fixtures, dev-tooling, cad-glyph]

must_haves:
  truths:
    - "Maintainer 可在本機跑 `python scripts/sanitize_fixture.py --help` 看到 4 個必要 args(--in / --out / --supplier-name / --region-rect)+ 1 個 optional(--synthesize)"
    - "`tests/fixtures/cad-glyph/` 目錄下存在恰好 3 個 sanitized PDF + 3 個 sidecar JSON manifest + 1 個 README.md(共 7 個 entries)"
    - "Sanitization script 對任一輸出 PDF 跑完後 self-assert pass — metadata 全空、原供應商名不出現於 `page.get_text()`、`count_zero_area_fills_fully_inside(page, REGION)` ≥ 原 count × 0.9"
    - "Sidecar manifest 採 split-coordinate schema(`region_rect_pdf_points` + `region_rect_px`)— 此為 Phase 6 canonical schema,取代 CONTEXT D-B4 單一 `region_rect`(per Claude's Discretion on manifest schema);Phase 7 unit tests / consumer code 對齊本 schema"
    - "若任一 fixture 為 synthetic,Phase 6 close 標記 PROVISIONAL — `06-01-SUMMARY.md` 與 Phase 6 close-out SUMMARY 寫入「Phase 6 fixture 構成: N real + M synthetic — close 條件 PROVISIONAL until 工程師交付 + 重跑 sanitize_fixture.py」,且 STATE.md 同步記錄 open blocker「Phase 6 fixture replenishment — 以真實 supplier PDF 替換 synthetic-{text|figure}」"
    - "Production code `app/**/*.py` 完全未動 — `git diff --stat HEAD app/` 顯示 0 changed files"
    - "AGPL fitz seam 未破 — `grep -rn 'import fitz' app/` 只回傳一行 `app/services/pdf_engine.py`;`tests/test_redact.py::test_fitz_import_confined_to_engine_seam` 仍綠燈"
    - "Repo root 的 `3013A-13A-C6-XX-3D02-A01-00040.pdf`(含原供應商商標)已被處置 — 移到 `.planning/debug/scratch/illustrator-attack-2026-05-28-archived/`(等 06-02 重命名後)或加入 `.gitignore`,public repo 中不再含原供應商 IP"
  artifacts:
    - path: "scripts/sanitize_fixture.py"
      provides: "CLI tool: metadata clear + XMP clear + brand-glyph zero-area block delete + TESTCO wordmark inject via fitz Shape API + post-state assert"
      contains: "def main(", "argparse", "doc.set_metadata({})", "doc.set_xml_metadata", "doc.update_stream", "shape.commit", "TESTCO"
      min_lines: 200
    - path: "tests/fixtures/cad-glyph/README.md"
      provides: "Committed-binary exception 文件 — D-A6 5 項必要內容"
      contains: "scripts/sanitize_fixture.py", "AGPL", "TESTCO", "繁中"
    - path: "tests/fixtures/cad-glyph/text-glyph-01.pdf"
      provides: "Sanitized 文字 glyph 主體 supplier CAD-glyph PDF(committed binary)"
    - path: "tests/fixtures/cad-glyph/text-glyph-01.json"
      provides: "Sidecar manifest with region_rect_pdf_points + region_rect_px + dpi + page_index + expected_zero_area_count_pre_process + sanitization_script_commit_sha + created_at_iso"
      contains: "region_rect_pdf_points", "region_rect_px", "dpi", "page_index", "expected_zero_area_count_pre_process"
    - path: "tests/fixtures/cad-glyph/figure-glyph-01.pdf"
      provides: "Sanitized 圖形 glyph 主體 supplier CAD-glyph PDF"
    - path: "tests/fixtures/cad-glyph/figure-glyph-01.json"
      provides: "Sidecar manifest(同上 schema)"
    - path: "tests/fixtures/cad-glyph/mixed-glyph-01.pdf"
      provides: "Sanitized 文字 + 圖形 mixed CAD title block PDF"
    - path: "tests/fixtures/cad-glyph/mixed-glyph-01.json"
      provides: "Sidecar manifest(同上 schema)"
  key_links:
    - from: "scripts/sanitize_fixture.py"
      to: "app.services.pdf_engine.count_zero_area_fills_fully_inside"
      via: "import from app + post-state self-assert ≥ 0.9 × original count(D-A3 驗證點)"
      pattern: "from app.services import pdf_engine"
    - from: "scripts/sanitize_fixture.py"
      to: "fitz.Document.update_stream / set_metadata / set_xml_metadata / Page.new_shape"
      via: "direct fitz API call(scripts/ 不在 AGPL guard scope);Shape API 用於 TESTCO wordmark 注入(Option B verbatim per PATTERNS S4)"
      pattern: "doc.update_stream\\(content_xrefs\\[0\\]"
    - from: "tests/fixtures/cad-glyph/*.pdf"
      to: "tests/fixtures/cad-glyph/*.json"
      via: "shared file stem(.pdf 與 .json 同名)— sanitize script 同時產出"
      pattern: "with_suffix\\('.json'\\)"
---

<objective>
建立 Phase 6 紅燈基線的「fixture 補給線」— 一次性 CLI 工具 `scripts/sanitize_fixture.py` 把工程師交付的真實 supplier CAD-glyph PDF 脫敏成可 commit 進 public repo 的 fixture,並以 sidecar JSON manifest 攜帶 region rect + 預期 zero-area count。

Purpose: Phase 6 attack regression test 需要 ≥3 個保留同型 zero-area `type='f'` attack 面、但 visual signature 不含原供應商 IP 的 sanitized PDF。手動 hex-edit 不安全 + 不可重現;一次性 sanitization script 是 D-A4 鎖定的方案。

Output:
- `scripts/sanitize_fixture.py`(新 CLI,~250-300 行,fitz import 直接放,在 AGPL guard scope 外)
- `tests/fixtures/cad-glyph/` 目錄 + README.md + 3 個 sanitized PDF + 3 個 sidecar manifest
- `samples/3013A-...pdf` + repo root 同檔的處置(.gitignore 屏蔽未來 commit + tracked `samples/` 版於 Plan 06-02 Task 4 走 `git rm` 移除;本 plan 先加 .gitignore + archived 目錄 anchored guard)

Scope boundaries:
- 不寫 attack regression test 本體(Plan 06-02 負責)
- 不寫 06-SECURITY.md(Plan 06-02 負責)
- 不退役 scratch .py 檔(Plan 06-02 負責)
- production code `app/**/*.py` 0 改動
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-regression-foundation-threat-model-re-evaluation/06-CONTEXT.md
@.planning/phases/06-regression-foundation-threat-model-re-evaluation/06-RESEARCH.md
@.planning/phases/06-regression-foundation-threat-model-re-evaluation/06-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- 關鍵 type / API surfaces,executor 不需自行 grep。 -->

From app/services/pdf_engine.py (production helper that sanitize script's post-state assert imports):

```python
# line 699 approx; AGPL fitz seam — only file in app/ allowed to import fitz
def count_zero_area_fills_fully_inside(
    page: fitz.Page,
    rect: tuple[float, float, float, float] | fitz.Rect,
    tolerance: float = _DEGENERATE_BBOX_EPS,
) -> int:
    """Count zero-area type='f' fills whose bbox is fully inside `rect`."""
```

From .planning/debug/scratch/illustrator-attack-2026-05-28/_attack_delete_image_xobject.py (source-of-truth attack mechanics — lines 40-115 to be REUSED VERBATIM in helpers within sanitize script):

```python
# lines 40-49: image XObject discovery (NOT used by sanitize — but the imports/regex pattern set carry over)
# lines 70-74: XObject resource name resolution
# lines 84-94: q...Do...Q content-stream regex strip (will be ADAPTED for brand-glyph block strip)
# lines 104-115: multi-stream update_stream write-back pattern (REUSED VERBATIM)

# CRITICAL multi-stream write-back pattern (Pitfall 3 — preserve EXACT shape):
new_bytes = stream_text.encode("latin-1")
content_xrefs = page.get_contents()
if len(content_xrefs) == 1:
    doc.update_stream(content_xrefs[0], new_bytes, compress=True)
else:
    doc.update_stream(content_xrefs[0], new_bytes, compress=True)
    for xref in content_xrefs[1:]:
        doc.update_stream(xref, b"", compress=True)
```

From scripts/smoke_02_03.py:1-35 (CLI shell skeleton — REUSE the sys.path prepend + stdout reconfigure boilerplate):

```python
from __future__ import annotations
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parent.parent))  # so `from app.services import ...` works
try:
    sys.stdout.reconfigure(encoding="utf-8")
except Exception:
    pass
```

From tests/conftest.py:1-12 (license-to-import-fitz precedent — note style, not literally importable here):

```python
import fitz  # only the test harness may use fitz directly to BUILD fixtures
```

From tests/test_redact.py:711-728 (TESTCO wordmark construction pattern — Shape.draw_rect(W=0) for zero-area type='f' fills):

```python
# zero-area type='f' fill production via fitz Shape API (proven in baseline 301 passing)
n = pdf_engine.ZERO_AREA_RASTER_THRESHOLD + 20  # 100 + 20 = 120 — well over 0.9 × typical N
for i in range(n):
    x = 55.0 + i * 2.0
    shape = page.new_shape()
    shape.draw_rect(fitz.Rect(x, 110.0, x, 190.0))  # W=0 -> zero-area
    shape.finish(fill=(0.0, 0.0, 0.0), color=None, width=0)
    shape.commit()
```

JobSpec / RegionMark shape (from app/models.py lines 48-99) — IMPORTANT FOR MANIFEST SCHEMA:

```python
class RegionMark(BaseModel):
    page: int = Field(ge=0)
    px_rect: List[float]  # (x0, y0, x1, y1) in image pixels at the job dpi — NOT pdf points

class JobSpec(BaseModel):
    dpi: int  # >= MIN_DPI, <= MAX_DPI
    regions: List[RegionMark]
    logo_id: str | None = None
    auto_logo: bool = False
    rotations: Dict[int, int] = {}
```

Implication: sidecar manifest MUST carry BOTH the PDF-point rect (for the attack helper's `fitz.Rect(*rect)` clip + `count_zero_area_fills_fully_inside`) AND the pixel rect + dpi (for the pipeline's `process_job(session_id, JobSpec(dpi=DPI, regions=[RegionMark(page=P, px_rect=[...])]))` consumption).
</interfaces>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 建立 scripts/sanitize_fixture.py CLI 工具(metadata clear + brand-glyph 整塊 strip + Shape-API TESTCO inject + self-assert)</name>
  <files>scripts/sanitize_fixture.py</files>
  <read_first>
    - .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-CONTEXT.md(D-A1..D-A6 鎖定)
    - .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-RESEARCH.md § Pattern 1, Pattern 2, Pitfall 1-3 + 7-8(metadata/XMP/multi-stream/CMap 邊界)
    - .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-PATTERNS.md § `scripts/sanitize_fixture.py` Pattern Assignments + Shared Pattern S1 + S4(Shape API)
    - scripts/smoke_02_03.py(lines 1-35, 53-55, 160-162 — CLI shell skeleton verbatim)
    - .planning/debug/scratch/illustrator-attack-2026-05-28/_attack_delete_image_xobject.py(lines 84-115 — content-stream surgery 機制 verbatim,sanitize 不刪 image XObject 但複用同樣的 regex + multi-stream write-back pattern 在 brand-glyph block strip)
    - tests/test_redact.py(lines 722-728 — Shape.draw_rect(W=0) zero-area type='f' fill 生成 pattern,複用為 TESTCO wordmark builder)
    - app/services/pdf_engine.py(line 699 approx,`count_zero_area_fills_fully_inside` 簽名 — sanitize 結尾 self-assert 會 import 此 helper)
    - tests/conftest.py(line 1-12 — fitz import license note 措辭參考)
  </read_first>
  <action>
建立新檔 `scripts/sanitize_fixture.py`(per D-A4 一次性 CLI 工具)。

**Sidecar manifest schema decision(per Warning #8 — explicit cross-CONTEXT note):**本檔產出的 sidecar 採 **split-coordinate schema** — 同時寫 `region_rect_pdf_points`(給 fitz.Rect / count_zero_area_fills_fully_inside 用)與 `region_rect_px`(給 RegionMark.px_rect 用)兩個 key,而非 CONTEXT D-B4 範例中的單一 `region_rect`。此 schema 為 Phase 6 canonical(Claude's Discretion on manifest schema per CONTEXT § Claude's Discretion);Phase 7 unit tests + downstream consumer 對齊本 schema。

**檔案結構(目標 ~250-300 行):**

1. **CLI shell skeleton** — copy 自 `scripts/smoke_02_03.py:1-35` 的 sys.path prepend + stdout UTF-8 reconfigure boilerplate;module docstring 為繁中,說明此工具用途、與 AGPL guard 的關係(scripts/ 在 guard scope 外)、+ 為何 raw PDF 不可 commit(D-A4 鎖定)、+ sidecar manifest 採 split-coordinate schema 的理由(一行繁中 note)。

2. **`import fitz`** with inline comment `# AGPL guard scope = app/**/*.py only — scripts/ is OUT of scope, fitz import here is safe(per PATTERNS Shared Pattern S1 + tests/conftest.py:12 precedent)`。同時 `import argparse, hashlib, json, re, sys, datetime, subprocess; from pathlib import Path; from app.services import pdf_engine`(後者用於 self-assert)。

3. **`parse_args()`** — argparse 4 個必要 args(per D-A4)+ 數個 optional:
   - `--in PATH`(input raw supplier PDF;**必要**;`--synthesize` 模式下可省略)
   - `--out PATH`(output sanitized PDF;**必要**;**MUST 是 `tests/fixtures/cad-glyph/` 底下**,否則 exit 非零 — 對應 PATTERNS Risk Callout「Raw supplier PDF accidentally committed」)
   - `--supplier-name STR`(用於 find-replace + self-assert;**必要**;`--synthesize` 模式下可省略,預設 `"SYNTHETIC_TESTCO"`)
   - `--region-rect "x0,y0,x1,y1"`(PDF points;**必要**;parser 用 explicit `float(x)` per token + `ValueError` handling — **若解析失敗 print 繁中錯誤 + `sys.exit(1)`**,V5 input validation per PATTERNS L26 + RESEARCH Security Domain)
   - `--page-index INT`(default `0`)
   - `--dpi INT`(default `144`,用於計算 sidecar manifest 的 `region_rect_px` — pipeline 端的 RegionMark.px_rect 跟 JobSpec.dpi 必須對應)
   - `--synthesize`(optional flag — fallback 合成 mode,若工程師未交付實檔 + 此 plan tasks 跑時來不及。**注意:**D-A1 鎖定不採合成 PDF 作 primary;此 flag 是 RESEARCH Open Question 1 + CONTEXT specifics 的 contingency,只用於 ≤2 個 missing 的情況)

4. **`main()` pipeline — 採 Option B linear recipe(per checker Blocker #1 — Shape API + commit + save,**不**做 content-stream find-replace step):**

   執行順序鎖死、無分支、無 parenthetical 反向 override:

   - **Step 1: 開檔 + Metadata 清空** — `doc = fitz.open(in_path)`;`doc.set_metadata({})`(per PyMuPDF 1.27 API)+ `doc.set_xml_metadata("")`(後者覆蓋 XMP,per RESEARCH Pitfall 2)。
   - **Step 2: 找原 brand glyph bbox + 記錄基準 count** — `page = doc[page_index]`;用 `page.get_drawings()` 過濾 `type='f'` + bbox 與 `region_rect` 有交集的 fill,取 union bbox(per RESEARCH Pitfall 7 — TESTCO 替換 bbox 必須 ⊂ region_rect)。記下 `original_zero_area_count = pdf_engine.count_zero_area_fills_fully_inside(page, region_rect)` 作為 self-assert 基準。
   - **Step 3: Brand-glyph 整塊 strip(content-stream surgery)** — `stream_bytes = page.read_contents()`;`stream_text = stream_bytes.decode("latin-1")`;以 step 2 取得的 union bbox 定位原 supplier brand glyph 的 `q ... Q` block(用 byte-offset / regex 啟發式 — 見下方 implementation note),用 `re.subn` 刪除原 block。Multi-stream write-back 用上方 interfaces 區塊 scratch lines 104-115 verbatim(`update_stream(content_xrefs[0], new_bytes)` + 其餘 `update_stream(xref, b"")`)。**重要:** 此步驟**不做**任何 `stream_text.replace(supplier_name, "TESTCO")` find-replace — supplier name 殘留改由 step 5 self-assert `supplier_name not in page.get_text()` 把關;若 self-assert fail,executor 改走 fallback(見 implementation note B)。
   - **Step 4: TESTCO 零面積 wordmark 注入(Shape API,Option B verbatim per PATTERNS S4)** — 在 Step 3 完成 update_stream 之後,**重新讀** page(`page = doc[page_index]`)以確保 fitz 內部 cache 與新 content stream 對齊;然後用 PATTERNS S4 lines 112-119 verbatim 的 `Shape.draw_rect(fitz.Rect(x, y0, x, y1))`(W=0 → zero-area `type='f'`)pattern,在 Step 2 取得的原 union bbox 內鋪 ≥ `original_zero_area_count × 0.95` 個 zero-width 縱線:
     ```
     n_target = max(int(original_zero_area_count * 0.95), 1)
     x_start, y_top, x_end, y_bot = union_bbox
     for i in range(n_target):
         x = x_start + i * (x_end - x_start) / max(n_target, 1)
         shape = page.new_shape()
         shape.draw_rect(fitz.Rect(x, y_top, x, y_bot))   # W=0 -> zero-area type='f'
         shape.finish(fill=(0, 0, 0), color=None, width=0)
         shape.commit()
     ```
     **shape.commit() 會把 ops 寫入 page 的 content stream**(fitz 自動管理 — append 到既有 stream / 開新 stream xref,均在 fitz 內部);**不需要再呼叫 update_stream** 一次。
   - **Step 5: Self-assert(in-memory,pre-save;任一失敗 → print 繁中錯誤 + `sys.exit(1)`,且不執行 step 6 save):**
     - `assert all((v is None or v == "") for v in doc.metadata.values())` — metadata 清乾淨
     - `assert supplier_name not in page.get_text()` — 原供應商名不出現於可 decode 文字(per D-A3 驗證點;此 assert 取代了被刪除的 step 3 find-replace,因為 brand-glyph block strip 已把 supplier 文字一併移除;若 supplier 用 CMap encoding 導致 get_text 仍 decode 出供應商名,executor 走 implementation note B fallback)
     - `assert pdf_engine.count_zero_area_fills_fully_inside(page, region_rect) >= 0.9 * original_zero_area_count` — 保留同型 attack 面 ≥90%(per D-A3)
     - `assert str(out_path).startswith("tests/fixtures/cad-glyph/")` — 拒寫到 repo 其他位置(rebuild safety)
   - **Step 6: Save + close** — `doc.save(out_path, garbage=4, deflate=True, clean=True)` + `doc.close()`。

5. **Sidecar manifest 生成** — 在 Step 6 save 之後同步寫 `out_path.with_suffix(".json")`,split-coordinate schema(per Warning #8 schema decision note 在 docstring + 此處):
   ```
   {
     "region_rect_pdf_points": [x0, y0, x1, y1],
     "region_rect_px": [x0_px, y0_px, x1_px, y1_px],  // = pdf_points * dpi / 72.0
     "dpi": 144,
     "page_index": 0,
     "expected_zero_area_count_pre_process": <int — original_zero_area_count>,
     "original_supplier_name_sha256": "<hashlib.sha256(supplier_name.encode()).hexdigest()[:16]>",
     "sanitization_script_commit_sha": "<subprocess.check_output(['git', 'rev-parse', '--short', 'HEAD']).decode().strip()>",
     "created_at_iso": "<datetime.datetime.now(timezone.utc).isoformat()>"
   }
   ```

6. **CLI 繁中 messages**(per memory feedback_language + PATTERNS Shared Pattern S4):`print("步驟 1/6:清空 metadata 與 XMP …")`、`print(f"  ✓ metadata 清空完成({len(doc.metadata)} 欄)")` 等。

7. **`if __name__ == "__main__": sys.exit(main())`**。

**`--synthesize` mode 行為(contingency only,RESEARCH Open Question 1):**若 `--synthesize` flag 提供,input PDF 由 fitz 從零建構(`fitz.open()` + `new_page`),page size A4;**跳過 Step 1 doc.open(in_path)、Step 2 get_drawings 過濾、Step 3 content-stream surgery**,直接執行 Step 4 Shape API 在 `--region-rect` 內鋪 TESTCO 零面積 wordmark(n_target=120 hardcoded,因無原 count 可參考)+ 設 metadata 為 empty + 寫 manifest(`expected_zero_area_count_pre_process` 取 synthesize 完之 count)。此模式 `supplier_name` 預設 `"SYNTHETIC_TESTCO"`。

**Implementation note A(Step 3 brand-glyph block locator):**最簡可行方法 — 把 `region_rect` 包進 fitz `page.get_drawings()` 過濾出的 fill ops,看每個 fill 的 `op["seqno"]` 範圍對應到 content stream 哪段 byte slice;若 supplier brand 是連續的 `q ... Q` block,用 regex `r"q\s+[^Q]*?(<bbox-anchor-bytes>)[^Q]*?Q"` 在 latin-1 decoded text 上 strip。**bbox-anchor 找不到的退路:**直接以最內層 `m`/`l`/`f` 算子序列 byte offset 範圍刪除(stream_text[start:end] = "")。Executor 在程式內 comment 紀錄選用哪條路徑。

**Implementation note B(supplier-name CMap-encoded fallback):**若 Step 5 `supplier_name not in page.get_text()` self-assert fail(代表 supplier 名以 CMap + 自訂 font 編碼,brand-glyph block strip 漏掉文字 run),executor 在程式內加一個 **最後一道 content-stream find-replace pass**:`stream_text2 = stream_text.replace(supplier_name, "TESTCO")` + 同樣 multi-stream write-back;此 fallback 只在 self-assert fail 時觸發,且 print 繁中警示 `"⚠ supplier name CMap fallback 觸發,正在套用 find-replace ..."`,然後重跑 Step 5 self-assert。若重跑仍 fail → exit 1。**主路徑不執行此 find-replace,以保 Option B linear recipe(checker Blocker #1)。**

**Implementation note C(self-assert 觸發證明 — per Warning #7):**`if __name__ == "__main__"` 區塊內加一個 `--self-test` 暗門 flag(不對使用者公開):若 `--region-rect` 解析失敗或 input 不存在等任一前置條件被違反,parser 必須 print 繁中錯誤 + exit code != 0。本 task acceptance criteria 含一個自動 smoke test 直接驗證此 behavior(`--synthesize --region-rect "bogus-not-a-rect"` → exit non-zero)。

**完全不動 `app/**/*.py`**(5330290 教訓 + CONTEXT carrying-forward AGPL seam)。
  </action>
  <verify>
    <automated>python scripts/sanitize_fixture.py --help</automated>
    <automated>python -c "import ast,sys; t=ast.parse(open('scripts/sanitize_fixture.py',encoding='utf-8').read()); assert any(isinstance(n,ast.FunctionDef) and n.name=='main' for n in t.body), 'missing main()'; print('AST ok')"</automated>
    <automated>python -c "import sys; sys.path.insert(0,'.'); import scripts.sanitize_fixture as s; assert hasattr(s,'main'), 'main missing'; print('importable')"</automated>
    <automated>python scripts/sanitize_fixture.py --synthesize --out /tmp/should_fail.pdf --region-rect "bogus-not-a-rect"; test $? -ne 0 && echo "rect-parser self-assert OK"</automated>
  </verify>
  <acceptance_criteria>
    - `python scripts/sanitize_fixture.py --help` exits 0 且輸出包含 `--in`, `--out`, `--supplier-name`, `--region-rect`, `--synthesize` 五個 args
    - `grep -c "doc.set_metadata({})" scripts/sanitize_fixture.py` ≥ 1
    - `grep -c "doc.set_xml_metadata" scripts/sanitize_fixture.py` ≥ 1
    - `grep -c "doc.update_stream" scripts/sanitize_fixture.py` ≥ 1(multi-stream write-back pattern present in Step 3)
    - `grep -c "shape.commit" scripts/sanitize_fixture.py` ≥ 1(Shape API TESTCO injection in Step 4 — Option B verbatim)
    - `grep -c "count_zero_area_fills_fully_inside" scripts/sanitize_fixture.py` ≥ 1(self-assert imports production helper)
    - `grep -c "TESTCO" scripts/sanitize_fixture.py` ≥ 2(synthetic supplier placeholder + 注釋 / CMap fallback target)
    - `grep -v "^#" scripts/sanitize_fixture.py | grep -c "import fitz"` == 1(scripts/ 在 AGPL guard 外,允許 import;只 import 一次避免雜訊)
    - `grep -rn "import fitz" app/` 仍 == `app/services/pdf_engine.py` 一行(AGPL seam 不破)
    - `git diff --stat HEAD app/` 顯示 0 changed files(production code 0 動)
    - **Self-assert proof:** `python scripts/sanitize_fixture.py --synthesize --out /tmp/should_fail.pdf --region-rect "bogus-not-a-rect"` exits with non-zero status(rect parser self-assert 觸發 → 證明 self-assert 路徑 reachable,per Warning #7)
  </acceptance_criteria>
  <done>
CLI 工具可用、`--help` 顯示完整 args、AST 通過、Step 3 brand-glyph strip + Step 4 Shape API TESTCO inject 各別呼叫 update_stream / shape.commit、self-assert smoke test 證明 rect parser 自我把關、production code 0 動、AGPL fitz seam 仍只在 `app/services/pdf_engine.py` 一處 import。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 跑 sanitize_fixture.py 產出 3 個 fixture + manifest;補 README.md + .gitignore;處置 repo root 既有 raw PDF</name>
  <files>
    tests/fixtures/cad-glyph/README.md,
    tests/fixtures/cad-glyph/text-glyph-01.pdf,
    tests/fixtures/cad-glyph/text-glyph-01.json,
    tests/fixtures/cad-glyph/figure-glyph-01.pdf,
    tests/fixtures/cad-glyph/figure-glyph-01.json,
    tests/fixtures/cad-glyph/mixed-glyph-01.pdf,
    tests/fixtures/cad-glyph/mixed-glyph-01.json,
    .gitignore
  </files>
  <read_first>
    - .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-CONTEXT.md(D-A1 fixture 來源、D-A5 三個 coverage slot、D-A6 README.md 必要內容、CONTEXT § specifics fallback 條款、CONTEXT § Claude's Discretion 對 `samples/3013A-...pdf` + repo root 副本處置的 planner 評估指引)
    - .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-RESEARCH.md(Open Question 1 contingency + Pitfall 7 region_rect ≥ TESTCO bbox + Anti-Pattern「raw supplier PDF accidentally committed」)
    - .planning/phases/06-regression-foundation-threat-model-re-evaluation/06-PATTERNS.md § `tests/fixtures/cad-glyph/README.md` Pattern Assignments(5 項必要內容)+ Open follow-up(samples/ 處置由 planner 決定)
    - scripts/sanitize_fixture.py(Task 1 產出)
    - .gitignore(既有,加入 root-anchored + archived-anchored guards)
    - logos/manifest.json(JSON sidecar 慣例,但 schema 不同 — 本 task 用 per-fixture object 而非 array)
  </read_first>
  <action>

**Precondition — pytest 安裝檢查(per Warning #5):**

```
python -c "import pytest; print(pytest.__version__)" || pip install "pytest>=8.0"
```

(本 task 的 verify 步驟用 `python -m pytest`,Plan 06-02 的所有 task 亦同;若 env 無 pytest 必須先安裝。)

**Step A — 對 3 個 supplier raw PDF 跑 sanitize_fixture.py 產出 fixtures(per D-A5 三個 coverage slot):**

執行 3 個指令(executor 須掌握工程師交付的實檔位置 + 確切供應商名 + region rect — 若 STATE.md 顯示工程師延遲交付,走 fallback C):

1. **`tests/fixtures/cad-glyph/text-glyph-01.pdf`**(文字 glyph 主體 supplier wordmark):
   ```
   python scripts/sanitize_fixture.py \
     --in <raw text-glyph supplier.pdf> \
     --out tests/fixtures/cad-glyph/text-glyph-01.pdf \
     --supplier-name "<原供應商名 — 由 maintainer 提供>" \
     --region-rect "<x0,y0,x1,y1 in PDF points>" \
     --page-index 0
   ```
2. **`tests/fixtures/cad-glyph/figure-glyph-01.pdf`**(圖形 glyph 主體 — icon/monogram 純向量幾何):
   ```
   python scripts/sanitize_fixture.py --in <raw figure-glyph supplier.pdf> --out tests/fixtures/cad-glyph/figure-glyph-01.pdf --supplier-name "..." --region-rect "..." --page-index 0
   ```
3. **`tests/fixtures/cad-glyph/mixed-glyph-01.pdf`**(文字 + 圖形 mixed CAD title block — 對應 repo root 既有 `3013A-13A-C6-XX-3D02-A01-00040.pdf`,這是 2026-05-28 attack reproduction file,D-A1 鎖定為第 1 個 fixture):
   ```
   python scripts/sanitize_fixture.py \
     --in 3013A-13A-C6-XX-3D02-A01-00040.pdf \
     --out tests/fixtures/cad-glyph/mixed-glyph-01.pdf \
     --supplier-name "<原供應商名 — 從該 PDF metadata / 視覺檢視取得>" \
     --region-rect "<原 attack 框選區 — 參 .planning/debug/scratch/illustrator-attack-2026-05-28/_attack_delete_image_xobject.py 的 REGION 常數>" \
     --page-index 0
   ```

每個指令完成後 `tests/fixtures/cad-glyph/` 內應同時出現 `<slot>.pdf` + `<slot>.json`(sanitize script 同步寫 manifest)。

**Fallback C — 工程師延遲交付(per CONTEXT § specifics + RESEARCH Open Question 1 + Warning #6 close-blocking semantics):**

若 plan 執行時 STATE.md 顯示工程師尚未交付 ≥2 個額外 supplier PDF(只有 repo root 既有 `3013A-13A-C6-XX-3D02-A01-00040.pdf` 一個 real source):
- `mixed-glyph-01.pdf` 仍從 3013A 真實檔產出(走主路徑)
- `text-glyph-01.pdf` + `figure-glyph-01.pdf` 用 `--synthesize` mode 產出:
  ```
  python scripts/sanitize_fixture.py --synthesize --out tests/fixtures/cad-glyph/text-glyph-01.pdf --region-rect "100.0,100.0,300.0,200.0" --page-index 0 --dpi 144
  python scripts/sanitize_fixture.py --synthesize --out tests/fixtures/cad-glyph/figure-glyph-01.pdf --region-rect "200.0,300.0,400.0,400.0" --page-index 0 --dpi 144
  ```

**若任一 fixture 為 synthetic,executor MUST 同步執行下列 close-blocking steps(per Warning #6 — D-A1 不採 synthetic primary,故 Phase 6 close 標 PROVISIONAL):**

1. 在 `tests/fixtures/cad-glyph/README.md` Section 2 表格內,synthetic fixture 的「original supplier」欄寫 `—(synthetic — 工程師延遲交付 contingency;Phase 6 close 為 PROVISIONAL)`。
2. 在本 task 的 commit message 內加一行 `[fixture PROVISIONAL: N real + M synthetic — 待工程師交付後重跑 sanitize_fixture.py]`。
3. 在 `06-01-SUMMARY.md`(plan 結束時產出)頂部 banner 寫:`> ⚠ Phase 6 fixture 構成: N real + M synthetic — close 條件 PROVISIONAL until 工程師交付全部 3 個 real PDFs 並重跑 sanitize_fixture.py。`
4. 在 `.planning/STATE.md` 的 blocker / pending 區塊新增一條:`- [ ] Phase 6 fixture replenishment — 以真實 supplier PDF 替換 synthetic-{text|figure} fixtures(待工程師交付)`(executor 用 Edit 工具加入,不重寫 STATE.md)。
5. Phase 6 close-out 階段(Plan 06-02 完成後彙整),SUMMARY 必須 cite 本 PROVISIONAL 狀態並指向 STATE.md blocker;Phase 6 不能在 PROVISIONAL 狀態下被「無 caveat 標 closed」。

(若全 3 fixture 都是真實:跳過上述 5 步,Phase 6 close 為 FINAL。)

**Step B — 寫 `tests/fixtures/cad-glyph/README.md`(per D-A6 + PATTERNS § README 5 項必要內容):**

繁中 markdown,5 個區塊(順序固定):

1. **「為什麼這是 `tests/` 唯一 committed-binary 例外」** — cross-reference `tests/conftest.py:1-6` 既有「never commit binary fixtures」哲學;說明 Illustrator-attack regression 需要真實供應商 CAD content stream 算子序列,合成 builder(`_build_pdf`)無法忠實重現 Illustrator/AutoCAD 出口 PDF 的 q…Q wrap 慣例與 XObject 命名習慣(per D-A1)。
2. **「每個 fixture 的脫敏記錄」** — markdown 表格 4 欄:`fixture` | `original supplier (initial only)` | `sanitization date (UTC ISO)` | `script commit sha`。Synthetic fixtures 在 `original supplier` 欄寫 `—(synthetic — 工程師延遲交付 contingency;Phase 6 close 為 PROVISIONAL)`(per Fallback C 步驟 1)。
3. **「Immutability rule」** — 任何 fixture 變更必須走 `scripts/sanitize_fixture.py` 重跑;**禁止 hex-edit、禁止 `git rebase` 改 bytes**;cross-ref RESEARCH § Anti-Patterns「手動編輯 sanitized fixture PDF」+ D-A6 c。
4. **「AGPL §13 statement」**(per CONTEXT § specifics):「all fixtures sanitized via `scripts/sanitize_fixture.py`, no original supplier IP retained — script self-asserts metadata empty + supplier name absent from extracted text before writing output」。
5. **「Cross-references」** — pointers to `scripts/sanitize_fixture.py`、`tests/test_illustrator_attack_regression.py`(Plan 06-02 將建)、`.planning/REQUIREMENTS.md TEST-01`、`.planning/phases/06-regression-foundation-threat-model-re-evaluation/06-SECURITY.md`(Plan 06-02 將建)、`.planning/phases/06-regression-foundation-threat-model-re-evaluation/06-CONTEXT.md` D-A1-A6。

**Step C — 加 `.gitignore` 防護(per CONTEXT § Claude's Discretion samples/3013A 處置 + RESEARCH Anti-Pattern「raw supplier PDF accidentally committed」+ checker Blocker #2 — archived dir 也要 anchored guard):**

讀現有 `.gitignore`,在合適段落(若無 anchor 則加新 section)加入(逐行附繁中註解):
```
# Raw supplier PDFs — must never be committed (Phase 6 D-A4).
# Sanitized fixtures live in tests/fixtures/cad-glyph/ and ARE committed (D-A2 exception).
/3013A-13A-C6-XX-*.pdf
/samples/3013A-*.pdf
# Archived scratch dir (Plan 06-02 Task 4 將既有 raw supplier PDF 物理移入此處;
# .gitignore 確保移入後不被 git track,讓 git rm + 物理 mv 為兩步乾淨操作):
/.planning/debug/scratch/illustrator-attack-2026-05-28-archived/3013A-*.pdf
*-supplier-raw.pdf
```
(root-anchored 的兩個 entry 對應目前 repo 已存在的 raw file;archived-anchored 的 entry 對齊 Plan 06-02 Task 4 的物理移入路徑;`*-supplier-raw.pdf` 是 future-proof convention encourage maintainer 用此 suffix 命名 raw input)。

**Step D — 處置 repo root + samples/ 既有 raw PDF(per CONTEXT Claude's Discretion;Plan 06-01 只加 .gitignore + 文件化,實際 `git rm` + 物理移動由 Plan 06-02 Task 4 完成):**

當前 repo state(已驗證):
- Repo root `3013A-13A-C6-XX-3D02-A01-00040.pdf`(untracked per gitStatus → `.gitignore` 上方 pattern `/3013A-13A-C6-XX-*.pdf` 屏蔽,無需 `git rm`)
- `samples/3013A-13A-C6-XX-3D02-A01-00040.pdf`(**已驗證 tracked** — 必須由 Plan 06-02 Task 4 用 `git rm` 移除,而非 `mv`;本 plan 只加 `.gitignore` pattern `/samples/3013A-*.pdf` 防止未來再 `git add`)

本 task 只做兩件事:
1. **`.gitignore` 加入 root-anchored + samples-anchored + archived-anchored guards(Step C 已含)**。
2. **在 cad-glyph/README.md 的 Cross-references 區塊明確 note:**「`3013A-13A-C6-XX-3D02-A01-00040.pdf`(repo root,untracked)是 mixed-glyph-01.pdf 的 raw source;`samples/3013A-...pdf` 為 tracked 副本;**Plan 06-02 Task 4 將其(a)用 `git rm` 從 git index 移除 samples/ 版本,(b)物理移動 root 副本到 `.planning/debug/scratch/illustrator-attack-2026-05-28-archived/`(`.gitignore` 已 archived-anchored 屏蔽,移入後不被 track)**」。

**完全不動 `app/**/*.py`、`tests/conftest.py`、`tests/test_*.py`、scratch dir**(這些是 Plan 06-02 的 surface)。
  </action>
  <verify>
    <automated>python -c "import pytest" || pip install "pytest>=8.0"</automated>
    <automated>ls tests/fixtures/cad-glyph/text-glyph-01.pdf tests/fixtures/cad-glyph/text-glyph-01.json tests/fixtures/cad-glyph/figure-glyph-01.pdf tests/fixtures/cad-glyph/figure-glyph-01.json tests/fixtures/cad-glyph/mixed-glyph-01.pdf tests/fixtures/cad-glyph/mixed-glyph-01.json tests/fixtures/cad-glyph/README.md</automated>
    <automated>python -c "import json,pathlib; required={'region_rect_pdf_points','region_rect_px','dpi','page_index','expected_zero_area_count_pre_process'}; missing=[p.name for p in pathlib.Path('tests/fixtures/cad-glyph').glob('*.json') if not required.issubset(set(json.loads(p.read_text(encoding='utf-8')).keys()))]; assert not missing, f'manifests missing required keys: {missing}'; print('manifests ok')"</automated>
    <automated>python -c "import fitz; problems=[]; [problems.append(p) for p in __import__('pathlib').Path('tests/fixtures/cad-glyph').glob('*.pdf') if (lambda d: any(d.metadata.get(k) for k in ('author','producer','title','keywords','subject','creator')))(fitz.open(p))]; assert not problems, f'metadata leak: {problems}'; print('metadata clean')"</automated>
    <automated>grep -c "scripts/sanitize_fixture.py" tests/fixtures/cad-glyph/README.md</automated>
    <automated>grep -c "3013A-13A-C6-XX" .gitignore</automated>
    <automated>grep -c "illustrator-attack-2026-05-28-archived" .gitignore</automated>
    <automated>grep -rn "import fitz" app/</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/cad-glyph/` 含恰好 7 個 entries:3 .pdf + 3 .json + 1 README.md
    - 每個 .json 含 5 個 required keys: `region_rect_pdf_points`, `region_rect_px`, `dpi`, `page_index`, `expected_zero_area_count_pre_process`(可能再含 3 個 optional 元資料)
    - 每個 .pdf 用 fitz 讀後 `doc.metadata` 所有 author/producer/title/keywords/subject/creator 欄皆空字串或 None
    - README.md 包含字串 `scripts/sanitize_fixture.py`(至少 2 處 cross-reference)、`AGPL`(§13 statement)、`TESTCO`(脫敏 marker)、`Immutability` 或繁中等效詞 「不可手動編輯」
    - `.gitignore` 包含 `3013A-13A-C6-XX-` root-anchored 字串(Step C line 1)+ `illustrator-attack-2026-05-28-archived` archived-anchored 字串(Step C line 4 — 對應 checker Blocker #2)
    - 若 Fallback C 觸發(任一 synthetic fixture),README.md Section 2 表格的「original supplier」欄含字串 `synthetic`,且 commit message 含 `PROVISIONAL`,且 STATE.md 新增 1 條 `Phase 6 fixture replenishment` blocker
    - `grep -rn "import fitz" app/` 仍 == `app/services/pdf_engine.py` 一行
    - `git diff --stat HEAD app/` 顯示 0 changed files
    - `python -m pytest --co -q tests/test_redact.py::test_fitz_import_confined_to_engine_seam` 顯示 1 collected(test 仍存在且 collectable;Plan 06-02 會實際跑此 test 驗綠燈,本 task 只確認沒誤刪)
  </acceptance_criteria>
  <done>
3 個 sanitized .pdf + 3 個 sidecar .json + README.md 全部就位、metadata 清乾淨、.gitignore 防護 root-anchored + archived-anchored(Plan 06-02 Task 4 物理移動到 archived 後即無 git track)、AGPL seam 不破、production code 0 動。若任一 fixture synthetic,close-blocking semantics 全部 propagate(README banner + commit msg + STATE.md blocker)。Plan 06-02 可接手 archived 目錄重命名 + `git rm samples/...` + raw PDF 物理移動 + attack regression test + 06-SECURITY.md。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer 機器 → Public Git | 唯一 untrusted-output crossing:raw supplier PDF 是商業機密,sanitize script 必須在 commit 前移除所有可識別 IP。Public repo(AGPL §13 lockfile per memory project_deployment_licensing)= 全世界可讀。 |
| Sanitize script → fitz API | scripts/ 直接 import fitz;不破 AGPL guard scope(`app/**/*.py` only)但仍需正確處理 multi-stream content streams + XMP metadata 避免脫敏不完全。 |
| Sanitize script → app/services/pdf_engine | self-assert 階段 import production helper `count_zero_area_fills_fully_inside`;此 import 不算 production code 改動(call site 在 scripts/,不在 app/)。 |

## STRIDE Threat Register

Phase 6 是 hardening phase,**production code 0 動**,Plan 06-01 不引入新 runtime attack surface;但本 plan 引入的 sanitize script 在 maintainer 機器上跑,若 script 有 bug 可能洩露原供應商 IP 進 public repo,需要明確 mitigation。完整 STRIDE register(含 T-02-07 RE-OPENED + T-06-01)由 Plan 06-02 寫入 `06-SECURITY.md`,本 plan 的 threat 紀錄如下;**`06-SECURITY.md` 是 Phase 6 的 canonical threat-model deliverable**。

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PLAN06-01-01 | I — Info disclosure | scripts/sanitize_fixture.py | mitigate | 4 條 self-assert(metadata 空、supplier name 不出現於 get_text、zero-area count ≥ 0.9 × original、out path 必須在 `tests/fixtures/cad-glyph/`)在 doc.save 前執行;任一失敗 → exit 1 + 不寫 output。XMP 額外清(`set_xml_metadata("")`,per RESEARCH Pitfall 2)。Smoke test(--region-rect "bogus-not-a-rect")驗證 self-assert reachable。 |
| T-PLAN06-01-02 | I — Info disclosure | tests/fixtures/cad-glyph/*.pdf | mitigate | Step A 每個 fixture 跑完 self-assert 通過才會落地;README.md 明列 immutability rule(D-A6 c)避免後續手動編輯繞過 sanitization;`.gitignore` root-anchored + samples-anchored + archived-anchored guard 防止 raw PDF 再次誤 commit / 被移入 archived 後又被 track。 |
| T-PLAN06-01-03 | T — Tampering(integrity)| scripts/sanitize_fixture.py | accept (low — maintainer-trust)| Script 是一次性 dev 工具,跑在 maintainer 機器(同 git push 權限),tamper 攻擊面已被「能 push code 即能改 fixtures」的 trust model 包含。不引入 signed-artifacts CI(過度設計)。 |
| T-PLAN06-01-04 | E — Elevation(AGPL seam break)| scripts/sanitize_fixture.py | mitigate | scripts/ 在 AST guard scope 外(per PATTERNS Shared Pattern S1 + tests/conftest.py:12 precedent);`tests/test_redact.py::test_fitz_import_confined_to_engine_seam` 仍只掃 `app/**/*.py`,本 plan acceptance criteria 跑 `grep -rn "import fitz" app/` 驗證 seam 不破。 |

**Phase 6 整體 attack surface delta:** 0(no production code change)。本 plan 引入的所有 fitz import 均在 scripts/ 與後續 plan 的 tests/,皆在 AGPL guard scope 外。
</threat_model>

<verification>
**Phase 6 hardening invariants(Plan 06-01 段):**

1. **AGPL seam intact:** `grep -rn "import fitz" app/` 必須只回傳 `app/services/pdf_engine.py` 一行(全 phase 不變)。
2. **Production code 0 動:** `git diff --stat HEAD app/` 顯示 0 changed files。
3. **既有 301 passed + 3 skipped baseline 不退步:** 本 plan 不跑 full pytest(test 改動由 Plan 06-02 進行);但 Plan 06-02 verification 會跑 full suite,本 plan 必須確保自己沒誤動 production / tests / conftest。執行 `python -m pytest --co -q 2>&1 | tail -3` 確認 collection count 與 baseline 對齊(301 + 3 + (07-02 之後)3 xfailed = 在本 plan 完成時仍是「301 + 3」未增,因為 attack test 尚未建檔)。
4. **Sanitize script self-asserts 通過:** 每個 fixture .pdf 必須是 sanitize script self-assert pass 的產物(script 設計上若 self-assert fail 即不寫 output;executor 不可手動 bypass)。Task 1 smoke test(`--region-rect "bogus-not-a-rect"`)額外驗證 self-assert 路徑 reachable。
5. **No raw supplier PDF newly tracked:** `git ls-files | grep -E '3013A-13A-C6-XX'` 顯示已 tracked 的 `samples/3013A-...pdf`(由 Plan 06-02 Task 4 用 `git rm` 處理);本 plan 至少不能新增 tracked raw PDF。
6. **README.md 5 區塊到齊:** 上述 Step B 列舉的 5 個 section 必須全在,且包含繁中文案。
7. **若任一 fixture synthetic,close-blocking 紀錄全部 propagate:** README banner、commit msg、STATE.md blocker 三處到位(per Warning #6)。
</verification>

<success_criteria>
- `scripts/sanitize_fixture.py` 存在,`--help` 正確顯示 5 個 args(含 optional `--synthesize`)
- Task 1 self-assert smoke test 通過:`--region-rect "bogus-not-a-rect"` exits non-zero
- `tests/fixtures/cad-glyph/` 含 3 個 sanitized PDF + 3 個 sidecar JSON manifest + 1 個 README.md,共 7 個 entries
- 每個 sanitized PDF 的 metadata 完全清空(author/producer/title/keywords/subject/creator 全空或 None)
- 每個 sidecar JSON 含 5 個 required keys(split-coordinate schema:`region_rect_pdf_points` + `region_rect_px` + `dpi` + `page_index` + `expected_zero_area_count_pre_process`)
- `.gitignore` 包含 root-anchored + samples-anchored + archived-anchored raw supplier PDF guard(3 條 / 1 個 `*-supplier-raw.pdf` future-proof line)
- README.md 文件化 committed-binary 例外的 5 項必要內容(per D-A6),含繁中文案 + cross-references;若 fallback 觸發,Section 2 表格含 `synthetic` 標記
- 若任一 fixture synthetic,Phase 6 close 標 PROVISIONAL,STATE.md 含 fixture replenishment blocker(per Warning #6)
- `app/` 0 改動,`grep -rn "import fitz" app/` 仍只回 `app/services/pdf_engine.py` 一行
- Plan 06-02 開工時可直接 import `from tests.fixtures.cad-glyph` 風格的路徑讀 sidecar manifest 並跑 attack regression test
</success_criteria>

<output>
After completion, create `.planning/phases/06-regression-foundation-threat-model-re-evaluation/06-01-SUMMARY.md` documenting:
- Sanitize script 實作要點(Option B linear recipe — Step 3 brand-glyph strip via update_stream,Step 4 TESTCO inject via Shape API + shape.commit;**無 supplier-name find-replace 主路徑**;CMap fallback 是否觸發)
- 3 fixtures 構成(每個 fixture 來源 = real or synthetic、原供應商名 hash、commit SHA)
- 若任一 fixture synthetic:**頂部 banner** 寫「⚠ Phase 6 fixture 構成: N real + M synthetic — close 條件 PROVISIONAL until 工程師交付全部 3 個 real PDFs 並重跑 sanitize_fixture.py」+ 確認 STATE.md 新增 blocker(per Warning #6)
- Sidecar manifest schema 採 split-coordinate(`region_rect_pdf_points` + `region_rect_px`)— 取代 CONTEXT D-B4 單一 `region_rect`,Phase 7 align(per Warning #8)
- README.md 的 5 個 section 標題
- `.gitignore` 加入的 patterns(含 archived-anchored 一條,對應 Plan 06-02 Task 4 物理移動)
- 任何在實作中發現的 corner case(例:某 supplier PDF 用 CMap encoding 觸發 implementation note B fallback;某 PDF 有 multi-stream content stream → multi-stream write-back pattern 觸發 + 驗證)
- Self-assert smoke test 結果(`--region-rect "bogus-not-a-rect"` exits non-zero — Warning #7 證明)
- 待 Plan 06-02 接手的 known issue:`samples/3013A-...pdf` tracked 副本待 `git rm`、repo root 副本待物理移動到 archived dir(本 plan 已 .gitignore archived-anchored 屏蔽)
</output>
