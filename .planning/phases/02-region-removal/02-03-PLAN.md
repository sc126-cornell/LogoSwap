---
phase: 02-region-removal
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - web/js/regions.js
  - web/js/api.js
  - web/js/app.js
  - web/js/viewer.js
  - web/styles/tokens.css
  - web/styles/app.css
  - web/index.html
autonomous: false
requirements: [REGION-01, REGION-02, REMOVE-04, OUTPUT-01]
tags: [vanilla-js, overlay, region-drawing, side-panel, before-after-toggle, deferred-mutation, cross-page, dual-theme, traditional-chinese, no-build]

must_haves:
  truths:
    - "User can drag on the page to draw one or more rectangles; rectangles may overlap; a wrong one can be deleted or all cleared (no resize handles)"
    - "Each page keeps its own region list; paging swaps which page's regions show in the overlay and the side-panel list"
    - "User can press 套用移除 to apply removal on the server, then toggle 原圖 / 移除結果 to compare before/after on the single page-stage"
    - "User can download the processed PDF (原名_logoswap.pdf) after a fresh result exists"
    - "Region rectangles stay pixel-aligned to the true render box across CSS-scale zoom"
  artifacts:
    - path: "web/js/regions.js"
      provides: "Region overlay (pointer draw in image-pixel space) + per-page region model + side-panel list + before/after toggle controller"
      min_lines: 120
    - path: "web/styles/tokens.css"
      provides: "Appended region/layout tokens (--side-panel-width, --overlay-z, --color-region-*) incl. dark overrides"
      contains: "--color-region-border"
    - path: "web/index.html"
      provides: "Side-panel region-list markup, before/after toggle, apply/download action group (Traditional-Chinese strings)"
      contains: "套用移除"
  key_links:
    - from: "web/js/regions.js"
      to: "web/js/api.js processJob"
      via: "套用移除 -> api.processJob(sid, {dpi, regions}) on the work copy"
      pattern: "processJob"
    - from: "web/js/regions.js"
      to: "#page-frame (viewer render box)"
      via: "overlay is a child of #page-frame, position:absolute inset:0; rects derived from the true render box"
      pattern: "page-frame"
    - from: "web/js/api.js"
      to: "POST /sessions/{id}/process and GET .../result"
      via: "the sole server seam adds processJob / resultImageURL / resultDownloadURL"
      pattern: "/process"
---

<objective>
Close the user-facing loop. Add a transparent region-drawing overlay over the existing Phase-1 page-stage, a per-page region list in the reserved side-panel, a before/after toggle, and apply/download actions — all vanilla JS, no build, reusing the Phase-1 tokens, dual theme, and Traditional-Chinese copy. The user draws rectangles (image-pixel space, deferred-mutation D-05), presses 套用移除 (which POSTs to the Plan 02-02 `/process` endpoint and redacts the work copy server-side), toggles 原圖 / 移除結果 to verify, and downloads `原名_logoswap.pdf`. The browser holds only image-pixel rects; the server (Plan 02-02) is the only place that converts to PDF points and mutates documents.

Purpose: Deliver REGION-01 (draw one/many rectangles, overlap allowed, delete/clear), REGION-02 (per-page lists, cross-page), the UI half of REMOVE-04 (before/after toggle showing the server's 移除結果 render), and the UI of OUTPUT-01 (download). Implements the 02-UI-SPEC contract exactly.
Output: `web/js/regions.js`; appended region tokens + CSS; side-panel/toggle/action-group markup in index.html; `api.js` extended with the process/result seam; `app.js`/`viewer.js` wired so paging swaps per-page regions and the overlay tracks the render box.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-region-removal/02-UI-SPEC.md
@.planning/phases/02-region-removal/02-CONTEXT.md
@.planning/phases/02-region-removal/02-02-SUMMARY.md

<read_first>
- `.planning/phases/02-region-removal/02-UI-SPEC.md` — THE design contract: region rectangle treatment (slate/cool, NOT accent), new tokens to append (`--side-panel-width:320px`, `--overlay-z:10`, `--color-region-*` light + dark), every Traditional-Chinese string (套用移除 / 重新套用 / 下載 PDF / 原圖 / 移除結果 / 區域 {n} / 已框選 {n} 個區域 / 清除全部 / 刪除此區域 / empty + error + stale copy), the action-group accent rule (exactly ONE accent button at a time), the before/after single-page-toggle behavior, per-page scope, stale-result handling, keyboard (Delete/Backspace/Escape), drag threshold (~4px), crosshair cursor.
- `.planning/phases/02-region-removal/02-02-SUMMARY.md` — the exact backend contract to call: `POST /sessions/{id}/process` body `{dpi, regions:[{page, px_rect}]}` -> `{output_filename, page_count, regions:[{page, removed, clamped}]}`; result-render `GET /sessions/{id}/result/pages/{n}/image`; download `GET /sessions/{id}/result`. Use these URLs/shapes verbatim.
- `web/js/viewer.js` — the render-box source of truth: `#page-stage`, `#page-frame` (sized to `renderBox.cssW/H` × zoom in `applyZoom`), `#page-image`, the `state` object (sessionId, pageIndex, pageCount, renderBox), `renderPage(index)`, the `renderToken` generation guard, and the per-page `/meta` fetch. The overlay must mount inside `#page-frame` and re-derive rect positions when `applyZoom` runs; paging (renderPage) must notify regions.js to swap the page's region set. Add a minimal hook (e.g. an exported callback or a custom event the viewer dispatches on page-change + zoom) — do NOT fork the viewer's render logic.
- `web/js/api.js` — the SOLE server seam (`API_BASE`, `ApiError`, `createSession`, `getSession`, `pageImageURL`, `pageMeta`). Add `processJob`, `resultImageURL`, `resultDownloadURL` here ONLY; no other web file may contact the server (threat T-02-12).
- `web/js/app.js` — the four-state stage machine (empty/uploading/loaded/error), `initViewer` call on upload success, `resetViewer` on replace, the verbatim-copy error mapping. Wire region UI activation to the `loaded` state; reset region state on new upload.
- `web/index.html` — `lang="zh-Hant"` shell: `.toolbar` (with `[data-doc-control]` clusters + `.toolbar__spacer` + `#replace-file` + `#theme-toggle`), `<main class="main">` = `[#page-stage 1fr | aside#side-panel(aria-hidden, collapsed)]`, the `.state--loaded` block containing `#page-frame > #page-image`. Expand the side-panel, add the toggle + action group, add the region-list markup.
- `web/styles/tokens.css` — append the new tokens to the existing `:root` and `[data-theme="dark"]` blocks (do NOT create a new file; do NOT touch existing tokens). Note: `--color-scrim` already exists from Phase 1 (reused by the clear-all dialog).
- `web/styles/app.css` — append region/overlay/side-panel/toggle/action-group styles using `var()` tokens only (no raw theme hexes — matches the Phase-1 discipline); reuse the existing `.error-block`, spinner, `--control-hit` 40px, focus-ring.
</read_first>

<interfaces>
<!-- Backend endpoints to call (from 02-02-SUMMARY — cite it for the FINAL exact shapes): -->
#   POST {API_BASE}/sessions/{id}/process    body: {dpi:int, regions:[{page:int, px_rect:[x0,y0,x1,y1]}]}
#                                            -> 200 {output_filename, page_count, regions:[{page, removed, clamped}]}
#                                            -> 4xx {detail:{code,message}}  (codes incl. session_not_found, redaction failure)
#   GET  {API_BASE}/sessions/{id}/result/pages/{n}/image   -> image/png (the 移除結果 after-image; n 0-based)
#   GET  {API_BASE}/sessions/{id}/result                   -> application/pdf attachment (原名_logoswap.pdf); 404 result_not_ready before a run

<!-- Existing api.js seam to extend (web/js/api.js): -->
export const API_BASE; export class ApiError; export async function createSession(file);
export async function getSession(id); export function pageImageURL(id,n,dpi); export async function pageMeta(id,n);

<!-- Existing viewer.js seam (web/js/viewer.js) the overlay attaches to: -->
#   #page-frame  — position:relative wrapper sized to the displayed render box (applyZoom sets width/height)
#   state.pageIndex (0-based), state.pageCount, state.sessionId, state.renderBox {cssW,cssH}
#   renderPage(index) — call site that changes the page; export initViewer({session_id,page_count}) / resetViewer()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Region tokens + side-panel / toggle / action-group markup + the api.js process seam</name>
  <files>web/styles/tokens.css, web/styles/app.css, web/index.html, web/js/api.js</files>
  <read_first>
    - .planning/phases/02-region-removal/02-UI-SPEC.md (New tokens block; Component Inventory; Copywriting Contract — copy every string verbatim; accent reserved-for rule)
    - web/index.html (the shell to extend — side-panel is aria-hidden/collapsed; toolbar has the spacer + theme toggle)
    - web/styles/tokens.css + web/styles/app.css (append-only; var()-token discipline)
    - web/js/api.js (the sole seam — add the three functions, mirror the existing ApiError handling)
    - .planning/phases/02-region-removal/02-02-SUMMARY.md (exact endpoint URLs/shapes)
  </read_first>
  <action>
    Append to `tokens.css` exactly the UI-SPEC "New tokens" block: on `:root` add `--side-panel-width: 320px` and `--overlay-z: 10` plus the LIGHT region colors (`--color-region-border` #475569, `--color-region-border-strong` #1E293B, `--color-region-fill` rgba(71,85,105,0.14), `--color-region-fill-active` rgba(71,85,105,0.10)); add the DARK overrides under `[data-theme="dark"]` (#CBD5E1 / #E2E8F0 / rgba(203,213,225,0.16) / 0.12). Do not modify existing tokens.

    Edit `index.html` (keep `lang="zh-Hant"`):
      - Expand the side-panel: give `aside#side-panel` a header (heading 框選區域 at `--font-size-heading`, the count `已框選 {n} 個區域`, and a 清除全部 destructive text button), the per-page scope note `目前顯示第 {current} 頁的框選`, an empty-state block (heading 尚未框選任何區域 + the teaching body string), and a scrollable `<ul>` region list container. Mark it `aria-live="polite"` for the count/status.
      - Add a before/after segmented toggle (原圖 / 移除結果, aria-label 切換移除前後對照) in the toolbar doc-control area, hidden until loaded.
      - Add the action group (套用移除 primary + 下載 PDF) — exactly ONE accent button visible per state per the UI-SPEC; include the disabled-state hints and a small status line (aria-live) near it.
      - Add a clear-all confirm dialog (using the existing Phase-1 `--color-scrim` backdrop) with 取消 / 清除全部 (danger) buttons.
      - All copy from the UI-SPEC table verbatim; icon-only buttons get `aria-label`s; dynamic values are placeholders to be filled by regions.js via textContent.

    Append to `app.css` (var() tokens only): the side-panel expanded layout (`main` grid becomes `1fr var(--side-panel-width)` when a doc is loaded, `1fr 0` otherwise — drive via a class/attribute on the shell toggled by app.js), region-row styling (`--space-sm` padding, `--color-border` divider, `--color-neutral-hover` row hover, delete icon `--color-danger` on hover), the overlay host rules (`.region-overlay { position:absolute; inset:0; z-index:var(--overlay-z); cursor:crosshair }`), committed/drawing/hovered rectangle treatments per the UI-SPEC table (using `var(--color-region-border)` / `var(--color-region-fill)` etc.), the segmented toggle (selected segment = `--color-neutral-hover`, NOT accent), and the action-group/dialog styling. No raw theme hexes.

    Extend `web/js/api.js` with: `processJob(id, jobSpec)` (POST JSON to `/sessions/{id}/process`, throw `ApiError` from a non-2xx body exactly like `createSession`), `resultImageURL(id, n)` (string for the after-image `<img src>`), and `resultDownloadURL(id)` (the `/result` URL for the download link). No other file may reference `API_BASE`.
  </action>
  <verify>
    <automated>node --check web/js/api.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check web/js/api.js` passes; `api.js` exports `processJob`, `resultImageURL`, `resultDownloadURL` and references `/process` + `/result` (grep finds them).
    - `tokens.css` contains `--side-panel-width`, `--overlay-z`, `--color-region-border` (light) AND a `[data-theme="dark"]` override for `--color-region-border` (grep both blocks).
    - `index.html` contains the verbatim strings 套用移除, 下載 PDF, 原圖, 移除結果, 清除全部, 框選區域, 尚未框選任何區域 (grep each).
    - `app.css` styles the region rectangle via `var(--color-region-border` and the overlay via `var(--overlay-z` — no raw `#475569`/`#CBD5E1` hex in app.css (grep returns nothing for those hexes).
    - No `innerHTML` introduced in api.js (grep returns nothing).
  </acceptance_criteria>
  <done>The region tokens, side-panel/toggle/action-group markup (all verbatim Traditional-Chinese), and the api.js process/result seam exist; styling uses tokens only; the seam is parseable and additive.</done>
</task>

<task type="auto">
  <name>Task 2: regions.js — overlay drawing, per-page model, side-panel list, render-box alignment</name>
  <files>web/js/regions.js, web/js/viewer.js, web/js/app.js</files>
  <read_first>
    - web/js/viewer.js (the render-box + paging seam: #page-frame, applyZoom, renderPage, state.renderBox/pageIndex/pageCount; add the page-change + zoom hook here)
    - web/js/app.js (loaded-state activation + reset-on-new-upload)
    - web/index.html (the side-panel list + overlay host markup from Task 1)
    - .planning/phases/02-region-removal/02-UI-SPEC.md (drawing behavior: ~4px threshold, crosshair, no handles, overlap allowed; per-page scope; region row + delete + clear-all + confirm; keyboard Delete/Backspace/Escape; bidirectional row↔rect hover; region-number badge)
  </read_first>
  <action>
    Create `web/js/regions.js` owning the region model + overlay + side-panel list:
      - Model: a per-page map `pageIndex -> [{ id, pxRect:[x0,y0,x1,y1] }]`. Rectangles are stored in IMAGE-pixel space at the render DPI (deferred-mutation D-05) — derive image pixels from the overlay-local pointer coordinates by dividing by the current zoom factor and multiplying by `devicePixelRatio` so the stored rect matches the server's pixel space regardless of CSS zoom (mirror viewer.js's `renderBox` math: displayed CSS px = image px / dpr; so image px = displayed CSS px × dpr). Keep the rect anchored to the true render box, never the viewport.
      - Overlay: create a `.region-overlay` child of `#page-frame` (position:absolute inset:0, z-index var(--overlay-z), crosshair). Pointerdown→move→up draws a rubber-band rectangle (dashed while dragging, solid on commit); a sub-~4px drag is ignored (no zero-area region); Escape cancels an in-progress drag. Overlap is allowed (no collision rejection). NO resize/move handles (D-01). Render committed rectangles as absolutely-positioned `<div>`s positioned by converting the stored image-pixel rect back to displayed CSS px at the current zoom (so they stay pinned across zoom).
      - Side-panel list: render one row per region on the CURRENT page (label 區域 {n}, ordinal, tabular-nums; a 刪除此區域 icon button). Update the count `已框選 {n} 個區域` and the scope note `目前顯示第 {current} 頁的框選`. Empty page -> show the empty-state block. Hovering a row highlights the matching rectangle (border-strong + ordinal badge) and vice-versa (bidirectional). Delete a single region immediately (no confirm). 清除全部 opens the confirm dialog (取消 / 清除全部 danger) scoped to the CURRENT page only.
      - Keyboard: Delete/Backspace removes the focused region (row or rect focused); region rows are Tab-reachable with the focus ring; do not hijack the jump input/buttons.
      - Re-projection hooks: subscribe to the viewer's page-change + zoom signals. On page-change, swap the overlay + list to the new page's regions and clear any draw-in-progress. On zoom (applyZoom), re-position all rectangle `<div>`s from their stored image-pixel rects (the source of truth never changes — only the displayed projection). Expose `initRegions({sessionId, pageCount})`, `resetRegions()`, and getters the action group uses (`getJobRegions()` -> the flat `[{page, px_rect}]` payload, `getTotalRegionCount()`).

    Edit `web/js/viewer.js` minimally: after a successful page render and inside `applyZoom`, emit a signal regions.js subscribes to (a CustomEvent on `#page-stage` like `page:changed` {index} and `page:zoomed`, OR call exported callbacks set by regions.js). Do NOT duplicate the render/zoom logic. Keep the `renderToken` guard authoritative.

    Edit `web/js/app.js`: on upload success call `initRegions(...)` alongside `initViewer(...)`, expand the side-panel (toggle the shell grid class so it becomes `1fr var(--side-panel-width)`), and on `更換檔案`/new upload call `resetRegions()` + collapse the panel. Region UI is active only in the `loaded` state.
  </action>
  <verify>
    <automated>node --check web/js/regions.js && node --check web/js/viewer.js && node --check web/js/app.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check` passes on regions.js, viewer.js, app.js.
    - `regions.js` references `#page-frame` / `page-frame`, `var(--overlay-z)` or the overlay class, and stores per-page rects in image-pixel space (grep finds a per-page map keyed by page index and a `devicePixelRatio` conversion).
    - `regions.js` uses `textContent`/`createElement` for all dynamic strings; grep returns NO `innerHTML` in regions.js.
    - `regions.js` does NOT reference `API_BASE`, `fetch`, or any server URL (server contact only via api.js — T-02-12); grep confirms.
    - `viewer.js` change is additive (a page-change/zoom signal); grep finds the new event/callback name and the file still defines `renderPage` + `applyZoom`.
  </acceptance_criteria>
  <done>Drawing, per-page region lists (delete one / clear all / overlap allowed / no handles), bidirectional row↔rect highlight, keyboard delete/escape, and zoom-stable rectangle re-projection all work; regions are stored in image-pixel space; the viewer hook is additive; no web file but api.js contacts the server.</done>
</task>

<task type="auto">
  <name>Task 3: Apply / before-after toggle / download wiring + stale-result state machine</name>
  <files>web/js/regions.js, web/js/app.js</files>
  <read_first>
    - web/js/regions.js (the model + getJobRegions getter from Task 2 — the action group reads it)
    - web/js/api.js (processJob / resultImageURL / resultDownloadURL from Task 1)
    - web/js/viewer.js (#page-image src + the page-change signal — the before/after toggle swaps the displayed src)
    - .planning/phases/02-region-removal/02-UI-SPEC.md (action-group states: no-regions/ready/applying/result-ready/stale; ONE accent at a time; before/after single-page toggle hides the overlay in 移除結果; stale notice + 重新套用; all status/error copy verbatim)
    - .planning/phases/02-region-removal/02-02-SUMMARY.md (process response shape incl. per-region removed/clamped flags; result_not_ready)
  </read_first>
  <action>
    Wire the action group + before/after toggle in `regions.js` (and `app.js` for state activation):
      - 套用移除: gather `getJobRegions()` + the current render DPI (the server default 200, sourced the same way viewer.js gets it — do not invent a per-zoom DPI; the rects are in 200-DPI image pixels), call `api.processJob(sessionId, {dpi, regions})`. Show the busy state (正在套用移除…) on the stage/button; on success store a "result fresh" flag, surface 移除結果已就緒… , flip the action group so 下載 PDF becomes the single accent button and 套用移除 demotes to neutral 重新套用. If the response flags a region `removed:false` everywhere (nothing removable), show the 框選區域內沒有可移除的內容 notice (non-blocking, reuse `.error-block`). If a region was `clamped`, show the 框選超出頁面範圍,已自動調整… inline notice.
      - Disabled/empty: with 0 regions on the document, 套用移除 is disabled with the 先框選至少一個區域… tooltip; the count/status reflect it.
      - Before/after toggle (原圖 / 移除結果): switching to 移除結果 sets `#page-image.src = api.resultImageURL(sessionId, currentPage)` and HIDES the overlay rectangles (UI-SPEC: result mode hides overlay); switching back to 原圖 restores `pageImageURL` and re-shows the overlay. If 移除結果 is selected with no fresh result, show 尚無移除結果,請先套用移除 and stay on 原圖. The toggle follows page-changes (re-fetch the correct page's after-image when paging while in result mode).
      - Stale handling: any region edit (draw/delete/clear) after a result exists clears the "fresh" flag, shows 框選已變更,請重新套用以更新結果, switches the accent back to 重新套用, and disables 下載 PDF until re-applied (download only against a fresh result — D-05/D-07 safety).
      - 下載 PDF: navigate/anchor to `api.resultDownloadURL(sessionId)` (browser handles the attachment); show 正在準備下載… transient status; on a `result_not_ready`/error surface the 下載失敗… copy with 重試. Map any `ApiError` to the verbatim UI-SPEC error copy by code (removal failed / result-render failed / download failed) — never show a raw server message (T-02-11).
      - Reset on new upload (via app.js): clear the fresh flag, reset the toggle to 原圖, hide the action group until loaded.
  </action>
  <verify>
    <automated>node --check web/js/regions.js && node --check web/js/app.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check` passes on regions.js and app.js.
    - `regions.js` calls `api.processJob`, builds `#page-image.src` from `api.resultImageURL` for the 移除結果 view, and uses `api.resultDownloadURL` for download (grep finds all three).
    - Stale logic present: editing regions after a result clears a freshness flag and re-shows 重新套用 + the stale notice (grep finds the stale copy `框選已變更` and a freshness flag toggle).
    - Error copy mapped by code (grep finds the verbatim 套用移除時發生問題 / 下載失敗 strings) and NO raw server message is injected as HTML (grep: no `innerHTML`; server message not concatenated into the DOM).
    - Exactly-one-accent invariant respected in the state transitions (apply demotes to neutral 重新套用 when 下載 PDF is the accent) — verifiable by the human checkpoint below.
  </acceptance_criteria>
  <done>套用移除 redacts via the server, the before/after toggle swaps the single page-stage image (overlay hidden in 移除結果), download fetches 原名_logoswap.pdf, editing after a result marks it stale and requires 重新套用, and all status/error copy is the verbatim UI-SPEC Traditional-Chinese — one accent button at a time.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify the region-removal UI (visual + before/after + rotated-page placement)</name>
  <action>
    This is a human-verification checkpoint, not an automated implementation step. After Tasks 1–3 land and the backend (Plans 02-01/02-02) is verified green, run the server and walk the <how-to-verify> script below to confirm the human-facing visual/interaction contract (region drawing, per-page scope, before/after correctness incl. a rotated page, accent discipline, true-removal in the downloaded PDF). Pause for the user's resume signal.
  </action>
  <what-built>
    The full Phase-2 region-removal UI on top of the running backend: draw rectangles on the page, per-page region list (delete one / clear all / overlap), 套用移除 (server true-removal on the work copy), 原圖 / 移除結果 before/after toggle, and 下載 PDF (原名_logoswap.pdf). Backend (Plan 02-02) and coords (Plan 02-01) are already verified by automated tests; this checkpoint verifies the human-facing visual/interaction contract that automated checks cannot (UI-SPEC defaults flagged "confirm later", accent discipline, before/after correctness on a real document, rotated-page placement).
  </what-built>
  <how-to-verify>
    1. Start the server: `./.venv/Scripts/python -m uvicorn app.main:app --reload` and open http://127.0.0.1:8000/ .
    2. Upload a vector PDF that has a supplier logo / text (ideally include one ROTATED-page PDF and one multi-page PDF). The page renders as in Phase 1.
    3. Drag a rectangle over the logo/text. Confirm: a slate (NOT blue/amber accent) translucent rectangle appears; the side-panel shows 區域 1 and 已框選 1 個區域; drawing a second overlapping rectangle is allowed; a sub-tiny click makes no region; crosshair cursor over the page.
    4. Hover the list row -> the matching rectangle highlights (border-strong + ordinal badge) and vice-versa. Delete one region (immediate, no confirm). 清除全部 -> confirm dialog (取消 / 清除全部) scoped to the current page only.
    5. Page to another page -> the first page's regions are gone from the overlay+list (per-page scope, REGION-02); draw a region there; page back -> the original regions return. Zoom in/out -> rectangles stay pinned over the same content (render-box alignment).
    6. Press 套用移除. Confirm the busy text, then 移除結果已就緒… ; 下載 PDF becomes the single accent button and 套用移除 becomes neutral 重新套用 (exactly ONE accent button).
    7. Toggle 移除結果 -> the page shows the content GONE (overlay hidden); toggle 原圖 -> content + overlay return. On a rotated page, confirm the removal landed exactly on the boxed area (REMOVE-03 visual proof).
    8. Edit a region (draw/delete) -> the result goes stale (框選已變更,請重新套用…), 下載 PDF disabled until 重新套用.
    9. Re-apply, then 下載 PDF -> a PDF named like `<原名>_logoswap.pdf` downloads; open it: the removed area extracts NO text (Ctrl+A/Ctrl+C over it yields nothing) and ALL pages are present (D-07).
    10. Confirm dark mode (theme toggle) keeps region rectangles visible and the accent discipline intact.
  </how-to-verify>
  <resume-signal>Type "approved" if the visual/interaction contract holds (incl. rotated-page placement and true removal in the downloaded PDF), or describe the issues to fix.</resume-signal>
  <done>The user has visually confirmed (and typed "approved") that region drawing, per-page scope, before/after toggle, rotated-page placement, stale handling, dark-mode visibility, and a downloaded 原名_logoswap.pdf with truly-removed (non-extractable) content all hold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser overlay → api.js → server | The drawn rectangles (image-pixel) are assembled client-side and POSTed via the single api.js seam to `/process`. The client is untrusted; the server (Plan 02-02) re-validates and clamps. |
| server messages → DOM | Server error `detail.message` and the result image must render without becoming an XSS/HTML-injection vector. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Information disclosure / Tampering (XSS) | Server `detail.message`, filenames, region labels written into the side-panel/status DOM | mitigate | All dynamic text via `textContent`/`createElement` (never `innerHTML`); errors mapped to FIXED UI-SPEC copy by `detail.code`, the raw server message is not injected as HTML (mirrors the Phase-1 T-01-14/T-01-15 discipline). Grep gate: no `innerHTML` in regions.js/app.js additions. |
| T-02-12 | Tampering / SSRF-shape | A web module hardcoding a server URL bypassing the configurable embedding seam | mitigate | Only `web/js/api.js` references `API_BASE` / contacts the server (`processJob`/`resultImageURL`/`resultDownloadURL`); regions.js builds image/download URLs only via those api.js helpers. Grep gate: regions.js has no `fetch`/`API_BASE`. |
| T-02-13 | Tampering / DoS | Client sending a huge region list or out-of-bounds rects | mitigate | The client caps drawing to sane interaction, but the AUTHORITATIVE guard is server-side (Plan 02-02 `MAX_REGIONS` + `clamp_px_rect`); the UI surfaces the server's `clamped` flag (框選超出頁面範圍…) and never assumes a region was applied without the server's per-region result. |
| T-02-14 | Spoofing / Confused-deputy | Result image / download fetched for the wrong session | mitigate | `sessionId` comes only from the in-memory viewer state established at upload (server-issued token); all result URLs are built from it via api.js; no cross-session id is constructible from the UI. |
</threat_model>

<verification>
- `node --check` passes on all four JS modules (api.js, regions.js, viewer.js, app.js).
- Grep gates: api.js exports the three new seam functions; tokens.css has the region tokens (light + dark); index.html has the verbatim Traditional-Chinese strings; app.css styles rectangles via region tokens with no raw region hexes; no `innerHTML` in the new JS; regions.js never references `API_BASE`/`fetch`.
- Human checkpoint confirms: slate (non-accent) rectangles, overlap allowed, no handles, per-page scope, zoom-stable alignment, one-accent-at-a-time, before/after correctness incl. a rotated page, stale handling, and a downloaded `原名_logoswap.pdf` whose removed area extracts no text and keeps all pages.
</verification>

<success_criteria>
- User can draw one/many overlapping rectangles, delete one, and clear all — no resize handles (REGION-01).
- Each page keeps its own region list; paging swaps the overlay + list (REGION-02).
- 套用移除 runs server removal on the work copy; the 原圖 / 移除結果 single-page toggle compares before/after, overlay hidden in result mode (REMOVE-04 UI half).
- 下載 PDF downloads `原名_logoswap.pdf` after a fresh result; editing marks it stale until re-applied (OUTPUT-01 UI; D-05/D-07).
- Region rectangles stay pixel-aligned to the true render box across CSS zoom; the entire UI uses Phase-1 tokens, dual theme, and verbatim Traditional-Chinese copy.
</success_criteria>

<output>
After completion, create `.planning/phases/02-region-removal/02-03-SUMMARY.md`. Record: the regions.js public API (`initRegions`/`resetRegions`/`getJobRegions`/etc.), the viewer.js hook added (event/callback names), the api.js seam additions, the action-group state machine + stale logic, and confirmation of the human-verify checkpoint outcome (rotated-page placement + true-removal-in-download).
</output>
