---
phase: 05-ubuntu
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - app/config.py
  - app/storage.py
  - app/services/integrity.py
  - app/services/janitor.py
  - app/services/ingest.py
  - app/services/pipeline.py
  - app/api/sessions.py
  - app/api/process.py
  - app/main.py
  - web/js/app.js
  - web/styles/app.css
  - tests/test_storage.py
  - tests/test_ingest.py
  - tests/test_process_api.py
  - tests/test_integrity.py
  - tests/test_janitor.py
  - tests/test_health.py
autonomous: true
requirements:
  - SC-2  # 大型或含旋轉頁面 PDF 不崩潰(DPI cap + workers + 60s timeout)
  - SC-3  # 暫存檔處理後清理 + 原始檔雜湊驗證
  - D-B1  # 同步觸發 janitor(三點:startup / sessions POST 結尾 / process 結尾)
  - D-B2  # TTL 1 小時硬上限
  - D-B3  # 4-kind 一起刪
  - D-B4  # race 防護:rmtree try/except + TTL ≫ process timeout
  - D-C1  # original_sha256 存 meta.json,atomic 寫入
  - D-C2  # 每次 /process 前驗證
  - D-C3  # 失敗 → 503 + structured log + .corrupted sentinel
  - D-C4  # 只驗 originals/,不驗 pristine/
  - D-C5  # 新測試覆蓋驗證機制本身
  - D-D1  # /process 維持同步(不上 BackgroundTasks)
  - D-D2  # workers default 2(Plan 05-01 已加 env;此 plan 不重複)
  - D-D3  # /process 60s timeout(asyncio.wait_for + to_thread)
must_haves:
  truths:
    - "上傳一個 PDF 後,`work/{sid}/meta.json` 含 `original_sha256` 欄位,值為 64-char hex,等於上傳 bytes 的 SHA-256"
    - "若有人手動竄改 `originals/{sid}/source.pdf` 後再呼叫 /process,server 回 503 `original_tampered` + 寫 structured log + 寫 `work/{sid}/.corrupted` sentinel + 同 session 後續 /process 全部回 410 `session_corrupted`"
    - "建立一個 session 並把 4-kind dir 的 mtime 倒撥到 3700 秒前,呼叫 `janitor.sweep_expired_sessions()` 後 4 kind dir 完全消失;若 mtime 在 1h 內則完全保留"
    - "/process 觸發點呼叫 `pipeline.process_job`(內含 PyMuPDF 工作)若超過 `PROCESS_TIMEOUT_SECONDS`(預設 60s)未回,HTTP 回 504 `processing_timeout`;後台 thread 繼續跑(Python 無法 kill thread,但 workers=2 確保預覽不被卡)"
    - "Legacy session(Phase 1–4 留下、無 `original_sha256` 欄位的 meta.json)在 Phase 5 部署後呼叫 /process 會被視為 corrupted → 410 `session_corrupted`,1h TTL 內自然過期不影響使用者"
    - "前端 `web/js/app.js` 對三個新 server 錯誤碼(`original_tampered` / `session_corrupted` / `processing_timeout`)有對應繁中 UI 訊息;沿用既有 ApiError pathway,無新 UI scaffolding"
    - "上傳成功後 UI 顯示『此次處理 1 小時內完成下載 — 逾時需重新上傳』繁中提示(D-B2 session TTL 對使用者揭露);提示以 `aria-live="polite"` 區域動態插入,沿用雙主題 token + 繁中,失效時(GET /sessions/{id} 回 404)替換為『此次處理已過期,請重新上傳此檔』"
    - "app startup / POST /sessions 結尾 / POST /process 結尾 三處皆呼叫 `janitor.sweep_expired_sessions()`,且 janitor 失敗 try/except 包住絕不污染 response(/process 與 /sessions 仍 200)"
  artifacts:
    - path: "app/config.py"
      provides: "Phase 5 後三個常數:SESSION_TTL_SECONDS / PROCESS_TIMEOUT_SECONDS / CORS_ALLOW_ORIGINS(05-01 已加 UVICORN_WORKERS + APP_BASE_PATH)"
      contains: "SESSION_TTL_SECONDS"
    - path: "app/services/integrity.py"
      provides: "新模組:IntegrityError(code, message)+ compute_original_hash(bytes)→ hex + verify_original_hash(sid)→ 失敗 raise + 寫 .corrupted sentinel + structured log;無 fitz import"
      exports: ["IntegrityError", "compute_original_hash", "verify_original_hash"]
    - path: "app/services/janitor.py"
      provides: "新模組:sweep_expired_sessions(now?)→ int + _on_rm_error handler(cross-platform Windows chmod 0o444 fix)+ _safe_rmtree + _session_max_mtime;無 fitz import"
      exports: ["sweep_expired_sessions"]
    - path: "app/storage.py"
      provides: "atomic write_session_meta(用 tempfile + os.replace)+ 新增 list_session_ids / session_age_seconds / delete_session / mark_session_corrupted / is_session_corrupted 五個 helper;全部 route 走 subdir() 路徑防護"
      contains: "mark_session_corrupted"
    - path: "app/services/ingest.py"
      provides: "在 _ingest_pdf / _ingest_image_to_pdf 寫 originals 後 compute_original_hash 並寫進 meta.json(D-C1 baseline 與 write 同 transaction)"
    - path: "app/services/pipeline.py"
      provides: "process_job 開頭呼叫 integrity.verify_original_hash(sid)→ IntegrityError → re-raise as PipelineError(code, msg)route 到 main._PROCESS_STATUS"
    - path: "app/api/process.py"
      provides: "處理流程:(1) is_session_corrupted 短路 → 410;(2) asyncio.wait_for(asyncio.to_thread(process_job), timeout=PROCESS_TIMEOUT_SECONDS)包住;TimeoutError → 504 processing_timeout;(3) finally janitor.sweep(try/except)"
    - path: "app/api/sessions.py"
      provides: "POST /sessions 結尾 finally janitor.sweep(try/except)"
    - path: "app/main.py"
      provides: "lifespan startup 跑 janitor.sweep + 擴充 _PROCESS_STATUS 加三個新 code 到 status code mapping(original_tampered:503 / session_corrupted:410 / processing_timeout:504)"
    - path: "web/js/app.js"
      provides: "COPY 表新增三個繁中錯誤訊息 + messageForError switch 新增三個 case;新增 session TTL 上傳成功動態提示(D-B2)+ 404 過期友善訊息渲染(aria-live polite)"
    - path: "tests/test_integrity.py"
      provides: "compute_original_hash 等同 hashlib + verify_original_hash 三條路徑(pass / tampered / legacy missing field)+ AGPL seam 守門(integrity 模組無 fitz import)"
    - path: "tests/test_janitor.py"
      provides: "sweep TTL / max-mtime / chmod 0o444 cross-platform / race / non-token-dir / lifespan + endpoint 觸發點 / failure-does-not-taint(7+ 條測試)"
    - path: "tests/test_health.py"
      provides: "health 五欄位 + 多 session count + unreadable -1 + 不洩漏 session_id"
  key_links:
    - from: "app/services/ingest.py 寫 originals 後"
      to: "storage.write_session_meta(... original_sha256=compute_original_hash(data))"
      via: "atomic baseline 寫入"
      pattern: "original_sha256=compute_original_hash"
    - from: "app/services/pipeline.process_job 入口"
      to: "integrity.verify_original_hash(session_id)"
      via: "前置驗證 → IntegrityError → re-raise PipelineError"
      pattern: "verify_original_hash\\(session_id\\)"
    - from: "app/api/process.process_session"
      to: "asyncio.wait_for(asyncio.to_thread(pipeline.process_job, ...), timeout=config.PROCESS_TIMEOUT_SECONDS)"
      via: "stdlib timeout enforcement(Pitfall 1 — thread 不會被 kill,僅回 504)"
      pattern: "asyncio\\.wait_for\\(asyncio\\.to_thread"
    - from: "app/api/process.process_session"
      to: "storage.is_session_corrupted(sid)"
      via: "前置短路 410"
      pattern: "is_session_corrupted"
    - from: "app/main.py lifespan"
      to: "janitor.sweep_expired_sessions()"
      via: "startup hook(D-B1 trigger a)"
      pattern: "sweep_expired_sessions"
    - from: "app/api/sessions.py POST 結尾"
      to: "janitor.sweep_expired_sessions()"
      via: "finally try/except 包住"
      pattern: "janitor\\.sweep_expired_sessions"
    - from: "web/js/app.js messageForError"
      to: "三個新 case(original_tampered / session_corrupted / processing_timeout)"
      via: "ApiError.code → 繁中 message mapping"
      pattern: "original_tampered"
    - from: "web/js/app.js 上傳成功後(POST /sessions 201 之後)"
      to: "page-stage 區動態插入 TTL 提示節點"
      via: "DOM textContent + aria-live="polite";D-B2 + RESEARCH Pattern 9 文案"
      pattern: "此次處理 1 小時內完成下載"
    - from: "web/js/app.js 偵測 GET /sessions/{id} 404"
      to: "TTL 提示節點切換為『此次處理已過期』訊息"
      via: "session-expired 友善訊息;沿用 ApiError pathway 過 404"
      pattern: "此次處理已過期"
---

<objective>
Phase 5 Plan 02 — **穩固化 slice(hardening)**。在 Plan 05-01 既有的可部署 image 上,把「正確性穩固化」整套裝起來:`originals/` SHA-256 baseline + verify、`work/{sid}/.corrupted` 防覆用、`/process` 60s timeout、暫存 janitor(三點同步觸發 + 1h TTL + Windows chmod 0o444 cross-platform fix)、前端對應錯誤訊息。Phase 5 success criteria #2 + #3 在此 slice 完成。

整個 slice 為 **一條垂直的「/process 對 50MB 大檔 + 旋轉頁不會 hang 不會崩潰、原始檔被竄改抓得到、暫存自動清」**。SHA-256 / janitor / timeout / corrupted sentinel 不是三個獨立 plan 而是同一片完整功能 — 拆開會破壞「使用者一次完整 /process」的端到端故事。

Purpose: 確保 Phase 5 部署後 production 不出現「worker hang 30 分鐘」、「同事不小心換掉 originals/」、「磁碟塞滿 sessions」這三種失敗情境。
Output: 7 修改 + 4 新檔 + 3 新測試檔。13 個 artifacts。AGPL seam 維持(integrity / janitor 全為 stdlib,無 fitz)。
</objective>

## Phase Goal

**As a** LogoSwap 內部使用者(貿易公司同事),**I want to** 上傳大型或旋轉頁面 PDF 並完成移除/置入流程,即便伺服器同時有其他任務、即便我或他人不小心動到原始檔,**so that** 系統能在 60 秒內給我明確回應、保護原始檔不被靜默竄改、且不會因暫存累積而磁碟塞滿。

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ubuntu/05-CONTEXT.md
@.planning/phases/05-ubuntu/05-RESEARCH.md
@.planning/phases/05-ubuntu/05-PATTERNS.md
@.planning/phases/04-raster-image-support/04-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/phases/05-ubuntu/05-01-PLAN.md
@app/config.py
@app/storage.py
@app/main.py
@app/services/ingest.py
@app/services/pipeline.py
@app/services/logo.py
@app/api/sessions.py
@app/api/process.py
@web/js/app.js
@tests/conftest.py
@tests/test_storage.py
@tests/test_ingest.py
@tests/test_process_api.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: config + storage helpers + integrity 模組(基礎設施:常數 + atomic meta + SHA-256 算/驗 + sentinel)</name>
  <files>app/config.py, app/storage.py, app/services/integrity.py, tests/test_storage.py, tests/test_integrity.py</files>
  <read_first>
    - `app/config.py`(既有 `_env_int` 行 19–48 + 既有常數 area 行 50–84;Plan 05-01 已加 `UVICORN_WORKERS` + `APP_BASE_PATH`,本 task 接續往下加三個)
    - `app/storage.py` 全檔(尤其行 47–53 `InvalidSessionId`、行 80–101 `subdir`、行 167–195 `write_session_meta` / `read_session_meta`、行 240–251 `session_exists`)
    - `app/services/logo.py` 行 1–60(模組 docstring + 安全屬性聲明 + typed Error 範本 — 為新 `IntegrityError` 的 analog)
    - `app/services/redact.py` 行 1–50(AGPL seam / purity 模組頂部聲明範本)
    - `app/services/pipeline.py` 行 107–137(work/pristine 路徑解析 + 結構性 guards;Task 2 會在此插入 verify_original_hash 呼叫,本 task 先讀以理解 PipelineError 介面)
    - `tests/test_storage.py` 行 13–143(既有測試 pattern + autouse `isolated_data_dir` fixture)
    - `tests/conftest.py`(觀察 `isolated_data_dir` / `valid_pdf_bytes` / `client` fixture 形狀)
    - `tests/test_ingest.py` 行 26–30(既有 SHA-256 assertion 範例;本 task 新測試以此為模板)
    - `.planning/phases/05-ubuntu/05-RESEARCH.md` Pattern 5(atomic meta 寫入)+ Pattern 6(integrity 模組範例;行 620–711)
    - `.planning/phases/05-ubuntu/05-PATTERNS.md` §"app/config.py" / §"app/storage.py" / §"app/services/integrity.py" / §"tests/test_storage.py" / §"tests/test_integrity.py"(每處精確行範圍 + 範例)+ Pitfall Index(Pitfall 3:Windows chmod 0o444 + rmtree;Pitfall 4:legacy session 無欄位;A7:tempfile.mkstemp dir= 強制同 FS)
  </read_first>
  <behavior>
    **TDD cycle(D-C5 鎖定「測試從『程式碼自然成立』改為『驗證機制本身有作用』」):**

    紅(寫測試,RUN 失敗)→ 綠(寫實作至最小通過)→ 重構(必要時)。先以下測試,**RUN 並確認 RED**,再實作:

    `tests/test_storage.py` 新增:
    - `test_write_session_meta_includes_original_sha256` — meta.json 三欄位齊全(page_count / filename / original_sha256)
    - `test_write_session_meta_is_atomic_on_simulated_crash` — monkey-patch `json.dump` 在 partial 時 raise → dest 檔仍是舊 content(或不存在),tmp file 已清乾淨;**驗證 `os.replace` 是原子 unit**(部分寫入永不可見)
    - `test_list_session_ids_unions_across_kinds` — 在 originals/{a}, work/{b}, outputs/{c} 各放一個 sid → list_session_ids() yields {a,b,c} 去重
    - `test_session_age_seconds_uses_max_mtime` — outputs/{sid}/ 用 `os.utime` 設 mtime now,originals/{sid}/ 設 now-9000 → age == 0(取最大,非最小)
    - `test_delete_session_removes_all_four_kinds` — 建 4-kind 都有 {sid} → delete_session(sid) → 4-kind 都不存在
    - `test_delete_session_handles_readonly_original` — write_original 後 mode 為 0o444 → delete_session 必須能刪(Pitfall 3 cross-platform — _on_rm_error 重新 chmod 0o644 後 retry)
    - `test_mark_and_is_session_corrupted_round_trip` — mark_session_corrupted → is_session_corrupted True;未 mark → False;
    - `test_is_session_corrupted_rejects_invalid_session_id` — `is_session_corrupted("../../etc")` 回 False(InvalidSessionId 內部 swallow,不外洩)

    `tests/test_integrity.py`(NEW)新增:
    - `test_compute_original_hash_matches_hashlib` — `compute_original_hash(b"hello") == hashlib.sha256(b"hello").hexdigest()`
    - `test_verify_original_hash_passes_on_unchanged_session` — 完整 ingest → verify 不 raise
    - `test_verify_original_hash_raises_on_tampered_original` — ingest → chmod 0o644 → 覆寫 originals/source.pdf bytes → chmod 0o444 → `verify_original_hash(sid)` raise `IntegrityError("original_tampered", ...)`,且 `is_session_corrupted(sid)` 在 raise **之前** 已被 mark(side effect 先於 raise)
    - `test_verify_treats_legacy_session_as_corrupted` — 手動刪 meta.json `original_sha256` 欄位後 → `verify_original_hash(sid)` raise `IntegrityError("session_corrupted", ...)`(Pitfall 4)
    - `test_verify_treats_missing_meta_as_corrupted` — 刪掉 meta.json 整檔 → verify raise `IntegrityError("session_corrupted", ...)`
    - `test_integrity_module_does_not_import_fitz` — `import app.services.integrity; assert "fitz" not in sys.modules` 或讀 module file source assert 無 `import fitz` 字串(AGPL seam 守門)

    RED 確認:`pytest tests/test_storage.py tests/test_integrity.py -x` 全紅(模組或函式不存在 / 既有 write_session_meta 不收 original_sha256 kwarg)。
  </behavior>
  <action>
    紅→綠。實作三檔修改 + 一檔新增。

    **app/config.py** — append 三個 Phase 5 後常數(05-01 已加 UVICORN_WORKERS + APP_BASE_PATH;此 task 補完):
    ```
    # Phase 5: hardening(Plan 05-02)
    SESSION_TTL_SECONDS: int = _env_int("SESSION_TTL_SECONDS", 3600)  # D-B2 — 1h 硬上限
    PROCESS_TIMEOUT_SECONDS: int = _env_int("PROCESS_TIMEOUT_SECONDS", 60)  # D-D3 — /process 超時
    CORS_ALLOW_ORIGINS: str = os.environ.get("CORS_ALLOW_ORIGINS", "")  # Claude's discretion — 預設關
    ```

    **app/storage.py** — 五處修改,沿用 PATTERNS.md §"app/storage.py" 精確範例:
    1. 頂部 imports 加 `import tempfile`(若尚未 import)
    2. **upgrade `write_session_meta`** — keyword-only 加 `original_sha256: str`(必填);用 `tempfile.mkstemp(prefix=".meta.", suffix=".json.tmp", dir=str(dest.parent))`(`dir=` 是 A7 防呆;若 Windows 預設 tmp 在別 drive,os.replace 會跨 FS 失敗 atomic)+ `os.fdopen(fd, "w", encoding="utf-8")` json.dump → `os.replace(tmp, dest)` 原子。失敗 except: `os.unlink(tmp_path)` 清乾淨 + re-raise。
    3. **`list_session_ids() -> Iterator[str]`** — 走 `_KINDS` iterate 每個 kind 目錄的 entries,只 yield 通過 `_SESSION_ID_RE.fullmatch(name)` 的;以 `seen: set` 去重。
    4. **`session_age_seconds(session_id: str) -> float | None`** — 走 `subdir(kind, sid)` 拿 4-kind path,取存在者的 mtime,回 `time.time() - max(mtimes)`;若無任一存在回 None。**用 max 不是 min**(outputs 剛產出 5min 前要保護,即便 originals 是 55min 前寫的)。
    5. **`delete_session(session_id: str) -> None`** — 走 4-kind 各自 `shutil.rmtree(subdir(kind, sid), onerror=_on_rm_error)`;每個 kind 獨立 try/except 避免一個失敗整批中斷。`_on_rm_error` handler 與 janitor 共用 — 此 task 在 storage.py 內 declare 一次,Task 3 janitor 可 import。**handler**:接 `func, path, exc_info` 三參數;若是 PermissionError + func in (os.unlink, os.rmdir, os.remove),re-chmod path 為 0o644 後 retry func(path);其他情況 log warning(沿用 RESEARCH.md 行 784–822)。
    6. **`mark_session_corrupted(session_id: str) -> Path`** — `subdir("work", sid).mkdir(parents=True, exist_ok=True)` → sentinel = subdir / `.corrupted` → `sentinel.touch(exist_ok=True)` → return sentinel。
    7. **`is_session_corrupted(session_id: str) -> bool`** — try `(subdir("work", sid) / ".corrupted").is_file()`;`InvalidSessionId` swallow 回 False(避免從 API 層流出 traceback)。

    **app/services/integrity.py**(NEW)— 沿用 RESEARCH.md Pattern 6 + PATTERNS.md §"app/services/integrity.py":
    ```
    """Phase 5 integrity verification — runtime D-05 enforcement.

    Computes + verifies the SHA-256 baseline of originals/{sid}/source.pdf written
    at ingest time. Uses hashlib stdlib only — confirms AGPL seam (no fitz import).
    """
    from __future__ import annotations
    import hashlib, logging, time
    from pathlib import Path
    from .. import storage

    logger = logging.getLogger(__name__)

    class IntegrityError(Exception):
        def __init__(self, code: str, message: str) -> None:
            super().__init__(message)
            self.code = code
            self.message = message

    def compute_original_hash(data: bytes) -> str:
        return hashlib.sha256(data).hexdigest()

    def verify_original_hash(session_id: str) -> None:
        meta = storage.read_session_meta(session_id)
        if meta is None or "original_sha256" not in meta:
            storage.mark_session_corrupted(session_id)
            raise IntegrityError(
                "session_corrupted",
                "此工作階段為舊版或資料不完整,請重新上傳檔案。",
            )
        expected = meta["original_sha256"]
        original = storage.original_path(session_id)
        actual = hashlib.sha256(Path(original).read_bytes()).hexdigest()
        if actual != expected:
            logger.error(
                "original_tampered",
                extra={
                    "session_id": session_id,
                    "expected_hash": expected,
                    "actual_hash": actual,
                    "path": str(original),
                    "timestamp": time.time(),
                },
            )
            storage.mark_session_corrupted(session_id)
            raise IntegrityError(
                "original_tampered",
                "系統偵測到原始檔異常,此工作階段已停用,請重新上傳檔案。",
            )
    ```
    繁中 message 沿用 PATTERNS.md §"繁中錯誤訊息" 鎖定字串;不要自創。

    **綠**:確認上述七測 + 八測全綠(`pytest tests/test_storage.py tests/test_integrity.py -x`)。

    **AGPL seam**:`test_integrity_module_does_not_import_fitz` 是非交涉守門 — integrity.py 嚴禁 `import fitz`,所有 hash 算用 `hashlib`,所有 I/O 走 stdlib `pathlib` / `storage`。

    **Threat model mapping**:T-05-04(SHA-256 forge — accept 為 informational only,內網不防密碼學攻擊;沿用 T-04 wording)、T-05-07(path traversal — 全部走 `subdir()` 既有 _SESSION_ID_RE)、Pitfall 4(legacy session migration — fail-closed 為 corrupted,1h TTL 自然汰換)、A7(tempfile cross-drive — 強制 `dir=dest.parent`)。
  </action>
  <verify>
    <automated>pytest tests/test_storage.py tests/test_integrity.py -x --no-header 2>&1 | tail -10 # 全綠;python -c "import app.services.integrity as m; import sys; assert 'fitz' not in sys.modules" # AGPL seam 確認</automated>
  </verify>
  <acceptance_criteria>
    - Source: `app/config.py` 含 `SESSION_TTL_SECONDS: int = _env_int("SESSION_TTL_SECONDS", 3600)`(精確字串)
    - Source: `app/config.py` 含 `PROCESS_TIMEOUT_SECONDS: int = _env_int("PROCESS_TIMEOUT_SECONDS", 60)`(精確字串)
    - Source: `app/storage.py` 含 `def list_session_ids` / `def session_age_seconds` / `def delete_session` / `def mark_session_corrupted` / `def is_session_corrupted` 五個函式定義
    - Source: `app/storage.py` 的 `write_session_meta` signature 含 `original_sha256: str`(keyword-only)
    - Source: `app/storage.py` 的 `write_session_meta` 用 `tempfile.mkstemp(... dir=str(dest.parent) ...)` + `os.replace(`(grep 命中)
    - Source: `app/services/integrity.py` 存在,含 `class IntegrityError`、`def compute_original_hash`、`def verify_original_hash` 三個 export
    - Source: `app/services/integrity.py` **不含** `import fitz`(grep -c 應為 0)
    - Test: `pytest tests/test_storage.py -x` 退出 0
    - Test: `pytest tests/test_integrity.py -x` 退出 0,**且** 含 `test_integrity_module_does_not_import_fitz` 通過
    - Test: `pytest tests/ -x -k "not janitor and not health and not test_process_api"` 退出 0(既有測試零迴歸 — process_api 由 Task 3/4 在後續任務補)
    - Behavior: `python -c "from app.services.integrity import compute_original_hash; import hashlib; assert compute_original_hash(b'x')==hashlib.sha256(b'x').hexdigest()"` 退出 0
  </acceptance_criteria>
  <done>config 三常數齊;storage atomic meta 寫入 + 5 helper;integrity 模組與三條路徑(pass / tampered / legacy)就位;15+ 條新測試全綠;AGPL seam 守門通過。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: janitor 模組 + ingest hash baseline + pipeline verify(垂直串接:寫入 → 驗證 → 清理)</name>
  <files>app/services/janitor.py, app/services/ingest.py, app/services/pipeline.py, tests/test_janitor.py, tests/test_ingest.py</files>
  <read_first>
    - **Task 1 產出的** `app/storage.py`(`list_session_ids` / `session_age_seconds` / `delete_session` / `_on_rm_error`)+ `app/services/integrity.py`(`compute_original_hash` / `verify_original_hash` / `IntegrityError`)
    - `app/services/ingest.py` 行 253–267(`_ingest_pdf` 寫 originals + work + pristine + meta 區塊)+ 行 296–310(`_ingest_image_to_pdf` 對應區塊)
    - `app/services/pipeline.py` 行 107–137(`process_job` 入口 + work/pristine 路徑解析 + `work_copy_misconfigured` structural guard)+ 行 40–55(`PipelineError` class 定義 — Task 4 main.py 也會擴 `_PROCESS_STATUS` dict)
    - `app/services/logo.py` 行 1–60(模組 docstring 安全屬性聲明 — janitor 比照)
    - `app/storage.py` 行 34(`_KINDS` tuple — janitor 重用)、行 80–101(`subdir` 路徑防護)
    - `.planning/phases/05-ubuntu/05-RESEARCH.md` Pattern 7(行 750–908,janitor 完整範例 + race protection 理由)+ §"verify_original_hash" 整合於 pipeline.process_job 範例(行 716–729)
    - `.planning/phases/05-ubuntu/05-PATTERNS.md` §"app/services/janitor.py" / §"app/services/ingest.py" / §"app/services/pipeline.py" / §"tests/test_janitor.py" / §"tests/test_ingest.py"
    - `.planning/phases/04-raster-image-support/04-CONTEXT.md`(image 上傳的 hash 必須對 user 原始 image bytes,不是 normalized A4 PDF — D-C4 / 維持 D-05 invariant)
  </read_first>
  <behavior>
    **TDD cycle**:先紅後綠。

    `tests/test_janitor.py`(NEW)寫測試:
    - `test_janitor_sweeps_expired_session` — 建 session,`os.utime(path, (now-3700, now-3700))` 倒撥 4-kind dir 到 3700s 前,呼叫 `sweep_expired_sessions()` 應回 ≥1,4-kind dir 全消
    - `test_janitor_keeps_active_session_under_ttl` — 新建 session(mtime 為 now),sweep 應回 0,4-kind dir 完整
    - `test_janitor_max_mtime_protects_recent_outputs` — originals/ mtime 倒撥到 now-9000,outputs/ 設 now-100 → sweep 不刪(取最大 mtime 看是 now-100,小於 3600)
    - `test_janitor_handles_chmod_0o444_originals_cross_platform` — write_original 後源檔為 0o444,倒撥 mtime → sweep 必須能刪(Pitfall 3;`_on_rm_error` 重新 chmod 0o644 retry)
    - `test_janitor_returns_zero_on_no_sessions` — 全空目錄 → sweep 回 0
    - `test_janitor_skips_non_token_dir_names` — `data/work/not-a-session-token/` 手動建 → sweep 不碰(_SESSION_ID_RE 過濾)
    - `test_janitor_failure_does_not_raise` — monkey-patch `_safe_rmtree` 內部 raise → `sweep_expired_sessions` 不外洩 exception(內部 log warning,外部回原本 success count)

    `tests/test_ingest.py` 新增:
    - `test_ingest_writes_original_sha256_into_meta_for_pdf` — ingest PDF → `read_session_meta(sid)["original_sha256"] == hashlib.sha256(pdf_bytes).hexdigest()`
    - `test_ingest_image_hash_is_over_raw_image_bytes` — ingest PNG / JPG / TIFF 各一 → meta.json `original_sha256` 等於 user **原始 image bytes** SHA-256(不是 normalized A4 PDF)— D-C4 一致 + 維持 Phase 4 D-05 invariant
    - `test_pipeline_raises_pipeline_error_original_tampered_on_hash_mismatch` — 完整 ingest → 手動 chmod 0o644 → mutate bytes → chmod 0o444 → 呼叫 `pipeline.process_job(sid, JobSpec(...))` raise `PipelineError` 且 `.code == "original_tampered"`;同時 `.corrupted` sentinel 已被寫入 work dir
    - `test_pipeline_raises_session_corrupted_on_legacy_meta` — 刪 meta.json 的 `original_sha256` 欄位 → process_job raise `PipelineError("session_corrupted", ...)`
    - `test_pipeline_does_not_touch_originals_on_verify_failure` — verify 失敗時 raise 之前 + 之後,`originals/source.pdf` 的 SHA-256 不變(D-05 invariant)+ `pristine/` 也沒被動

    RED 確認:`pytest tests/test_janitor.py tests/test_ingest.py -x` 全紅(janitor 模組不存在 + ingest 還沒寫 hash + pipeline 還沒呼叫 verify)。
  </behavior>
  <action>
    **app/services/janitor.py**(NEW)— 沿用 RESEARCH.md Pattern 7 + PATTERNS.md §"app/services/janitor.py":
    ```
    """Phase 5 temp file janitor — 4-kind TTL sweep.

    Runs synchronously at three trigger points (D-B1):
      (a) app startup (lifespan hook in app/main.py)
      (b) end of POST /sessions (app/api/sessions.py)
      (c) end of POST /sessions/{sid}/process (app/api/process.py)

    Uses stdlib only (shutil, os, errno, stat, time) — no fitz, no asyncio,
    no APScheduler. AGPL seam preserved.

    TTL hardcoded to config.SESSION_TTL_SECONDS (1h default per D-B2). Race
    protection: TTL (3600s) >> process timeout (60s default), so an in-flight
    /process cannot age out mid-job. Defensive try/except on _safe_rmtree
    catches concurrent rmtree ENOTEMPTY.
    """
    from __future__ import annotations
    import errno, logging, os, shutil, stat, time
    from pathlib import Path
    from .. import config, storage

    logger = logging.getLogger(__name__)

    _KINDS = ("originals", "work", "outputs", "pristine")  # mirrors storage._KINDS

    def _on_rm_error(func, path, exc_info) -> None:
        # cross-platform chmod 0o444 → retry; Pitfall 3
        ...  # 沿用 PATTERNS.md §"janitor" 行 178–204 範例
    def _safe_rmtree(path: Path) -> bool: ...
    def _session_max_mtime(sid: str) -> float | None: ...
    def _enumerate_session_ids() -> set[str]: ...
    def _delete_session(sid: str) -> bool: ...

    def sweep_expired_sessions(now: float | None = None) -> int:
        """Sweep sessions with max-mtime older than TTL. Returns count deleted.
        Never raises (catches all I/O errors, logs, returns count of successes).
        """
        if now is None:
            now = time.time()
        ttl = config.SESSION_TTL_SECONDS
        deleted = 0
        try:
            sids = _enumerate_session_ids()
        except OSError as err:
            logger.warning("janitor: enumerate failed: %s", err)
            return 0
        for sid in sids:
            try:
                mtime = _session_max_mtime(sid)
                if mtime is None:
                    continue
                age = now - mtime
                if age >= ttl:
                    if _delete_session(sid):
                        deleted += 1
            except Exception as err:
                logger.warning("janitor: failed on %s: %s", sid, err)
        return deleted
    ```
    完整 `_on_rm_error` / `_safe_rmtree` / `_session_max_mtime` / `_enumerate_session_ids` 沿用 RESEARCH.md 行 784–875。`_enumerate_session_ids` 必須 reuse `storage._SESSION_ID_RE` 過濾,只回合法 token(防 path traversal 走漏)。**`_delete_session` 可 reuse `storage.delete_session`(Task 1 產出),或 inline 寫一遍;選 reuse 較 DRY。**

    **app/services/ingest.py** — 兩處修改:
    1. 頂部 imports 加 `from .integrity import compute_original_hash`(若已 import `from . import integrity` 風格也可,但 patterns.md 行 393 範例為 `from .integrity import compute_original_hash` 直接 import 函式)
    2. `_ingest_pdf`(行 253–267 區塊)在 `storage.write_pristine_copy(session_id, data)` **之後**、`storage.write_session_meta(...)` **之前 / 同次呼叫** — 在 write_session_meta 呼叫加入 `original_sha256=compute_original_hash(data)` kwarg。`data` 變數已在 scope 內(上傳 bytes,尚未 chmod 0o444 disk read)。
    3. `_ingest_image_to_pdf`(行 296–310 區塊)同樣處理:**hash 是對 `data`(user 原始 image bytes,即 originals/source.<ext> 寫入的內容)**,不是對 normalized A4 PDF 的 bytes — 與 D-C4 + Phase 4 D-05 一致(originals/ 存原始格式,verify 也是讀原始格式 → 算 SHA-256 一致)。

    **app/services/pipeline.py** — 在 `process_job` 入口、`work_copy_misconfigured` structural guard **之後**、`shutil.copyfile(pristine, work)` reset **之前**,插入 verify_original_hash 呼叫:
    ```
    try:
        integrity.verify_original_hash(session_id)
    except integrity.IntegrityError as err:
        raise PipelineError(err.code, err.message) from err
    ```
    頂部 imports 加 `from . import integrity`。**重要**:此 re-raise 走既有 `main.py:108–114` PipelineError handler;Task 4 會擴 `_PROCESS_STATUS` dict 加 `original_tampered:503` 與 `session_corrupted:410` 兩個 mapping。

    **綠**:確認新測 13 條全綠 + Task 1 七測仍綠 + 既有 ingest / pipeline 測試零迴歸(`pytest tests/test_janitor.py tests/test_ingest.py tests/test_integrity.py tests/test_storage.py -x`)。

    **Threat model mapping**:T-05-04(SHA-256 v1 為 informational,accept)、T-05-05(janitor race — TTL ≫ timeout 大幅縮 race window;`_safe_rmtree` try/except ENOTEMPTY;`storage.subdir()` 防止 token-poison;`_session_max_mtime` 用 max 防止「outputs 剛產出但被刪」)、T-05-07(path traversal — _SESSION_ID_RE)、Pitfall 3(Windows chmod 0o444 + rmtree — `_on_rm_error` cross-platform)。
  </action>
  <verify>
    <automated>pytest tests/test_janitor.py tests/test_ingest.py tests/test_integrity.py tests/test_storage.py -x --no-header 2>&1 | tail -10 # 全綠;python -c "import app.services.janitor as m; import sys; assert 'fitz' not in sys.modules" # AGPL seam 確認;python -c "from app.services.pipeline import process_job; import inspect; assert 'verify_original_hash' in inspect.getsource(process_job)" # pipeline 已插入 verify</automated>
  </verify>
  <acceptance_criteria>
    - Source: `app/services/janitor.py` 存在,含 `def sweep_expired_sessions(now: float | None = None) -> int:` signature
    - Source: `app/services/janitor.py` **不含** `import fitz`(grep -c 應為 0)
    - Source: `app/services/janitor.py` 含 `_on_rm_error` 函式 + 對 PermissionError 的 chmod retry 處理(grep `chmod` 命中)
    - Source: `app/services/ingest.py` 含 `compute_original_hash(data)` 在 ingest 區塊內(grep 命中,出現於 `_ingest_pdf` 與 `_ingest_image_to_pdf` 兩個 path)
    - Source: `app/services/pipeline.py` `process_job` 函式內含 `verify_original_hash(session_id)` 字串(grep 命中)
    - Source: `app/services/pipeline.py` 含 `from . import integrity` 或等價 import
    - Test: `pytest tests/test_janitor.py -x` 退出 0,且包含至少 7 個測試(grep `^def test_` tests/test_janitor.py | wc -l 應 ≥ 7)
    - Test: `pytest tests/test_ingest.py -x` 退出 0(既有 + 新測試)
    - Test: `pytest tests/test_integrity.py tests/test_storage.py -x` 退出 0(零迴歸)
    - Behavior: 完整端對端 — 建 session、`os.utime` 4-kind 到 mtime-now-3700、呼叫 `janitor.sweep_expired_sessions()` 回 1 + 4-kind dir 全消
    - Behavior: 完整端對端 — ingest PDF → meta.json 含 original_sha256 → 手動 chmod 0o644 + 覆寫 originals + chmod 0o444 → 呼叫 process_job → raise PipelineError(code='original_tampered') + `is_session_corrupted(sid)` 為 True
  </acceptance_criteria>
  <done>janitor 模組就位(stdlib + cross-platform);ingest 寫 hash baseline;pipeline verify 入口插入;13+ 條新測試全綠;既有測試零迴歸;AGPL seam 維持。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: /process timeout + corrupted gate + 三點 janitor 觸發 + main.py 擴 _PROCESS_STATUS + 前端錯誤訊息</name>
  <files>app/api/process.py, app/api/sessions.py, app/main.py, web/js/app.js, web/styles/app.css, tests/test_process_api.py, tests/test_health.py</files>
  <read_first>
    - **Task 1 / 2 產出的** 新檔(integrity / janitor)+ pipeline verify hook
    - `app/api/process.py` 全檔(尤其行 41–46 `_require_session`、行 49–67 既有 `process_session` handler 用 `run_in_threadpool`)
    - `app/api/sessions.py` 行 43–77(`create_session` ingest entry)
    - `app/main.py` 行 34 `app = FastAPI(...)`(Plan 05-01 已加 `root_path` + `lifespan` 骨架)、行 92–96 `_PROCESS_STATUS` dict、行 108–114 `_handle_pipeline_error`、行 166–169 `/health` endpoint(Plan 05-01 已加強)
    - `web/js/app.js` 行 18–53 既有 `COPY` 表(繁中 message library)+ 行 115–146 `messageForError(err)` switch
    - `web/js/api.js`(瀏覽 `ApiError` shape — server detail.code 如何 surface 到前端)
    - `tests/test_process_api.py` 行 26–120 `_upload` + happy-path harness + 行 90–107 既有 SHA-256 invariant assertion pattern
    - `tests/test_api.py`(/health 既有測試形態)
    - `.planning/phases/05-ubuntu/05-RESEARCH.md` Pattern 4(行 479–545,asyncio.wait_for 60s timeout 範例 + Pitfall 1 「thread 不會被 kill」說明)
    - `.planning/phases/05-ubuntu/05-PATTERNS.md` §"app/api/process.py" / §"app/api/sessions.py" / §"app/main.py" / §"web/js/app.js" / §"tests/test_process_api.py" / §"tests/test_health.py"
  </read_first>
  <behavior>
    TDD cycle。

    `tests/test_process_api.py` 新增(沿用 PATTERNS.md §"tests/test_process_api.py" 五條):
    - `test_meta_original_sha256_written_at_ingest` — POST /sessions → meta.json 含 64-char hex original_sha256
    - `test_original_tampered_returns_503` — ingest → chmod 0o644 + mutate originals/source.pdf + chmod 0o444 → POST /process → 503 + `detail.code == "original_tampered"` + 繁中 message
    - `test_corrupted_session_blocked_from_process` — 手動 `storage.mark_session_corrupted(sid)` → POST /process → 410 + `detail.code == "session_corrupted"`
    - `test_legacy_session_without_sha256_treated_as_corrupted` — 完整 ingest 後 remove meta.json `original_sha256` 欄位 → POST /process → 503 (`original_tampered` 走 verify_original_hash 路徑;legacy 邏輯為 session_corrupted → 410 — 兩種行為都接受,**測試 assert code in {"session_corrupted", "original_tampered"}** 即可,語義皆「禁止使用、請重新上傳」)
    - `test_process_timeout_returns_504` — monkeypatch `config.PROCESS_TIMEOUT_SECONDS=0.2` + monkeypatch `pipeline.process_job` 為 `lambda *a, **kw: time.sleep(2)` → POST /process → 504 + `detail.code == "processing_timeout"`
    - `test_process_corrupted_check_runs_before_timeout` — mark sid corrupted 後 monkey patch process_job 為 sleep 10s → 仍立即(<1s)回 410(corrupted 短路在 timeout wrapper 之前)
    - `test_sessions_post_calls_janitor_at_end` — monkey patch `janitor.sweep_expired_sessions` 計數器 → POST /sessions → 計數器 ≥ 1
    - `test_process_post_calls_janitor_at_end` — 同上,POST /process → 計數器 ≥ 1
    - `test_janitor_failure_does_not_taint_process_request` — monkey patch janitor.sweep_expired_sessions raise → POST /process **仍然** 回 200(janitor 失敗不污染 response)

    `tests/test_health.py`(NEW)新增(沿用 PATTERNS.md §"tests/test_health.py" 六條):
    - `test_health_returns_ok_status` — GET /health → 200 + `status == "ok"`
    - `test_health_includes_uptime_seconds` — uptime_seconds float ≥ 0
    - `test_health_includes_active_sessions_count` — 上傳 2 個 → active_sessions ≥ 2
    - `test_health_includes_data_dir_fields` — data_dir_bytes int / data_dir_pct float 在 [0, 100]
    - `test_health_does_not_leak_session_ids` — response body grep `[a-f0-9]{32}` 應 0 matches(T-05-08 info disclosure guard)
    - `test_health_active_sessions_minus_one_on_unreadable_originals` — 部分平台可 skip(chmod 0 on Windows 行為不同 — pytest.skip if Windows + 行為驗不出來)

    RED:`pytest tests/test_process_api.py tests/test_health.py -x` 全紅(尚未實作)。
  </behavior>
  <action>
    四檔修改 + 兩檔測試新增。

    **app/api/process.py** — 沿用 RESEARCH.md Pattern 4 行 502–542 + PATTERNS.md §"app/api/process.py" 三個修改:
    1. 頂部 imports 加 `import asyncio` + `from fastapi import HTTPException`(若尚未)+ `from .. services import janitor` + `from .. import storage`(若尚未)
    2. handler body 重寫:
       ```
       @router.post("/sessions/{session_id}/process")
       async def process_session(session_id: str, job: JobSpec) -> dict:
           _require_session(session_id)
           # D-C3: corrupted sentinel 短路 410
           if storage.is_session_corrupted(session_id):
               raise HTTPException(
                   status_code=410,
                   detail={"code": "session_corrupted",
                           "message": "此工作階段已標記為異常,請重新上傳檔案。"},
               )
           # D-D3: 60s timeout 包同步 process_job
           try:
               return await asyncio.wait_for(
                   asyncio.to_thread(pipeline.process_job, session_id, job),
                   timeout=config.PROCESS_TIMEOUT_SECONDS,
               )
           except asyncio.TimeoutError as err:
               raise HTTPException(
                   status_code=504,
                   detail={"code": "processing_timeout",
                           "message": f"處理逾時(超過 {config.PROCESS_TIMEOUT_SECONDS} 秒),"
                                      "請改用較小檔案或減少框選區域數量。"},
               ) from err
           finally:
               # D-B1 trigger (c): /process 結尾 sweep
               try:
                   janitor.sweep_expired_sessions()
               except Exception:
                   pass  # janitor 內部已 log;絕不污染 response
       ```
       **Pitfall 1 註解**(必須在 timeout block 上方 inline 寫明 — 給未來 reviewer 看):
       ```
       # Pitfall 1 (RESEARCH Pitfall 1): asyncio.wait_for(asyncio.to_thread(...))
       # 在 timeout 後 Python 無法 kill underlying thread。HTTP 立即回 504,但
       # PyMuPDF 工作執行緒會跑完才釋放(典型 10–30s,因為 MAX_RENDER_PIXELS=40MP
       # + MAX_PAGES=30 收緊了 worst case)。workers=2(D-D2)確保預覽不被卡。
       # 若未來真實濫用 → ProcessPoolExecutor 子進程隔離(D-D1 deferred)。
       ```

    **app/api/sessions.py** — `create_session` 結尾加 `finally: try: janitor.sweep_expired_sessions() except Exception: pass`。沿用 PATTERNS.md §"app/api/sessions.py":類似 process.py 但無 timeout(ingest 上限由 MAX_UPLOAD_BYTES + streaming guard 控制)。頂部 imports 加 `from ..services import janitor`。

    **app/main.py** — 三處修改:
    1. **`_PROCESS_STATUS` dict** 擴(行 92–96)加三個 mapping:
       ```
       "original_tampered": 503,
       "session_corrupted": 410,
       "processing_timeout": 504,  # 直接由 api/process.py raise HTTPException;此 mapping 為 defense-in-depth(若某天某處再 raise PipelineError 同 code)
       ```
    2. **lifespan body** — Plan 05-01 已加骨架,此處填 startup sweep:
       ```
       @asynccontextmanager
       async def lifespan(app: FastAPI):
           try:
               janitor.sweep_expired_sessions()
           except Exception:
               pass
           yield
       ```
       頂部 imports 加 `from .services import janitor`(若尚未)。
    3. **(可選但建議)CORS middleware** — 預設關;若 config.CORS_ALLOW_ORIGINS 非空字串則啟用(沿用 PATTERNS.md §"app/main.py" 末段):
       ```
       if config.CORS_ALLOW_ORIGINS:
           from fastapi.middleware.cors import CORSMiddleware
           app.add_middleware(
               CORSMiddleware,
               allow_origins=[o.strip() for o in config.CORS_ALLOW_ORIGINS.split(",") if o.strip()],
               allow_credentials=False, allow_methods=["GET", "POST"], allow_headers=["*"],
           )
       ```

    **web/js/app.js** — 兩處修改沿用 PATTERNS.md §"web/js/app.js":
    1. `COPY` 物件加三個 key(繁中 message,沿用 integrity / api/process.py 已鎖定文案):
       ```
       originalTampered: "系統偵測到原始檔異常,此工作階段已停用,請重新上傳此檔。",
       sessionCorrupted: "此工作階段已過期或無法使用,請重新上傳檔案。",
       processingTimeout: "處理逾時,請改用較小檔案或減少框選區域數量後再試一次。",
       ```
    2. `messageForError(err)` switch 在 default 之前加三個 case:
       ```
       case "original_tampered": return COPY.originalTampered;
       case "session_corrupted": return COPY.sessionCorrupted;
       case "processing_timeout": return COPY.processingTimeout;
       ```
       **不動** 既有 XSS guard(`textContent` 路徑)— 字串為 static literal,既有 ApiError pathway 直接承載,無 innerHTML 注入。

    3. **Session TTL UI 提示 + 404 過期友善訊息**(D-B2;RESEARCH Pattern 9 文案):
       a. **COPY 表新增兩個 key**(沿用 D-B2 + RESEARCH Pattern 9 鎖定的繁中字串):
          ```
          sessionTtlHint: "此次處理 1 小時內完成下載 — 逾時需重新上傳。",
          sessionExpired: "此次處理已過期,請重新上傳此檔。",
          ```
       b. **DOM 區塊建立**:在 page-stage 區域(預覽載入完成的容器旁,沿用既有 layout class 不新建 component)動態插入一個 `<p class="app-session-hint" aria-live="polite" role="status">` 節點(JS createElement + textContent 設定,**禁用 innerHTML** — XSS guard);初始 hidden,POST /sessions 成功(201)的 handler 內 set textContent 為 `COPY.sessionTtlHint` 並顯示。
       c. **404 過期友善訊息**:在現有的 ApiError handler(或 fetch wrapper)裡偵測 `GET /sessions/{id}` 回 404 的情況 — 將同一個 `.app-session-hint` 節點 textContent 替換為 `COPY.sessionExpired`,並保持顯示(aria-live="polite" 會 announce 給螢幕閱讀器);提示文字維持單行,不另開 modal。**不動** /process 路徑的 410 `session_corrupted` 既有訊息(由 messageForError switch 處理),兩條訊息互不重複。
       d. **樣式**:在 `web/styles/app.css` 內 append 一個極簡 `.app-session-hint` 區塊(token-only,雙主題;沿用 .app-footer 同樣的 font-size-small / color-text-muted token);無新色彩,**禁止** 硬寫 hex。
       e. **不在 web/index.html 加任何靜態提示**(避免與 AGPL footer 的角色混淆;TTL hint 是 session-aware 動態提示,放在動作流程結束後比 footer 更有用)。


    **綠**:確認所有新測 + 既有測試全綠(`pytest tests/ -x`)。

    **Threat model mapping**:T-05-05(janitor race — try/except 包住絕不污染)、T-05-06(corrupted sentinel bypass — accept 為 LAN 信任 fs write 等於攻擊者已入侵)、T-05-08(/health 不洩漏 session_id — Task 1 已 mitigate;此 task `tests/test_health.py` 加 grep guard)、T-05-10(timeout 後 thread 仍跑 — workers=2 + accept;註解 inline 寫明)、Pitfall 1(thread 不可 kill — code 旁註解 + README Known Limitations)。
  </action>
  <verify>
    <automated>pytest tests/test_process_api.py tests/test_health.py -x --no-header 2>&1 | tail -10 # 全綠;pytest tests/ -x --no-header 2>&1 | tail -5 # 全測試零迴歸;grep -q "asyncio.wait_for" app/api/process.py && grep -q "is_session_corrupted" app/api/process.py && grep -q "sweep_expired_sessions" app/api/sessions.py && grep -q "sweep_expired_sessions" app/main.py # 四個 hook 全部就位; grep -q "originalTampered\|original_tampered" web/js/app.js # 前端錯誤訊息已加; grep -q "此次處理 1 小時內完成下載" web/js/app.js && grep -q "此次處理已過期" web/js/app.js && grep -q "aria-live" web/js/app.js # D-B2 session TTL UI 提示 + 404 過期友善訊息 + a11y live region 就位; grep -q "\.app-session-hint" web/styles/app.css # TTL hint 樣式就位</automated>
  </verify>
  <acceptance_criteria>
    - Source: `app/api/process.py` 含 `asyncio.wait_for(asyncio.to_thread(pipeline.process_job` 字串(完整)
    - Source: `app/api/process.py` 含 `is_session_corrupted(session_id)` 字串
    - Source: `app/api/process.py` 含 `janitor.sweep_expired_sessions()` 字串(finally block)
    - Source: `app/api/sessions.py` 含 `janitor.sweep_expired_sessions()` 字串
    - Source: `app/main.py` 的 `_PROCESS_STATUS` dict 含 `"original_tampered": 503` / `"session_corrupted": 410` / `"processing_timeout": 504` 三個 entries
    - Source: `app/main.py` lifespan body 含 `janitor.sweep_expired_sessions()` 字串
    - Source: `web/js/app.js` 含 `originalTampered` / `sessionCorrupted` / `processingTimeout` 三個 key + `case "original_tampered"` / `case "session_corrupted"` / `case "processing_timeout"` 三個 switch case
    - Source: `web/js/app.js` 含 `sessionTtlHint` key 與字串 `此次處理 1 小時內完成下載`(精確 D-B2 + RESEARCH Pattern 9 文案);亦含 `sessionExpired` key 與字串 `此次處理已過期`
    - Source: grep `此次處理 1 小時內完成下載` 在 `web/js/app.js` 命中 ≥ 1(亦可命中 `web/index.html`,擇一即可,規範為 dynamic 路徑優先)
    - Source: `web/js/app.js` 含 `aria-live` 字串(TTL hint 節點以 polite live region 動態插入)
    - Source: `web/js/app.js` 含 `app-session-hint` class 名稱
    - Source: `web/styles/app.css` 含 `.app-session-hint` 區塊,**不含** raw hex(`grep '\.app-session-hint.*#[0-9a-fA-F]' web/styles/app.css` 應 0 matches — 雙主題 token-only)
    - Behavior: 端到端 — `curl -X POST -F "file=@sample.pdf" http://localhost:8000/sessions` → 201;以 TestClient + BeautifulSoup(或正則)驗 POST 成功後渲染流程中 `app-session-hint` 節點 textContent 含 `此次處理 1 小時內完成下載`
    - Behavior: 端到端 — 對不存在的 sid `GET /sessions/00000000000000000000000000000000` → 404;前端在此 path 上將 `app-session-hint` textContent 切換為 `此次處理已過期,請重新上傳此檔`(可由前端單元測試或手動 UAT 驗證 — 本 plan 接受 source grep + DOM 結構 acceptance 為主,實機 UAT 屬 phase-level)
    - Test: `pytest tests/test_process_api.py -x` 退出 0(包括新 9 條測試)
    - Test: `pytest tests/test_health.py -x` 退出 0
    - Test: `pytest tests/ -x` 退出 0(完整零迴歸)
    - Behavior: 端到端 — `curl -X POST -F "file=@sample.pdf" http://localhost:8000/sessions` → 200 + session_id;手動 chmod + mutate originals → `curl -X POST -d '{"dpi":200,"regions":[]}' -H "Content-Type: application/json" http://localhost:8000/sessions/{sid}/process` → 503 + JSON `detail.code == "original_tampered"`
    - Behavior: 端到端 — 完整 session,monkeypatch process_job 為 sleep(2) + config.PROCESS_TIMEOUT_SECONDS=0.5 → /process 在 ~0.5s 內回 504 + `detail.code == "processing_timeout"`
    - Behavior: /health 響應 grep `[a-f0-9]{32}` 0 matches(T-05-08 mitigation 仍有效)
  </acceptance_criteria>
  <done>三點 janitor 觸發到位;/process 60s timeout + corrupted 410 短路;_PROCESS_STATUS 擴三個 code;前端三個錯誤訊息對應 + session TTL 上傳成功提示(D-B2)+ 404 過期友善訊息渲染(aria-live polite,雙主題 token-only);新 15+ 條測試全綠;Phase 1–4 既有測試零迴歸;Pitfall 1 註解 inline + README 已說明(05-01 已寫)。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-uploaded bytes → originals/source.pdf | chmod 0o444 + SHA-256 baseline 寫入 meta.json(此 plan 強化)|
| originals/{sid}/source.pdf → /process 讀路徑 | 每次 /process 入口先 verify_original_hash(D-C2)|
| Active sessions → janitor sweep | TTL 1h(D-B2);TTL ≫ process timeout 60s 防 race(D-B4)|
| /process worker thread → HTTP timeout | 60s wait_for;thread 無法 kill(Pitfall 1)— 仍 acceptable for v1 LAN |
| /health → 公網 / LAN caller | accept(內網無 auth);資訊最小化(不洩漏 sid)|

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | S(Spoofing) | SHA-256 baseline | accept (informational only) | v1 LAN 不防密碼學攻擊;攻擊者改 originals 同時也能改 meta.json,SHA-256 為「內部完整性檢測」非「密碼學防護」。延 Phase 4 T-04 wording。Task 1 不加 HMAC / key signing。 |
| T-05-05 | T / A | Janitor race(worker A 刪 / worker B 處理同 sid)| mitigate (P1) | TTL=3600s ≫ PROCESS_TIMEOUT=60s(60x);`_safe_rmtree` try/except ENOTEMPTY;`storage.subdir()` _SESSION_ID_RE 過濾;`_session_max_mtime` 取 max(outputs 剛產出保護);janitor 失敗 try/except 絕不污染 response。Task 2 acceptance: race 測試覆蓋。 |
| T-05-06 | T(攻擊者刪 .corrupted sentinel 重跑 /process)| accept(P2)| LAN 工具;能寫 fs 等於攻擊者已入侵,sentinel 防的是誤觸而非惡意。若未來公開外網升級 HMAC-signed sentinel。 |
| T-05-07 | I / E(path traversal on sid)| mitigate (P0) | janitor / integrity / storage helpers 全部走 `storage.subdir()` + `_SESSION_ID_RE` `fullmatch`;`is_session_corrupted` swallow InvalidSessionId 回 False(避免錯誤訊息洩漏)。Task 1 / 2 acceptance 涵蓋。 |
| T-05-10 | A(Availability — timeout 後 thread 仍跑)| mitigate by workers (P1) | workers=2(D-D2 + 05-01 Dockerfile)確保 1 worker 卡住預覽仍可用;MAX_RENDER_PIXELS=40MP + MAX_PAGES=30 收緊 worst case 到 10–30s 非無限;Pitfall 1 in-source 註解 + README Known Limitations;升級路徑 = ProcessPoolExecutor(D-D1 deferred)。Task 3 acceptance: workers=2 + inline 註解。 |
| Pitfall-1 | A | asyncio.wait_for thread 不可 kill | mitigate (acceptable) | 同 T-05-10 |
| Pitfall-3 | A(Windows chmod 0o444 + rmtree)| mitigate (P0) | `_on_rm_error` 在 storage.py + janitor.py 共用;PermissionError + os.unlink/rmdir/remove → chmod 0o644 retry;Task 1+2 acceptance: Windows / Linux 測試皆通過。 |
| Pitfall-4 | A(legacy session migration)| mitigate by fail-closed | meta.json 無 original_sha256 → `IntegrityError("session_corrupted")` → 410;1h TTL 內自然汰換(Phase 5 部署 1h 後 legacy 全消)。Task 1 acceptance 含 legacy 測試。 |
| A7 | I / T(atomic write cross-drive)| mitigate (P1) | `tempfile.mkstemp(dir=str(dest.parent))` 強制 tmp 與 dest 同 FS,os.replace 原子。Task 1 acceptance: source grep 命中 `dir=str(dest.parent)`。 |
| T-05-08 | I | /health endpoint | accept(LAN)+ mitigate by minimization | 五欄位只回 count / bytes / pct,不洩漏 sid / filename;Task 3 acceptance: `test_health_does_not_leak_session_ids` grep guard 命中 0。**未來公開外網升級 basic auth(deferred)**。 |

**Phase 5 全局 STRIDE 收口**:Phase 4 已收 17/17;Plan 05-01 處理 T-05-01..03 / 08 / 09 + Pitfall 2/5/7/10;Plan 05-02(本)處理 T-05-04..07 / 10 + Pitfall 1/3/4/A7。Phase 5 結束預期 27/27 closed(15 mitigate + 7 accept + 5 「accept by mitigation 等價」)。
</threat_model>

<verification>
本 plan 結束時必須通過:
1. `pytest tests/ -x --no-header` 全綠;測試總數從 Phase 4 的 243 增到 ≥ 270(預估新增 30+ 條:test_storage ~8 + test_integrity ~6 + test_janitor ~7 + test_ingest ~3 + test_process_api ~9 + test_health ~6)
2. AGPL seam 維持:`python -c "import app.services.integrity, app.services.janitor; import sys; assert 'fitz' not in sys.modules"` 退出 0;既有 `test_fitz_import_confined_to_engine_seam` 仍綠
3. 端到端「正確性穩固化」:
   - `curl -X POST -F "file=@sample.pdf" http://localhost:8000/sessions` 拿 sid
   - `cat .planning/.../meta.json | python -c "import sys,json; m=json.load(sys.stdin); assert 'original_sha256' in m and len(m['original_sha256'])==64"` 退出 0
   - 手動 `chmod 644 data/originals/{sid}/source.pdf && echo CORRUPTED >> data/originals/{sid}/source.pdf && chmod 444 data/originals/{sid}/source.pdf` 後 POST /process → 回 503 + `detail.code == "original_tampered"`
   - 後續 POST /process(同 sid)→ 410 + `detail.code == "session_corrupted"`
4. /process timeout:`PROCESS_TIMEOUT_SECONDS=1 uvicorn app.main:app --port 18003` 啟動 + monkey-patch pipeline.process_job to sleep(3)→ POST /process 在 ~1s 後 504
5. 暫存 janitor:`SESSION_TTL_SECONDS=2 python -c "from app.services import janitor; import time; time.sleep(3); janitor.sweep_expired_sessions()"`(假設先 ingest 一個 session)後驗 4-kind dir 全消
6. Pitfall 1 inline 註解:`grep -A 2 "Pitfall 1" app/api/process.py` 命中(可審查性 — 未來人懂為何不 kill thread)
7. Phase 5 Plan 05-01 + 05-02 整套就位:Phase 5 success criteria #1 / #2 / #3 全可被「目視 + curl」驗證
</verification>

<success_criteria>
- `originals/` SHA-256 baseline 於 ingest 寫入 meta.json,每次 /process 前驗證,失敗 → 503 + corrupted sentinel + 後續 410
- /process 同步包 60s timeout(asyncio.wait_for + to_thread),超時 → 504 + 繁中錯誤訊息;workers=2 確保 1 worker 卡死預覽仍可服務
- Janitor 三點同步觸發(startup / sessions POST 結尾 / process POST 結尾),1h TTL 硬上限,4-kind 一起刪,Windows chmod 0o444 cross-platform,失敗 try/except 絕不污染 request
- Phase 1–4 既有 243 測試零迴歸 + Phase 5 新增 30+ 條覆蓋驗證機制本身(D-C5)
- AGPL seam 維持:integrity / janitor 均為 stdlib(`hashlib` / `shutil` / `os` / `tempfile`),無 fitz import;既有 enforcement 測試仍綠
- 前端對三個新錯誤碼(`original_tampered` / `session_corrupted` / `processing_timeout`)有對應繁中 message;沿用既有 ApiError pathway 無新 UI scaffolding
- Pitfall 1(thread 不可 kill)inline 註解 + README Known Limitations 已記錄(Plan 05-01 Task 3 完成 README)
- Phase 5 STRIDE 全部 disposition 明確(mitigate / accept / accept by mitigation 等價)
</success_criteria>

<output>
完成後建立 `.planning/phases/05-ubuntu/05-02-SUMMARY.md` 記錄:
- artifacts modified(7 既有 + 4 新檔 + 3 新測試檔)
- 測試總數:Phase 4 末 243 → Phase 5 末 N(實測填)
- 端到端三條穩固化路徑實測 sample(SHA-256 tamper / timeout / janitor sweep)各一段 curl 輸出 + 對應 log line
- STRIDE 收口:T-05-04..07 / 10 全部 disposition 確認;Phase 5 累計 closed 數量
- AGPL seam 守門測試輸出
- 已知限制 update(若 timeout 在實機觀察到典型 process 耗時 / Windows rmtree 行為與 Linux 差異 / FastAPI root_path 與 StaticFiles redirect quirk 任何 surprise)
- 留待 phase-level UAT 的人工項:(a) Zeabur 實機部署測 SHA-256 篡改流程;(b) Ubuntu nginx strip-prefix 嵌入;(c) 大型 50MB CAD PDF 實機 timeout 行為 — 此三項由 phase-level UAT 處理,不在 plan scope
</output>
