---
phase: 05-ubuntu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - .dockerignore
  - docker-compose.example.yml
  - LICENSE
  - README.md
  - app/__main__.py
  - app/config.py
  - app/main.py
  - web/index.html
  - web/styles/app.css
autonomous: true
requirements:
  - SC-1  # Docker 在 Ubuntu 可安裝並執行(Uvicorn + Nginx)
  - D-A1  # App image 不含 nginx;uvicorn 直接服 static + API
  - D-A2  # APP_BASE_PATH env var → FastAPI root_path,前端 window.PDFTOOL_API_BASE 沿用
  - D-A3  # 多階段 Dockerfile(python:3.12-slim)+ 本機也可不用 Docker 跑
  - D-A4  # 三部署目標:Zeabur / 本機 Python 套裝 / Ubuntu 入口
  - D-D4  # Dockerfile HEALTHCHECK 指向 /health;/health 加強回傳
  - AGPL-§13  # public GitHub + LICENSE + UI source link(memory locked)
must_haves:
  truths:
    - "工程師可在 Ubuntu / WSL / macOS / Windows 用 `docker build -t logoswap .` 成功建出 image,build 全程無 build-tool 漏到 runtime layer"
    - "工程師可 `docker run -d -p 8000:8000 -v $(pwd)/data:/data logoswap` 啟動容器,等 ≤10 秒後 `curl http://localhost:8000/health` 回 200 + JSON 含 status/uptime_seconds/active_sessions/data_dir_bytes/data_dir_pct 五個欄位"
    - "本機開發者可 `python -m app` 啟動 uvicorn,瀏覽器自動開到 http://127.0.0.1:8000,看到 Phase 1–4 既有 UI"
    - "Repo root 有 LICENSE(AGPL-3.0 全文)、README.md(三部署目標 + AGPL 聲明 + GitHub URL placeholder)"
    - "UI 底部有可見的 footer,內含 LogoSwap GitHub 連結 + AGPLv3 聲明(繁中);footer 沿用既有雙主題 token,深淺主題切換皆顯示"
    - "設定 `APP_BASE_PATH=/pdf-logo` env var 啟動後,FastAPI app 的 root_path 等於 `/pdf-logo`,OpenAPI schema 反映該 prefix"
  artifacts:
    - path: "Dockerfile"
      provides: "多階段 build(python:3.12-slim-bookworm)+ non-root user + VOLUME /data + HEALTHCHECK(用 stdlib urllib,Pitfall 2)+ CMD 用 sh -c 支援 $PORT / $APP_BASE_PATH env"
      contains: "FROM python:3.12-slim-bookworm AS builder"
    - path: ".dockerignore"
      provides: "排除 .git/.venv/__pycache__/.planning/tests/data/*.log 等避免進入 image"
    - path: "docker-compose.example.yml"
      provides: "Ubuntu 部署參考(app + 註解掉的 nginx strip-prefix block),named .example 不是 default"
    - path: "LICENSE"
      provides: "AGPL-3.0 verbatim 全文 (~34KB ASCII)"
      contains: "Version 3, 19 November 2007"
    - path: "README.md"
      provides: "三部署目標啟動指令 + env var 表 + 嵌入契約 + AGPL §13 三件套 + GitHub URL"
      contains: "AGPL"
    - path: "app/__main__.py"
      provides: "`python -m app` desktop entry — uvicorn.run + threading.Timer(1.0, webbrowser.open) 避免 race;workers default 1(desktop 用),env override"
      exports: ["main"]
    - path: "app/config.py"
      provides: "新增 APP_BASE_PATH / UVICORN_WORKERS 兩個 env-backed 常數(SESSION_TTL / PROCESS_TIMEOUT / CORS 由 05-02 補)"
      contains: "APP_BASE_PATH"
    - path: "app/main.py"
      provides: "FastAPI(root_path=APP_BASE_PATH) + 加強 /health 端點(uptime + active_sessions + data_dir_bytes + data_dir_pct)+ _START_TIME 全域(spawn-safe)"
    - path: "web/index.html"
      provides: "新增 <footer class='app-footer'> 區塊(內含 GitHub 連結 + AGPLv3 連結,繁中)"
      contains: "app-footer"
    - path: "web/styles/app.css"
      provides: ".app-footer 區塊(token-only,雙主題 OK)+ .app-shell grid-template-rows 加第三列"
      contains: ".app-footer"
  key_links:
    - from: "Dockerfile CMD"
      to: "uvicorn app.main:app"
      via: "sh -c 解 $PORT / $APP_BASE_PATH"
      pattern: "uvicorn app\\.main:app --host 0\\.0\\.0\\.0 --port \\$\\{PORT:-8000\\}"
    - from: "Dockerfile HEALTHCHECK"
      to: "GET /health"
      via: "python -c stdlib urllib.request"
      pattern: "urllib\\.request\\.urlopen.*health"
    - from: "app/main.py FastAPI(root_path=...)"
      to: "config.APP_BASE_PATH"
      via: "module-load reads env var via config"
      pattern: "root_path=.*APP_BASE_PATH"
    - from: "web/index.html footer"
      to: "GitHub repo URL"
      via: "<a href=...> anchor in <footer>"
      pattern: "github\\.com.*LogoSwap"
    - from: "app/__main__.py"
      to: "uvicorn.run(app.main:app)"
      via: "programmatic uvicorn boot + webbrowser.open"
      pattern: "uvicorn\\.run.*app\\.main:app"
---

<objective>
Phase 5 Plan 01 — **部署 slice(deploy)**。把 Phase 1–4 完成的 FastAPI + PyMuPDF + 純前端 SPA 包裝成 **同一份 code 跑三個部署目標**:
1. **Zeabur** 短期 PaaS(docker build → push public repo → Zeabur auto-deploy)
2. **本機 Python 套裝**(`pip install -r requirements.txt` → `python -m app` → 瀏覽器自動開)
3. **Ubuntu 公司入口網站功能模組**(docker-compose example + 公司主 nginx strip-prefix)

同步交付 AGPL §13 三件套(memory 鎖定):public GitHub repo prerequisite + `LICENSE`(AGPL-3.0 全文)+ UI footer 含 GitHub source link。/health 端點加強為可觀測(uptime + active_sessions + data_dir)。

Purpose: Phase 5 拆兩個 vertical slice — 此 plan 完成「程式可被 ship」,05-02 完成「程式 ship 後跑得穩」。AGPL 合規屬於 ship prerequisite,放在此 slice。
Output: 10 個 artifacts(7 NEW + 3 MODIFIED)— Dockerfile / .dockerignore / docker-compose.example.yml / LICENSE / README.md / app/__main__.py / web 三檔 + app/config.py + app/main.py。
</objective>

## Phase Goal

**As a** LogoSwap 部署工程師(同事或 IT),**I want to** 透過 Docker 或 `python -m app` 在 Zeabur、本機、Ubuntu 三種環境啟動本工具並通過 /health 探測,**so that** 內部使用者可以從這三個目標的任一個經瀏覽器存取 LogoSwap,而我們同時滿足 AGPL §13 的「對網路使用者揭露原始碼」義務。

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ubuntu/05-CONTEXT.md
@.planning/phases/05-ubuntu/05-RESEARCH.md
@.planning/phases/05-ubuntu/05-PATTERNS.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@requirements.txt
@app/main.py
@app/config.py
@web/index.html
@web/styles/app.css
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Dockerfile + .dockerignore + docker-compose.example.yml(部署載具)</name>
  <files>Dockerfile, .dockerignore, docker-compose.example.yml</files>
  <read_first>
    - `.planning/phases/05-ubuntu/05-RESEARCH.md` Pattern 1(行 280–374,多階段 Dockerfile 範例 + .dockerignore 清單)
    - `.planning/phases/05-ubuntu/05-PATTERNS.md` §"Dockerfile" 與 §"Cross-cutting Pitfall Index"(Pitfall 2: slim 無 curl;Pitfall 10: COPY 順序;Pitfall 3: chmod 0o444 + Windows 並非此 task,但會被 05-02 用到)
    - `.planning/phases/05-ubuntu/05-CONTEXT.md` D-A1 / D-A3 / D-A4(image 不含 nginx;python:3.12-slim;三部署目標)
    - `.planning/research/STACK.md` §"Containerization"(multi-stage、non-root、glibc 為何 slim 而非 alpine)
    - `requirements.txt` — 既有依賴(PyMuPDF / FastAPI / uvicorn[standard] / Pillow / python-multipart),Phase 5 不新增任何 runtime 套件
    - `app/` 目錄結構(Dockerfile COPY 路徑要對齊;`app/`、`web/`、`logos/` 三個目錄)
  </read_first>
  <action>
    建立三個 infra 檔。

    **Dockerfile**(repo root)— 沿用 RESEARCH.md Pattern 1 行 280–344 全文形態,具體要點:
    - `# syntax=docker/dockerfile:1.7` header
    - **Stage 1 (builder)**:`FROM python:3.12-slim-bookworm AS builder` → `WORKDIR /build` → `COPY requirements.txt .` 先於 app code 避免 layer cache 失效(Pitfall 10)→ `pip install --no-cache-dir --upgrade pip` + `pip install --no-cache-dir --target /install -r requirements.txt`
    - **Stage 2 (runtime)**:`FROM python:3.12-slim-bookworm` → `RUN groupadd -g 1000 app && useradd -u 1000 -g 1000 -m -s /usr/sbin/nologin app`(non-root UID 1000,T-05-02 mitigation)→ `WORKDIR /app` → `COPY --from=builder /install /install` → `ENV PYTHONPATH=/install PYTHONUNBUFFERED=1 PYTHONDONTWRITEBYTECODE=1 APP_BASE_PATH="" UVICORN_WORKERS=2 PROCESS_TIMEOUT_SECONDS=60 SESSION_TTL_SECONDS=3600 DATA_DIR=/data LOGOS_DIR=/app/logos`(把 Plan 05-02 即將用到的常數也預先 declare,避免 image rebuild)
    - `COPY --chown=app:app app/ /app/app/` / `web/ /app/web/` / `logos/ /app/logos/` / `LICENSE README.md /app/`(LICENSE 與 README 必須進 image — AGPL §13 / 可訪問性)
    - `RUN mkdir -p /data && chown -R app:app /data` → `VOLUME ["/data"]` → `USER app` → `EXPOSE 8000`
    - **HEALTHCHECK**(Pitfall 2 — slim 無 curl,必須用 stdlib urllib):
      ```
      HEALTHCHECK --interval=30s --timeout=5s --start-period=10s --retries=3 \
          CMD python -c "import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://127.0.0.1:8000/health',timeout=3).status==200 else 1)"
      ```
    - **CMD**(用 `sh -c` 是因為 exec-form CMD 不能 expand env var,但 sh exec 進 uvicorn 後 signals 仍正確 forward):
      ```
      CMD ["sh","-c","uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000} --workers ${UVICORN_WORKERS:-2} ${APP_BASE_PATH:+--root-path ${APP_BASE_PATH}}"]
      ```
      注意:`${APP_BASE_PATH:+--root-path ${APP_BASE_PATH}}` 是「只有非空才加 --root-path」的 shell pattern。

    **.dockerignore**(repo root)— 沿用 RESEARCH.md 行 354–373 列表:`.git/` / `.venv/` / `.venv*/` / `__pycache__/` / `**/__pycache__/` / `*.pyc` / `.pytest_cache/` / `.planning/` / `tests/` / `data/` / `*.log` / `*.tmp.*` / `CLAUDE.md` / `.gitignore` / `.dockerignore` / `Dockerfile` / `docker-compose.example.yml` / `zeabur.json` / `README.md`。**NOT** ignored: `LICENSE`(必須進 image)、`requirements.txt`、`app/`、`web/`、`logos/`。

    **docker-compose.example.yml**(repo root,**named .example** 不是 .yml — D-A1 鎖定)— 兩個 services:
    - `app`: build context `.` / image tag `logoswap:local` / `ports: ["8000:8000"]` / `volumes: ["./data:/data"]` / `environment` 包含 `APP_BASE_PATH=""` / `UVICORN_WORKERS=2` / `PROCESS_TIMEOUT_SECONDS=60` / `SESSION_TTL_SECONDS=3600` 等;`restart: unless-stopped`。
    - **註解掉的 `nginx` reverse-proxy 範例 block** — 包含 `location /pdf-logo/ { proxy_pass http://app:8000/; proxy_read_timeout 90s; ... }` snippet 內嵌 YAML 註解(`#` 前綴每一行),示範 Ubuntu 公司入口走 strip-prefix 怎麼接(D-A2)。

    **Threat model mapping**:T-05-01(多階段 + pinned requirements + slim base)、T-05-02(USER app 非 root)、T-05-09(Dockerfile 強制 bind 0.0.0.0,但本機透過 `app/__main__.py` 127.0.0.1)。
  </action>
  <verify>
    <automated>docker build -t logoswap:test . 2>&1 | tail -20 # 成功;確認 stage 1 / stage 2 分階段、最終 image size < 250MB; docker history logoswap:test --no-trunc | grep -E "HEALTHCHECK|USER app|VOLUME"</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `docker build -t logoswap:test .` 退出 0
    - CLI: `docker images logoswap:test --format '{{.Size}}'` 顯示 < 250MB(slim base + wheels-only;比 single-stage 約 80MB 而非 150MB+)
    - Source: `Dockerfile` 含 `FROM python:3.12-slim-bookworm AS builder` 與第二個非 `AS` 的 `FROM python:3.12-slim-bookworm`(grep `^FROM` 應有 2 行)
    - Source: `Dockerfile` 含 `USER app` 行(non-root,T-05-02)
    - Source: `Dockerfile` 含 `urllib.request.urlopen` 字串(HEALTHCHECK 用 stdlib,Pitfall 2)
    - Source: `Dockerfile` 含 `${PORT:-8000}` 字串(Zeabur PORT injection 相容)
    - Source: `Dockerfile` 含 `${APP_BASE_PATH:+--root-path ${APP_BASE_PATH}}` 字串(D-A2)
    - Source: `.dockerignore` 含 `.planning/` 與 `tests/`,但 **不含** `LICENSE` 或 `requirements.txt`(這兩個必須進 image)
    - Source: `docker-compose.example.yml` 檔名以 `.example.yml` 結尾(不是 `docker-compose.yml`)
    - Source: `docker-compose.example.yml` 內含 commented `nginx` block(grep `^# *nginx:` 或 `^#.*proxy_pass` 找得到)
    - Behavior: 啟容器 `docker run -d --name lc-test -p 18001:8000 -v $(pwd)/data:/data logoswap:test && sleep 12 && curl -sf http://localhost:18001/health` 退出 0(/health 在 Plan 05-01 加強前先回基本 `{"status":"ok"}` 仍滿足);收尾 `docker rm -f lc-test`
  </acceptance_criteria>
  <done>Dockerfile / .dockerignore / docker-compose.example.yml 三檔建立並通過 docker build。Image size < 250MB,non-root,HEALTHCHECK 用 stdlib urllib。Compose 範例的 nginx block 為註解形態示範。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: app/config.py + app/main.py + app/__main__.py(APP_BASE_PATH + /health 加強 + 本機 desktop entry)</name>
  <files>app/config.py, app/main.py, app/__main__.py</files>
  <read_first>
    - `app/config.py` 全檔(既有 `_env_int` helper 行 19–48 + 既有常數命名 pattern 行 50–84;Phase 5 新常數須 append 在 API_TITLE 之後)
    - `app/main.py` 全檔(既有 `_PROCESS_STATUS` 行 92–96、既有 PipelineError handler 行 108–114、既有 `/health` 行 166–169、既有 `app = FastAPI(...)` 構造行 34、router 註冊行 37–40、StaticFiles mount)
    - `app/storage.py` 行 80–101(`subdir` + `_SESSION_ID_RE`)— /health 列 active_sessions 時必須 reuse 這個 regex 過濾合法 session token,不可信任目錄名
    - `.planning/phases/05-ubuntu/05-RESEARCH.md` Pattern 2(行 376–410,FastAPI root_path 與 StaticFiles 的已知 redirect quirk #12151)、Pattern 3(行 412–474,app/__main__.py 範例)、Pattern 8(行 973–1023,/health 加強範例)
    - `.planning/phases/05-ubuntu/05-PATTERNS.md` §"app/config.py" / §"app/main.py" / §"app/__main__.py"(精確的 analog 行範圍 + spawn-safe `_START_TIME` 說明 + Pitfall 7 / 8)
    - `.planning/phases/05-ubuntu/05-CONTEXT.md` D-A2(APP_BASE_PATH → root_path)/ D-D2(workers default 2)/ D-D4(/health 五欄位)
    - `requirements.txt` 確認 `uvicorn[standard]` 已在(`python -m app` 啟動依賴)
  </read_first>
  <action>
    分三檔修改 / 建立。**順序重要:先 config,再 main,最後 __main__**(__main__ import config + 仰賴 main 模組 import-safe)。

    **app/config.py** — 在既有 `API_TITLE` 行之後 append 兩個 Phase 5 常數(其他三個 SESSION_TTL_SECONDS / PROCESS_TIMEOUT_SECONDS / CORS_ALLOW_ORIGINS 由 Plan 05-02 補,以免本 plan 引入 unused 常數):
    ```
    # Phase 5: deployment / embedding(Plan 05-01)
    UVICORN_WORKERS: int = _env_int("UVICORN_WORKERS", 2)  # D-D2 — 預覽不被 /process 卡住
    APP_BASE_PATH: str = os.environ.get("APP_BASE_PATH", "")  # D-A2 — FastAPI root_path
    ```
    沿用既有命名規範與 docstring 風格(每個常數一行 inline 註解標出 D-XX 來源)。

    **app/main.py** — 三點修改:
    1. **頂部 imports** 加入 `import shutil`、`import time`、`from contextlib import asynccontextmanager`、`from pathlib import Path`(若尚未 import)。
    2. **`app = FastAPI(...)` 構造**(原行 34)改為帶入 `root_path=config.APP_BASE_PATH`。注意 D-A2 lock + Pitfall 5:預設 APP_BASE_PATH="" 等於 root mount(無 prefix),既有 StaticFiles mount 行為不變;若使用者 set APP_BASE_PATH=/pdf-logo,FastAPI OpenAPI 會 emit `/pdf-logo/...` 但 StaticFiles redirect 行為可能 quirky — 此 plan 維持「`html=True` StaticFiles mount on `/` 沿用 Phase 1–4」並把這個限制寫進 README 嵌入文件章節。
    3. **`/health` 端點**(原行 166–169 只回 `{"status":"ok"}`)替換為 Pattern 8 加強版(沿用 RESEARCH.md 行 973–1023):
       - 在 module top declare `_START_TIME: float = time.time()`(spawn-safe — 每個 worker process 自己 capture,符合「per-worker uptime」語意,Pitfall 7)
       - 端點 body 計算 `uptime_seconds`、`active_sessions`(用 `Path(config.DATA_DIR) / "originals"` iterdir + `storage._SESSION_ID_RE.fullmatch(entry.name)` 過濾 — 必須走 regex 防 path traversal-style 殘留 dir 算進去;失敗 try/except 設為 -1)、`data_dir_bytes` / `data_dir_pct`(`shutil.disk_usage(str(config.DATA_DIR))`;失敗 try/except 設為 0 / 0.0)
       - 回傳 dict 五欄位完整:`{"status":"ok","uptime_seconds": float, "active_sessions": int, "data_dir_bytes": int, "data_dir_pct": float}`
       - **不可洩漏 session_id**(T-05-08 mitigation — 只回數量,不列名)
    4. **Lifespan startup hook 骨架(HARD 交付)** — declare:
       ```
       @asynccontextmanager
       async def lifespan(app: FastAPI):
           yield  # Plan 05-02 will fill in janitor.sweep_expired_sessions() here
       ```
       並把 `lifespan=lifespan` 傳給 FastAPI 構造(必填,非可選)。Plan 05-02 只需 fill in body,不必動 signature。

    **app/__main__.py**(NEW)— 沿用 RESEARCH.md Pattern 3 行 417–474 全文形態:
    - module docstring 標明用途(`python -m app` 桌面入口)+ 標明 spawn-safe 屬性
    - `from __future__ import annotations`
    - imports: `os`, `threading`, `webbrowser`, `uvicorn`
    - `_open_browser(url)` helper:`threading.Timer(1.0, lambda: webbrowser.open(url)).start()`(1 秒 delay,Pitfall 7 + RESEARCH.md 行 443–449)
    - `main()`:讀 `HOST=os.environ.get("HOST","127.0.0.1")`(D-A1 / T-05-09 mitigation — 預設 loopback,Docker / Zeabur 才走 0.0.0.0)、`PORT=int(os.environ.get("PORT","8000"))`、`workers=int(os.environ.get("UVICORN_WORKERS","1"))`(desktop 預設 1 而非 2 — Windows spawn-safe 但 spawn 多 worker 慢且 desktop 無需)、`url=f"http://{host}:{port}"`;若無 `UVICORN_NO_BROWSER` env 則 `_open_browser(url)`;然後 `uvicorn.run("app.main:app", host=host, port=port, workers=workers)`(不開 reload — workers>1 不相容 reload)
    - `if __name__ == "__main__": main()`

    **Threat model mapping**:T-05-08(/health 不洩漏 session_id)、T-05-09(`__main__.py` host 預設 127.0.0.1)、Pitfall 7(spawn-safe)、Pitfall 5(root_path + StaticFiles 已知 quirk — README 章節必須警示)。
  </action>
  <verify>
    <automated>python -c "from app import main; print(main.app.root_path)" # 應回空字串; python -c "from app import config; assert config.UVICORN_WORKERS == 2 and config.APP_BASE_PATH == ''" # 預設值正確; APP_BASE_PATH=/pdf-logo python -c "from app import main, config; assert main.app.root_path == '/pdf-logo' and config.APP_BASE_PATH == '/pdf-logo'" # env override 生效; pytest tests/test_api.py tests/test_storage.py -x --no-header 2>&1 | tail -5 # 既有測試零迴歸</automated>
  </verify>
  <acceptance_criteria>
    - Source: `app/config.py` 含 `UVICORN_WORKERS: int = _env_int("UVICORN_WORKERS", 2)`(精確字串)
    - Source: `app/config.py` 含 `APP_BASE_PATH: str = os.environ.get("APP_BASE_PATH", "")`(精確字串)
    - Source: `app/main.py` 含 `root_path=config.APP_BASE_PATH`(grep 命中)
    - Source: `app/main.py` 含 `_START_TIME` 全域變數宣告
    - Source: `app/main.py` 含 `@asynccontextmanager` 裝飾子(HARD 交付:lifespan 骨架就位給 Plan 05-02 填入 body)
    - Source: `app/main.py` 含 `lifespan=lifespan` 字串(HARD 交付:FastAPI 構造綁定 lifespan)
    - Source: `app/main.py` 的 `/health` handler 回傳含 `status` / `uptime_seconds` / `active_sessions` / `data_dir_bytes` / `data_dir_pct` 五個欄位
    - Source: `app/__main__.py` 存在,含 `def main()` + `threading.Timer(1.0` 字串 + `host="127.0.0.1"` 或 `os.environ.get("HOST", "127.0.0.1")` 字串(預設 loopback,T-05-09)
    - Behavior: `python -c "from app.main import app; print(app.root_path)"` 預設輸出空字串
    - Behavior: `APP_BASE_PATH=/pdf-logo python -c "from app.main import app; print(app.root_path)"` 輸出 `/pdf-logo`
    - Behavior: 啟動 server(`uvicorn app.main:app --port 18002`)後 `curl -s http://127.0.0.1:18002/health | python -c "import sys,json; d=json.load(sys.stdin); assert {'status','uptime_seconds','active_sessions','data_dir_bytes','data_dir_pct'} <= set(d.keys())"` 退出 0
    - Behavior: /health 回應 body **不含** 任何 32-char session_id(grep `[a-f0-9]{32}` 應 0 matches — T-05-08 info disclosure guard)
    - Test: `pytest tests/test_api.py -x` 退出 0(既有 /health 測試若 assert exact JSON 需更新到新 schema — 此 task 順便修)
    - Test: `pytest tests/ -x -k "not janitor and not integrity and not health"` 全綠(尚未引入 05-02 測試)
  </acceptance_criteria>
  <done>config.py 新增 2 常數;main.py 加 root_path + /health 五欄位 + _START_TIME + lifespan 骨架;__main__.py 桌面入口含 webbrowser.open + workers default 1。既有測試零迴歸。env var override 行為驗證通過。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: LICENSE + README.md + UI footer + .app-footer CSS(AGPL §13 三件套 + 三部署目標文件)</name>
  <files>LICENSE, README.md, web/index.html, web/styles/app.css</files>
  <read_first>
    - `web/index.html` 全檔(既有 app-shell / toolbar / main / side-panel / modal 結構;footer slot 位置 — 行 39–410 之間 main 後、modal 前)
    - `web/styles/app.css` 行 1–80(既有 token-only styling + .app-shell grid 行 46–48)、`web/styles/tokens.css` 全檔(雙主題 var 名 — `--color-text` / `--color-panel` / `--color-border` / `--color-text-muted` / `--space-sm/lg/xs` / `--font-size-small`)
    - `.planning/phases/05-ubuntu/05-RESEARCH.md` Pattern 9(行 1042–1050,AGPL footer markup)+ §"Recommended File Tree"(README 結構)+ Pitfall 6(AGPL §13 內網 ≠ 豁免)
    - `.planning/phases/05-ubuntu/05-PATTERNS.md` §"LICENSE" / §"README.md" / §"web/index.html" / §"web/styles/app.css"(每個檔案的 analog + 精確 token 列表)
    - `.planning/phases/05-ubuntu/05-CONTEXT.md` §"AGPL UI source link"(Claude's Discretion — 已給文案 + 位置建議)
    - memory `feedback_quality_gates.md`(部署 / push 順序)+ `project_deployment_licensing.md`(三件套 + Zeabur 路徑)
  </read_first>
  <action>
    四檔。**AGPL §13 三件套必須同時就位:LICENSE + README + UI footer**(memory locked;任一缺失等於違反 §13 對網路使用者的義務)。

    **LICENSE**(repo root,NEW)— 從 https://www.gnu.org/licenses/agpl-3.0.txt 取得 AGPL-3.0 全文,**verbatim** 寫入 LICENSE(ASCII / ~34KB / 不可修改)。檔案開頭包含 `GNU AFFERO GENERAL PUBLIC LICENSE` 與 `Version 3, 19 November 2007`。**注意**:若離線無法 fetch,於 task 內以 WebFetch 抓 https://www.gnu.org/licenses/agpl-3.0.txt 整篇 text → 寫進 LICENSE。**不可** 用 SPDX 簡稱 / short header 替代;§13 要求全文 distribution。

    **README.md**(repo root,NEW)— 沿用 RESEARCH.md Recommended File Tree §"README.md" 結構;繁中為主、必要英文(部署指令、env var 名)。內容章節按序:

    1. **# LogoSwap — PDF 商標替換工具**(一行專案描述繁中)
    2. **License & Source(AGPL §13 揭露)**:三句話聲明 — (a) 本工具以 AGPLv3 授權;(b) 完整原始碼公開於 `https://github.com/<OWNER>/LogoSwap`(**OWNER placeholder 留待真實 repo URL 替換 — 部署前必須改**);(c) LICENSE 全文見專案根目錄 `LICENSE`。
    3. **Architecture(極簡)**:一段話描述「FastAPI + PyMuPDF + 純前端 SPA;單容器 / `python -m app` desktop / 公司入口三模式」。
    4. **Deploy Target 1 — Zeabur**:子標題下列指令 — `git push public` → Zeabur 連 repo → 自動 build Dockerfile → 設 env `APP_BASE_PATH=""`(或全留空)→ Zeabur 提供 sub-domain → 完成。標註「**部署前須完成:public GitHub repo + LICENSE 推上去 + README 寫好 OWNER URL**」。
    5. **Deploy Target 2 — 本機 Python 套裝(桌面工具)**:跨平台指令(Windows PowerShell / macOS / Linux 各一例)— `git clone` → `python -m venv .venv` → `source .venv/bin/activate`(或 `.\.venv\Scripts\Activate.ps1`)→ `pip install -r requirements.txt` → `python -m app` → 瀏覽器自動開到 `http://127.0.0.1:8000`。env var `UVICORN_NO_BROWSER=1` 可關閉自動開瀏覽器。
    6. **Deploy Target 3 — Ubuntu 公司入口網站功能模組**:`docker-compose.example.yml` 為起點 → 公司主 nginx 加 `location /pdf-logo/ { proxy_pass http://app:8000/; proxy_read_timeout 90s; }` 範例 → 設 `APP_BASE_PATH=/pdf-logo`(實驗性 — 必須先測過 proxy 才可信賴,Pitfall 5)。提供另一個替代方案:**sub-domain 模式**(`pdf-logo.internal` → 直接 root mount,APP_BASE_PATH="")— 更穩定。
    7. **Environment Variables**(表格):`DATA_DIR` / `LOGOS_DIR` / `APP_BASE_PATH` / `UVICORN_WORKERS` / `PROCESS_TIMEOUT_SECONDS`(05-02 引入)/ `SESSION_TTL_SECONDS`(05-02 引入)/ `CORS_ALLOW_ORIGINS`(05-02 引入)/ `MAX_UPLOAD_BYTES` / `MAX_PAGES` / `PORT` / `HOST` / `UVICORN_NO_BROWSER` — 每行包含 default / 範例 / 簡要說明。
    8. **Embedding Contract**:`window.PDFTOOL_API_BASE`(Phase 1 frontend seam,瀏覽器端)與 `APP_BASE_PATH`(Phase 5 backend seam,FastAPI root_path)兩個 knob 的對應關係。
    9. **Known Limitations**:(a) AGPL §13 — 本工具於外部可達網路部署前必須公開 source;(b) Session TTL 1h(05-02);(c) /process 60s timeout(05-02);(d) 大型 / 旋轉 PDF 自動降 DPI(WR-06);(e) FastAPI root_path + StaticFiles `html=True` 在 strip-prefix proxy 下有已知 redirect quirk(#12151)— 預設 `APP_BASE_PATH=""` 是穩定路徑,prefix 模式為實驗性。
    10. **Build & Test**:`pytest tests/` / `docker build -t logoswap .` / `docker run -p 8000:8000 logoswap`。

    **web/index.html** — 在 `<div class="app-shell">` 內、`</main>` 之後、modal 區塊之前,新增 footer 區塊(沿用 RESEARCH.md Pattern 9 行 1042–1050):
    ```
    <footer class="app-footer" role="contentinfo">
      <p class="app-footer__text">
        本工具為 <a class="app-footer__link" href="https://github.com/<OWNER>/LogoSwap" target="_blank" rel="noopener">LogoSwap</a> — 依
        <a class="app-footer__link" href="https://www.gnu.org/licenses/agpl-3.0.html" target="_blank" rel="noopener">AGPLv3</a> 授權。
      </p>
    </footer>
    ```
    **不放** session TTL 提示(屬於 05-02 上傳完成後 UI 動態提示)。`<OWNER>` 字串保留為待替換 placeholder — README 與 footer 同步,部署前必須替換。

    **web/styles/app.css** — 兩處修改:
    1. **`.app-shell` grid-template-rows** 從 `auto 1fr`(行 ~46–48)改為 `auto 1fr auto`(加第三列給 footer)。
    2. **append** `.app-footer` 區塊(沿用 PATTERNS.md §"web/styles/app.css" 給的範例):
       ```
       .app-footer {
         padding: var(--space-sm) var(--space-lg);
         background: var(--color-panel);
         border-top: 1px solid var(--color-border);
         font-size: var(--font-size-small);
         color: var(--color-text-muted);
         text-align: center;
       }
       .app-footer__text { margin: 0; }
       .app-footer__link { color: var(--color-text); text-decoration: underline; }
       .app-footer__link:hover { color: var(--color-accent); }
       ```
       **規則**:全部用 var() token,**不可** 硬寫 hex(Phase 1 鎖定);accent 只在 hover state,文字本體不能搶 primary CTA 的視覺權重。**雙主題自動切換**(token 在 :root 與 [data-theme=dark] 都已定義,雙主題免修)。

    **Threat model mapping**:T-05-03(AGPL §13 — LICENSE + README + UI footer 三件套同時就位)、Pitfall 6(內網誤解 — README §"Known Limitations" 必寫)。
  </action>
  <verify>
    <automated>test -f LICENSE && grep -q "Version 3, 19 November 2007" LICENSE && wc -c LICENSE | awk '{ exit ($1 < 30000) }' # LICENSE 全文 ≥30KB; test -f README.md && grep -q "AGPLv3\|AGPL-3.0\|AGPL" README.md && grep -q "github.com" README.md # README 有 AGPL 與 GitHub URL; grep -q "app-footer" web/index.html && grep -q "AGPLv3" web/index.html # footer 已加; grep -q ".app-footer" web/styles/app.css && grep -q "grid-template-rows: auto 1fr auto" web/styles/app.css # CSS 正確; python -c "from app.main import app; from fastapi.testclient import TestClient; r = TestClient(app).get('/'); assert 'app-footer' in r.text and 'AGPLv3' in r.text" # static 仍能服 index 且含 footer</automated>
  </verify>
  <acceptance_criteria>
    - Source: `LICENSE` 存在,首 10 行內含 `GNU AFFERO GENERAL PUBLIC LICENSE` 與 `Version 3, 19 November 2007`(精確字串)
    - Source: `LICENSE` 檔案大小 ≥ 30,000 bytes(AGPL-3.0 全文約 34KB,verbatim 不可截短)
    - Source: `README.md` 存在,含 `AGPLv3` 或 `AGPL-3.0` 字串,含 `github.com` 字串,含 `python -m app` 字串(三部署目標一個一個都要找得到)
    - Source: `README.md` 含「Deploy Target 1」/「Deploy Target 2」/「Deploy Target 3」三個章節(中或英,grep 任一形式找得到)
    - Source: `README.md` 含 env var 表(`APP_BASE_PATH` / `UVICORN_WORKERS` / `DATA_DIR` 三個 token 在同一文件出現)
    - Source: `web/index.html` 含 `app-footer` class 字串、含 `AGPLv3` 字串、含 `github.com/<OWNER>/LogoSwap` 或 `github.com/.*/LogoSwap` pattern(OWNER placeholder 仍在)
    - Source: `web/styles/app.css` 含 `.app-footer {` 區塊、含 `grid-template-rows: auto 1fr auto`、**不含** raw hex color 在 `.app-footer*` 區塊內(grep `\.app-footer.*#[0-9a-fA-F]` 應 0 matches)
    - Behavior: `python -c "from fastapi.testclient import TestClient; from app.main import app; r=TestClient(app).get('/'); assert r.status_code==200; assert 'app-footer' in r.text; assert 'AGPLv3' in r.text"` 退出 0
    - Test: `pytest tests/test_api.py -x` 退出 0
  </acceptance_criteria>
  <done>AGPL §13 三件套就位:LICENSE 全文 + README 三部署目標 + UI footer 含 GitHub anchor + AGPLv3 link。CSS token-only 雙主題;app-shell grid 加第三列。OWNER placeholder 保留待部署前替換(README + footer 同步)。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → Reverse proxy(Zeabur LB / Ubuntu nginx)| 公網 / LAN 邊界;TLS 終結在此 |
| Reverse proxy → app container | 內部明文 HTTP;反向代理已驗來源 |
| Container → /data volume | trusted process(non-root UID 1000)讀寫 session 檔 |
| 桌面模式:本機 user → 127.0.0.1:8000 | loopback only,T-05-09 強制 host="127.0.0.1" |
| Browser → /health | 任何使用者可探(無 auth)— T-05-08 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T(Tampering) | Docker image | mitigate (P1) | 多階段 Dockerfile(stage 1 pip install / stage 2 wheels-only COPY)+ `python:3.12-slim-bookworm`(security-patched LTS 級基礎)+ pinned requirements.txt;runtime layer 無 pip / 無 build toolchain。Task 1 acceptance: image size < 250MB 是這個 mitigation 的可觀測訊號。 |
| T-05-02 | E(Elevation of Privilege) | Container runtime | mitigate (P0) | Dockerfile `RUN groupadd -g 1000 app && useradd ... && USER app`,容器內非 root 跑 uvicorn + PyMuPDF。`/data` chown app:app。Task 1 acceptance: `docker run logoswap whoami` == `app`(非 root)。 |
| T-05-03 | I(Information Disclosure)/合規 | AGPL §13 | mitigate (P0) | 三件套必須同時就位:(a) public GitHub repo(部署前置 — memory 鎖定)、(b) `LICENSE` 全文進 repo + 進 image、(c) `web/index.html` footer 含 GitHub anchor。任一缺失違反 §13。Task 3 acceptance 三件全核。 |
| T-05-08 | I | /health endpoint | accept(v1)+ mitigate by minimization | /health 不洩漏 session_id(只回 count)— Task 2 acceptance 含 grep guard;不洩漏 active sid 名稱、不洩漏 user-uploaded filename、不洩漏 disk 路徑。**未來公開外網時** 加 basic auth(deferred,memory)。 |
| T-05-09 | T(Tampering / unintended exposure) | `app/__main__.py` host bind | mitigate (P1) | `__main__.py` 預設 `host="127.0.0.1"`(loopback only)— 本機 desktop 工具不應 listen 0.0.0.0;Docker / Zeabur 走 Dockerfile CMD 明確 `--host 0.0.0.0`。Task 2 acceptance: source grep 命中 127.0.0.1 字串。 |
| Pitfall-2 | A(Availability) | Dockerfile HEALTHCHECK | mitigate (P1) | slim base 無 curl/wget;HEALTHCHECK 用 stdlib `python -c urllib.request.urlopen`。Task 1 acceptance: grep `urllib.request.urlopen` 命中。 |
| Pitfall-5 | A | FastAPI root_path + StaticFiles(html=True)| accept + document | 已知 #12151 quirk;預設 APP_BASE_PATH="" 是穩定路徑,prefix 模式為實驗性;README Known Limitations 警示;sub-domain 模式為推薦替代。 |
| Pitfall-7 | A | uvicorn workers > 1 on Windows(spawn)| mitigate(P2)| `app/main.py` 的 `_START_TIME` 為 module-top capture(spawn-safe — 每 worker 自己的時鐘);`app/__main__.py` 預設 workers=1。Task 2 acceptance: 全域 `_START_TIME` 存在但不依賴 fork 繼承。 |
| Pitfall-10 | A(image rebuild cost)| Dockerfile layer cache | mitigate(P2)| `COPY requirements.txt .` 在 stage 1 排序於 `COPY app/` 之前;code-only 變動不觸發 pip 重灌。Task 1 acceptance: Dockerfile 中 `COPY requirements.txt` 行號 < `COPY .*app` 行號。 |

**Phase 5 Plan 05-01 scope outside this register**:T-05-04..07 / T-05-10 由 Plan 05-02 mitigate(SHA-256 baseline / janitor / corrupted sentinel / 60s timeout)。Out-of-scope:TLS(D-A1 — 反向代理負責)、auth(v1 內網免登入)、CORS(預設關;Plan 05-02 預埋 env var)。
</threat_model>

<verification>
本 plan 結束時必須通過:
1. `docker build -t logoswap .` 退出 0;image size < 250MB;`docker images logoswap --format '{{.Size}}'` 驗證
2. `docker run -d --name lc-verify -p 18000:8000 -v $(pwd)/data:/data logoswap && sleep 12 && curl -sf http://localhost:18000/health` 退出 0;清理 `docker rm -f lc-verify`
3. `python -m app` 啟動 uvicorn 且 1 秒後瀏覽器自動 open(`UVICORN_NO_BROWSER=1` 可關)。手動 Ctrl-C 結束。
4. `APP_BASE_PATH=/pdf-logo python -c "from app.main import app; assert app.root_path == '/pdf-logo'"` 退出 0
5. `pytest tests/ -x --no-header` 退出 0 — 既有測試零迴歸(本 plan 未動 services / api / storage 行為,只動 main + config + 新檔)
6. AGPL §13 三件套就位:`test -f LICENSE && grep -q "Version 3" LICENSE && grep -q "AGPLv3" web/index.html && grep -q "AGPLv3\|AGPL-3.0\|AGPL" README.md` 退出 0
7. /health 回傳五欄位:`curl http://localhost:18000/health | python -c "import sys,json; d=json.load(sys.stdin); assert set(d.keys()) >= {'status','uptime_seconds','active_sessions','data_dir_bytes','data_dir_pct'}"`
8. /health 不洩漏 session_id:`curl http://localhost:18000/health | grep -E "[a-f0-9]{32}"` 應 1 個 match 都沒有(exit 1)
9. UI footer 在預設主題 + dark 主題皆可見、皆可點(手動驗證 deferred 到 phase-level UAT — 此 plan 不要求)
</verification>

<success_criteria>
- Dockerfile 可 build,image 為 multi-stage / non-root / HEALTHCHECK / VOLUME 完整
- `python -m app` 桌面入口可啟,瀏覽器自動開到 127.0.0.1:8000
- `APP_BASE_PATH` env var 控制 FastAPI root_path,預設空(root mount)
- `/health` 回 status + uptime_seconds + active_sessions + data_dir_bytes + data_dir_pct 五欄位,不洩漏 session_id
- AGPL §13 三件套就位:LICENSE(34KB 全文)+ README(三部署目標 + GitHub URL + AGPL 聲明)+ UI footer(GitHub anchor + AGPLv3 link,雙主題 token 沿用)
- 既有 Phase 1–4 測試零迴歸(`pytest tests/` 退出 0)
- `docker-compose.example.yml` 範例展示 Ubuntu 入口 strip-prefix 路徑
</success_criteria>

<output>
完成後建立 `.planning/phases/05-ubuntu/05-01-SUMMARY.md` 記錄:
- artifacts created(10 檔)
- image size 實測值(`docker images logoswap --format '{{.Size}}'`)
- /health 五欄位實測 sample
- 三部署目標啟動指令逐一驗證結果
- AGPL §13 三件套就位確認(LICENSE 大小 / README 章節數 / footer 在 dark+light 視覺確認)
- Pitfall 5(root_path + StaticFiles redirect quirk)實測結果記錄
- 已知留待 Plan 05-02 補完的項目:janitor / SHA-256 baseline / /process timeout / corrupted sentinel
- OWNER placeholder 替換時機:由使用者於 Zeabur 部署前手動替換,屬於部署 ops,不在本 plan scope
</output>
